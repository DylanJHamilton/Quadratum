{%- comment -%}
  Quadratum — q-icon
  Usage:
    {%- render 'q-icon', name: b.icon -%}

  Expects SVG files at:
    assets/icons/<name>.svg

  Notes:
  - Renders as <img> for maximum compatibility (no JS, no inline-SVG hacks)
  - Decorative by default: alt="" + aria-hidden="true"
{%- endcomment -%}

{%- liquid
  assign icon_name = name | default: ''
  if icon_name == blank
    break
  endif

  assign file = 'icons/' | append: icon_name | append: '.svg'
-%}

<img
  src="{{ file | asset_url }}"
  alt=""
  aria-hidden="true"
  loading="lazy"
  decoding="async"
  width="1em"
  height="1em"
  style="width:1em;height:1em;display:block;"
>
