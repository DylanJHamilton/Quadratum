{% assign recover_form_id = 'Account-recover-' | append: section.id %}
{% form 'recover_customer_password', id: recover_form_id, class: 'account-main-account-login__form account-main-account-login__form--recover' %}
  <div class="account-main-account-login__form-header">
    <h2 class="account-main-account-login__form-title">
      {{ section.settings.recover_heading | escape }}
    </h2>

    {% if section.settings.recover_subheading != blank %}
      <p class="account-main-account-login__form-subtitle">
        {{ section.settings.recover_subheading | escape }}
      </p>
    {% endif %}
  </div>

  {% if form.posted_successfully? %}
    <div
      class="account-main-account-login__success"
      role="status"
      aria-live="polite"
      tabindex="-1"
    >
      {{ section.settings.recover_success_message | escape }}
    </div>
  {% endif %}

  {% if form.errors %}
    <div
      id="AccountRecoverErrors-{{ section.id }}" class="account-main-account-login__errors"
      role="alert"
      aria-live="assertive"
      tabindex="-1"
    >
      {{ form.errors | default_errors }}
    </div>
  {% endif %}

  <div class="account-main-account-login__field">
    <label for="AccountMainRecoverEmail-{{ section.id }}">
      {{ section.settings.email_label | default: 'Email' | escape }}
    </label>
    <input
      id="AccountMainRecoverEmail-{{ section.id }}"
      type="email"
      name="email"
      value="{{ form.email | escape }}"
      autocomplete="email"
      autocorrect="off"
      autocapitalize="off"
      {% if form.errors %}
        aria-describedby="AccountRecoverErrors-{{ section.id }}" aria-invalid="true"
      {% endif %}
      required
    >
  </div>

  <div class="account-main-account-login__actions">
    <button
      type="submit"
      class="account-main-account-login__button account-main-account-login__button--{{ section.settings.button_style | escape }}"
    >
      {{ section.settings.recover_button_label | default: 'Reset password' | escape }}
    </button>
  </div>

  {% if section.settings.recover_display == 'panel' %}
    <button
      type="button"
      class="account-main-account-login__text-button"
      data-account-main-account-login-login-trigger hidden
    >
      {{ section.settings.back_to_login_label | escape }}
    </button>
  {% endif %}
{% endform %}