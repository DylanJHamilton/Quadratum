{%- comment -%}Compatibility entry point: use the shared search form/settings contract.{%- endcomment -%}
{%- assign form_context = context | default: 'header' -%}
{% render 'search-form-predictive',
  form_context: form_context,
  input_id: input_id,
  panel_id: panel_id,
  placeholder: placeholder,
  extra_class: 'qtm-search-predictive'
%}
