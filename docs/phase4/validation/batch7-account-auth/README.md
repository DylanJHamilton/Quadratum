Four native legacy authentication hosts, their four stylesheets/four controllers (one new), and the sole recovery helper independently reviewed. 173 dedicated actual-host settings/native-form/DOM/CSS/lifecycle cases and existing shared form controller regression pass. Native form rendering is an explicit fixture adapter, not Shopify service certification. All 489 source hashes reproduce; CSS build matches the protected baseline. Native login/activation/decline/recovery/reset/registration, saved settings, browser/AT/editor acceptance remain queued.
