# Quadratum Phase 4 — component hardening progress

Status: **IN PROGRESS — draft review only.** Phase 4 is not complete. This document must not be read as certification of unreviewed components.

**Owner-directed validation schedule (2026-09-20):** complete the remaining engineering work through Batches 4–9 before beginning the accumulated live Shopify storefront/theme-editor acceptance. REQUIRES LIVE VERIFICATION is a queued disposition, not a request to interrupt engineering for merchant testing. All existing live requirements remain; PR #22 stays DRAFT until the full register and eventual live acceptance are complete.

## Baseline and boundaries

Repository `DylanJHamilton/Quadratum`; fresh branch `release/v1-phase-4-component-hardening`. Owner-approved main baseline `13d9e03010de46595e32e80144c17f76e1c36a39` (PR #21 merged). GitHub compare confirmed no file differences from Phase 3 head `ff4b52c890560c4ba52fdcadbd48cd8448e28892`; tested tree `13dd9a4aa33206d8a34d9ee242f1ee98bcf09eaf`. Local starting commit `0dc7531` has that identical tree; local/published hashes differ because GitHub object APIs publish the commits.

150 sections, 102 Theme Blocks and both builder chains retained. `config/settings_data.json` SHA-256 remains `ffc244582d58f3e6e07713abd6ab1de2cdef7ba109fc00a60cef886235b6b499`. No main writes, merges, publication, preset-brand rollout, account redesign, settings redesign or CSS-delivery rewrite.

## Register coverage

`component-register.csv` inventories every section, block, snippet and deployed/root CSS/JS/static asset. `static-assets/register-*.csv` separately enumerates all 7,830 SVG files in the excluded icon library, with individual hashes and XML parsing results (zero XML errors). Inventory is not release approval. Untouched components remain explicitly NOT REVIEWED; static icons remain VISUAL REVIEW PENDING. Dynamic consumer tracing remains necessary beyond literal references.

## Batch status

| Batch | Status |
|---|---|
| 1 Global / Structural | Initial hardening plus Batch 3 shared-search settings/request/lifecycle repairs implemented; reopened predictive findings resolved. Dormant search data hook retained/source-reviewed. Live acceptance queued after Batch 9. |
| 2 Hero / Banner / CTA | Engineering/register closure: all 30 sections and 62 family components have dispositions. 57 await live verification, four dormant helpers retained, one build-only helper reviewed. B2-FINAL live acceptance remains open. |
| 3 Collection / Discovery | Source/fixture pass complete: all 44 family components have dispositions (30 live queued, 14 dormant retained). Two existing reviewed blog dependencies are now assigned to their actual owner. Blog, collection and Main Search dependencies resolved. All live acceptance after Batch 9. |
| 4 Product / Commerce | Engineering/register closure: all 69 family components have dispositions (64 live queued, five dormant retained). All five main-product hosts reviewed independently. B4-FINAL storefront acceptance remains mandatory after Batch 9. |
| 5 Content / Interactive | FORMALLY CLOSED for engineering: 52 assigned components — 51 live queued, one dormant retained, zero NOT REVIEWED. B5-FINAL live acceptance remains mandatory after Batch 9. |
| 6 Forms / Conversion | FORMALLY CLOSED for engineering: 20 assigned components — all live queued, zero NOT REVIEWED. B6-FINAL live acceptance remains mandatory after Batch 9. |
| 7 Social / Third Party / Utilities | FORMALLY CLOSED for engineering: 75 assigned components — 74 live queued, one dormant/source-reviewed, zero NOT REVIEWED. B7-FINAL live acceptance remains queued after Batch 9. Owner checkpoint required before Batch 8. |
| 8 Theme Blocks | FORMALLY CLOSED for engineering: 102/102 blocks and 27 dependencies individually reviewed; all 129 Family 8 entries have dispositions (128 live queued, one dormant retained), zero NOT REVIEWED. Published at `4af9dcb56c7791e198533303bcd452a564cc3a21`; earlier local/publication-pending records below are historical. B8-FINAL live acceptance remains queued after engineering batches. |
| 9 Snippet / Asset Closure | Inventory started; independent consumer/visual closure pending |

## Batch 1 findings and repairs

- **Headers One–Five lifecycle:** repeated asset execution and editor reloads could duplicate handlers; global listeners survived removed sections. Initialization now has explicit guards and abortable per-header listeners. Unload closes active navigation and clears relevant timers. Header One's existing initializer was moved into a deferred asset so editor loads receive the same behavior as initial loads. Its dropdown/drawer settings and layout remain.
- **Headers Two–Four mobile behavior:** immediate open-state focus, closing-state guards, reduced-motion close timing, opener restoration, proper dialog/fallback semantics and external-focus containment. Headers cooperate so opening one closes another; two simultaneous focus traps cannot fight. Headers Three/Four no longer transition visibility before focus. Header Five retains the Phase 3 contract and gains cleanup/cooperation.
- **Search/cart handoff:** Headers Three–Five release the mobile modal when a system search/cart trigger is used, avoiding competing focus traps. Header One/Two's existing handoff remains.
- **Search popup:** cancelled delayed focus after close; added Tab containment and outside-focus recovery. Singleton global ID and existing controller route preserved.
- **Global popup frequency:** boot previously called reset unconditionally, erasing once-per-session/day/week state on every load. Frequency now persists. Ordinary theme preview URLs no longer force Theme Editor behavior. Explicit false dataset settings take precedence over true global fallbacks. Blocked browser storage is handled. Reopening cancels the previous close timer; duplicate script/boot calls do not duplicate initialization.
- **Header Five mobile search:** `append: section_id` had been passed as an unused render argument, producing duplicate IDs. The ID is now assigned before rendering. Actual two-instance Liquid fixture proves distinct input IDs.
- **Footer Two–Five newsletter forms:** unique Shopify form IDs include section and block identity where applicable; input/label associations retained. Customer action and newsletter tags unchanged. Removed seven confirmed unused footer discovery flags/assignments while already reviewing those files.
- **Header/footer media:** added intrinsic image dimensions using each actual image object; square cropped thumbnails use their requested dimensions. Existing loading, crops, sizing and alt text retained. This is local layout-shift hardening, not a theme-wide image rewrite.
- **Header Four announcements:** first visible announcement no longer depends on the position of unrelated blocks in the section. Rotation now has a persistent pause/resume control, honors reduced-motion preference, pauses during hover/focus, and cancels its timer on unload. Countdown ticks no longer repeatedly announce every second; expiration remains a status message.
- **Search snippet maintenance:** removed stale generated migration commentary and unused render argument. No search behavior redesign.

## Deliberate non-changes and decisions

- No retained file deleted. `sections/header-basic.liquid` is a zero-byte file with no active consumer found in the layout/config/template trace: **DELETION CANDIDATE — OWNER REVIEW**, retained.
- Legacy `header-two-*` snippet chains and standalone `q-nav` remain for Batch 9 consumer closure. No automatic orphan deletion.
- Header/footer settings counts remain high. Removing controls or changing layout families solely to reduce lint counts would violate the feature-preservation brief.
- Source breakpoint review and rendered fixtures are not screenshots. Components awaiting real responsive/merchant-state verification are marked REQUIRES LIVE VERIFICATION, not PASS/HARDENED.
- The 605 historical Shopify-verification candidates remain separate. No candidate was promoted solely because it was unprovable offline.

## Batch 1 validation

From repository root; test dependencies are installed in the sibling analysis tools directory.

- `python tests/phase4/register.py` — inventories components, validates embedded schemas, refreshes dependency/hash evidence without overwriting recorded dispositions.
- `python tests/phase3/static-check.py` — protected architecture, schema and JavaScript checks.
- `python tests/phase3/closure/static-audit.py` — references, template/locale coverage and merchant state.
- `NODE_PATH=../analysis/phase3/test-tools/node_modules node tests/phase4/global-surfaces.cjs` — five header variants, duplicate/editor initialization, two instances, focus/Tab/Escape/return, rapid reopening, unload/reload, announcement pause/timer cleanup, popup persistence/storage denial, search focus and H5 IDs.
- `NODE_PATH=../analysis/phase3/test-tools/node_modules node tests/phase4/global-presets.cjs` — ten actual header/footer default/preset compositions, each rendered twice, unique IDs, label targets and required newsletter emails. Shopify form tags are fixture adapters, not live submission certification.
- All seven Phase 3 mechanism suites repeated; commerce, cart, predictive search, hero and other critical fixtures preserved.
- `npm run build:css` and `git -c core.whitespace=cr-at-eol diff --check`.
- Shopify Theme Check full JSON plus per-check movement retained in `validation/`. Existing diagnostics are not hidden or represented as a clean global lint result.

Theme Check movement: missing image dimensions **94 → 47**; unused assignments **72 → 65**. All other check counts unchanged. No new diagnostics; existing parser/complexity/settings/naming diagnostics remain.

## Live verification queue

**B1-VISUAL:** Headers One–Five and Footers One–Five at 1440, 1280, 768, 390 and 320px; long store names/menu labels, populated/empty menus, logos and explicit color overrides. Exercise every mode-dependent setting, newsletter success/error, localization, search/cart handoff, real editor add/remove/reorder and keyboard/screen-reader interaction. Header Four: mixed block order, pause/resume and expiration. Global popup: full navigation across pages with real frequency settings and editor preview.

This queue is bounded to actual component states. It is not permission to mark all remaining components reviewed. Batches 2–9 must still receive their individual engineering dispositions before Phase 4 can be completed.

## Batch 2 checkpoint — active banner and CTA behavior

This is a checkpoint within Batch 2, not completion of Batches 2–9. The owner authorized continued implementation; no additional permission is needed for remaining scoped repairs. The PR must remain draft until every retained component has a final disposition **and required validation/live queues are complete**.

- **CTA Featured Product:** the section did not load its variant controller or expose its required root hook. Fixed loading, exact variant matching, sold-out/nonexistent option guards, compare/savings appearing on later variants, and native fallback selection. Quantity remains in one form payload; the existing selling-plan snippet is reused. Moved media navigation into the same abortable lifecycle. Fixed custom-image and trust-badge settings that previously had no effect; removed contradictory mobile split rules and added visible radio focus. Neutralized unsubstantiated product/returns insertion copy. Existing merchant selections are untouched.
- **Carousel Hero:** inline scripts initialized every instance using one section's settings, duplicated clones/listeners, and skipped product controls for single-slide instances. A guarded deferred controller now owns each instance. Decorative clones retain the neighbor preview but are inert, disabled and stripped of duplicate IDs/scripts. Navigation wraps safely under rapid input and reduced motion; pause is persistent and timers/listeners are disposed. Variant fallback/select controls retain exact IDs and availability, with shared Phase 3 cart handling and selling-plan support. Styles containing merchant values are scoped by section identity, including custom-anchor cases. Explicit false carousel settings now stay false.
- **Slideshow Banner:** extracted the active `data-qsl` implementation, with duplicate-boot protection, inactive-slide inertness, persistent pause and unload cleanup. Inactive videos stop; embedded video sources are loaded only for the active slide outside reduced-motion/hidden-document states. Fixed a stray percent character in video markup. The unrelated `assets/section-slideshow.js` is not this section's active implementation and remains for Batch 9 tracing.
- **Current Sale:** section markup uses countdown blocks while its old asset expected obsolete root attributes. Each actual block now owns a timer; initially expired timers do not restart. Existing progress values are no longer overwritten by absent global settings. Expired preset deadlines, store-specific image references and fabricated preset progress were removed from new insertions. A shared local sale-link snippet preserves queries/fragments and encodes discounts for block-level destinations. Script loading is deferred. Real discount redemption remains a live check.

Tests: `banner-commerce.cjs` exercises production assets for option/ID/quantity, availability, invalid combinations, repeated boot, carousel wrapping/pause and editor disposal. `banner-presets.cjs` renders actual section presets twice with empty/product data; Current Sale's actual rendered blocks are executed to verify timers and unset-date editor guidance. Shopify form tags are adapters, not a live checkout claim. All seven Phase 3 regression suites and both Batch 1 suites remain passing. Validation evidence is in `validation/batch2/`.

**B2-BANNERS live queue:** four reviewed sections, all available layouts, 1440/1280/768/390/320px, long merchant text, actual images and video providers, keyboard/screen-reader behavior, real editor add/remove/reorder, carousel false settings and custom anchors. Featured Product/Carousel Hero: available/sold-out variants, quantity, required/optional selling plans, native submission and enabled/disabled AJAX drawer. Current Sale: independent deadlines, discount links with query/fragment and real redemption. Slideshow: video switching, reduced-motion preference changes and progress indicator behavior while paused. These checks remain open; no ready-for-review transition is authorized by this checkpoint.

Batch 2 Theme Check movement from Batch 1: image dimensions 47→46, hardcoded routes 11→10, parser-blocking scripts 9→8, unused assignments 65→64, variable naming 335→334. OrphanedSnippet 88→89: the new `cta-sale-link` is reported despite four explicit render consumers in Current Sale and passing render fixtures. This diagnostic is retained as a demonstrated tracing false positive, not suppressed. Other check counts are unchanged.

Batch 2 code publication: local `66826fe5e3a102a35f8bb82757a7aab2b48e0dde`; remote `a1a2e4f09af36aba81698e55fa6fc0493e10709c`; identical tree `7d40ebeec3c3ba155d87aa19e4c6470d6db3d8db`. No ready-for-review transition.

## Next source findings — not closed

These discoveries are recorded without promoting the components from NOT REVIEWED. Multipurpose Hero has active slide/content snippets, a gradient variable mismatch, broad button selectors, incomplete setting-to-CSS contracts, and unresolved media/product-picker handling. CTA Web3's active implementation is inline; when no URL is supplied it simulates connection without calling a wallet provider. Its similarly named external asset is not the active section implementation. Preserve the component and record a release decision rather than silently building a wallet connector or removing its intended behavior. The proxy snippets and q-banner-base have no literal consumers yet; dynamic tracing remains pending. tw-safelist-banners is a real Tailwind build input and is rendered by Grid Banner.

Current checkpoint: 447 primary components; 49 carry a reviewed disposition (mostly live-pending), 398 remain NOT REVIEWED, including partially inspected components with open engineering findings. Batches 3–9 have not been completed. The 7,830 excluded icons remain separately inventoried with visual review pending.

## Multipurpose Hero checkpoint

Five more register entries (section, two snippets, CSS and JS) now have a live-pending disposition. Register totals: 447 primary components, 393 NOT REVIEWED, 53 REQUIRES LIVE VERIFICATION and one retained owner-review deletion candidate. Earlier checkpoint counts above are historical.

Repairs restore solid/gradient overlays, slide fallback background, split ratios, stacking/reversal, content placement and width, card sizing and transition options. Shared-looking button rules are scoped to this component. Slides use a shared grid area so their content can establish height instead of being clipped by an absolute-only layout. Missing image selections no longer call image_url; selected product objects resolve directly, with legacy handle fallback. Existing image objects provide intrinsic dimensions. Product/info cards receive readable text on their light surfaces, and tiled panel imagery uses a valid background size.

Static and slideshow modes now suspend inactive, hidden-document and reduced-motion media, including deferred embedded video URLs. Unload clears playback and handlers; reload restores exactly one active slide. Pause exposes its pressed state. Default insertion now includes neutral copy and a valid collection destination; a storefront section with no slides is hidden, while the editor displays an instruction. Saved merchant state is unchanged.

Validation: actual Liquid presets and empty-state rendering, product-object resolution, native video/iframe activation and cleanup, preference/visibility changes, duplicate boot, root reload and the existing Phase 3 two-instance hero suite pass. All seven Phase 3 suites and existing Phase 4 fixtures pass. The Phase 3 JSDOM fixture now explicitly simulates a visible browser; its original timer/independence/pause assertions are retained. CSS build, schema/reference/architecture checks and Theme Check evidence accompany this checkpoint. No browser screenshot or live Shopify acceptance is claimed.

**B2-MULTIPURPOSE live queue:** every layout and override at 1440/1280/768/390/320px; long content, split stacking/reversal, desktop/mobile placement, panel colors/contrast and all animation choices. Use actual MP4/YouTube assets and real product selections. Exercise editor add/remove/reorder/block changes and existing parent-slide relationships. This remains open; the draft gate is unchanged.


## Featured Product Hero and Video Hero checkpoint

Register totals: **449 primary components; 391 NOT REVIEWED, 57 REQUIRES LIVE VERIFICATION, one retained owner-review deletion candidate**. Two new controllers account for the inventory increase. Batches 3–9 remain unfinished.

Featured Product Hero now leaves content visible when reveal is disabled or IntersectionObserver is unavailable. Reveal/parallax respect reduced motion and clean up on unload. Product and collection picker objects resolve directly; native forms use the localized cart route, exact selected variant, availability guards, current price and the existing selling-plan snippet. Its three insertion presets now apply their named alignment/panel settings; fallback colors, wrapping and control focus were corrected. Merchant configuration is unchanged.

Video Hero now defers embedded playback, offers a persistent pause control, respects initial reduced motion and hidden/viewport settings, and disposes playback/listeners on editor unload. Explicit user playback remains possible with reduced motion. Missing links no longer produce placeholder buttons; example video URLs and external demo CTA defaults were removed. YouTube embed/shorts URLs resolve along with existing formats. Poster imagery remains available before playback.

Validation in `validation/hero-media/`: all seven Phase 3 and seven Phase 4 suites pass, including actual two-instance Liquid, selected variant/price/availability, sold-out products, collection objects, missing-observer fallback, media suspension and editor disposal. CSS build, structural/reference checks pass; 150 sections and 102 Theme Blocks retained, no deletions, merchant settings hash unchanged. Theme Check remains nonzero for existing debt: image-dimension diagnostics 43→41 and hardcoded routes 10→8; all other check totals unchanged. Structural evidence reports the parent HEAD because it tested the working tree; source-hashes.json identifies the reviewed source.

**B2-HERO-MEDIA live queue (OPEN):** both sections at 1440/1280/768/390/320px, long merchant content, all layouts/presets/color overrides, reveal enabled/disabled, parallax, keyboard and screen reader, real editor add/remove/reorder. Featured Product Hero: available/sold-out variants, required/optional selling plans, localized native submission and AJAX drawer. Video Hero: real MP4/YouTube/Vimeo, poster/empty states, autoplay denial, persistent pause, reduced-motion changes and viewport hiding. No screenshots or live Shopify acceptance claimed. PR remains draft.


## Classic Hero checkpoint

Register totals: **450 primary components; 389 NOT REVIEWED, 60 REQUIRES LIVE VERIFICATION, one retained owner-review deletion candidate**. The controller adds one asset. Counts above describe earlier checkpoints.

Classic Hero's untracked interval and repeated global initialization were replaced by an instance-guarded, abortable controller. It supports static and slideshow modes, persistent manual pause, reduced motion, document/viewport hiding, focus/hover suspension, inactive media suspension and inertness, editor block selection, unload and reload. Embedded videos load on playback only. New CSS is scoped to this component's data attribute; the separate Sub Banner consumer remains unreviewed and unchanged.

Source fixes restore mobile font overrides and breakpoint height settings previously defeated by inline values; hide-on-desktop no longer hides mobile content. Custom anchors are emitted verbatim, decorative images include dimensions and skip missing selections, slideshow dots appear only in slideshow mode, and blank CTA links no longer become hash links. Controls have 44px targets and visible focus. Existing default/preset settings and merchant data are preserved.

Focused validation in `validation/classic/` exercises actual Liquid, two independent instances, exact timer counts, persistent pause, inactive media, reduced motion, editor block selection/unload/reload, blank image/link states and deferred YouTube parameters. Phase 3 hero regression, structural/reference checks and CSS build accompany the fixture. The prior hero-media checkpoint retains the complete seven Phase 3/seven Phase 4 regression run. Structural source_commit is parent HEAD; source-hashes.json identifies the tested working-tree files.

**B2-CLASSIC live queue (OPEN):** 1440/1280/768/390/320px, all nine placement choices, tablet/mobile height and text overrides, contained/full width, long content and custom anchors. Test real MP4/YouTube/Vimeo, slideshow images/overlays/Ken Burns, pause/focus/hover/reduced-motion changes, visibility settings, editor block selection and add/remove/reorder. Real browser layout/media and assistive-technology acceptance remain outstanding. Keep PR draft.

Classic Theme Check: ImgWidthAndHeight 41→40; all other check totals unchanged. Existing diagnostics remain visible in the full report.


## Breadcrumb Banner and Special Offers checkpoint

Register totals: **450 primary components; 387 NOT REVIEWED, 62 REQUIRES LIVE VERIFICATION, one retained owner-review deletion candidate**. No new production asset: Special Offers reuses the existing video controller. Earlier counts are historical.

Breadcrumb Banner now resolves article context explicitly, builds the blog/article chain in both visible markup and JSON-LD, and resolves heading defaults from the actual page context. Titles are escaped in markup; literal less-than characters in JSON are escaped without changing parsed values. Custom anchor characters no longer determine CSS selectors, breadcrumb alignment uses the full row, and long text can wrap. The previously unused overlap setting now applies the same gated margin/padding contract as sibling banners. References to nonexistent panel settings were replaced with their existing fallback values. Blank button destinations are skipped and reduced-motion/focus rules are explicit. Existing saved headings remain untouched.

Special Offers now uses the disposable video controller with its own 768px visibility boundary; original Video Hero keeps its 750px boundary. Native autoplay is deferred, manual pause is available, and hidden/editor-removed media stops. Missing media has a dark fallback surface; custom anchors and accessible labels are escaped, and the split promo receives its editor attributes. Grid offset participates in document flow so positive offsets cannot push cards below the clipped section. The grid and split insertion presets now create their required promo blocks, with neutral copy and a valid primary collection link.

Focused actual-Liquid/DOM fixtures cover eight breadcrumb contexts across both presets, visible/structured-data agreement, article chains, escaped titles, custom anchors and overlap gating; all three Offers presets, promo counts/editor attributes, two independent videos, viewport hiding and unload/reload. Existing Video Hero regression also passes. Evidence is in `validation/context-offers/`; this is not browser or Shopify acceptance.

**B2-CONTEXT-OFFERS live queue (OPEN):** both components at 1440/1280/768/390/320px, long titles, localized page/product/collection/blog/article/cart/search contexts, all color/layout/spacing overrides and keyboard/screen-reader navigation. Breadcrumb: plain/media, overlap on/off with actual header, structured-data validation on rendered storefront URLs. Offers: hero/grid/split, zero/one/multiple promo blocks, both offset extremes, all width/ratio choices, real MP4/poster/image fallback, paused/reduced-motion/visibility states and real editor add/remove/reorder. Original Video Hero should retain its existing breakpoint behavior. Phase 4 remains unfinished and PR stays draft.

Context/Offers validation: structural/reference checks and CSS build pass. Theme Check totals are unchanged from Classic; existing debt remains in the full JSON. The package launcher stalled and was stopped; the installed Shopify CLI completed the recorded run. Structural source_commit identifies the parent HEAD, while source-hashes.json identifies the tested working-tree files.


## Boxed Content Banner checkpoint

Register totals: **450 primary components; 386 NOT REVIEWED, 63 REQUIRES LIVE VERIFICATION, one retained owner-review deletion candidate**. No production asset added. Batches 3–9 remain pending and the rest of Batch 2 is unfinished.

The unscoped h2 selector is now scoped to its section, and generic animation names are component-specific. Buttons no longer depend on animation to override a persistent zero-opacity base: reduced-motion users retain visible CTAs. The pop animation explicitly ends at full opacity, and keyboard focus reveals animated buttons immediately. All three layouts use the existing video controller for reduced-motion defaults, explicit play/pause and unload cleanup; selected images provide posters. Custom IDs are preserved, labels/links escaped, default insertion copy is neutral with a valid collection destination, and long content/controls can wrap. Existing layout families, full-bleed geometry and merchant settings are retained.

Validation in `validation/boxed/`: actual Liquid for card/boxed-background/panel, two instances, existing insertion preset, custom anchors, primary links, reduced-motion media, explicit play and unload, missing-video image fallback, empty media/CTA states, scoped CSS selectors/keyframes and visible animation endpoints. Inventory/schema, references and CSS build are recorded. The preceding context-offers checkpoint contains structural protection checks; this source-only follow-up has no JS asset or architectural change. Source hashes identify the tested files.

**B2-BOXED live queue (OPEN):** card/band/panel at 1440/1280/768/390/320px, container widths, full bleed, min-height/spacing/color overrides, long text, mobile vertical and CTA alignment, all banner/content/button animation choices including zero duration and pop, reduced motion and keyboard focus, actual MP4/poster, editor add/remove/reorder. Browser layout and live Shopify acceptance are not claimed. PR remains draft.

Boxed Content Theme Check completed with unchanged check totals from Context/Offers. Existing debt remains visible; no clean global lint claim.


## Split, Standout and Transparent Hero checkpoint

Register: **451 primary components; 381 NOT REVIEWED, 69 REQUIRES LIVE VERIFICATION, one retained owner-review deletion candidate**. The new Transparent Hero controller adds one asset. This remains a Batch 2 checkpoint; remaining Batch 2 and Batches 3–9 are not complete.

Split Banner: corrected exact spacing-token parsing (`py-20` no longer matches `py-2`), desktop inheritance and inline mobile-padding precedence. Contained mode no longer forces viewport width. Custom anchors remain literal and CSS uses the Shopify section identity. Missing desktop/mobile images and destinations are guarded; no-image background mode retains its readable panel. Visibility, wrapping and reduced-motion rules are explicit. The new-insertion anchor default no longer duplicates `features`; badge insertion text no longer promises a shipping policy. Existing merchant data and setting IDs are preserved.

Standout: false mobile stacking is honored; grid reversal and all split ratios work through scoped rules. Desktop/mobile placement uses CSS properties, not unsupported attribute evaluation. All stylesheet selectors are scoped, including button/width utility rules previously affecting other sections. All six button styles have consumers, theme radius/border/primary/secondary tokens are wired, mobile-only imagery and image dimensions are handled, and native videos use the existing disposable Video Hero controller with a visible pause control.

Transparent: replaced the inline timer/observer script with an instance-guarded controller covering static and slideshow modes, persistent pause, reduced motion, document/setting visibility, inactive media and focus, scroll completion, keyboard navigation and editor block selection/unload. Shared header ownership prevents one hero's removal from clearing another's state; the original header tone/class returns when the last owner leaves. All setting-dependent CSS is scoped independently of custom anchors. Content alignment has usable height, dot buttons are labeled and keyboard-operable, missing CTA destinations are skipped and empty storefront sections do not emit a full-height blank hero. No viewport fallback overwrites merchant-selected height.

Focused actual-Liquid/DOM fixtures cover both Split mobile/container modes and spacing contracts; four Standout layouts and isolated media; Transparent static/slideshow, two instances, timers, video, inertness, persistent pause, header restoration and editor teardown. These are source/mechanism checks, not browser screenshot or Shopify acceptance. Build/schema/reference and diagnostic evidence is under `validation/split-standout-transparent/`.

**B2-SPLIT-STANDOUT-TRANSPARENT live queue (OPEN):** all layouts and settings at 1440/1280/768/390/320px; long content, image-only/mobile-only/empty media, both stacking orders, contained/full widths, custom anchors, color contrast and keyboard/screen reader. Real editor add/remove/reorder; actual MP4 and controls; reduced-motion changes; Transparent touch/trackpad scrolling, header overlap with every retained header, and multiple instances. Saved merchant configuration is unchanged. Keep PR draft.

Theme Check comparison: image dimensions 40→38, complexity 13→12, unused assignments 64→54, naming 334→331. All other counts unchanged, including five pre-existing Liquid parser errors. The Standout snippet orphan warning remains a tracing false positive with two explicit render consumers. No diagnostic is suppressed.


## Web3 Hero motion checkpoint — section review remains open

Register: **452 primary components; 381 NOT REVIEWED, 70 REQUIRES LIVE VERIFICATION, one retained owner-review deletion candidate**. The new motion controller is source-reviewed; Web3 Hero remains NOT REVIEWED because its primary action and complete setting/preset audit are not closed.

Confirmed repairs: false autoplay/effect flags remain false; manual pause no longer restarts on mouseleave; inactive slides are inert; timers, observers, handlers and queued animation frames are disposed on editor removal. Native hero video obeys pause/reduced motion/visibility. Reveal stays visible without IntersectionObserver or JavaScript. Existing parallax, tilt, cursor glow and magnetic-button effects are retained with motion guards and cleanup. Literal anchors no longer have section IDs appended. All generated CSS is instance scoped; heading size/letter spacing and hover values render correctly rather than leaking Liquid syntax. Slideshow card border, background position and content measure are no longer overwritten, and mobile arrows remain reachable. No-media hero content uses the full grid. Existing saved settings are unchanged.

**Unresolved source issue:** the primary hero button emits `data-q-wallet-connect`, but no repository consumer was found. The hook is preserved; no wallet interaction or simulated connection was invented. Define the primary action and finish the remaining setting/preset review before closing this section. This differs from the separate CTA Web3 simulation finding already documented above.

Validation: actual hero/slideshow Liquid, multiple instances, false settings, no-observer reveal, timers, persistent pause, slide inertness, reduced-motion/video, editor selection/unload and scoped CSS. Motion fixture does not certify wallet behavior or real browser geometry. Evidence: `validation/web3-motion/`.

**B2-WEB3-MOTION live queue (OPEN):** actual MP4, every effect and motion-preference change, slideshow navigation, mobile controls, all saved layouts and custom anchors, responsive widths 1440/1280/768/390/320px, long content and real editor lifecycle. Keep draft; the section remains source-incomplete.

Next untouched component: Grid Banner. Source inspection found mobile inline hero height overriding the desktop height rule, autoplaying MP4/YouTube without pause/lifecycle controls, anchor-derived CSS, and inline CTA analytics listeners without editor cleanup. These are recorded findings only; Grid Banner remains NOT REVIEWED and unmodified.

Web3 checkpoint validation: schema/reference checks and CSS build pass; final Theme Check counts are unchanged from Split/Standout/Transparent, including the five pre-existing parser errors. Plain-text reference fixture logs now use `.txt`; the erroneous `.json` extension was corrected and the final rerun no longer reports that artifact-format error.


## Grid Banner and Modern Sub Banner checkpoint

Register: **453 primary components; 379 NOT REVIEWED, 73 REQUIRES LIVE VERIFICATION, one retained owner-review deletion candidate**. Grid Banner's delegated analytics asset adds one component. Remaining Batch 2 work and Batches 3–9 are unfinished.

Grid Banner now exposes the previously missing hero CTA text/link/product/collection/article/accessibility settings using the existing implementation IDs. Desktop hero height is no longer defeated by an inline mobile height. Heights and grid rows are minimums so long content can expand; tile column spans cannot exceed the configured column count. Literal custom anchors are separate from CSS scope; mobile-only image picks render, image/video positioning is wired, and the section tone acts as the default for automatic tiles. Native/YouTube playback uses the existing disposable video controller with a pause button and deferred iframe sources. Analytics uses one guarded document listener, supporting editor insertions without retaining removed sections. Existing event names/payloads and opt-in setting are preserved. Merchant state is unchanged.

Modern Sub Banner now scopes CSS by Shopify section identity independently of the merchant anchor, skips buttons with missing labels/destinations, contains long text, and respects reduced motion on button interactions. Its already-implemented subheading size now has an editor control. All existing panel, zero-value, overlay, alignment, manual overlap and centering controls remain.

Focused actual-Liquid/DOM tests cover Grid's three modes/preset tiles, all span breakpoints, picker CTA destinations and accessible labels, mobile-only images, scoped styles, paused/reduced-motion media, unload and exactly-once analytics. Modern's tests cover all overlay/alignment combinations, two instances, literal anchors, zero panel/button values, typography and explicit button overrides. Build/schema/reference evidence is in `validation/grid-modern/`.

**B2-GRID-MODERN live queue (OPEN):** all layouts/settings at 1440/1280/768/390/320px, long merchant text, image object positions, min-height and row expansion, text contrast, real MP4/YouTube, keyboard/screen reader, editor add/remove/reorder, saved CTA picker objects, click events and store consent configuration. No screenshot/live Shopify acceptance claimed. No section deletion or merchant-state change. Keep draft.

Final validation: all retained schemas and asset JavaScript parse, references pass, CSS build passes, and merchant settings remain byte-identical to the protected baseline. CLI 4.8.0 was restored in the tool cache; both the unchanged local baseline `8250344` and final source were checked with that exact version. Diagnostic counts are unchanged; existing parser errors remain open. Gradient checkboxes are also independent of solid overlay opacity, verified with zero-opacity hero/tile image fixtures. No release-readiness claim.


## Sub-banners and CTA checkpoint — eleven additional sections

Authoritative register: **455 primary components; 368 NOT REVIEWED, 86 REQUIRES LIVE VERIFICATION, one retained DELETION CANDIDATE — OWNER REVIEW**. Batch 2 has 30 sections: 27 now carry a source/fixture disposition with live acceptance pending; CTA Reviews, CTA Web3 and Web3 Hero remain NOT REVIEWED. Earlier numbered checkpoint totals are historical. Batch 2 is **not complete**; Batches 3–9 remain pending.

- **Classic Sub Banner:** replaced the undisposed inline timer with the existing Classic controller. Pause, inactive media/inertness, document/reduced-motion handling and editor teardown now work. CSS is scoped independently of merchant anchors; placement, tablet/mobile heights, extra height, mobile typography, margins and button controls have consumers. Empty/mobile-only slide images and blank links are guarded.
- **Split Sub Banner:** stable CSS scope, escaped custom anchors, restored already-implemented mobile-order/subheading-size controls, effective desktop padding, bounded flex columns, long text and button wrapping, blank destination guards and reduced motion.
- **Transparent Sub Banner:** uses the existing Transparent controller. Measurement-only header overlap preserves header tone. Static/slideshow media, persistent pause, inactive focus, block selection and teardown are covered. Content placement has usable height; all generated styles are scoped. Dots are labeled buttons; heading level is exposed; empty storefront sections are omitted and editor guidance remains.
- **Web3 Sub Banner:** uses the Classic controller for paired media/content panels. Explicit false video flags survive defaults; slides are manually reachable when autoplay is off; inactive media stops and inactive content is inert. Images have dimensions, styles/keyframes are scoped and long content is bounded. This section has ordinary CTA links and is separate from the unresolved wallet contracts in Web3 Hero and CTA Web3.
- **Social Proof / Icon List / Featured Sale:** repaired visibility, video pause/cleanup, narrow-width overflow, accessible image/link output, theme button fallbacks and empty storefront behavior. Social Proof's new-insertion defaults use explicit metric placeholders instead of invented ratings/customer counts. Saved merchant data is unchanged.
- **Audience Source:** one guarded document controller re-evaluates each section after editor insertion; false matching, unmatched sources, default `none`, multiple instances and editor block visibility work. Removed hardcoded anchor CSS, restored exact gap controls, bounded columns, supplied local button variants and video lifecycle.
- **Card Stack:** background now fills the root instead of a zero-height sibling. Removed global visibility selectors, respected theme button variants, retained grid/flex modes, corrected gutter units, guarded empty CTAs, added media controls and removed the duplicate anchor default for new insertions.
- **Split Content:** uses selected video provider type/ID for embeds, exposes a hosted-video pause control, uses bounded grid tracks and an actual flex container for content alignment. Gradient alpha no longer relies on appending hex suffixes. New insertion does not claim free shipping/customer totals or assume an About page exists.
- **CTA Banner:** checked Classic/Web3/Modern/Product/Newsletter modes. Fixed CSS scope, zero-radius and background-opacity precedence, hover-token overrides, long content and product-image bounds. Newsletter inputs/forms use section identities and native required email/consent validation; customer tags and UTF-8 fields are present. External action/method and return-to settings remain. Button/product/form block editor hooks are restored. Actual Shopify submissions remain a required live check.

**Shared dependencies:** existing Classic/Transparent controllers were extended locally and all original host suites repeated. New `cta-icon` maps existing bundled CTA icon paths to symbols in deployed `assets/icons.svg`, instead of nonexistent/excluded per-icon uploads. Custom flat asset filenames and Social Proof external hosts are retained. All preset-referenced symbols were checked in the actual sprite; this does not independently approve the entire sprite or the 7,830 excluded icons. Audience Source adds one JS asset; no architecture removals.

**Remaining source work:** CTA Reviews intake found unscoped CSS, anchor mismatch, forced autoplay, incomplete slideshow disposal, inactive focus/media issues and duplicate app widget IDs. Its manual layout/settings/preset/structured-data review remains open. CTA Web3 still simulates connection without a provider when no URL is set. Web3 Hero still has an unconsumed wallet hook and remaining settings/preset work. Neither wallet component is represented as working integration. The banner proxy snippets, q-banner-base and Tailwind safelist also remain unreviewed supporting entries; their independent consumer closure is outstanding.

**B2-SUB-CTA live queue (OPEN):** all eleven sections and every layout at 1440/1280/768/390/320px; long text; real images, aspect/crop modes and native/YouTube/Vimeo media; pause, reduced motion and visibility changes; keyboard/screen-reader behavior; literal anchors; real Theme Editor add/remove/reorder/block selection. Check external sprite rendering, source/UTM navigation and custom icon hosts. CTA Banner: real Shopify customer/newsletter success/error, required consent/email, configured external endpoints and redirects; actual product picker objects and destinations. Source fixtures are not responsive screenshots, app integration approval or Shopify certification.

Validation evidence is in `validation/sub-banners-static-cta/`: all seven Phase 3 suites; all completed Phase 4 suites including six new focused files; schema/reference/architecture checks; CSS build; source hashes and full Theme Check movement. Merchant settings SHA-256 remains `ffc244582d58f3e6e07713abd6ab1de2cdef7ba109fc00a60cef886235b6b499`. All 150 sections, 102 Theme Blocks and protected builders/product/header/footer/Quick View architecture are retained. PR #22 stays draft; no merge or Shopify publication.

Checkpoint validation result: **7/7 Phase 3 and 22/22 Phase 4 suites pass**, plus architecture/schema/reference checks and CSS build. Theme Check: missing image dimensions 38→33; unused assignments 54→42; naming 331→325. OrphanedSnippet 89→90 is the new `cta-icon` tracing false positive: three explicit render consumers and fixture-verified sprite symbols. Other diagnostic counts are unchanged, including five pre-existing parser errors; CTA Banner retains its existing complexity warning. Nothing is suppressed or presented as a clean global lint result.

### GitGuardian incident clarification

The owner-supplied incident screenshot identifies the SHA256 field on the retained empty `sections/header-basic.liquid` row. Its value was recomputed from the zero-byte file and matches exactly; it is a file integrity checksum, not an API credential. The adjacent prose ended in a word containing `auth`, followed by a CSV comma and the checksum, matching the generic detector assignment shape. Reworded that note to “Retained pending owner decision.” while preserving the integrity value and deletion-candidate disposition. No credential rotation or history rewrite is warranted for this confirmed checksum. Historical incident closure remains a GitGuardian dashboard action; no detector exclusions were added.

### CTA Reviews motion/layout checkpoint — partial, NOT REVIEWED

Continued Batch 2 with CTA Reviews. Removed its inline per-render global slideshow initializer and introduced a guarded, abortable asset. Explicit autoplay false is respected; manual navigation and pause remain paused after hover/focus, reduced motion stops automatic playback, editor selection reveals the chosen slide, and unload releases timers/listeners/media. Review videos remain user-controlled and stop automatic rotation when played. Decorative video has a separate visible motion control. No-script slideshow content remains readable.

All stylesheet selectors are now section-scoped; inline settings use the Shopify wrapper independently of literal merchant anchor IDs. Grid column controls cover every allowed combination, filmstrip mode gains its missing scroll layout, card selectors match the markup, and text/buttons/grid children have bounded sizing. Corrected inherited description alignment and CTA flex alignment. Blank CTA links are suppressed. Plain text and attributes are escaped; review JSON uses JSON serialization and a literal less-than escape so merchant text cannot terminate its script. New insertion presets no longer fabricate reviewer identities/testimonials/dates or default to verified-purchase and SEO claims. Existing merchant state was not edited.

The section and its CSS/JS deliberately remain NOT REVIEWED: legacy Judge.me/Loox/Yotpo/Stamped initialization and singleton IDs require contract review; photo/video/location/verified-purchase settings still differ across the four layouts; complete media/settings/schema parity remains open. No provider support or live certification is claimed. CTA Web3 and Web3 Hero remain open for their previously documented action/provider findings; no wallet functionality was invented.

Validation: all seven Phase 3 and all 23 Phase 4 suites pass, plus structural/reference checks and CSS build. Actual Liquid fixtures cover four modes, all four insertion presets, two instances, false autoplay, pause/reduced motion/visibility, editor cleanup, safe text and JSON. Evidence: `validation/reviews-motion/`. Live queue B2-REVIEWS: responsive views at 1440/1280/768/390/320px, manual review media, actual installed review apps and multiple instances, keyboard/screen reader, editor lifecycle and structured-data acceptance after source closure.

Current register: **456 primary components; 369 NOT REVIEWED; 86 REQUIRES LIVE VERIFICATION; one retained DELETION CANDIDATE — OWNER REVIEW**. The increase includes the new disposable Reviews controller, which remains NOT REVIEWED with its host. Batch 2 remains 27 of 30 section source/fixture dispositions; supporting asset closure and live acceptance remain separate. All 150 sections, 102 Theme Blocks, protected architecture and merchant settings are retained. PR #22 remains draft and must not merge automatically.

Shopify CLI 4.8.0 Theme Check diagnostic counts match the preceding sub-banner/CTA checkpoint exactly. Existing parser and other lint debt remains; the command exits 1 for that existing debt.

### Batch 2 continuation — review content parity and honest Web3 actions

CTA Reviews now renders all four layout modes through `cta-review-card.liquid`, replacing four inconsistent markup copies. Reviewer photos, locations, verified badges, review video, images and product names/links now share one contract. Missing media no longer reserves the slideshow image column. Images use actual alt text with product fallback; review videos remain manual. Review JSON includes only entries with a product URL, reviewer name and review text, clamps the rating and uses the shop origin for product URLs. Free-form display dates are not represented as machine-readable dates. Existing editor, pause, motion and safe-JSON protections remain.

CTA Web3 no longer declares a wallet connected after an ordinary click. Existing primary URLs are real links, absent actions produce editor-only setup guidance, and the retained simulation setting only previews a disabled appearance inside the editor. Its previously unused JS asset is now the single disposable progressive-reveal controller; content remains visible without JS/IntersectionObserver and with reduced motion. Removed dead inline wallet/event/timer code. Added bounded text/media/grid sizing, scoped buttons and focus states, deployed sprite references with a text fallback for the missing WalletConnect glyph, and neutral insertion content without presumed uploaded images or onboarding pages. Existing merchant state was not reset.

Web3 Hero's unconsumed wallet hook is replaced by a primary URL setting and native navigation. A blank destination omits the storefront action and displays configuration guidance only in the editor. No provider, wallet connection or verification is simulated. The current source behavior is now accurately represented in the merchant controls.

**Remaining source findings:** Reviews still needs actual contracts for the legacy Judge.me/Loox/Yotpo/Stamped widget mounts and singleton IDs. CTA Web3 needs legacy nested custom-icon/filled-library compatibility closure. Web3 Hero still needs the remaining complete settings/preset audit, including its unused `button_mode` setting. These sections and the new shared review snippet remain NOT REVIEWED; no family completion or live certification is inferred from partial repairs. The unused legacy CTA Web3 stylesheet remains separately unreviewed for asset closure.

Validation: all **7 Phase 3 and 24 Phase 4 suites pass**, plus schema/reference checks and CSS build. Fixtures include actual review content in all four modes, all four review presets, CTA Web3's preset and six layout/background combinations, configured/absent links, editor-only simulation, bundled/custom icon rendering, two-instance reveal, reduced motion and editor cleanup, and Web3 Hero primary-link behavior. Evidence: `validation/reviews-web3-actions/`. Live queues remain those documented above, with actual legacy icon settings and native destination navigation added to Web3 acceptance.

Current authoritative ledger: **457 primary components; 370 NOT REVIEWED; 86 REQUIRES LIVE VERIFICATION; one retained DELETION CANDIDATE — OWNER REVIEW**. The extra component is the shared review-card snippet replacing duplicated markup, not an approved component. Batch 2 remains unfinished at 27 of 30 section source/fixture dispositions. All 150 sections, 102 Theme Blocks, both builders, protected product/header/footer/Quick View architecture and merchant settings are retained. PR #22 remains draft.

Shopify CLI 4.8.0 diagnostic movement for this continuation: UnusedAssign 42 → 41; OrphanedSnippet 90 → 91. The added orphan warning names `cta-review-card.liquid`, despite its four explicit render calls in CTA Reviews and passing actual Liquid rendering in every mode; retained as documented tracing evidence, not hidden by an exclusion. All other counts, including the five pre-existing parser errors, are unchanged.

### Batch 2 engineering/register closure — live acceptance still open

All **30 Hero / Banner / CTA sections** now have engineering dispositions. The entire registered Batch 2 family contains **62 components: 57 REQUIRES LIVE VERIFICATION, four RETAINED — DORMANT SOURCE REVIEWED, and one REVIEWED — BUILD ONLY**. There are no remaining NOT REVIEWED entries within this family. This closes the source/fixture pass, not live acceptance or Phase 4. Earlier checkpoint findings/counts above are historical and are superseded by this closure entry.

**Reviews integration repair:** removed hard-coded empty Judge.me/Loox/Yotpo/Stamped containers and their duplicate singleton IDs. Added standard `@app` schema support and `{% render block %}` alongside section-defined manual reviews. App blocks render once in their own container; filtered manual review blocks alone control slides, dots, cards and manual review JSON. The legacy selector is retained with editor migration guidance. Repository merchant/template inspection found only manual mode (`none`) configured; no merchant state was rewritten. This is the platform integration boundary described in [Shopify's app-block documentation](https://shopify.dev/docs/storefronts/themes/architecture/blocks/app-blocks). It does not install an app or promise that every vendor exposes every widget as a block. Any legacy merchant selection requires adding the installed app's actual block. Real provider rendering, app embeds and installation are explicitly live acceptance.

**Web3 Hero settings closure:** repaired all seven previously unconsumed fields: background description, gradient endpoints, three glow colors, and primary button fill. Added the missing gradient button effect with pause/reduced-motion handling; outline mode, zero radius/border and disabled shadow now have consistent effects. Corrected content-height alignment, escaped remaining plain text/link fields, restored readable no-JavaScript slides and editor unload fallback, bounded/wrapped slide dots, and supplied neutral heading-only insertion slides. The primary action remains ordinary configured URL navigation; no wallet provider or connection claims were introduced. Tests exercise all select options and range endpoints in hero/slideshow modes, plus targeted output and lifecycle assertions. Presence of a setting reference alone is not treated as approval.

**CTA Web3 compatibility closure:** normalized legacy bundled `icons/...` and Tabler subdirectory references to deployed symbols. Existing filled/outline choices are documented aliases to the shared bundled glyphs; custom flat SVG uploads retain their tint through masks. Missing WalletConnect imagery and unsupported arbitrary nested custom paths receive text fallback; unsupported custom paths also get editor guidance. Removed the unused media grid cell when no image exists. Connected-state simulation remains editor-only and disabled. Storefront actions require real configured links.

**Supporting components:** reviewed the two one-line Featured Product Hero content proxies and retained them without deletion because no current consumer is found. Reviewed the inactive legacy CTA Web3 stylesheet; it remains retained and unloaded. Scoped dormant `q-banner-base` variables and all selectors/media-query selectors to the owning Shopify section, with no output without section context. These four dormant entries are not certification for future reactivation; any future host needs its own review. Converted the Tailwind banner safelist to a Liquid comment: the build scanner still sees its classes, while Grid Banner no longer emits hidden inventory markup. Verified generated dynamic grid spans after the CSS build.

**Validation:** all seven Phase 3 and all **25 Phase 4** suites pass; schema/reference/architecture checks and CSS build pass. Shopify CLI 4.8.0 diagnostic counts are unchanged from the immediately preceding checkpoint, including existing parser errors and the documented shared-review-card orphan-tracing false positive. Evidence is in `validation/batch2-closure/`. The app rendering fixture uses an explicit platform adapter and cannot certify an installed app. The authoritative register now totals **457 primary components: 358 NOT REVIEWED, 93 REQUIRES LIVE VERIFICATION, four retained dormant/source-reviewed helpers, one build-only reviewed helper, and one retained deletion candidate requiring owner review**. All 150 sections, 102 Theme Blocks, both builders, protected product/header/footer/Quick View architecture and the exact merchant-state hash remain intact.

**B2-FINAL live acceptance queue:** every Batch 2 active component must be exercised with its host at 1440, 1280, 768, 390 and 320px; long/translated content; blank/populated media and links; all conditional settings and insertion presets; two simultaneous instances; keyboard and screen-reader navigation; reduced-motion and pause persistence; no-JavaScript fallback; and actual editor insertion/removal/reorder/block selection. Retain the previously itemized product/cart, offer/countdown, newsletter submission and external media checks. Reviews additionally requires an installed app block, provider embed/configuration, app-only/manual/mixed compositions and no duplicate widget instances. Web3 sections additionally require real configured destinations, uploaded custom SVG/tint and legacy icon mapping checks. No wallet connection, signature or provider validation is claimed. Dormant helpers require only confirmation they remain unloaded; future activation is outside this approval. Record actual results in the register rather than promoting fixtures to live certification.

PR #22 remains DRAFT. Batch 3 can begin as the next engineering batch; Phase 4 cannot be marked ready until all batches and the required live queues are complete. No merge or Shopify publication was performed.

Final app-only fixture also verified that Reviews' configured CTA is rendered independently of manual cards, missing destinations emit no action spacing, and an app-only instance emits no empty manual-review JSON. The targeted Reviews suite was rerun after this correction; `final-review-cta.txt` records that result. Full regression evidence precedes this localized correction; unaffected suites were not redundantly repeated.

### Batch 3 start — Collection List source and fixture checkpoint

Batch 3 is ACTIVE. `main-collection-list`, `collection-list-card`, and the section's CSS/JS now have source/settings/preset and fixture dispositions of **REQUIRES LIVE VERIFICATION**. The other discovery components remain NOT REVIEWED. This is the first four-component checkpoint, not Batch 3 closure.

**Repairs:** automatic collection discovery now iterates actual collection objects, following [Shopify's collections object contract](https://shopify.dev/docs/api/liquid/objects/collections), instead of treating each collection as a key/value pair. Manual empty blocks are skipped; the automatic maximum remains automatic-only as described by the schema. The page heading and empty state now share the section's width/padding container. Card output safely omits missing destinations, escapes plain text and URLs, and uses one native link without a misleading nested button role. Blank decorative button labels produce no output.

Carousel controls now have one disposable controller per instance, editor unload/reload and block selection handling, resize/image-load boundary updates, reduced-motion navigation and RTL scroll direction. Controls remain hidden until initialized; the native scroller and links remain usable without JavaScript. Mobile column widths no longer have a conflicting minimum, peek now exposes following content, adapt media preserves its natural ratio, long text wraps, and focus styling stays visible within clipped cards. Live browser layout/scroll behavior remains unverified.

**Validation:** all **seven Phase 3 and 26 Phase 4 suites pass**, including the new actual-Liquid/DOM Collection List suite. Tests cover manual/automatic sources in grid/carousel, automatic limits, empty state, insertion defaults, headings, overrides, control suppression, two instances, duplicate script evaluation, LTR/RTL bounds, reduced motion, resizing and editor lifecycle. Schema/reference checks and CSS build pass. Shopify CLI 4.8.0 Theme Check has no added or removed diagnostics compared with Batch 2 closure when matched by file/check/severity/message; existing lint/parser debt remains and the command exits 1. Evidence: `validation/batch3-collection-list/`, including source hashes, register totals and diagnostic movement.

**B3-LIST live acceptance queue:** test manual and automatic collections, empty and populated insertion, all layout/column/spacing/ratio/display settings, long translated content and image overrides at 1440/1280/768/390/320px. Verify actual mobile peek, native touch/trackpad and RTL scrolling, arrow bounds after resize/image load, keyboard and screen-reader names/focus, reduced-motion changes, no-JavaScript fallback, two instances, and real editor insert/remove/reorder/block selection. Fixtures are not Shopify/browser certification.

The register now contains **457 primary components: 354 NOT REVIEWED, 97 REQUIRES LIVE VERIFICATION, four RETAINED — DORMANT SOURCE REVIEWED, one REVIEWED — BUILD ONLY, and one DELETION CANDIDATE — OWNER REVIEW**. All 150 sections, 102 Theme Blocks, both builders and protected product/header/footer/Quick View architecture are retained. `config/settings_data.json` retains SHA256 `ffc244582d58f3e6e07713abd6ab1de2cdef7ba109fc00a60cef886235b6b499`. The excluded icon inventory is unchanged and does not imply approval.

Next review target: Collection Grid feed and its shared Collection Card, including collection selection/filtering and host-dependent settings. These remain NOT REVIEWED until their findings, callers and presets are resolved. Batch 1 and Batch 2 live queues remain open. PR #22 remains DRAFT; no merge or Shopify publication.

### Batch 3 continuation — Collection Grid filters and shared-card repairs

Implemented the next repair checkpoint in `feeds-collection-grid` and its shared `collection-card`. Both deliberately remain **NOT REVIEWED**: the feed's five card-preset choices emit classes without matching repository styles, and complete settings/inheritance review of the Spotlight host remains open. Successful repair fixtures do not close those findings.

The feed now uses one rendering/filtering loop for manual and automatic collections. Replaced the ineffective `map: 'strip'` property mapping with case/space normalization and exact comma-separated handle membership; include/exclude precedence and title matching are covered by fixtures. Cards without collection destinations are omitted without empty grid cells. Automatic mode uses the global collection source, and its default empty message no longer falsely limits availability to the collection-list template. Existing saved merchant text is untouched. Percentage opacity is now passed to the shared card as a fraction, restoring its actual CSS effect; fallback matches the schema's 85 percent default. Header and empty-state plain text are escaped, long grid content is bounded, and the static grid no longer advertises unnecessary live-region updates.

The shared card now guards missing destinations instead of using an invalid top-level `break`. Escaped collection titles, overlay content, URLs, labels and the Spotlight host's text/textarea subtitle/body inputs; the feed's richtext subheading remains richtext. CTA destination fallback and separate links are retained. Added bounded/wrapped card content, border-box CTA sizing, inset focus outlines and reduced-motion handling. The snippet remains shared by both hosts; no duplicate card implementation was introduced.

**Validation:** all seven Phase 3 and **27 Phase 4 suites pass**, including new actual-Liquid/DOM fixtures for both sources, exact handle matching, case/spaces, include/exclude precedence, title filtering, missing collections, empty results, opacity endpoints, false display settings, safe plain text, CTA fallback and all three Spotlight layout render contracts. Schema/reference checks and CSS build pass. Shopify CLI 4.8.0 diagnostic signatures and counts are unchanged from the preceding Collection List checkpoint; existing lint/parser debt remains. Evidence: `validation/batch3-collection-grid/`. The Spotlight rendering fixtures establish shared-card compatibility only, not complete settings/preset or live acceptance.

**B3-GRID live queue (after source closure):** manual/automatic source and filter settings, opacity/colors, images and long content at all five standard viewport widths, keyboard/screen reader, reduced motion, simultaneous feed/Spotlight instances and real editor insertion/removal/settings changes. Complete the preset and host inheritance findings before disposition promotion.

Register totals are unchanged: **457 primary components; 354 NOT REVIEWED; 97 REQUIRES LIVE VERIFICATION; four retained dormant/source-reviewed; one build-only reviewed; one retained deletion candidate requiring owner review**. All 150 sections, 102 Theme Blocks and protected architecture remain. The merchant settings hash is unchanged. Batch 3 remains active, all earlier live queues remain open, and PR #22 remains DRAFT. No merge or Shopify publication.

### Batch 3 continuation — Collection Card presets and Spotlight source closure

Closed the preceding source findings for `feeds-collection-grid`, `collection-card` and `featured-content-collection-spotlight`. These three entries now require live verification; this does not close Batch 3. Earlier partial-checkpoint notes are historical.

The existing five card presets now have shared CSS treatments: catalog keeps the standard media ratio, editorial uses a taller ratio, tech uses a tighter radius and stronger border, minimal removes default chrome while respecting explicit colors, and pill rounds the card/media. Existing image-aspect, typography, density and color controls remain available. Existing primary/secondary/outline/ghost/link CTA choices now have corresponding card styles and scoped Spotlight footer styles instead of depending on absent global classes. Visual quality and merchant-selected contrast remain live acceptance.

The shared card no longer emits default inline typography variables when callers omit those parameters, so Spotlight's configured title/subtitle sizes inherit correctly. Explicit feed typography still overrides the shared defaults. Spotlight's media ratio takes precedence over the card preset default. Connected the media overlay to a noninteractive image overlay, applied muted/title colors, and made the existing shadow control independent of the custom-color checkbox. Zero radius/shadow remain valid values.

Spotlight now counts only valid collections, selects the first valid featured collection and retains all remaining valid blocks in split mode instead of silently dropping blocks four through six. Fewer than three valid entries use its existing grid fallback. All layouts pass editor block attributes into the shared card. The native carousel is named/focusable, sized relative to its container, and remains JavaScript-free. Added editor-only empty guidance, escaped plain heading/link text, suppressed blank-label global CTAs, scoped host selectors, bounded long content and retained reduced-motion handling.

**Validation:** all seven Phase 3 and **28 Phase 4 suites pass**, plus schema/reference checks and CSS build. The new Spotlight suite exercises 0/1/2/3/6 valid blocks with blank blocks, all three layouts, no missing/duplicated destinations, editor identities, typography inheritance, zero values, blank CTA, two scoped hosts, all select/checkbox values and range endpoints, and preset CSS computed-style checks supported by JSDOM. Input traversal alone is not visual approval. Existing Collection Grid repair fixtures and both shared-card host contracts continue to pass. Evidence: `validation/batch3-spotlight/`.

**B3-SPOT/B3-GRID live acceptance:** all card/CTA choices, layout and conditional settings, colors/contrast, typography, density, overlay and shadow at 1440/1280/768/390/320px; zero/one/two/three/six collections including blanks; long translated content and varied image ratios; simultaneous feed and Spotlight instances; native touch/RTL carousel navigation; keyboard/screen reader and focus; reduced motion and no-JavaScript behavior; real editor insertion, selection, reorder, removal and preset insertion. Source/DOM fixtures do not certify live Shopify/browser behavior.

Authoritative totals: **457 primary components; 351 NOT REVIEWED; 100 REQUIRES LIVE VERIFICATION; four retained dormant/source-reviewed; one build-only reviewed; one retained deletion candidate requiring owner review**. All 150 sections, 102 Theme Blocks, both builders and protected product/header/footer/Quick View architecture remain. Merchant state retains its recorded hash. Next Batch 3 target: Collection CTA, then the remaining discovery/search/collection hosts. All earlier live queues remain open; PR #22 remains DRAFT, without merge or Shopify publication.

Theme Check evidence for this checkpoint: no new errors; one new `BlockIdUsage` warning in Spotlight for comparing each supporting block's current runtime ID to the selected featured block's current runtime ID. Neither side is a literal ID or persisted identifier; the comparison only excludes that same block within the current render. This diagnostic is retained and documented, not suppressed. All other diagnostic signatures/counts match the previous checkpoint; existing lint/parser debt remains.

### Batch 3 continuation — Collection CTA source and fixture checkpoint

`call-to-action-collection` now has a source/settings/preset disposition of **REQUIRES LIVE VERIFICATION**. All four layouts and four insertion presets are retained. Batch 3 remains active.

Replaced the simulated, JavaScript-dependent whole-card link and nested CTA with one native anchor and a decorative CTA label. This preserves normal navigation, modifier clicks and no-JavaScript use with a single tab stop per card; the removed inline listeners no longer need editor initialization/cleanup. Missing collection destinations are skipped, plain text/URLs are escaped, blank CTA labels are omitted, and editor block attributes remain. Empty insertion has setup guidance inside the editor.

Image selection now honors the existing first-product setting: custom override, then the selected source, then collection featured-image fallback. Automatic image ratios use the selected image's actual aspect ratio when available. Responsive image sizes reflect configured columns. Restored four distinct gap choices and removed the self-referencing gap variable. Theme fonts/colors/spacing/container/radius inherit through the existing theme tokens instead of being reset locally.

Removed unscoped fixed masonry spans and duplicated patch rules. Masonry now uses native CSS columns with unbroken cards and adjustable image minimum heights; card content can grow. **Its visual order is top-to-bottom within each column**, following the DOM, and requires merchant/live acceptance. The mobile masonry-overlay single-column behavior remains. Overlay text participates in sizing, long titles/buttons wrap, and text clamping/extra elevated-card spacer no longer hides content or reserves a white footer. All selectors are section-scoped; focus and reduced-motion behavior are covered at source level.

**Validation:** all seven Phase 3 and **29 Phase 4 suites pass**, plus schema/reference checks and CSS build. The new actual-Liquid/DOM suite covers four layouts × three CTA modes, native link semantics, custom/product/collection media precedence and fallback, automatic ratio, singular/plural/hidden counts, blank selections/labels, zero overlay opacity, four gap outputs, all select/checkbox values and range endpoints, all four insertion presets, editor-only guidance, scoped CSS and two-instance identities. Traversing settings and inspecting DOM/CSS cannot certify real browser layout. Evidence: `validation/batch3-collection-cta/`.

**B3-CTA live queue:** all layouts/presets and conditional settings at 1440/1280/768/390/320px; 0/1/12 selected collections, blanks, varied and missing images, source fallback and long translated titles/labels; native column ordering and card fragmentation; background/card overlay opacity and color contrast; keyboard/screen reader, focus, modifier-click navigation, reduced motion and no-JavaScript behavior; two instances and actual editor insertion, selection, reorder and removal. Native masonry geometry and Shopify product-image access remain live acceptance requirements.

Authoritative register: **457 primary components; 350 NOT REVIEWED; 101 REQUIRES LIVE VERIFICATION; four retained dormant/source-reviewed; one reviewed build-only; one retained deletion candidate requiring owner review**. All 150 sections, 102 Theme Blocks and protected architecture remain; merchant state retains its recorded hash. Next Batch 3 target: Main Search and its host contracts, followed by the remaining collection/discovery hosts. All previous live queues remain open. PR #22 remains DRAFT; no merge or Shopify publication.

Shopify CLI 4.8.0 Theme Check for Collection CTA has no added or removed diagnostic signatures compared with the Spotlight checkpoint. Existing parser/lint debt and the documented runtime-block-ID warning remain; exit 1 is not a clean Theme Check certification.

### Batch 3 continuation — Main Search form and product-layout repairs (partial)

Main Search remains **NOT REVIEWED**. Its initial repair checkpoint fixes the nonexistent `search_mode` lookup by using `search_default_form_type` and `enable_predictive_search`; an explicit disabled master switch renders the static form. The configured default placeholder now reaches this page. Removed the second empty-state form so the original query stays in one search field. Escaped section-owned titles, excerpts, labels and URLs, plus the legacy predictive helper's submit label/action. Empty-state article navigation now requires an existing `news` blog and uses its localized URL; the home link is labelled Home.

Product list mode previously passed empty `section.settings` to a Collection Simple card whose styles only apply under the collection host. It now reuses the existing search grid's shared product-card call, restoring prices and shared controls, with section-scoped desktop list geometry. This does not certify the shared product card's commerce contract; that component remains NOT REVIEWED. Missing article/collection images no longer reserve an empty media column. Added bounded long content, border-box sizing and focus styles. The CSS rebuild removes only the unused `.grow` utility that had been retained by the old Collection CTA comments; no new generated rules were added.

**Open source findings:** result types are currently hidden after pagination, so disabled sources can leave blank pages while the header reports a nonzero server count. Full search accepts product/page/article types, while collection suggestions belong to predictive search; see [Shopify storefront query parameters](https://shopify.dev/docs/storefronts/themes/navigation-search/search) and the [search object contract](https://shopify.dev/docs/api/liquid/objects/search). No collection-search backend was invented. Request-time type filtering and all-disabled handling need closure across search entry points. Shared product-card actions/settings still require host review.

The host review also confirmed that `predictive-search.js` ignores the existing group-heading, thumbnail, view-all and product-layout display settings, and the legacy `predictive-search` snippet does not pass those options. **Both entries are reopened to NOT REVIEWED.** Earlier lifecycle/request repairs and their passing fixtures remain, but they do not close this settings finding. This is an explicit correction to Batch 1's prior source disposition, not live acceptance.

**Validation:** all seven Phase 3 and **30 Phase 4 suites pass**, plus structural/reference checks and CSS build. The new actual-Liquid/DOM fixture covers both form choices/master-switch states, both product layouts and prices, localized search actions, retained query/placeholder, escaped section text, initial/empty/result states, existing-blog recommendations, one empty-state form and two-instance IDs. It also reproduces the still-open source/count mismatch. Shopify globals and already-paginated arrays are explicit fixture adapters; live pagination and commerce behavior are not certified. Theme Check signatures/counts match the preceding Collection CTA checkpoint. Evidence: `validation/batch3-main-search/`.

**B3-SEARCH live queue after source closure:** real Shopify result types/counts/page navigation, mixed/empty/all-disabled sources, predictive requests and display controls, product navigation/add/quick-view, localized routes, long content at all five viewport widths, keyboard/screen reader, multiple instances and actual editor load/unload. Main Search and the reopened predictive contracts must resolve their source findings before promotion to live-only status.

The register now totals **457 primary components: 352 NOT REVIEWED; 99 REQUIRES LIVE VERIFICATION; four retained dormant/source-reviewed; one build-only reviewed; one retained deletion candidate requiring owner review**. The increase in unreviewed entries is the two explicitly reopened search components. All 150 sections, 102 Theme Blocks, protected architecture and merchant-state hash remain intact. Collection CTA remains source-reviewed/live-pending at the preceding checkpoint. Continue Main Search's filtering/predictive contracts next. PR #22 remains DRAFT; no merge or Shopify publication.


### Batch 3 continuation — shared search requests, settings and lifecycle

**Live acceptance schedule:** the owner has deferred all live Shopify storefront/theme-editor verification until engineering through Batches 4–9 is done. Continue source repairs and offline regressions without waiting for merchant testing. All existing B1/B2/B3 live queues remain required before PR readiness. This changes scheduling, not the meaning of a completed source review or release acceptance.

Main Search, Header Two's two native forms and both shared form snippets now build supported full-search `type` requests through one small reusable `search-source-types` helper. Source choices are applied when requesting Shopify results. Main Search renders the returned page intact instead of hiding types after pagination; the server count and native pagination stay together. Direct/external URLs retain Shopify's requested/default result types; the theme does not relabel or fabricate their count. Standalone forms and Theme Blocks remain subject to their assigned batch reviews. Full search supports products/articles/pages; collection suggestions remain predictive-only. See [Shopify's storefront search parameters](https://shopify.dev/docs/storefronts/themes/navigation-search/search).

When all supported full-search types are disabled, shared forms submit no query and have disabled submit controls; Main Search presents no misleading count or empty results grid. Collections-only predictive search still offers collection destinations, with no unsupported full-search or view-all request. Its no-JavaScript guidance links to the existing collection index when available. All-off search has an explicit unavailable state. Ordinary supported searches retain native form submission without JavaScript.

The legacy `predictive-search` helper now delegates to `search-form-predictive`, preserving its existing entry point while eliminating divergent settings/defaults. The predictive master switch falls back to the shared static form in every shared host. The popup now honors its existing static/predictive choice instead of always rendering static search. Labels, identifiers and form actions are escaped. Header Two retains its native layout and applies the same type mapping to both forms.

Predictive group headings, thumbnails, view-all visibility and product list/compact/grid choices now affect output. All thirteen existing panel/item/color/size variables have CSS consumers; minimal/soft/outline/glass styles, zero radius, wrapping and visible focus are retained. API limits now apply per resource type, using the maximum enabled type limit, then each group's configured cap. Product prices use the API's decimal currency amounts, including numeric/string values and free products, instead of treating them as cart API cents. See [Shopify's Predictive Search API](https://shopify.dev/docs/api/ajax/reference/predictive-search). Actual currency/localization, contrast and geometry remain live requirements.

The controller now has one asset initializer and disposable per-root listeners. New queries clear stale keyboard options immediately; superseded/aborted responses and late failures cannot overwrite or close current results. IME input waits for composition completion. Arrow keys include the view-all option; Tab clears the popup and Escape first closes suggestions. Editor unload cancels requests/timers and removes handlers; reload binds once. Popup closure cancels pending suggestions, and the next Escape closes the dialog. Its results participate in a scrollable dialog instead of clipping outside it. Existing focus containment and opener restoration remain covered.

The dormant `search-controller` data hook has no repository render/include consumer or reader. Its obsolete mode/limit lookups now match current schema-backed settings, explicit false and bounded defaults; four form-mode/master combinations are fixture-tested. It remains retained and inactive, with future host review required before use. This adds one retained dormant/source-reviewed disposition outside Batch 2; no component was deleted.

**Source disposition:** the two reopened predictive components return to REQUIRES LIVE VERIFICATION. The new type-mapping helper and touched active search hosts/assets share that queued disposition. **Main Search remains NOT REVIEWED** for its shared product-card commerce/settings dependency: raw product title output, localized cart action, variant/add behavior and quick-view integration need the Batch 4 host review. This checkpoint does not certify the product-card family or move ahead into its broader review.

**Validation:** all seven Phase 3 and **31 Phase 4 suites pass**, along with schema/reference checks and CSS build. Shopify CLI 4.8.0 evidence is retained in `validation/batch3-search-contracts/`. New fixtures cover all sixteen source combinations across the static/current/legacy forms, native typed Header Two requests, both master states, four panel styles × three product layouts through both predictive entry points, false display toggles and zero radii, emitted CSS-variable consumers, decimal prices, per-type limits, escaped text, keyboard/IME, empty/error responses, stale requests, two simultaneous hosts, popup closure and editor unload/reload. The Main Search fixture now verifies unfiltered server counts/page links and all-off/collections-only states. The earlier Header Five identity assertion was narrowed to search inputs so the added hidden type field is not mistaken for an extra search input. All identity and containment checks remain. These are source/Liquid/DOM fixtures with explicit platform adapters, not Shopify/browser certification.

**B3-SEARCH live queue — after Batches 4–9:** actual Shopify request types, direct typed/untyped URLs, counts and multiple pages; all source/master/popup/display settings including collections-only and all-off states; real prices/currencies, group limits and API errors; panel styles and product layouts at 1440/1280/768/390/320px including short viewport heights; long/translated text, contrast, keyboard/screen reader/IME, reduced motion and no-JavaScript paths; simultaneous header/page/popup instances and actual editor insertion/unload/reload. Product-card actions remain queued after their source review.

Authoritative register: **458 primary components: 350 NOT REVIEWED; 101 REQUIRES LIVE VERIFICATION; five retained dormant/source-reviewed; one reviewed build-only; one retained deletion candidate requiring owner review**. The increase of one component is the shared type-mapping helper. All 150 sections, 102 Theme Blocks, both builders, protected products/Quick View and Headers/Footers 1–5 remain. Merchant settings retain their recorded hash. Next: continue the remaining Batch 3 collection/discovery hosts; carry the explicit Main Search commerce dependency into Batch 4. PR #22 remains DRAFT, with no merge or Shopify publication.

Theme Check comparison: no new error signatures. Header Two retains one existing LiquidComplexity warning with its measure updated from 205 to 211. One additional OrphanedSnippet warning reports `search-source-types`, despite direct render references in Header Two, Main Search and both shared form helpers and successful actual-Liquid execution. These warnings remain in the evidence without suppression. Existing parser/lint debt remains; CLI exit 1 is not a clean Theme Check certification.

## Batch 3 engineering closure and Batch 4 opening — 2026-09-20

**Current stage: Batch 4 — Product / Commerce.** Batch 3's source/fixture pass is complete: all **42** registered Collection / Discovery components have dispositions (**27 REQUIRES LIVE VERIFICATION; 15 retained dormant/source-reviewed**). The shared product-card review opens Batch 4 and resolves Main Search's earlier commerce dependency. Broader product hosts, commerce flows and Batches 5–9 remain pending. Earlier checkpoint paragraphs above describe historical open findings; this checkpoint supersedes those findings and counts.

**Blog hosts and cards.** Feeds Blogs now renders Shopify's article array inside native pagination instead of treating `paginate.items` as an array or using a nonexistent count field. Its three layouts retain native page links without JavaScript. A shared item renderer removes duplicated paths. Off-route tag filtering is explicitly a limited preview of available articles, with a correctly encoded link to the complete tagged archive; it does not claim a filtered total or invent client pagination. Matching tagged archive requests use Shopify's filtered array. Empty picker/context states hide storefront output and give editor guidance. Load more updates the native fallback, rejects repeated pages, prevents duplicate requests, and aborts/removes handlers on editor unload.

Blog Card handles blank articles, numeric heading levels, escaped owned text, a hidden CTA, missing media and rounded-up reading time. Main Blog Collection uses a real merchant description setting for its existing description toggle (Liquid blog has no description property), respects hidden auxiliary content and Read more, and fixes the current-page marker. Main Blog Single uses native `blog.previous_article`/`next_article` article objects, actual menu links, properly formed media/share URLs, empty-related/share guards, title-off naming and escaped owned text. RTE/article/custom Liquid remains merchant content. Removed obsolete literal SECTION_ID CSS selectors and bounded long article/banner content. Social SVG output was source reviewed without changing the icon library. Shopify references: [blog object](https://shopify.dev/docs/api/liquid/objects/blog), [article object](https://shopify.dev/docs/api/liquid/objects/article), [paginate object](https://shopify.dev/docs/api/liquid/objects/paginate).

**Active collection hosts.** Classic's filter controls now belong to a separate native GET form, so product POST forms are no longer nested. Filter markup appears once, IDs include the section identity, native submit remains available for sort, and filter/sort state stays together. Price inputs use decimal currency values, boolean filters use Shopify values, and current-page/previous navigation no longer treats ellipses as pages. The existing three layouts and merchant state remain.

Simple now supplies result counts, renders its filter-bar setting, keeps sort/filter/search state in native forms, provides a real Apply control, and uses the shared facet implementation. Its drawer has one enhanced form and a native fallback, naming/focus/Escape/return behavior and editor cleanup. Alternate collection selection clears search context. Direct mixed search URLs retain every server result and the matching count/page links; new collection search controls request products. All six layouts and both card engines remain. Existing modern-card controls have corrected labels and consumers, including real rating data, image ratios, price visibility, hover/color styles and subscription routing. Dead wishlist/compare/swatch/quick-view branches had neither schema controls nor handlers in this active engine; those misleading controls were removed without adding services. [Shopify filter values](https://shopify.dev/docs/api/liquid/objects/filter_value) and [storefront filtering](https://shopify.dev/docs/storefronts/themes/navigation-search/filtering/storefront-filtering) informed native request handling.

Both active collection hosts share the disposable drawer/pagination controller. Numbered links remain native; load-more/infinite modes have same-origin, single-flight, response/root validation, abort/stale-response protection, error fallback and reduced-motion behavior. Opening another collection modal closes the prior one. CSS remains bounded and scoped; no CSS-delivery redesign was introduced.

**Retained compatibility sources.** Literal and dynamic tracing found no active loader for the old Modern controller or three old collection stylesheets. The controller can dynamically request the protected Quick View section and reuse the active native controller; that dynamic edge is documented despite the register's literal-only consumer column. Fifteen Batch 3 components are retained/dormant, not deleted or approved for future activation. Legacy sort/facet aliases delegate to common contracts; URLs and blank guards are repaired. The old “Compare” action navigated only to the first selected product. Its retained selection tray now provides a native link to each selected product and claims no comparison provider. The protected Quick View fragment has one native variant form, localized actions, accurate image dimensions, selling-plan/sold-out handling, price updates, bounded gallery controls and disposable requests/focus. Legacy JS/CSS explicitly exclude Main Collection Simple. A constructed host fixture exercises these retained contracts; it is not proof of an active storefront host.

**Batch 4 first component — shared Product Card.** Localized cart actions, escaped text/URLs, valid blank guards and subscription routing resolve the discovery hosts' source findings. Size-only quick add is limited to genuinely single-option products; multi-option products link to selection instead of silently choosing a color. Dropdowns select an available variant and disable all-sold-out submission. Keyboard actions, pointer behavior, missing media, long content and reduced motion are repaired. Existing badge positions/styles, button styles/sizes/full width, card presets, scroll media and range/detailed prices now have consumers. This is the shared component pass; each remaining product host and its settings still receives its own Batch 4 review. Main Product Classic, Simple, Compact, Modern Variant and Master remain protected and queued individually.

**Validation.** All **seven Phase 3 and 33 Phase 4 suites pass (40 total)**, including new actual-Liquid/DOM blog and collection suites, plus schema/reference/protected-architecture checks and CSS build. Tests cover section schema choices/booleans/range endpoints/presets, native form ownership and submitted values, variant/locale/selling-plan cases, blank/long text, drawer focus, two instances, failed and superseded requests, reduced-motion/no-JS contracts, and editor disposal/reload. Shopify globals and already-paginated arrays use explicit fixture adapters; no live Shopify/browser certification is claimed. Evidence: `validation/batch3-closure-batch4-card/`.

**B3-BLOG / B3-COLLECTION / B3-SEARCH / B4-CARD live queue — after Batches 4–9 engineering:** all layouts, presets and conditional controls at 1440/1280/768/390/320px; actual blogs/tags/article pagination and navigation; currencies, filters, sorting, multiple pages, product variants/selling plans/cart and shared Quick View; long/translated content, missing images, masonry order, keyboard/screen readers, contrast, touch and short-height drawers; reduced motion and JavaScript disabled; simultaneous sections and real editor insertion/selection/unload/reload. Existing B1/B2 and earlier B3 queues remain. Dormant components require a separate future host review before activation.

**Authoritative register:** **461 primary components — 315 NOT REVIEWED; 124 REQUIRES LIVE VERIFICATION; 20 retained dormant/source-reviewed; one reviewed build-only; one DELETION CANDIDATE — OWNER REVIEW.** Three added reusable runtime components are Blog Feed Items, Feeds Blogs JS and Collection Query Fields. The excluded 7,830 SVGs remain inventoried, not approved. All 150 sections, 102 Theme Blocks, both builder chains, protected products/Quick View and Headers/Footers 1–5 remain. `config/settings_data.json` is unchanged. PR #22 remains DRAFT; no merge or Shopify publication.

Theme Check at this checkpoint: **670 diagnostics (37 errors, 633 warnings), down from 684 (46 errors, 638 warnings); no new error signatures.** Eight missing-image-dimension errors and Classic's script parse error are removed. Fixing that parse exposes the already-existing 115-setting Classic schema warning; Blog Collection's existing count changes from 56 to 57 for its working description setting. Two new helpers receive OrphanedSnippet warnings despite direct render consumers and passing actual-Liquid tests. All diagnostics remain in evidence; CLI exit 1 is existing debt, not a clean Theme Check certification.


## Batch 4 checkpoint — Compact and shared purchase controls — 2026-09-20

**Current stage remains Batch 4 — Product / Commerce.** Main Product Compact and its four shared purchase/selling-plan helpers now have source/fixture dispositions. Including the earlier Product Card pass, **six of 35** Batch 4 family components await live acceptance; **29 remain NOT REVIEWED**. This is a bounded engineering checkpoint, not Batch 4 completion. Batches 5–9 and all accumulated live acceptance remain pending.

**Compact.** Removed the invalid top-level blank-product break, with a quiet blank storefront and editor guidance. The selected variant now supplies the initial and updated image, with product-image fallback, intrinsic dimensions, responsive candidates bounded to source width and a blank-media state. Text is escaped while merchant rich text stays intact. Existing singleton controls are guarded against duplicate blocks; future title/variant/quantity/buy blocks have singleton schema limits. Repeated price blocks still work and all receive updates. Quantity has an associated label and uses the selected variant's minimum, maximum and increment, including when its visible block is absent. The initial selected/required selling plan supplies the displayed price. Layout, zero/end-range spacing, long content, focus and rich-text overflow are bounded. JavaScript-disabled visitors receive native variant links so Shopify can refresh price, image, quantity rules and eligible purchase options. The existing native product form opts into the shared cart drawer only when the global drawer mode is enabled.

**Shared purchase controller and data.** Each root now owns its JSON; the old wrapper-wide lookup could read another product's variants. Classic and Master receive only that dependency-placement repair and a product-URL association. Exact variant matching preserves unavailable combinations without silently changing the customer's choice. Price, compare, sale, SKU, availability, quantity rules and supported plan prices update together. Native submit guards reject unavailable variants, invalid quantity and incompatible plans before downstream cart handlers. URL/back state preserves unrelated parameters and belongs to the matching product page; another product instance cannot overwrite it. Duplicate assets do not add handlers/history entries, and editor unload aborts per-root listeners. JSON embeds escape the literal less-than character, including hostile closing-script text; formatted monetary amounts come from Liquid rather than client-side currency guesses. Optional image data is emitted only for Compact.

**Selling plans.** Initial options reflect the selected variant's allocations and selected plan, with compatible defaults for required plans. Unsupported options are disabled. The singleton controller validates/caches its minimal per-variant data, updates compatible selections, notifies the shared purchase controller, blocks invalid native/AJAX submissions and clears editor caches. Existing labels, optional one-time purchase, required-plan behavior and form payloads remain. This helper review does not certify every host's presentation or subscription provider. Shopify references: [variant fields and selected allocations](https://shopify.dev/docs/api/liquid/objects/variant), [selling-plan allocation prices](https://shopify.dev/docs/api/liquid/objects/selling_plan_allocation), [quantity rules](https://shopify.dev/docs/api/liquid/objects/quantity_rule), and [image sizing](https://shopify.dev/docs/api/liquid/filters/image_url).

**B4-CLASSIC-OPEN — deliberately NOT REVIEWED.** Source inspection found that Classic's wishlist button emits `q:wishlist:add` without a repository consumer, while the existing saved-list engine uses a different contract. Its integration, storage fallback and lifecycle need repair; no new wishlist provider is being invented. The opt-in variant UI does not implement its declared style/swatch-source settings and reverts sparse unmatched combinations, making some valid combinations unreachable. Its lifecycle and setting/preset review remain open. Classic's lightbox uses the configured media limit for previous/next targets even when fewer images exist or media types differ; its parent remains hidden from assistive technology and modal keyboard behavior needs review. Classic and these dependencies remain NOT REVIEWED. Master also remains NOT REVIEWED: its scoped data placement and price fixture are not an individual host review. Next is Classic and these existing dependencies, followed by the remaining product hosts.

**Validation.** All **seven Phase 3 and 34 Phase 4 suites pass (41 total)**. The new `compact-purchase.cjs` renders real Liquid with explicit Shopify filter/form/comment adapters: all Compact settings/spacing endpoints, default preset and every block type, repeated singleton blocks, two roots, blank/default-only/selected/missing media, text and JSON escaping, native variant links, both script load orders, required/optional/unavailable plans, plan prices, quantity limits, native/AJAX cart payloads, URL restoration, malformed/missing data and editor unload/reload. Classic/Master coverage is limited to the changed shared dependency. Final focused Compact and Phase 3 acceptance reruns cover the no-JavaScript/image-width follow-up. Schema/reference/protected-state checks and CSS build pass. Evidence: `validation/batch4-compact-purchase/`. These are source/Liquid/DOM checks, not Shopify or browser certification.

**B4-COMPACT-PURCHASE live queue — after Batches 4–9 engineering:** Compact's left/right layouts, full preset/block/spacing matrix at 1440/1280/768/390/320px; actual selected/default/sold-out/unavailable variants, variant images and image-free products; optional/required plans and allocation prices in actual currencies; customer-context quantity rules, native cart and enabled/disabled AJAX drawer; no-JavaScript variant navigation and resulting server state; long/translated text, rich-text overflow, focus/screen readers, two products and real editor insertion/unload/reload. Accelerated checkout in other hosts and real subscription/provider behavior remain host/live requirements. All earlier live queues remain deferred as directed.

**Authoritative register:** **461 primary components — 310 NOT REVIEWED; 129 REQUIRES LIVE VERIFICATION; 20 retained dormant/source-reviewed; one reviewed build-only; one DELETION CANDIDATE — OWNER REVIEW.** No primary component was added or removed. The excluded 7,830 SVGs remain inventory only. All 150 sections, 102 Theme Blocks, both builder chains, protected products/Quick View and Headers/Footers 1–5 remain; merchant settings are unchanged. PR #22 stays DRAFT, with no merge or Shopify publication.

Theme Check for this checkpoint remains **670 diagnostics (37 errors, 633 warnings)** under Shopify CLI 4.8.0. The comparison against the Batch 3 closure checkpoint has **zero added and zero removed diagnostic signatures**; changed line numbers are ignored. Existing lint debt is retained without suppression, and CLI exit 1 is not represented as clean certification.


## Batch 4 checkpoint — individual product hosts and saved-list dependencies — 2026-09-20

**Batch 4 remains active. This is not its closure checkpoint.** All five protected product hosts now have individual source/fixture dispositions, including the earlier Compact pass. The former B4-CLASSIC-OPEN findings are superseded by this checkpoint. The standalone Wishlist section retains explicit open visual-setting findings; the remaining merchandising/helper surfaces are still NOT REVIEWED.

The family now contains **39 components: 20 REQUIRES LIVE VERIFICATION, two retained dormant/source-reviewed, 17 NOT REVIEWED**. Two new opt-in assets extract the existing product-host gallery mechanisms with scoped cleanup; two pre-existing `section-product-wishlist` assets are correctly assigned to Product / Commerce. No section or Theme Block was added or removed. The complete register is **463 primary components: 296 NOT REVIEWED; 143 REQUIRES LIVE VERIFICATION; 22 retained dormant/source-reviewed; one build-only; one deletion candidate requiring owner review**. The 7,830 excluded SVGs remain inventoried, not approved.

### Individual host dispositions

| Host | Source repairs and individual fixture evidence | Remaining live acceptance |
|---|---|---|
| Classic | Existing wishlist saves to the existing list. Dropdown/button/list/swatch controls now consume their settings; sparse partial choices remain reachable. Selected media survives the configured cap. Native video/model output replaces placeholders. The lightbox uses a native dialog, navigates actual images and returns focus. Initial plan price/quantity rules, SKU visibility, labels/content, native form and editor cleanup are covered. | All styles/swatches and merchant presets, app blocks, native dialog/screen-reader behavior, provider/model playback, actual carts/plans and responsive/editor acceptance. |
| Simple | Keeps its own settings and markup while using the shared purchase contract. Exact option resolution clears invalid combinations; plan/price/compare/SKU/quantity/availability/URL state is consistent. Native selector/variant links remain without JS. Stacked/column/thumbnail modes, mobile thumbnail scope, missing media, native video looping, metafield output and disposal are repaired. | Actual media fit/size/sticky layouts, galleries, native/accelerated carts, plans, keyboard and editor behavior. |
| Modern Variant | Independently tests default preset and every section/block setting, combined vs separate quantity/buy blocks, and absent optional blocks. One authoritative variant and quantity control, required subscriptions, prices/SKU and sparse selection use the existing shared contracts. Wishlist mode/labels/icons now work with the existing list; identifiable current-product legacy entries migrate, unrelated legacy entries remain. | Actual block ordering/styling, app interactions, storage across pages/tabs, native/accelerated carts, media and editor acceptance. |
| Master | Preserves all eight layout modes, native variant selector and anchor content navigation. Selected/capped/native media, plan prices and quantity rules are repaired. Real availability replaces the visible placeholder. Blank metafields no longer create broken tab links; repeated apps share a unique review panel. Font controls work independently of the color toggle; responsive bounds and sticky ordering are repaired. | Every layout/sticky/thumbnail/content combination, real metafield types and app blocks, native media/model rendering, carts/plans and editor acceptance. |

`product-gallery-options.js` is an explicit opt-in mechanism for the equivalent Simple/Modern image gallery and option controls. `product-stacked-media.js` serves Classic/Master's existing stacked media; the Classic dialog is optional. Both hosts of each asset received their own tests. The shared purchase helper adds only opt-in nested SKU hooks; purchase data adds optional media identity metadata. Compact's defaults and image fallback remain intact. The old `main-product-modern.js`/`.css` pair has no asset include or matching repository host; both are retained dormant, without deletion or reactivation.

The existing saved-list engine preserves `q:wishlist:v1`, validates and caps entries, shares page-memory fallback, uses localized selected-variant fragments, supports removal even when a product cannot load, restores focus and aborts obsolete/editor requests. It adds no provider or account infrastructure. **B4-WISHLIST-OPEN:** the standalone section's `color_scheme` lacks a repository color-scheme group/consumer, and its primary/secondary button styles still need local completion. Its section and CSS stay NOT REVIEWED; shared storage/button/renderer dependencies have independent source dispositions. Merchant color values and global settings are preserved.

### Validation and debt

Evidence: [`validation/batch4-product-hosts/`](validation/batch4-product-hosts/). **All seven Phase 3 plus 39 Phase 4 suites pass (46 total)**, including the affected Compact/Product Card suites and five new commerce suites. The Modern Phase 3 fixture now renders the complete actual host and executes the extracted assets; it retains the original one-ID, variant/price/sold-out/invalid-submit/quantity/media assertions instead of extracting an obsolete inline JS slice. Additional final wishlist checks cover write denial, storage-event synchronization and focus after removal. Fixtures use explicit Liquid/platform/dialog adapters and do not certify Shopify or browser rendering.

Schema/reference/protected-state checks and the CSS build pass. Shopify CLI **4.8.0 exits 1** with **655 diagnostics: 25 errors / 630 warnings**, compared with 670 (37 / 633). Twelve missing-image-dimension errors and five unused assignments are removed. Classic's existing complexity warning rises from 167 to 179; Modern (135) and Master (127) add two complexity warnings. These warnings are recorded without suppression; the result is not clean certification. No broad architecture extraction is undertaken solely to reduce a complexity score.

150 sections, 102 Theme Blocks, both builder/row/column chains, all protected products/Quick View and Headers/Footers 1–5 remain. Protected merchant state is unchanged; its recorded digest is in the evidence. No main writes, merge or Shopify publication.

**B4-PRODUCT-HOSTS live queue — after Batch 9:** each host independently needs required/optional/no-allocation/variant-specific plans, allocation price and compare-at changes, default/single/multi/sparse/sold-out variants, quantity min/max/increment, localized native/AJAX/accelerated cart payloads, product-owned URL/back/forward behavior, no-JavaScript variant navigation, mixed/limited/missing media, long text and 1440/1280/768/390/320px layouts, keyboard/screen readers/native dialogs, simultaneous sections and real editor unload/reload. Shopify provider/model playback and app blocks need actual runtime evidence. Earlier live queues remain mandatory.

Next engineering targets are the remaining 17 registered surfaces and their related active assets, including Recently Viewed's missing renderer request, Recommendations' loading/instance handling, cross-sell, bundles, product content/helper hosts and the outstanding Wishlist settings. PR #22 stays DRAFT; continue engineering through Batches 4–9 before accumulated live QA.

## Batch 4 formal engineering closure — 2026-09-20

**Batch 4 Product / Commerce engineering is complete.** Every assigned component has an individual justified disposition: **69 components (20 sections, 17 snippets, 32 assets): 64 REQUIRES LIVE VERIFICATION and five retained dormant/source-reviewed; zero NOT REVIEWED.** This closes source findings, including B4-CLASSIC-OPEN and B4-WISHLIST-OPEN. It does not close storefront acceptance or Phase 4. PR #22 stays DRAFT and must not be merged.

The earlier 35/39-component snapshots were provisional family inventories. This checkpoint adds seven small active dependencies and correctly assigns 23 existing commerce dependencies/hosts: global Quick View, FAQ/review assets, cart surfaces, Product Slider and retained legacy bundle helpers. No section or Theme Block was added, removed or renamed. The family assignment function now recognizes these commerce names; ledger dispositions still require actual review. Featured Deals and other content/third-party hosts retain their own pending batch reviews; shared Product Card coverage does not approve those hosts.

**Authoritative register:** **470 primary components — 256 NOT REVIEWED; 187 REQUIRES LIVE VERIFICATION; 25 retained dormant/source-reviewed; one reviewed build-only; one DELETION CANDIDATE — OWNER REVIEW.** The excluded 7,830 SVGs remain inventoried, not approved. Earlier counts in this report are historical checkpoints.

### Remaining host and dependency dispositions

| Component or host | Repairs and source evidence | Live or future-host obligation |
|---|---|---|
| Recently Viewed | Requests its actual section instance and extracts its own native Product Card template; saved card settings survive Section Rendering. Local/session/memory history, current-product exclusion, count limits, stale request cancellation, carousel bounds and editor cleanup are covered. | Actual cross-page history, denied storage, Shopify Section Rendering, merchant card settings and responsive/editor behavior. |
| Recommendations / Cross-sell | Native recommendation response state and ten-product API limit; manual/collection fallback survives empty/error responses. Direct product Drops, deduplication, current-product exclusion, scoped identities/requests and actual block attributes. | Shopify recommendation data/intent, every source/card setting, collection availability, carousel geometry and editor acceptance. |
| Product Feed / Spotlight | Native collection sort/page links plus scoped append/infinite enhancement; failed requests retain content. Existing collection filter link replaces an inert toggle. Spotlight repairs collection/hero positioning, dim/fit/media sizing and editor-only placeholders; configured autoplay has persistent pause and media disposal. | Real collection pagination/filtering, all card/layout settings, media policies, long content, keyboard and editor insertion. |
| Product Slider | Individual source/select/range/preset review. Manual/collection limits and related exclusion agree with output; slide counts work at each breakpoint. Local controls replace incomplete inline fallbacks with dots/arrows/loop, persistent pause, reduced-motion/visibility handling and per-instance/editor cleanup. Shared card contracts remain intact. | Actual native scrolling/RTL, card settings and purchases, all breakpoints, screen readers and editor lifecycle. |
| Bundles | Existing manual/metaobject/collection sources retained; native `.value` fields and replace/append precedence repaired. Fixed variant IDs, min/max/increment normalization and duplicate-ID aggregate limits, unavailable/required-plan guards and one localized multi-item add. A refresh failure is distinct from a failed add. | Native multi-item Shopify responses, customer quantity rules, optional/required subscriptions, Markets currencies/rounding and all settings/layouts. Displayed savings do not create discounts. |
| Product Block Section | Host settings and three nested presets checked; width/overflow fix preserves sticky layout and both builder chains. | Actual host layout/editor behavior. Individual Theme Block behavior remains Batch 8 work; the host fixture adapts `content_for` and does not certify its children. |
| Reviews / Meta Panels | Reviews normalize valid native rating scales, skip malformed ratings, use native rich text and accessible unique identities. Meta Panels shares one existing-content resolver across layouts; native metafields and explicit metaobject field keys replace unsupported field iteration. Radio focus and editor selection are repaired. | Real field types, configured metaobject keys, all styles and content lengths, keyboard/screen-reader and editor acceptance. |
| Story / FAQ | Native metaobject/media/RTE fields, stack/split/manual visibility and media fit repaired. FAQ skips incomplete entries, keeps answers visible without JS, limits embedded playback to visible content, and cleans up on collapse/unload. Native model output and full-size image links preserve existing intended media behavior. | Actual media/model providers and file types, configured JSON/metaobjects, all placements/sticky settings, assistive technology and editor acceptance. |
| Wishlist | Existing saved-list engine retained. The unused color-scheme selector is replaced with inherited-color guidance; primary/secondary button styles consume existing theme tokens. | Real persistence, denied storage, cross-tab state, selected variants, all button/card settings and responsive/editor acceptance. |
| Global Quick View | Uses an inert native purchase template in the retained Collection Modern fragment. Native money/forms/variant IDs/plans/quantities replace unsafe client-built purchase markup. Shared controller boot/dispose and an explicit URL-ownership opt-out isolate the modal. Stale requests, focus/overflow, cart/collection modal handoff and editor cleanup are covered. | Real global/private Quick View coexistence, subscriptions/currencies, native/AJAX carts, screen readers and all launch/display modes. |
| Main Cart / drawer / helpers | Flat native cart form retains every quantity and checkout association; native removal links replace nested forms. Strict quantity validation prevents blank/decimal values becoming removals. Native line keys, plans, instructions, properties, discounts and taxes remain server-owned. Add-ons route required-plan/variant choices to PDP; eligible single-variant forms use the native minimum. In-flight guards, recovery without duplicate POST, equivalent-control focus restoration, short-view overflow and lifecycle cleanup are covered. | Actual cart/checkout updates, native/AJAX/no-JS paths, line instructions, discounts/properties/currencies, errors, all settings, screen readers and editor behavior. |
| Reward helpers | Positive thresholds, closest next target, bounded progress and escaped labels; existing three entry points retained. Merchant guidance explicitly identifies display-only progress and separately configured Shopify rewards. | Actual currency thresholds, shipping/discount/gift configuration and all display positions/styles. The drawer has no independent reward/add-on blocks; no new configuration system was added. |
| Dormant bundle helpers | No render/include consumer found. Isolated fixtures cover corrected product/ID/total deduplication, escaped content, unavailable/required-plan/quantity guards, localized routes and retry/duplicate-load behavior. CSS bounds reviewed. | Retained inactive with the two earlier legacy Modern assets. Any future activation needs a separate host review; isolated fixtures grant no activation approval. |

Shared Product Card and both collection-card implementations now route products with required subscriptions or minimum quantities above one to purchase options instead of adding one item incorrectly. The protected Collection Modern fragment retains its own behavior. Changes to shared purchase helpers are opt-in dynamic boot/dispose and Quick View URL isolation; all existing host suites were repeated. No host's assumptions became the defaults for another host.

Three inert controls were retired after tracing no effective consumer: Wishlist `color_scheme` (no color-scheme group), Story `metaobject_expected_type`, and Cart `cart_density` (always overridden by explicit spacing). Saved merchant values remain untouched. Meta Panels uses its existing key input for comma-separated metaobject field keys; no provider or data migration was invented.

### Closure validation and retained debt

Evidence: [`validation/batch4-commerce-closure/`](validation/batch4-commerce-closure/). **All seven Phase 3 and 46 Phase 4 suites pass (53 total)**, including every completed Phase 4 suite, the affected Compact/Product Card/Collection Modern suites, and seven new commerce suites. Final affected checks cover recommendation editor attributes, FAQ cleanup and dormant bundle deduplication. The Collection Modern fixture's response wait now awaits actual Liquid rendering before delivery; its original behavior assertions remain. Earlier timing failure is not attributed to Shopify or dismissed as a live pass.

Schema/reference checks, protected architecture/state checks, CSS build and whitespace checks pass. Shopify CLI **4.8.0 exits 1** with **645 diagnostics (22 errors / 623 warnings)**, compared with 655 (25 / 630) at the product-host checkpoint. No new error signatures. Existing Bundles/FAQ complexity measures change, Product Card adds one complexity warning, and two new directly referenced helper snippets receive OrphanedSnippet warnings. Main Cart's existing excessive-settings warning decreases from 50 to 49 settings. Full diagnostics and additions/removals are retained without suppression; this is not a clean Theme Check result.

All 150 sections, 102 Theme Blocks, both builders/row/column chains, five protected product hosts, Collection Modern Quick View and Headers/Footers 1–5 remain. No file deletion. `config/settings_data.json` retains its recorded digest and exact bytes. No main update, merge or Shopify deployment.

**B4-FINAL mandatory live acceptance — after engineering Batches 5–9:** carry forward B4-CARD, B4-COMPACT-PURCHASE and B4-PRODUCT-HOSTS, then execute every live item in the table and register. Independently test each product host with required/optional/no-allocation/variant-specific plans; selling-plan and compare-at price changes; single/multiple/sparse/sold-out variants; min/max/increment; malformed/missing purchase data; product-owned URLs/back/forward; native/AJAX/accelerated carts and no-JS fallback; multiple simultaneous instances; media/model/provider behavior; and real Theme Editor load/unload/reload. Exercise all relevant defaults/presets/settings at 1440/1280/768/390/320px, short viewports, long/translated text, keyboard/screen readers and reduced motion. Real currency/rounding, cart errors, recommendation/Section Rendering responses and app blocks remain runtime obligations. These Liquid/DOM tests use explicit platform adapters and are not browser screenshots or Shopify certification.

No Batch 4 owner decision blocks further engineering. The retained global Header Basic deletion candidate remains a separate owner decision, with no deletion performed. **Continue Batch 5 Content / Interactive, then Batches 6–9, before the accumulated live acceptance.** PR #22 remains DRAFT.


## Batch 5 opening checkpoint — Featured Deals and Blog Spotlight — 2026-09-20

Batch 4 remains closed for engineering at published commit `4bf574cd97b18dd205bae5666a475d648c55b23f`; its complete live queue remains open. [GitGuardian passed that exact checkpoint](https://github.com/DylanJHamilton/Quadratum/runs/106137693462). Batch 5 is active and unfinished. No owner decision blocks continued engineering.

**Featured Deals & Promotions / q-deal:** start/end visibility now uses one native rendered-day calculation for hero selection and card counts; an end date includes its whole day. Malformed dates are ignored rather than becoming epoch-zero expiry. The empty scheduled state explains itself in Theme Editor. Merchant guidance explicitly notes that visibility does not create/end Shopify discounts and that cached output can update later. Shopify documents that `now` reflects the time Liquid was rendered, which is not necessarily every view: [date filter](https://shopify.dev/docs/api/liquid/filters/date). Per-deal background/text variables now have consumers; plain metadata/CTA labels and links are escaped, with local wrapping, focus and reduced-motion rules. The optional shared Product Card contract is retained.

**Blog Spotlight:** selected Article Drops and blog articles are rendered directly; manual order/editor attributes and automatic tag/limit order remain. The actual excerpt text now obeys the configured word limit without displaying escaped rich-text tags. Corrected line-height scaling, featured-split and mobile-peek units, one-article layout, blank CTA protection, long text and keyboard scroller access. All featured/grid/scroll settings and the existing preset receive host-specific fixtures; the previously reviewed Blog Card is reused unchanged.

**Validation:** all seven Phase 3 plus 47 Phase 4 suites pass (**54 total**). The new `featured-content.cjs` uses actual source with explicit date/Shopify/DOM fixture context, including past/future/current/invalid dates, hero/card limits, editor empty state, product subscription destinations, selected article objects, all select settings/presets, excerpts, false toggles and two instances. Schema/reference/architecture/merchant-state checks and CSS build pass. Shopify CLI 4.8.0 exits 1 with **644 diagnostics (22 errors / 622 warnings)**, down one confirmed unused assignment; no new diagnostic signatures. Evidence: `validation/batch5-featured/`. No live browser or Shopify acceptance is claimed.

Register: **470 primary components — 253 NOT REVIEWED; 190 REQUIRES LIVE VERIFICATION; 25 dormant/source-reviewed; one build-only; one retained owner-review deletion candidate.** Three newly disposed files, no production file addition/deletion; Batch 5 currently contains 76 entries, six live queued (including three previously reviewed shared dependencies) and 70 NOT REVIEWED. All architecture and merchant-state protections remain.

**B5-FEATURED live queue after Batch 9:** real scheduled dates/shop timezone/caching and editor changes; hero/card/shared/per-deal presentation and optional product actions; real manual and automatic article selections/tag filtering; all preset/style/toggle/spacing endpoints at 1440/1280/768/390/320px; long/translated content, contrast, keyboard/screen readers and actual editor insertion/reordering. Related testimonial/media-carousel hosts remain NOT REVIEWED with explicit findings: shared initialization/disposal/pause/media behavior, host settings and optional avatar/role controls, image dimensions, quote types and misleading insertion placeholders. Those are the next engineering group; no pass is inferred from other carousel work. PR #22 remains DRAFT.

## Batch 5 checkpoint — testimonial and media carousels — 2026-09-20

**Batch 5 remains open.** Nine individually reviewed files move to REQUIRES LIVE VERIFICATION: Featured Testimonial, Interactive Testimonials, Media/Logos, Testimonial Card, the existing empty-state helper, and four carousel assets. The global register now has **470 primary components: 244 NOT REVIEWED, 199 REQUIRES LIVE VERIFICATION, 25 retained dormant/source-reviewed, one build-only and one retained deletion candidate requiring owner review**. Current provisional Batch 5 assignment: 76 components, 15 live queued / 61 NOT REVIEWED. Default family assignments still include some forms/global/social dependencies; reconcile their actual owners during continuing review, without treating inventory changes as approvals. Batch 4 remains closed at 69 justified engineering dispositions; its live queue is unchanged.

**Individual hosts:** Featured Testimonial retains its static hero/split and carousel modes. Empty quotes no longer become visible placeholders; the selected first three valid blocks keep editor identities. Textarea quotes are escaped, the CTA link style and background overlay work independently, the malformed preset name/trailing code fence are repaired, and narrow layouts are bounded. Interactive Testimonials independently exercises rich text, avatar/role/rating/badge toggles, responsive per-view settings and real block identities. Full-review destinations remain as explicit links so rich-text links cannot form nested anchors. Its unused `layout` selector had no CSS/controller consumer and was retired; existing per-view settings and saved merchant values are preserved. New testimonial blocks start blank/unrated rather than publishing invented reviews. Existing merchant testimonial content is not rewritten.

Media/Logos independently covers both presets, image/logo/linked-video/inline-video modes, captions and alt labels, mobile peek, minimum logo width and arrow positions. Inline media is inert until the shopper chooses Play video, then stops when hidden, offscreen, navigated away or unloaded. Existing embed HTML is retained; the fallback URL is explicitly described as a provider embed URL. Linked video remains a poster link; no-JS output retains the configured native URL or embed. Real provider and merchant-embed behavior remains live acceptance.

**Shared dependencies:** Testimonial Card now distinguishes plain quotes from explicit rich text, bounds ratings, avoids claiming unprovided verification, uses native preview images with intrinsic dimensions and keeps native product links/prices. The small empty-state helper is independently reviewed through real empty host renders without changing its production source. The existing carousel engine measures each host's actual geometry, includes gaps/end clamping and RTL, and uses a new explicit ownership attribute so unrelated gallery/text/slideshow controllers are not double-bound. It mounts once, disposes listeners/observers/timers/media, preserves manual pause across other pause reasons, follows changing reduced motion, handles editor block selection and preserves pagination focus on resize. The testimonial entry point delegates to this engine instead of emitting unconsumed events. Featured Testimonial's unmanaged inline engine is removed in favor of the same reviewed contract. Controls start hidden until enhanced; ordinary page buttons replace the unsupported tab semantics. Manual navigation gets a polite page announcement; automatic rotation does not move focus or repeatedly announce. This follows the applicable [WAI carousel functionality guidance](https://www.w3.org/WAI/tutorials/carousels/functionality/) without claiming live accessibility certification.

**Validation:** all seven Phase 3 plus 48 Phase 4 suites pass (**55 total**); final affected carousel fixtures also pass. `content-carousels.cjs` renders actual host sources/presets/select options/range endpoints and checks blank/default content, false toggles, plain/rich quotes, native preview media, safe review destinations, no-JS content, deferred media, three simultaneous hosts, unrelated-controller exclusion, measured pages/edges/RTL/keyboard, persistent pause/focus/visibility/reduced motion/offscreen, media disposal and editor unload/reload. Explicit geometry, media and platform adapters are documented by the fixture. Schema/reference/protected-state checks and CSS build pass. Shopify CLI 4.8.0 exits 1 with **639 diagnostics (19 errors / 620 warnings)**, down three missing-image-dimension errors and two unused assignments, with no new diagnostic signatures. Full debt is retained in `validation/batch5-carousels/`; this is not clean Theme Check certification.

**B5-CAROUSELS live queue after Batch 9:** each host's presets/settings/real saved blocks, authentic metadata and actual product preview data; native responsive scroll geometry at 1440/1280/768/390/320px, extreme per-view/peek/gutter/padding settings, long and translated quotes, custom colors/contrast, RTL and real touch/keyboard/screen readers; provider/embed controls and playback/stop behavior, no-JS fallback; OS reduced motion, hidden-tab/offscreen transitions, multiple mixed hosts and real Theme Editor insertion/reordering/selection/unload/reload. The other Media Gallery and Text Carousel hosts remain NOT REVIEWED and are next. 150 sections, 102 Theme Blocks, both builders and row/column chains, all protected product/Quick View/Header/Footer variants and protected merchant state remain intact. PR #22 stays DRAFT; no merge or Shopify deployment.

## Batch 5 checkpoint — Media Gallery and Text Carousel — 2026-09-20

**Batch 5 remains open.** Five additional files have justified REQUIRES LIVE VERIFICATION dispositions: both hosts, Gallery Item, gallery CSS and gallery JS. The already-reviewed shared carousel receives one explicit host setting (`data-announce=false`) and its previous three consumers are retested. Register: **470 primary components — 239 NOT REVIEWED, 204 REQUIRES LIVE VERIFICATION, 25 dormant/source-reviewed, one build-only and one retained owner-review deletion candidate**. Current provisional Batch 5: 76 entries, 20 live queued / 56 NOT REVIEWED. These counts do not close its outstanding family-assignment audit.

**Media Gallery:** grid, masonry, carousel and image-accordion modes each receive settings/preset review. The lightbox shell previously had no controller; it now uses a native image dialog with exact configured images, captions, dimensions, looping/edge controls, keyboard navigation, focus return and the existing cart/Quick View modal handoff. Native image/link destinations remain without JS; no-action content is no longer a fake button. Accordion expansion uses a separate button, preserving image/link actions. All panels remain available by scrolling instead of discarding blocks beyond the desktop visible setting; the default panel is clamped and the mobile stack/carousel choice has a consumer. Below-caption colors, bounded column widths, single-image layout, hover/focus/touch captions and responsive/reduced-motion styles are repaired. The unimplemented “future thumbnails” checkbox was removed without adding thumbnail functionality or rewriting saved state.

**Text Carousel:** its advertised carousel settings previously had no active engine. It now explicitly uses the existing measured shared contract and its own responsive per-view values. The CSS ticker remains CSS-driven and gains a persistent native checkbox pause usable without JavaScript. Duplicate visual content is inert/hidden from assistive technology and has no duplicate editor identities; single-item, reduced-motion and editor views remain readable. Card/badge/line layouts now preserve labels, subtext and metadata in both modes. Padding tokens have local fallbacks, opacity/scale units are corrected, muted/accent colors have consumers, normal text is escaped and new-block defaults no longer assert free shipping or verification. The inert “fade if supported” selector is retired; saved settings and working scroll behavior remain. Manual announcement opt-out is explicit per host; automatic rotation is never repeatedly announced.

**Validation:** all seven Phase 3 plus 49 Phase 4 suites pass (**56 total**), including prior commerce and carousel suites, with a final affected host pass. Actual Liquid presets/all select values/range endpoints, destinations/captions/accordion inventory, mixed instances, image dialog navigation/edge/focus/modal/editor lifecycle, text layout/content/announcements, native no-JS pause/clone identity and independent IDs are covered using explicit adapters. Schema/reference/protected-state checks and CSS build pass. Shopify CLI 4.8.0 exits 1 with **637 diagnostics (18 errors / 619 warnings)**: one missing-image-dimension error and one unused assignment removed. Existing excessive-setting warnings remain, with Gallery's message changing 55 → 54 and Text Carousel's 50 → 49. No new error signatures or check categories, no suppression. Full output/comparison is in `validation/batch5-gallery-text/`.

**B5-GALLERY-TEXT live queue after Batch 9:** all modes/presets/settings with saved merchant content; native dialog, screen readers, focus/keyboard/modal handoff and image navigation; actual browser scroll/RTL/touch, image ratios/fit, long content/captions, minimum columns and access to every accordion panel; actual ticker animation/pause/direction/OS reduced motion and native no-JS behavior; all 1440/1280/768/390/320px layouts and real editor insertion/reordering/selection/unload/reload. This is source/fixture evidence, not live certification.

**Next traced dependency finding:** `global-theme-vars.liquid` is actively rendered in the normal and password `<head>` but contains a second global popup body, while the normal layout separately renders the reviewed `global-popup` snippet. It remains NOT REVIEWED with an explicit ledger finding. Correct this scoped head include using the existing theme-token/utility helpers before continuing the basic content hosts; do not redesign global Theme Settings or duplicate the popup controller. All section/block/builder/product/header/footer and merchant-state protections remain. PR #22 stays DRAFT and unmerged.


## Scoped global head dependency repair — 2026-09-20

The duplicate popup finding traced during Batch 5 is closed. `global-theme-vars` now provides only the existing token/utility CSS wrapper; normal layout keeps its original compiled-CSS → tokens/utilities → styles order and renders the dedicated popup exactly once in the body. Password layout receives the same head CSS wrapper without popup body markup. Inert script text previously emitted inside token CSS is removed; no theme-toggle controller or global Settings redesign is added. Full `theme-tokens` and utility mapping/consumer reviews remain NOT REVIEWED for Batch 9.

The thin wrapper is individually REQUIRES LIVE VERIFICATION and correctly assigned to Global / Structural. Register: **470 components — 238 NOT REVIEWED, 205 REQUIRES LIVE VERIFICATION, 25 dormant/source-reviewed, one build-only, one retained owner-review deletion candidate**. Provisional Batch 5: 75 entries, 20 live queued / 55 NOT REVIEWED. Batch 4 formal engineering closure remains unchanged; basic content hosts are next.

All seven Phase 3 plus 50 Phase 4 suites pass (**57 total**). Actual normal/password layout fixtures cover enabled/disabled/editor popup states, single body ownership, head markup, CSS order, custom CSS and the actual popup controller using explicit platform adapters. Schema/reference/protected-state checks and CSS build pass. Shopify CLI 4.8.0 exits 1 with **636 diagnostics (17 errors / 619 warnings)**, removing the duplicate popup's missing-image-dimension error; no new diagnostic signatures or suppression. Evidence: `validation/layout-head/`.

**B1-HEAD live queue after Batch 9:** actual normal and password templates, native CSS cascade and merchant/custom token values, popup settings/editor preview and responsive/assistive-technology behavior. These are fixtures, not Shopify/browser certification. All protected sections, Theme Blocks, builder chains, product/Quick View/Header/Footer variants and `config/settings_data.json` remain intact. PR #22 stays DRAFT and unmerged.


## Batch 5 checkpoint — basic content and 404 — 2026-09-20

**Batch 5 is still active.** Content Embed, Section Divider, Basic Text, Main 404, its CSS and q-icon now have individual source/fixture passes and remain REQUIRES LIVE VERIFICATION. The unused utility-404 helper has an isolated source disposition and remains dormant. Grid Builder's shared-icon adapter is tested but its full review stays NOT REVIEWED; Column/Grid/Split Content are next.

Content Embed and Basic Text use valid content guards with native editor-only guidance. New embeds start empty instead of loading an unrelated demo video; saved provider HTML/scripts are preserved. Embed custom colors now apply to the element that owns the variables; blank custom colors fall back, ratio-mode scrolling remains usable and padding is bounded. Basic Text preserves rich content, escapes plain labels, separates the surface and opacity classes and connects surface/color fallbacks to existing tokens. No universal third-party controller is invented.

Section Divider respects blank labels, uses a native chapter heading and meaningful local spacing/width/type/border fallbacks, and preserves its alternate-background class. Blank or legacy default `section-divider` anchors now generate `section-divider-<section id>` to prevent duplicate IDs. Explicit other custom anchors are preserved after handle normalization; links to the old repeated default require the generated ID or an explicitly unique custom name. q-icon reuses the existing flat sprite and validates selected symbol names in the two current hosts. Shopify does not support arbitrary theme subdirectories: [theme architecture](https://shopify.dev/docs/storefronts/themes/architecture). The excluded icon library remains inventoried, not approved; the sprite's broader asset review remains pending.

Main 404 honors its image setting in centered and both split modes, retains native image output, skips empty helpful navigation and preserves block identities. Plain message/labels/links are escaped, line breaks retained and native GET search remains usable without JS. Default home/collections destinations use Shopify's localized [routes object](https://shopify.dev/docs/api/liquid/objects/routes); explicit merchant destinations are preserved. CSS now uses native color fallbacks, bounded content/buttons, keyboard focus and reduced motion. The old utility helper receives the corresponding local corrections without activation.

**Ledger reconciliation:** the protected Collection Modern Quick View fragment was left marked dormant even though Batch 4's reviewed global Quick View now requests it and imports its native purchase template. Its existing individual source/fixture disposition is correctly REQUIRES LIVE VERIFICATION; the old collection loader remains dormant. Batch 3 is consequently 42 components: 28 live queued / 14 dormant retained. This is a ledger correction, no additional commerce behavior change. Batch 4's 69 dispositions and mandatory live queue remain unchanged.

Register: **470 components — 231 NOT REVIEWED, 212 REQUIRES LIVE VERIFICATION, 25 dormant/source-reviewed, one build-only and one retained owner-review deletion candidate**. Current provisional Batch 5: 75 entries, 26 live queued / one dormant / 48 NOT REVIEWED. Family assignment audit remains open.

**Validation:** all seven Phase 3 plus 51 Phase 4 suites pass (**58 total**), including actual settings/presets, select/range endpoints, false toggles, native empty/editor output, multi-instance anchors, icon references, locale/search/404 media and the affected Quick View suite. Schema/reference/protected-state checks and CSS build pass. Shopify CLI 4.8.0 exits 1 with unchanged **636 diagnostics (17 errors / 619 warnings)**, no suppression. Evidence: `validation/batch5-basic-content/`. This is source/fixture evidence, not live Shopify/browser acceptance.

**B5-BASIC live queue after Batch 9:** each host's presets/settings/saved content, long and translated text, color/contrast and 1440/1280/768/390/320px geometry; real iframe/raw provider embeds and editor unload/reload; native sprite rendering; generated/custom anchor destinations; all 404 layouts, actual native images, locale links and GET search, keyboard/screen readers/editor. All existing queues remain mandatory. Merchant state, all protected architecture and DRAFT PR status are preserved.

**Security evidence follow-up:** GitGuardian incident 37480944 flags a source-file checksum stored under a filename key in the earlier head-layout evidence commit. Every digest was recomputed and matched its source. Evidence now uses explicit `path`/`sha256` fields. The [follow-up scan](https://github.com/DylanJHamilton/Quadratum/runs/106147395927) still flags only the original historical occurrence; owner false-positive triage is queued. No credential was exposed by this evidence, no scanning suppression or history rewrite occurred, and engineering continues. This unresolved scan is not reported as a pass.


## Batch 5 checkpoint — content builders — 2026-09-20

Column Builder, Grid Builder and Split Content have independent source/settings/block/preset dispositions: **REQUIRES LIVE VERIFICATION**. The separate protected Theme Block builders and row/column chains are unchanged; no child Block approval is inferred.

Column Builder retains its flat tile architecture and exact column choices. Existing spacing/type/radius/shadow/surface selector values now resolve locally; the surface and per-tile icon color no longer self-reference. Tile alignment, bounded padding/buttons/media, valid empty/editor handling and the existing flat icon sprite are covered. Grid Builder independently preserves all three configured mobile columns, uses native [color_extract](https://shopify.dev/docs/api/liquid/filters/color_extract) for channels, respects zero opacity and skips blank headers/actions. Its accent has a real icon/link consumer; card/radius/media bounds, logical dividers, focus and reduced motion are repaired.

Split Content parses whole supported padding tokens independently for mobile and desktop; desktop values now reach CSS instead of being overridden by mobile inline styles. Buttons and badges render only from their own valid block types, with editor identities and no blank links. Native desktop/mobile-only images, distinct alt descriptions, semantic captions, plain content and the mobile background fallback are covered. CSS scope is independent of custom anchors; old generated split-content IDs remain unique, while explicit custom anchors work literally. Full mode follows the native section width without the overflowing viewport breakout; its label now says full section width/no side padding. Contained mode keeps its padding. Visibility settings use one breakpoint and stay reachable in editor mode. Unreachable rating markup (no corresponding schema controls) is removed; new badge copy is neutral, existing merchant content untouched.

**Family reconciliation:** 32 default Content / Interactive assignments are corrected by actual consumers and file type, with no disposition changes: two reviewed blog dependencies to Discovery; three form assets to Forms; six social/third-party helpers to Batch 7; four content-block dependencies to Batch 8; 17 shared/dormant/global assets/helpers to Batch 9 closure. The evidence records each mapping. Discovery now has 44 components, 30 live queued / 14 dormant retained. Batch 5 has **43 components: 26 live queued, one retained dormant and 16 NOT REVIEWED** (15 section hosts plus People Card). Inventory reassignment is not approval. All 102 Theme Blocks remain pending individual review, alongside four assigned dependencies.

Register: **470 components — 228 NOT REVIEWED, 215 REQUIRES LIVE VERIFICATION, 25 dormant/source-reviewed, one build-only and one retained owner-review deletion candidate**. All seven Phase 3 plus 52 Phase 4 suites pass (**59 total**). Host/block presets and every select/range/checkbox endpoint, token resolution, native colors/images, blank actions/media, scope/anchors and simultaneous native hosts are covered with explicit platform adapters. Schema/reference/protected-state checks and CSS build pass. Shopify CLI 4.8.0 exits 1 with **632 diagnostics (16 errors / 616 warnings)**: one image error and three unused assignments removed, no added signatures or suppression. Evidence: `validation/batch5-content-builders/`.

**B5-BUILDERS live queue after Batch 9:** each host's actual saved presets/blocks/settings, token/color hierarchy and contrast, extreme columns/padding/gaps with long translated content at 1440/1280/768/390/320px, native sprite/images/alt and full/contained/mobile-background presentation, anchors, keyboard/reduced motion, editor insertion/reordering/unload/reload. No source fixture is browser or Shopify certification. Batch 4 closure and all earlier live queues remain mandatory. Protected merchant state and all architecture remain intact; PR #22 is DRAFT and unmerged. GitGuardian's historical checksum false-positive incident 37480944 remains queued for owner triage; the [latest basic-content scan](https://github.com/DylanJHamilton/Quadratum/runs/106149042357) reports that original occurrence only. Continued engineering next covers Service Process, Services List and Stats Strip.


## Batch 5 checkpoint — services and stats — 2026-09-20

Service Process, Services List, Stats Strip and its small scoped enhancement have individual source/fixture dispositions: **REQUIRES LIVE VERIFICATION**. Batch 5 now has **44 assigned components: 30 live queued, one dormant retained and 13 NOT REVIEWED** (12 sections plus People Card).

Service Process uses actual step counts, clamps/floors the current-step index and names its progress semantics independently of optional visible metadata. Indicator choices, number/icon precedence, blank headings/content/actions and image descriptions are respected. Its generic button styles are now scoped; wrapping, focus and reduced-motion behavior are bounded. The current-step control is explicitly a display setting, not live order tracking.

Services List uses its actual named service blocks, honors the custom-color toggle and list-only price column, omits empty action rails, and restores icons through the existing reviewed flat sprite helper. Media/type/spacing number inputs cannot generate negative dimensions. Both list and grid retain their intended controls, escaped valid destinations, native media precedence, scoped focus and responsive bounds.

Stats Strip omits fully blank values/labels consistently. New sections use a visible label-opacity default; existing explicit zero remains unchanged. Counter enhancement changes only an aria-hidden visual span while assistive technology retains the exact native value. A single-play controller accepts unambiguous numbers, preserves formatting/final text and leaves malformed or localized ambiguous strings untouched. It disposes animation frames, observers and listeners on editor unload, supports multiple instances and duplicate boot, and finishes immediately for reduced motion, hidden documents or editor previews. Native text is complete without JavaScript. Counter animation is presentation only; it does not establish the truth of merchant-entered statistics.

**Build compatibility:** resolving Split Content padding tokens natively would allow Tailwind to prune 38 existing spacing utilities. They are now explicitly safelisted; rebuilt `assets/theme.css` remains byte-identical to the previous published output, preserving saved merchant classes. Broader global CSS review remains in Batch 9.

Register: **471 components — 225 NOT REVIEWED, 219 REQUIRES LIVE VERIFICATION, 25 dormant/source-reviewed, one build-only and one retained owner-review deletion candidate**. All seven Phase 3 plus 53 Phase 4 suites pass (**60 total**). Schema/reference/protected-state checks and CSS build pass. Shopify CLI 4.8.0 exits 1 with **632 diagnostics (16 errors / 616 warnings)**, unchanged signatures and no suppression. Evidence: `validation/batch5-services-stats/`. All protected architecture and merchant configuration remain intact.

**B5-SERVICES-STATS live queue after Batch 9:** each actual saved preset/block/setting, progress and service list/grid layouts, long numbers/text at 1440/1280/768/390/320px, real images/native sprite/color contrast, keyboard and screen readers, viewport/visibility/reduced-motion changes, and editor insertion/reordering/unload/reload. Source fixtures are not browser or Shopify certification. PR #22 remains DRAFT; GitGuardian historical checksum false-positive incident 37480944 remains queued for owner triage. Continue with Team Members / People Card and Story Timeline.


### Source checksum evidence and security scan

The services/stats scan at `c0d77a5f5becad477432a5508eea64137db0a1f7` flags four new Generic High Entropy values in the register (incidents 37481256–37481259), alongside historical checksum incident 37480944. All 471 register SHA-256 values were recomputed from source bytes and match; the flagged values are file integrity digests, not credentials. `component-source-hashes.json` now holds explicit path/sha256 records, while `component-register.csv` remains the authoritative review/disposition ledger. The generator refreshes both deterministically. This separates integrity evidence from free-form review prose without losing evidence or disabling detection. Historical findings still require owner false-positive triage; [the failed scan](https://github.com/DylanJHamilton/Quadratum/runs/106154074227) remains visible. No history rewrite or scanner suppression.


## Batch 5 checkpoint — people and timeline — 2026-09-20

Team Members, People Card, Story Timeline and its rail controller have independent source/fixture dispositions: **REQUIRES LIVE VERIFICATION**. Batch 5 now has **45 assigned components: 34 live queued, one dormant retained and ten NOT REVIEWED**.

Team Members uses the real person-block count and explicit first-person fallback when no featured flag is set. The width control is correctly labeled as a percentage; its zero endpoint now means full width instead of an unusable zero-width layout. Columns are bounded by the selected maximum and minimum card width, reducing their count to fit narrow containers. Existing custom colors remain independent from radius and social-icon sizing; native color/shadow fallbacks now have actual consumers. The existing sprite provides decorative mail/LinkedIn/external-link icons alongside text labels. Social destinations are escaped, single-name initials no longer repeat, and initials are decorative beside the actual name. The previously ignored per-card bio clamp now leaves solo/featured profiles complete; grid biographies expand on hover or keyboard focus, including their existing links.

Story Timeline retains its one-milestone stepping and mobile stack. Navigation now measures relative card geometry and clamps to the real scroll boundary, avoiding page-offset and never-disabled end-arrow errors. The scoped controller supports keyboard/RTL, reduced motion, responsive resizing, independent instances and actual editor selection/unload/reload, without retained timers/listeners/observers. Native overflow and complete content remain usable without JavaScript; arrows appear only after successful enhancement with overflow. The existing horizontal connector setting now has a desktop rail consumer. Missing/disabled card media no longer reserves an empty left column; alternating marker/card placement and single-card overrides are coherent. Actual image dimensions/alt, escaped plain content, scoped native colors, wrapping/focus and reduced-motion styles are repaired. New milestone insertion text is neutral; saved content is unchanged.

Register: **472 components — 222 NOT REVIEWED, 223 REQUIRES LIVE VERIFICATION, 25 dormant/source-reviewed, one build-only and one retained owner-review deletion candidate**. All seven Phase 3 plus 54 Phase 4 suites pass (**61 total**). Schema/reference/protected-state checks and CSS build pass; generated CSS is unchanged. Shopify CLI 4.8.0 exits 1 with **632 diagnostics (16 errors / 616 warnings)**, identical signatures and no suppression. Evidence: `validation/batch5-people-timeline/`. Integrity digests are generated separately as explicit path/sha256 records; historical GitGuardian checksum findings remain visible for owner false-positive triage.

**B5-PEOPLE-TIMELINE live queue after Batch 9:** real saved presets/blocks/settings; solo/featured/grid profiles and biography access, image cropping and native social symbols; minimum-card/percentage-width extremes, all vertical/horizontal/media/connector layouts and long translated text at 1440/1280/768/390/320px; touch/trackpad/RTL scrolling, focus and screen-reader behavior, actual custom-color contrast/reduced motion and editor insertion/reordering/selection/unload/reload. These fixtures are not Shopify/browser certification. Shared purchase contracts, all protected architecture and merchant state remain intact. PR #22 remains DRAFT and unmerged. Continue with Tabs/Accordion and Table of Contents.


## Batch 5 checkpoint — Tabs/Accordion and Table of Contents — 2026-09-21

Recovered local people/timeline commit `c03934f` was verified against the saved services/stats tree and published as `51c06edaa88d050dc047d27cfb96ca3350bfa0cb`; matching tree `ec0c136deeba4c6cb004010cf569fa97138f0817`. Its focused regression, all 472 source digests and protected architecture/state checks were rerun successfully. That completed review was recovered, not repeated or discarded.

Tabs/Accordion now renders each rich-text block once. Native disclosures keep all content accessible without JavaScript; a disposable controller enhances the same panels into horizontal/vertical tabs and the selected mobile mode. Force-accordion applies at the configured mobile breakpoint. All existing settings/presets remain. Plain labels/icons are escaped, anchors are unique even with colliding suffixes, panels retain editor identities, and every style is scoped. Keyboard wrapping/RTL, focus transfer between responsive modes, single/multiple-open choices and empty blocks are covered. Initializing a section never writes a hash; only configured user actions update it. Immediate state changes eliminate the old uncancelled animation timer races.

TOC now counts valid manual destinations and renders editor guidance for empty manual insertion. Its native disclosure is open on desktop, with independent mobile state. Labels are escaped and fragments encoded. Auto mode generates collision-safe Unicode IDs within main content and refreshes when sections load, reorder or unload. Active links expose aria-current, scroll respects reduced motion and configured offset, targets receive focus, and modified clicks remain native. Observers, media listeners and animation frames dispose on unload. Sticky behavior remains bounded by the containing layout and is explicitly described in the setting.

**Disposition:** both hosts and their two new scoped controllers are REQUIRES LIVE VERIFICATION. Batch 5: **47 components — 38 live queued, one dormant retained, eight NOT REVIEWED**. Global register: **474 components — 220 NOT REVIEWED, 227 live queued, 25 dormant/source-reviewed, one build-only and one owner-review deletion candidate**.

**Validation:** four affected Phase 4 suites pass (Tabs/TOC, People/Timeline, Services/Stats and Content Builders); schema/reference/protected architecture/merchant-state checks and CSS build pass. Generated theme.css is unchanged. The preceding full-run evidence remains 61 passing suites; the new suite adds focused coverage without claiming a new full-run result. Shopify CLI 4.8.0 exits 1 with **632 diagnostics (16 errors / 616 warnings)** and unchanged signatures. No suppression or clean certification. Evidence: `validation/batch5-tabs-toc/`.

**B5-TABS-TOC live queue after Batch 9:** saved presets/settings/content, horizontal/vertical/mobile modes at 1440/1280/768/390/320px, long/translated/Unicode content and anchors, native disclosure/tab roles and screen readers, touch/RTL/focus/reduced motion/no JavaScript, hash navigation/browser history, actual sticky containing layout and editor insertion/reorder/selection/unload/reload. All earlier queues remain. Protected sections/blocks/builders/products/Quick View/Headers/Footers and merchant settings are preserved. PR #22 remains DRAFT. This recovered source checkpoint predates the subsequently published Cards/Portfolio and Location/Showcase checkpoints; its counts above are historical.


## Batch 5 checkpoint — Cards and Portfolio Grid — 2026-09-21

Cards, Portfolio Grid and the scoped flip controller have independent source/fixture dispositions: **REQUIRES LIVE VERIFICATION**. This checkpoint builds on published people/timeline `51c06edaa88d050dc047d27cfb96ca3350bfa0cb`. A separate active workspace is handling Tabs/Accordion and Table of Contents; its pending work is preserved and is not implicitly approved here.

Cards no longer nests rich-text links or flip controls inside whole-card anchors. Standalone primary/back links remain intact, and the flip button no longer covers the card's destinations. Both faces remain complete without JavaScript. Successful enhancement owns focus, inertness and ARIA state; keyboard toggle/Escape, safe hover, mobile stacked fallback, reduced motion and editor complete-content behavior are scoped and disposable. Grid/row/timeline variants retain their controls, including mobile column choices, centered timeline labels/connector, media ratios, actual image alt, scoped colors and zero values. Text/links are escaped, widths and padding are bounded, and row rails are keyboard reachable.

Portfolio keeps its whole-card destination through a stretched primary link while native rich-text links remain separate, valid and reachable. Empty titles get a named destination, native descriptions/images remain intact, both grid and masonry keep their selected columns, opacity is bounded and long labels/padding are contained. The color-scheme input had no repository group or consumer and is retired consistently with earlier hosts; inherited colors/custom card colors and saved merchant state remain intact. Category chips remain explicitly visual only.

Register at this checkpoint: **473 components — 220 NOT REVIEWED, 226 REQUIRES LIVE VERIFICATION, 25 dormant/source-reviewed, one build-only and one retained owner-review deletion candidate**. Batch 5 has **46 assigned components: 37 live queued, one dormant retained and eight NOT REVIEWED**. The affected Cards/Portfolio suite passes, with actual Liquid and explicit platform/DOM/media-query adapters. Schema/reference/protected-state checks and CSS build pass; generated CSS is unchanged. Shopify CLI 4.8.0 exits 1 with unchanged **632 diagnostics (16 errors / 616 warnings)** and no suppression. The prior full 61-suite result remains historical evidence, not a newly claimed full run. Evidence: `validation/batch5-cards-portfolio/`.

**B5-CARDS-PORTFOLIO live queue after Batch 9:** actual saved settings/presets; grid/row/timeline/masonry and all card variants; native media/alt, colors/contrast and long translated content at 1440/1280/768/390/320px; all whole-card/RTE/front/back destinations, hover/touch/keyboard/screen readers/RTL/reduced motion, and real editor insertion/reordering/unload/reload. All earlier live queues and Batch 4 closure remain unchanged. All 150 sections, 102 Theme Blocks and protected architecture remain; merchant state is unchanged. PR #22 stays DRAFT. No main write, merge or Shopify deployment.


## Batch 5 checkpoint — Location Panels and 3D Showcase — 2026-09-21

Location Panels, 3D Showcase, its scoped loader and the model runtime have independent source/fixture/provenance dispositions: **REQUIRES LIVE VERIFICATION**.

Location custom colors now override their defaults; the radius, shadow and heading size/weight controls work independently of the color toggle. Named empty blocks produce editor guidance instead of blank storefront panels. Optional headings/meta/tags/actions do not reserve empty rows. Addresses are escaped before line breaks; telephone/email/action destinations remain valid and escaped. Card/CTA width, spacing, native theme colors and focus stay scoped.

The Showcase referenced `q-model-viewer.min.js`, which did not exist. A pinned [upstream 4.3.1 distribution](https://github.com/google/model-viewer/releases/tag/v4.3.1) now supplies that theme asset, with downloaded digest verification and preserved license/notices/provenance. Its `qtm-model-viewer` registration is isolated from Shopify native commerce media; no existing product runtime is replaced. Loading is lazy and shared across hosts. Progress events, errors/timeouts, hidden/reduced-motion/editor rotation limits and complete disposal/stale-callback behavior are fixture-covered. Poster/gallery/direct file links remain available without the engine. Missing media no longer inflates gallery counts or reserves a viewer column; the existing custom aspect option now has a height control. Liquid trimming previously joined the camera/AR attributes and is corrected. The renderer still needs actual WebGL/model/network/AR acceptance; compressed model/texture decoders may request upstream assets, explicitly documented under `docs/vendor/model-viewer/`.

Register: **475 components — 218 NOT REVIEWED, 230 REQUIRES LIVE VERIFICATION, 25 dormant/source-reviewed, one build-only and one retained owner-review deletion candidate**. Batch 5: **48 assigned entries — 41 live queued, one dormant retained, six NOT REVIEWED**. The affected Location/Showcase suite passes with explicit native/DOM/model-event adapters. Schema/reference/protected-state checks and CSS build pass. Vendor internals are excluded from Tailwind utility extraction, keeping generated CSS byte-identical. Shopify CLI 4.8.0 exits 1: **631 diagnostics (16 errors / 615 warnings)**; one obsolete variable-name warning removed, no added signatures or suppression. Evidence: `validation/batch5-location-showcase/`. Earlier full-suite evidence remains historical; a fresh complete run is reserved for closure.

**B5-LOCATION-SHOWCASE live queue after Batch 9:** actual saved locations, contact links/custom colors/typography; all poster/gallery/viewer layouts, GLB/USDZ/decoder requests/CSP, WebGL/AR devices and native product-media coexistence; long translations, 1440/1280/768/390/320px, RTL/keyboard/screen readers/reduced motion and actual editor insertion/reordering/unload/reload. Protected architecture and merchant state remain unchanged. PR #22 remains DRAFT; no main write, merge or Shopify deployment. Tabs/Accordion/TOC from the other local checkpoint still need publication/reconciliation; remaining original hosts are Countdown/Progress, Predictive Search, Pricing Table and Block Section.


## Batch 5 checkpoint reconciliation — Tabs/Accordion and TOC — 2026-09-21

The completed local `d488aed` source checkpoint is reconciled onto published `47ea36aa856a9ce14a033387822c1ce756c3481d` without dropping the newer Cards/Portfolio or Location/Showcase work. All four recovered source files, their own regression and historical validation files are preserved exactly. Their source reviews are recovered rather than repeated. Both builders and every Theme Block retain their separate Batch 8 obligations.

Current register: **477 components — 216 NOT REVIEWED, 234 REQUIRES LIVE VERIFICATION, 25 dormant/source-reviewed, one build-only and one retained owner-review deletion candidate**. Batch 5: **50 components — 45 live queued, one dormant retained, four NOT REVIEWED**. The Tabs/TOC regression passes against the reconciled checkout; protected architecture/reference/state checks and CSS build also pass. Generated CSS remains byte-identical. Reconciliation outputs are recorded beside the historical evidence in `validation/batch5-tabs-toc/`. All B5-TABS-TOC live obligations above remain queued after Batch 9. The remaining four hosts are still NOT REVIEWED. PR #22 remains DRAFT; no main write, merge or deployment.

## Batch 5 checkpoint — Countdown / Progress — 2026-09-21

The host and its extracted scoped countdown controller are **REQUIRES LIVE VERIFICATION**. All three modes retain their saved settings. Calendar validation now rejects malformed strings and impossible/leap-century dates before native date parsing; the emitted epoch also requires numeric output. Blank inputs remain invalid. Progress clamps stay native, fractional step indices resolve to exactly one current step, and connector fill works without JavaScript. The static deadline remains usable without script; misleading zero countdown units are hidden until initialized. Expiry stops its timer at the actual deadline even when seconds are off. Visibility pauses, duplicate initialization, independent hosts, stale callbacks and editor unload/reload are covered; expired/invalid hosts remain discoverable in the editor. Invalid spacing/connector arithmetic, negative radii, overflowing unit minimums, inactive zero shadow and plain-text escaping are repaired with scoped styles.

Register: **478 components — 215 NOT REVIEWED, 236 REQUIRES LIVE VERIFICATION, 25 dormant/source-reviewed, one build-only and one owner-review deletion candidate**. Batch 5: **51 entries — 47 live queued, one dormant, three NOT REVIEWED**. The actual-source Countdown/Progress suite, schema/reference/protected-state checks and CSS build pass; CSS output is byte-identical. Shopify CLI 4.8.0 exits 1 with **631 diagnostics (16 errors / 615 warnings)**; no added signatures or diagnostic suppression. Evidence: `validation/batch5-countdown-progress/`.

**B5-COUNTDOWN-PROGRESS live queue after Batch 9:** actual Shopify store and UTC parsing, timezone/DST/leap dates/render caching/customer clock; all modes/settings, native fallback, expired/invalid states, responsive/RTL/keyboard/AT and real editor lifecycle. LiquidJS clock/date adapters do not certify Shopify timezone behavior. Shopify documents that [Liquid date output can reflect cached render time](https://shopify.dev/docs/api/liquid/filters/date); the countdown uses the customer clock against the rendered deadline. Merchant guidance explicitly distinguishes this display from discount scheduling. PR #22 remains DRAFT. Architecture and merchant state remain protected; no main write, merge or deployment.

## Batch 5 checkpoint — Content Predictive Search — 2026-09-21

The independent Content Search host and its scoped controller are **REQUIRES LIVE VERIFICATION**. The shared search system was already reviewed and is not reopened. The host now requests its native section ID, retaining Shopify’s existing section settings instead of sending unsupported custom parameters to a file-handle response. Locale-aware suggest/search URLs, one comma-separated resource-types parameter, `limit_scope=each` and the supported 1–10 request limit follow the [Predictive Search API](https://shopify.dev/docs/api/ajax/reference/predictive-search). Native section settings behavior follows the [Section Rendering API](https://shopify.dev/docs/api/ajax/section-rendering). Request baseline and per-group saved controls remain; the platform cap and partial collection filtering are explained in merchant settings. Collection filtering counts eligible results before deciding whether a group is empty. Full search remains explicitly unrestricted by collection.

All four layouts/scopes retain native Product Card or row rendering. Zero groups are omitted; query chips and typed locale search/view-all links work without JavaScript. Nested combobox/listbox/interactive roles are replaced with one input combobox and a labelled result dialog with native links/controls; arrows move actual focus, Tab remains native, and Escape restores the input. The scoped controller handles IME, immediate stale-query closure, cancellation, out-of-order completion, errors, missing fragments, timeouts, outside focus, optional validated recent storage, duplicate boot and independent editor disposal/reload. Scoped bounds, mobile padding, custom colors, focus and reduced motion remain available.

Register: **479 components — 214 NOT REVIEWED, 238 REQUIRES LIVE VERIFICATION, 25 dormant/source-reviewed, one build-only and one owner-review deletion candidate**. Batch 5: **52 entries — 49 live queued, one dormant, two NOT REVIEWED**. Actual-source Content Search plus the existing shared Search Contracts suites pass; schema/reference/protected-state checks and CSS build pass with byte-identical CSS. Shopify CLI 4.8.0 exits 1: **627 diagnostics (16 errors / 611 warnings)**; three hardcoded-route warnings and one obsolete variable-name warning removed, no new signatures or suppression. Evidence: `validation/batch5-content-search/`.

**B5-CONTENT-SEARCH live queue after Batch 9:** actual predictive requests using each saved dynamic section ID and settings; all layouts/scopes/limits, collection membership, variant-specific media/links, locale/currency and native full search, no-JS/error/throttle/unsupported locale states; widths/RTL/keyboard/AT and actual editor insertion/unload/reload. DOM/Liquid response fixtures do not certify the Shopify endpoint or screen readers. Both protected builders and remaining host reviews continue independently. PR #22 remains DRAFT; no main write, merge or deployment.

## Batch 5 checkpoint — Protected Content Hosts — 2026-09-21

Pricing/Data Table and Block Section are **REQUIRES LIVE VERIFICATION**. All three pricing modes, native Theme Block rendering and existing presets/child allowlists remain. Pricing counts only blocks belonging to its selected mode and requires at least one data-column label; empty modes provide editor guidance. Plain labels/cells/prices/links are escaped while merchant rich text remains intact. Highlight indices are bounded, including valid single-plan highlighting. Native data tables retain a caption and column headers when the visible heading row is disabled; sparse columns and empty cells remain correctly associated. Pricing rails and tables have native keyboard focus, corrected percentage sizing, bounded padding/grids, wrapping/RTL/focus/reduced motion and a repaired self-referencing shadow token.

Block Section keeps `content_for 'blocks'`, app support, row/column descendants, all three presets, background/overlay controls and optional native page content. Its section tag is bounded and the div variant has a named region. Hidden viewport settings no longer hide the host in the editor, and an unavailable page does not suppress empty guidance. Parent mobile rules now apply only to rows that select inheritance, preserving explicit row mobile modes. A limited `layout-row` dependency repair adds keyboard access to scrollable rows, editor visibility and an explicit **Use section spacing** setting: the default preserves the prior section-driven gap behavior; turning it off activates the row’s own existing spacing values. This is not an independent Theme Block review. The row stays **NOT REVIEWED** for Batch 8, as do all 102 Theme Blocks; no approval is inferred from the projection adapter.

Register: **479 components — 212 NOT REVIEWED, 240 REQUIRES LIVE VERIFICATION, 25 dormant/source-reviewed, one build-only and one owner-review deletion candidate**. Batch 5: **52 entries — 51 live queued, one dormant, zero NOT REVIEWED**. Protected Content Hosts, existing Product Content Hosts and Content Builders regressions pass, as do schema/reference/protected-state checks and CSS build; generated CSS remains byte-identical. Shopify CLI 4.8.0 exits 1 with **621 diagnostics (16 errors / 605 warnings)**; six confirmed unused pricing assignments removed, no added signatures or suppression. Evidence: `validation/batch5-protected-content-hosts/`. Formal Batch 5 closure awaits the fresh complete suite run.

**B5-PROTECTED-CONTENT live queue after Batch 9:** each price-list/plan/data mode and sparse/empty/long content, visible/hidden headers/captions, highlights/CTAs/colors and no-JS/AT scroll behavior; both builder chains with real nested blocks/apps, all presets, inherited/explicit row gaps/mobile modes, page output, background/visibility and real editor insertion/reordering/reload. All prior batch queues remain. 150 sections, 102 Theme Blocks, protected commerce/Quick View/Header/Footer variants and merchant state remain preserved. PR #22 stays DRAFT; no main write, merge or deployment.

## B5-FINAL — formal Content / Interactive engineering closure — 2026-09-21

**Batch 5 engineering is formally closed: 52 assigned components, 51 REQUIRES LIVE VERIFICATION, one RETAINED — DORMANT SOURCE REVIEWED, zero NOT REVIEWED.** The original 44-component scope is preserved; eight explicitly inventoried runtime dependencies added during repairs are included in this closure. Published source checkpoint: `29e645923abaf03a5c7b0838e9b4a387924c9556`, tree `23ce150bfb8f07046b46023fb86317285b910bb4`. The earlier people/timeline and recovered tabs/TOC checkpoints are retained; their completed reviews were not restarted. Each individual component’s settings, repairs, evidence and remaining live obligations stay in the register and checkpoint sections above.

A fresh complete run passes **all seven Phase 3 and 60 Phase 4 suites — 67 total**. Every one of the 479 inventoried source hashes reproduces from file bytes. Schema/reference/protected-architecture and merchant-state checks pass, all three protected content presets/child allowlists remain, and the CSS build is byte-identical to the owner’s saved `c0d77a5` output. The latest native Theme Check run covers the unchanged final source and **exits 1 with 621 diagnostics (16 errors / 605 warnings)**. Existing debt remains unsuppressed; this is not clean certification. Full evidence: `validation/batch5-content-closure/`.

Global register: **479 components — 212 NOT REVIEWED, 240 REQUIRES LIVE VERIFICATION, 25 dormant/source-reviewed, one build-only and one retained owner-review deletion candidate**. The 7,830 SVG files remain separately inventoried only. Both builders, all 150 sections/102 Theme Blocks, protected commerce/Quick View/Header/Footer variants and `config/settings_data.json` remain preserved. The limited row dependency repair does not close any Theme Block’s Batch 8 review. Batch 4 remains formally closed.

**B5-FINAL live acceptance after Batch 9** includes all named Batch 5 checkpoint queues: real Shopify settings/presets/data/providers/native requests/date parsing, desktop/mobile/RTL/long text/no-JS, keyboard/AT/motion/focus and actual editor insertion/reordering/unload/reload. Model WebGL/AR, native predictive API settings, theme block projection and date/timezone adapters are explicitly not runtime certification. All earlier batch queues continue unchanged.

**Continue immediately to Batch 6 Forms / Conversion:** 14 assigned NOT REVIEWED components — eight native form hosts, `form-core`, `form-dispatch`, two stylesheet dependencies and two form-logic assets. Review shared native submission/field/error/identity contracts first, then each host’s distinct settings/presets and lifecycle, publishing coherent groups. Continue Batches 7–9 before accumulated live acceptance. No owner decision blocks this continuation. PR #22 stays DRAFT; no main write, merge or Shopify deployment.

## Batch 6 checkpoint — native fields and dispatch (partial) — 2026-09-21

Batch 6 has started from formal B5 closure `7ddc1f43f18ca3b2db8ab242418daca71b84e5a7`. **All 14 Forms / Conversion entries remain NOT REVIEWED**: this is an incremental engineering checkpoint, not source/consumer completion. The global dispositions remain 479 components: 212 NOT REVIEWED, 240 live queued, 25 dormant/source-reviewed, one build-only and one owner-review deletion candidate.

The shared field renderer uses stable section/block identities, deterministic names for otherwise-colliding default fields, native values for inputs/textarea/select/radio, actual newline-delimited options, escaped plain copy/attributes and labelled radio groups. Password/file values are never prefilled and consent is never prechecked. Each native Shopify form retains a required `contact[email]` input, including a visible fallback when the merchant omitted an email block. Server-returned values and native error output are preserved. Dispatch now selects the configured Contact or Customer form, retains native validation/captcha wiring, preserves explicit false integration metadata and inherited button variants, and accepts only configured HTTPS/store-relative custom endpoints. Missing/malformed endpoints show an unavailable state instead of silently posting to Contact. Existing custom upload encoding remains multipart.

Contracts follow Shopify's [contact form documentation](https://shopify.dev/docs/storefronts/themes/customer-engagement/add-contact-form) and [native captcha integration](https://shopify.dev/docs/storefronts/themes/trust-security/captcha). Native Shopify forms no longer emit custom-provider token inputs. Custom-provider server verification, marketing persistence and actual endpoint delivery remain runtime obligations; metadata does not implement those services.

The new actual-source `forms-native.cjs` suite passes across both native routes, valid/invalid custom endpoints, fields/options/defaults/reordering/escaping, required email, server error/success states and four shared-renderer host presets. Its Shopify form/error adapters do not contact or certify Shopify. Schema/reference/protected-architecture/merchant checks and CSS build pass; generated CSS is byte-identical. Theme Check exits 1 with unchanged **621 diagnostics (16 errors / 605 warnings)**, no added signatures or suppression. The B5 full 67-suite result remains historical; this checkpoint adds focused coverage only. Evidence: `validation/batch6-native-fields/`.

**Still open before any Batch 6 source disposition:** overlapping controllers, real step grouping and progressive enhancement, condition/native-required interactions, unload/reload and stale callbacks, custom captcha failures, file/password endpoint capability handling, attribution/variant scope, responsive/control styles, and each of the eight hosts' distinct settings/presets/markup. Newsletter/Waitlist retain duplicated native markup pending their own reviews; Search has a separate native/predictive contract. Continue those engineering reviews before live acceptance after Batch 9. Batch 4 and B5 stay formally closed. PR #22 stays DRAFT; protected state is intact; no main write, merge or deployment.

## Batch 6 checkpoint — scoped form controller and progressive steps — 2026-09-21

Both legacy form-logic assets are independently **REQUIRES LIVE VERIFICATION**. They intentionally contain the same guarded controller, with source-parity regression coverage, so saved hosts can retain either entry point without competing initializers. Seven hosts now opt in explicitly and load their entry point with `defer`; unrelated forms are untouched. A newly inventoried `form-field` helper keeps native step grouping structurally balanced. It, the other snippets, both stylesheets and every host retain their independent unfinished reviews.

Native fields remain complete without JavaScript. Real step groups contain their fields; enhancement owns Back/Next, focus, inert/hidden state and final payload validation, and editor mode keeps all groups visible. Native constraint validity replaces restrictive custom email/telephone regexes. Conditions compare exact names instead of interpolated selectors, support arbitrary checked values, disable hidden payloads, restore control state and keep required native email available. Plain error copy uses text nodes. Duplicate legacy script loads, simultaneous instances and editor unload/reload are disposable. Nonblank attribution defaults are preserved; the shared controller no longer guesses a variant from unrelated product controls.

Shopify Contact/Customer submissions retain native hCaptcha ownership, including documented dynamic-form protection. Custom endpoints can use one shared SDK download per selected provider, a visible Turnstile challenge or fresh reCAPTCHA v3 token, with missing keys, failed loads, rejection, expiry and deadlines stopping the post. Editing/reset/removal cancels pending verification; delayed callbacks cannot submit. Successful verification re-enters `requestSubmit`, retaining native submit events and submitter data. Client tokens do not implement server verification or marketing fan-out. Contracts follow [Shopify captcha](https://shopify.dev/docs/storefronts/themes/trust-security/captcha), [Turnstile explicit rendering](https://developers.cloudflare.com/turnstile/get-started/client-side-rendering/) and [reCAPTCHA v3](https://developers.google.com/recaptcha/docs/v3).

The native and controller suites pass, including four actual step hosts with native CSS visibility, five actual shared-renderer presets, two assets/multiple roots, conditional FormData omissions, server errors, Shopify-vs-custom captcha, mocked provider success/rejection/error/expiry/deadline, shared SDK retries and stale callback/disposal cases. These explicit Liquid/DOM/provider adapters make no actual submission or Shopify/SDK certification. Schema/reference/merchant/protected-architecture checks pass. CSS build remains byte-identical; the existing `invisible` utility is explicitly retained after removing the unrelated old captcha string that happened to keep it in Tailwind output. Evidence: `validation/batch6-form-controller/`.

**Native Theme Check exits 1: 605 diagnostics (9 errors / 596 warnings).** Seven parser-blocking script errors and ten remote-script warnings are removed by deferred entry points and lazy custom-provider loading. Newsletter's existing complexity decreases from 201 to 197. One added `OrphanedSnippet` diagnostic names `form-field`; its literal `form-core` render is confirmed by both repository tracing and the native focused dependency graph. The unmodified whole-project graph already fails on a pre-existing non-theme project entry and reports all 96 previous snippets as orphaned; it now reports 97. This diagnostic and all existing debt are preserved, with no suppression, checker patch or clean certification. Direct parser inspection also identified pre-existing cross-capture wrapper markup in Full/Compact Quote; repairing those independently pending hosts is next.

Register: **480 components — 211 NOT REVIEWED, 242 REQUIRES LIVE VERIFICATION, 25 dormant/source-reviewed, one build-only and one owner-review deletion candidate**. Batch 6: **15 entries — two live queued, 13 NOT REVIEWED**, including the new field dependency. **B6-CONTROLLER live queue after Batch 9:** actual saved consumers and no-JS behavior; native Contact/Customer returns and Shopify hCaptcha; custom endpoint POST/server token verification, provider configuration/CSP/mobile challenge layouts, keyboard/AT/RTL and actual editor/browser-back lifecycle. Waitlist's separate variant code, each host's settings/layout/presets, native endpoint field capabilities and shared CSS/snippet completion remain open. All protected architecture, merchant state and closed B4/B5 engineering remain intact. PR #22 stays DRAFT; no main write, merge or deployment.

## Batch 6 checkpoint — Quote / Support hosts and native capabilities — 2026-09-21

CTA Quote, Full Quote, Compact and Support Request have independent **REQUIRES LIVE VERIFICATION** dispositions, alongside `form-core`, `form-field`, `form-dispatch` and the newly inventoried `form-background` dependency. The remaining Newsletter, CTA Newsletter, Waitlist, Search and two shared stylesheet reviews remain open; no host approval is inferred from a shared dependency.

CTA Quote keeps its Card/Split/Steps presets, custom anchor and native color/select controls. Its CSS now uses a stable section scope, and the inherited button setting reaches the shared renderer. Full Quote independently replaces cross-capture HTML fragments with balanced native surfaces; half/full field widths, split-media order, zero input borders and theme tokens work without forced text direction. Compact has its own correct schema/preset/root name, retains its documented rem-times-ten spacing scale and frameless surface, and receives equivalent structural repairs. Support preserves its separate card/frameless settings, has distinct elevation levels and an actual support source parameter; invented/unconsumed privacy-CC, SLA and subject attributes are removed. All four omit unavailable split media, escape plain copy, preserve explicit false metadata, keep hidden viewport settings reachable in the editor and bound long content/padding. The existing contrast helper keeps opaque form surfaces and white inputs readable; background-image contrast still requires live acceptance.

Native routing capabilities are explicit. Shopify [Customer forms subscribe the submitted email to marketing](https://shopify.dev/docs/storefronts/themes/customer-engagement/email-consent); the quote selectors now explain that purpose and the renderer adds an unchecked required marketing confirmation. Contact remains the inquiry route. One canonical native email is retained even when additional email fields reuse its reserved name. This theme offers file uploads and password inputs only to configured custom endpoints: native Shopify routes request a file link and omit password collection, with editor guidance. Custom uploads retain multipart encoding; actual endpoint processing/storage is not implemented by metadata. Native selects keep their browser affordance when no custom arrow control is present. Explicit custom destination identity prevents a user-configured `form_type` field from selecting native captcha handling. The controller's existing native/custom regressions cover that dependency repair.

Decorative media is shared through a native image/color fallback and strictly parsed MP4/YouTube/Vimeo inputs. Provider URLs become canonical HTTPS embeds; common legacy HTTP provider inputs and Vimeo private hashes are retained. The existing video controller supplies manual pause/play, reduced motion, visibility and unload/reload handling; its limited dependency change starts playback paused in the Theme Editor. The earlier Video Hero regression still passes, and all earlier media queues remain. Native poster/field content is available without JavaScript. Actual embedding permissions, media requests, autoplay and provider/CSP behavior are not certified by fixtures. Provider contracts: [YouTube parameters](https://developers.google.com/youtube/player_parameters), [Vimeo parameters](https://help.vimeo.com/hc/en-us/articles/12426260232977-About-Player-Parameters).

Four affected suites pass: the **185-case independent host preset/settings matrix**, native fields/routing, controller/lifecycle and existing Video Hero regression. All eight changed Liquid hosts/helpers parse with Shopify's native HTML/Liquid parser. Schema/reference/merchant/protected-architecture checks pass. CSS builds byte-identically; the existing `text-input` utility is explicitly preserved after obsolete comment text stopped keeping it in Tailwind's scan. Exact endpoint, image, color and provider behavior uses declared adapters; no real form submissions, pixels, SDK or Shopify acceptance are claimed. Evidence: `validation/batch6-quote-support/`.

**Theme Check exits 1 with 579 diagnostics (7 errors / 572 warnings).** Two native cross-capture syntax errors and 25 existing variable-name warnings are removed. One new orphan warning names the background helper despite four literal host references, also confirmed by the native focused graph. The previously documented whole-project graph failure now yields 98 orphan warnings; the unmodified checker and all diagnostics remain. No suppression or clean certification.

Register: **481 components — 204 NOT REVIEWED, 250 REQUIRES LIVE VERIFICATION, 25 dormant/source-reviewed, one build-only and one owner-review deletion candidate**. Batch 6: **16 entries — 10 live queued, six NOT REVIEWED**. **B6-QUOTE-SUPPORT live queue after Batch 9:** actual saved presets/blocks/controls; Contact delivery and Customer signup/consent/return states and appropriate merchant copy; custom POST, multipart uploads, server captcha/integration handling; all media/alt/colors/contrast, 1440/1280/768/390/320px, RTL/keyboard/AT/no-JS and actual editor insertion/reordering/unload/reload. Both builders, all 150 sections/102 Theme Blocks, protected commerce/Quick View/Header/Footer variants, merchant state and B4/B5 closure remain preserved. PR #22 stays DRAFT. Continue the remaining Batch 6 hosts and CSS before Batches 7–9 and accumulated live acceptance. No main write, merge or deployment.


## Batch 6 checkpoint — Newsletter hosts — 2026-09-21

Forms Newsletter and CTA Newsletter independently move to **REQUIRES LIVE VERIFICATION**. Every saved preset and supported layout/setting/field route is covered by the 117-case newsletter suite. Forms Newsletter now shows the saved heading in Inline mode, uses a bounded email/button row and stacks on small screens. Both hosts retain Card/Split controls, omit unavailable split media, preserve image alt text, scope CSS independently of custom anchors, escape plain copy, expose hidden viewport settings in the editor and use the previously reviewed decorative-media lifecycle. Explicit false marketing metadata remains false; custom source is `newsletter`. Existing setting IDs/defaults/presets and merchant state are preserved; control guidance explains endpoint/captcha/response capabilities.

CTA Newsletter's four duplicated native/custom/step field branches are replaced by the shared renderer. Shopify owns native Customer form action/captcha and supplies real error/success output. Step fields are complete without JavaScript, have stable block IDs and have independent navigation/disposal when enhanced. Native selects, real-line options/defaults, radio groups, inline errors and labels use the reviewed contracts. Both newsletter routes retain a required email even with no field blocks; custom fields retain explicit names, with a conventional `email` fallback when no name is configured. Normalizing a plain native `email` name before reserving it prevents another field from duplicating `contact[email]`.

Consent is deliberate: an explicitly labelled, required, unconditional checkbox/consent named `contact[consent_marketing]` (or its plain form) can supply marketing confirmation. Optional, conditional, unnamed or privacy-only fields do not replace the separate required unchecked Customer marketing confirmation. Thus the Forms Newsletter presets keep their one explicit marketing agreement, while the CTA preset retains its privacy agreement plus a separate marketing confirmation. No stored consent is preselected. Customer signup is a marketing subscription; actual native storage of other fields and confirmation-email behavior remain Shopify checks, and metadata does not implement external delivery or double opt-in.

Affected Newsletter, native fields/routing, shared controller and Quote/Support regressions pass. The newsletter suite covers 117 actual preset/settings/field-route cases; the retained Quote/Support matrix covers 185 cases. Changed Liquid parses with Shopify's native HTML parser. Schema/reference/protected architecture/merchant checks and the byte-identical CSS build pass. Fixtures use explicit native transport/DOM/color/image adapters; no real submission, browser pixels or Shopify acceptance is claimed. Evidence: `validation/batch6-newsletter/`.

**Theme Check exits 1 with 539 diagnostics (seven errors / 532 warnings)**, down from 579: 31 variable-name, eight unclosed HTML and one complexity warning removed. Existing errors and the documented whole-project orphan-graph debt remain unsuppressed. No clean certification.

Register: **481 components — 202 NOT REVIEWED, 252 REQUIRES LIVE VERIFICATION, 25 dormant/source-reviewed, one build-only and one owner-review deletion candidate**. Batch 6: **16 entries — 12 live queued, four NOT REVIEWED**. Next: Waitlist, Search, then both shared stylesheet consumer reviews and formal Batch 6 closure. **B6-NEWSLETTER live queue after Batch 9:** actual saved configurations/presets, native signup/consent/hCaptcha/return states, custom POST/server provider integration, double opt-in and actual native field storage; media requests/permissions, 1440/1280/768/390/320px, contrast/RTL/keyboard/AT/no-JS and native editor insertion/reordering/unload/reload. Earlier queues and B4/B5 closure remain intact; PR #22 stays DRAFT. No main write, merge, deployment or merchant-state rewrite.


## Batch 6 checkpoint — Product Waitlist — 2026-09-21

Waitlist and its two explicitly inventoried dependencies (`form-waitlist-fields` and `form-waitlist.js`) move to **REQUIRES LIVE VERIFICATION**. The host keeps its independent Inline/Card/Split/surface settings, zero spacing, image overlays, theme/button/input controls and existing presets. Plain copy/attributes are escaped; custom anchors no longer determine CSS selectors; split media is optional with native alt text; narrow controls and long labels are bounded. Viewport-hidden settings remain reachable in the editor. No merchant setting or protected commerce component was changed.

A published current product or explicit handle supplies native variant options. Blank/unpublished/empty products produce an unavailable state plus editor guidance, including the existing homepage placement that has no product override. No global product JSON or Ajax fetch is used. The actual selected variant is retained when outside the rendered list, and a locale-correct product link exposes additional choices. [Shopify's Product object](https://shopify.dev/docs/api/liquid/objects/product) limits the unpaginated variants list to 250; this is acknowledged, not treated as all-variant certification. Large catalogs, selection outside the initial list and all_products lookup limits remain queued.

The shared dispatcher owns native Customer/custom endpoint transport, real server return feedback, endpoint guards and captcha ownership. The dedicated fields provide native `contact[email]`, corrected `contact[first_name]`, descriptions/errors and a usable variant select. Custom endpoint variant ID and checked consent submit without JavaScript; no hidden consent mirror can remain false. Native signup preserves the merchant's waitlist agreement and separately requires unchecked marketing confirmation. [Shopify Customer forms are marketing signups](https://shopify.dev/docs/storefronts/themes/customer-engagement/email-consent): automatic restock delivery is not implemented by this theme's hidden fields or tags. Editor routing/success guidance makes the integration requirement explicit, while storefront native copy explains the marketing subscription. Native custom-field storage, existing-subscriber tags and actual notification delivery remain live/backend checks. Without JavaScript, product tags and the native email signup remain; variant tag enrichment explicitly requires enhancement.

Automatic binding is restricted to a uniquely identified purchase form on the matching product page, with exact product ID/handle/URL ownership. It ignores unrelated featured products, nested other-product forms and dialogs, and falls back to the still-visible picker when ownership is ambiguous. Selected native product options can supply an additional variant beyond the rendered list. Changes replace reserved product/variant context tags rather than accumulating stale variants. Other merchant/attribution tags remain. Reset, page restoration, duplicate assets, shared captcha cancellation and section unload/reload are scoped; direct picker choices survive page restoration. No cart/product controller was modified.

The **51-case independent preset/settings matrix**, additional native/custom payload/product ownership/high-variant/lifecycle cases, native fields/routing and shared controller suites pass. Changed Liquid parses natively. Schema/reference/protected architecture/merchant-state checks and byte-identical CSS build pass. Evidence: `validation/batch6-waitlist/`. Liquid, DOM, image/color and native form transport are explicit adapters, not Shopify/endpoint/pixel certification. Theme Check exits 1 with **527 diagnostics (seven errors / 520 warnings)**. Thirteen existing variable-name warnings are removed; the new literally referenced helper adds one unsuppressed orphan warning under the existing whole-project graph failure (99 orphan warnings total). Existing schema-count debt remains.

Register: **483 components — 201 NOT REVIEWED, 255 REQUIRES LIVE VERIFICATION, 25 dormant/source-reviewed, one build-only and one owner-review deletion candidate**. Batch 6: **18 entries — 15 live queued, three NOT REVIEWED**. Next: Site Search/Finder, the two shared CSS assets and formal Batch 6 closure, then Batches 7–9. **B6-WAITLIST acceptance after Batch 9:** actual saved PDP/landing-product selection, missing/unpublished/large-variant products, native signup/captcha/consent/storage/return/tag behavior, configured restock delivery and matching merchant copy, custom POST/server verification/consent/variant processing, 1440/1280/768/390/320px/RTL/contrast/keyboard/AT/no-JS and actual editor insertion/reordering/unload/reload. Existing live queues and B4/B5 closures remain intact. PR #22 stays DRAFT; no main write, merge or production deployment.


## Batch 6 checkpoint — Site Search / Finder — 2026-09-21

Site Search/Finder and its two inventoried dependencies (`form-search-results` and `form-search.js`) move to **REQUIRES LIVE VERIFICATION**. Native GET now owns search navigation. The saved target is localized; blank/default or invalid external/query/fragment targets fall back to Shopify search. The form retains query/filter values and valid full-search types without JavaScript. Basic/Finder inputs do not pretend to be predictive comboboxes, and this host remains outside the generic POST-form controller. Existing layouts, surfaces, media/alt, theme button/font/color controls, zero spacing and presets are retained, with stable CSS scopes, bounded small-screen geometry, escaped plain copy and editor visibility. Configured nonblank quick links remain usable natively.

[Shopify full search supports products, articles and pages](https://shopify.dev/docs/api/liquid/objects/search). The retained Collections setting applies to suggestions; full submission explicitly falls back to supported types. Finder now uses native `filter.p.t.category`, `filter.p.tag`, `filter.v.price.gte` and `filter.v.price.lte`, with real newline options and no invented default catalog values. Empty category/tag configurations omit those controls and show editor guidance. [Storefront filters must be enabled in Shopify admin and use their matching values](https://shopify.dev/docs/storefronts/themes/navigation-search/filtering/storefront-filtering); this is queued, not inferred from fixture GET parameters. Native minimum/maximum price controls work without JavaScript. Optional saved price-band shortcuts set these same controls, reject malformed/reversed bands and retain zero/open bounds. Under-price bands use an upper bound one cent below their value; ranges are inclusive. The settings explain shop-currency semantics. Cross-bound errors, reset and disposal are scoped.

[Predictive search](https://shopify.dev/docs/api/ajax/reference/predictive-search) now returns native Liquid panel HTML from the actual saved section ID, with per-source limits, native money/images and escaped result data. Raw JSON string interpolation, global inline initialization and unconditional dataLayer emission are removed. The already-reviewed Content Search controller supplies stale-request cancellation, timeout/failure fallback, composition handling, real keyboard link focus, Escape and editor disposal. Its limited dependency extension supports an optional changing resource selector and dynamic full-results link; the existing B5 Content Search regression still passes and that source review is not repeated. Localized requests keep `resources[limit_scope]=each`; Collections are valid suggestions, while fallback submission remains native search. No hardcoded unrelated section renderer or JSON/HTML endpoint mixing remains.

The **91-case independent preset/settings/scope matrix**, additional native GET/filter/result/price/lifecycle cases and earlier Content Search suite pass. Changed Liquid parses natively; schema/reference/protected architecture/merchant-state checks and byte-identical CSS build pass. Explicit Liquid/DOM/money/image/request adapters do not certify Shopify search, filter administration, market currency, browser pixels or AT. Evidence: `validation/batch6-search/`. **Theme Check exits 1 with 514 diagnostics (seven errors / 507 warnings)**: 14 variable-name warnings removed, one new referenced helper orphan warning under the existing whole-project graph failure (100 orphan warnings total). Existing schema-count/error debt remains; no suppression or clean certification.

Register: **485 components — 200 NOT REVIEWED, 258 REQUIRES LIVE VERIFICATION, 25 dormant/source-reviewed, one build-only and one owner-review deletion candidate**. Batch 6: **20 entries — 18 live queued, two NOT REVIEWED**. Both shared stylesheets still require their own consumer review before formal Batch 6 closure. **B6-SEARCH acceptance after Batch 9:** actual saved layouts/settings/presets, locale/custom target GET and enabled category/tag/price filter catalog values; shop/market currency and real price display; native predictive section-ID/scopes/per-source limits and Collection suggestion fallback; 1440/1280/768/390/320px, RTL/contrast/keyboard/AT/no-JS and native editor insertion/reordering/unload/reload. Earlier queues, B4/B5 closure and protected architecture/merchant state remain intact. PR #22 stays DRAFT. Continue stylesheet closure, then Batches 7–9; no main write, merge or deployment.


## Batch 6 formal engineering closure — shared styles and B6-FINAL — 2026-09-21

**Batch 6 is FORMALLY CLOSED for engineering: 20 assigned components, 20 REQUIRES LIVE VERIFICATION, zero NOT REVIEWED.** This completes the eight form hosts, six rendering dependencies and six CSS/JavaScript assets. Every earlier B6 checkpoint and live queue remains applicable. Native Shopify submission/storage, configured external integrations, storefront pixels and Theme Editor acceptance are not certified offline. Batches 4 and 5 remain closed; no completed source review was repeated to obtain this disposition.

The two shared stylesheets receive independent consumer reviews. Forms CSS belongs to the six Forms hosts and the two legacy Bulk Ordering/CRM wrappers; CTA CSS belongs to the actual CTA Quote/Newsletter section roots. Full Quote and Compact retain their saved legacy CTA class, but it no longer lets the CTA stylesheet override their layout when both assets are loaded. Low-specificity shared rules preserve stronger per-host setting controls regardless of asset order. Duplicate field/select rules and the fixed 220px field minimum are removed. Native text controls receive input chrome; DIV field wrappers, checkbox/radio inputs and checkbox labels no longer receive accidental text-input sizing. Selects retain native affordances unless the Quote host deliberately supplies its chevron; forced colors restore native arrows. Logical spacing/text, bounded controls/split columns, wrapping actions, focus and reduced motion are retained. Hidden metadata rows consume no layout space while their native payload remains present.

The new stylesheet regression checks **196 actual rendered consumer controls/fields in both asset orders**, plus bidirectional root isolation, Bulk wrapper exclusion, hidden metadata payload and forced-color rules. Bulk Ordering and CRM are CSS consumers here; their complete host reviews remain NOT REVIEWED in Batch 7. JSDOM checks supported computed properties and selector matches; it does not implement real CSS layout, media queries or variable resolution. Responsive pixels, RTL/zoom, system colors and native control rendering stay in acceptance.

A fresh full regression run passes **74 suites: seven Phase 3 and 67 Phase 4**. All 14 Batch 6 Liquid hosts/helpers parse with Shopify's native parser. Schema/reference/protected-architecture/merchant checks pass; all **485** committed source hashes reproduce. Generated CSS is byte-identical to the supplied `c0d77a5` baseline. Both builders/row-column chains, all 150 sections and 102 Theme Blocks, Main Product/Quick View and Headers/Footers 1–5 remain preserved. `config/settings_data.json` retains its recorded hash. Evidence: `validation/batch6-forms-closure/`.

**Unmodified Shopify Theme Check exits 1 with 514 diagnostics (seven errors / 507 warnings), unchanged from the Search checkpoint.** Existing parser/settings/graph/naming and other debt remains visible; no diagnostics are suppressed and no clean certification is claimed. The known whole-project graph failure still produces 100 orphan warnings, including literally referenced helpers.

Register: **485 primary components — 198 NOT REVIEWED, 260 REQUIRES LIVE VERIFICATION, 25 RETAINED — DORMANT SOURCE REVIEWED, one REVIEWED — BUILD ONLY and one DELETION CANDIDATE — OWNER REVIEW**. The 7,830 separately inventoried SVGs remain inventory only. **B6-FINAL acceptance after Batch 9:** execute the earlier native Contact/Customer/consent/captcha/return/storage queues; custom endpoint/multipart/server verification/integration queues; Waitlist product/variant/restock delivery and Search/filter/predictive/localization/currency queues; all actual saved host settings/presets and mixed-asset consumers at 1440/1280/768/390/320px, RTL/zoom/forced colors/contrast/keyboard/AT/no-JS and editor insertion/reordering/unload/reload. This queue does not block the authorized engineering sequence.

Next: independently review **Batch 7 Social / Third Party / Utilities**, then Batch 8 Theme Blocks and Batch 9 Snippet / Asset Closure including the previously recorded Batch 1 legacy dependency obligations. PR #22 remains DRAFT. No main write, merge, Shopify production deployment or broad merchant-state rewrite.


## Batch 7 checkpoint — Affiliate and Rewards program CTAs — 2026-09-21

Affiliate Program and Referral/Rewards CTA independently move to **REQUIRES LIVE VERIFICATION**. Both are native content/link sections, not implemented affiliate tracking or rewards services. Editor guidance now states the external integration requirement. New-insertion defaults/presets use neutral program details rather than invented commission, payout-speed or $10 reward claims; existing saved merchant content is unchanged. All setting IDs, layouts and block types remain.

Affiliate now uses the actual shared surface, font, container and button tokens, including brand backgrounds. Only enabled visible benefit/badge blocks count toward nonempty rendering. Native URLs are escaped, missing labels/targets omit actions, long buttons/badges are bounded, and left alignment follows reading direction on desktop while retaining centered mobile behavior. Disabled/empty sections remain reachable through editor guidance.

Rewards previously emitted merchant-dependent global class rules that could change another instance and style its generated outer wrapper. Every rule now targets a stable native section ID. Background style and max width reach the actual shared contracts; custom background works in every layout. All seven custom color controls remain, with readable primary-button text selected through the existing contrast helper. Border visibility no longer erases the separately selected shadow. Zero spacing/radius, configured columns, headings, program emphasis, mobile button width and blank-action collapse remain explicit. Empty benefit cards are omitted. The previous unconsumed icon hint now selects one of five existing sprite symbols, with a documented star fallback. Rich text/native links remain complete without JavaScript; focus, wrapping, logical alignment and reduced motion are scoped.

**106 independent actual preset/settings/color/zero/link/icon/empty cases pass**, including two opposite Rewards instances and per-instance CSS selector checks. Both changed hosts parse natively. Schema/reference/protected architecture/merchant-state checks pass and generated CSS remains byte-identical to `c0d77a5`. The previous full 74-suite Batch 6 closure run remains the last full run; this checkpoint adds and passes its affected suite. Explicit Liquid/DOM/color/CSS adapters do not certify real layout, Shopify or a provider service. Evidence: `validation/batch7-program-ctas/`.

**Unmodified Theme Check exits 1 with 513 diagnostics (seven errors / 506 warnings)**: one naming warning removed, all other check counts unchanged. Existing errors and the whole-project orphan-graph debt remain unsuppressed. No clean certification.

Register: **485 components — 196 NOT REVIEWED, 262 live queued, 25 dormant/source-reviewed, one build-only and one owner-review deletion candidate**. Batch 7: **68 entries — three live queued, 65 NOT REVIEWED**; the third live entry is the previously reviewed social icon. **B7-PROGRAM-CTAS acceptance after Batch 9:** actual saved layouts/settings/program copy and provider destinations; theme/dark/custom colors and contrast, real sprite rendering, 1440/1280/768/390/320px/zoom/long text/RTL/keyboard/AT/no-JS and editor insertion/reordering/blank-block selection. Existing Batch 4/5/6 closures and every earlier live queue remain intact. Next coherent group: Marketing Funnel CTA and Powered By. PR #22 stays DRAFT; no main write, merge, deployment or merchant-state rewrite.


## Batch 7 checkpoint — Marketing Funnel and Powered By — 2026-09-21

Marketing Funnel CTA and Powered By independently move to **REQUIRES LIVE VERIFICATION**. Both retain their layouts, settings IDs, native rich text and links; neither installs a provider, submits data or implements tracking/automation. Saved merchant settings remain untouched. New-insertion copy is neutral and external provider links are supplied by the merchant.

Funnel CTA no longer substitutes invented `/pages/funnel` or `/pages/how-it-works` destinations for blank links. Label/target pairs determine actual actions; escaping and separated target attributes preserve native link behavior. Stable instance IDs scope all CSS. Previously unstyled button variants now have real Solid/Outline/Ghost rules, including inherited global variants, three sizes and all custom color controls. Enabling custom colors preserves the selected variant. Actual theme surface/font/container controls reach every layout. Empty copy/actions no longer create a spurious desktop column; tag-only content does not create an empty layout box. Bounds, wrapping, logical alignment, focus and reduced motion are explicit. Disabled/empty sections provide editor guidance.

Powered By independently receives real width/font/button contracts and all six custom color controls, using the existing contrast helper for primary-button text. Grid/Strip retain their distinct column settings; the Grid-only column control is explained. Zero shadow now removes the shadow in both layouts and hover states, and logo radius cannot become negative. Blank provider cards, headings and unlabelled actions are omitted. Logos retain native alt text when they supply the name; duplicate logos beside a visible provider name are decorative. Initials handle repeated spaces, block metadata remains, and all four CTA styles have bounded native links. Custom borders survive hover; focus and reduced motion are present. Generic provider placeholders replace unconfigured brand links in new presets without rewriting saved provider content.

**127 actual independent preset/settings/link/variant/color/zero/logo/empty cases pass**, including separate instances and zero-shadow computed-property checks in Grid and Strip. Both hosts parse with Shopify's native parser. Schema/reference/protected architecture/merchant checks pass; generated CSS remains byte-identical to `c0d77a5`. The last complete run remains the 74-suite Batch 6 closure; both subsequent Batch 7 affected suites have passed independently. Evidence: `validation/batch7-funnel-providers/`. Explicit Liquid/DOM/CSS/image/color adapters are not Shopify/provider/pixel/AT certification.

**Theme Check exits 1 with 508 diagnostics (seven errors / 501 warnings)**, removing four naming warnings and one unused assignment. All other check counts and the known whole-project orphan graph debt remain unsuppressed; no clean certification.

Register: **485 components — 194 NOT REVIEWED, 264 live queued, 25 dormant/source-reviewed, one build-only and one owner-review deletion candidate**. Batch 7: **68 entries — five live queued, 63 NOT REVIEWED**. **B7-FUNNEL-PROVIDERS acceptance after Batch 9:** saved presets/layouts/settings, actual destination links and truthful connected-provider copy; inherited/custom variants, theme/dark/custom colors and contrast, real logos, 1440/1280/768/390/320px/zoom/long text/RTL/keyboard/AT/no-JS and editor insertion/reordering/blank-block selection. Earlier closures/queues and all protected architecture remain preserved. Next: Partner Fulfillment Strip and Shipping Explainer, then remaining Batch 7 hosts/dependencies. PR #22 remains DRAFT; no main write, merge, deployment or merchant-state rewrite.


## Batch 7 checkpoint — Partner Fulfillment Strip and Shipping Explainer — 2026-09-21

Partner Fulfillment Strip and Shipping Explainer independently move to **REQUIRES LIVE VERIFICATION**. These remain static merchandising/content sections; neither implements warehouse assignment or delivery automation. All controls/layouts remain, with neutral new-insertion delivery copy/estimates and no saved merchant-state rewrite.

Partner Strip previously ignored an unmatched configured tag and substituted unrelated products under the partner label. Matching is now strict and case-sensitive over an explicitly bounded first-50 native collection preview. Real collection picker objects and legacy handles work. Price/date sorting applies to that available preview. The retained Best selling choice is explicitly labelled as collection order and provides editor guidance to configure the collection accordingly; Liquid does not invent sales rankings. The native collection link carries the requested tag/sort, while custom URLs remain exact. [Shopify collection ordering](https://shopify.dev/docs/api/liquid/objects/collection) and [sort URLs](https://shopify.dev/docs/api/liquid/filters/sort_by) are platform contracts, not fixture certification. [Native tag URLs use handleized tags, and Shopify may redirect unused tags](https://shopify.dev/docs/storefronts/themes/navigation-search/filtering/tag-filtering); those setup/redirect checks remain queued. An unrepresentable tag does not silently become an unfiltered native link.

Displayed prices, compare prices and offer links now belong to the same selected/first native variant, instead of comparing one product-wide maximum against another variant's price. No cart or protected commerce component changes. Native images retain intrinsic dimensions and uncorrupted alt text; partner text is independent of logo visibility, and duplicate logos are decorative. Labelled view links, empty/editor guidance, bounded grids/long badges, carousel-only card widths, a keyboard-focusable native rail, visible focus and actual theme font/container/surface tokens are retained. Sample/native catalog ordering, large catalogs, market currency and real imagery remain acceptance checks.

Shipping Explainer now omits empty steps/header fields and numbers only rendered steps. Stacked mode actually omits connectors. Horizontal rails join adjacent badge centers with logical positioning; native scrolling has a labelled focus target when multiple steps exist. A real divider replaces the previously unstyled empty element. The header follows the chosen theme surface, while the existing color controls are explicitly labelled for cards/details. Highlight tint retains an opaque card base; zero border/radius, independent shadows and long lead-time copy are preserved. Introductory/step rich text remains native, block attributes survive, and disabled/empty states provide editor guidance. New presets no longer assert invented 4–6-week or per-step delivery estimates.

**114 actual independent preset/settings/collection/filter/sort/paired-price/media/timeline/empty cases pass**, including 60-product fixtures proving the first-50 bound, unmatched tags, legacy collection handles, escaped custom links, all three timeline modes/connector/index combinations and separate instances. Both hosts parse natively. Schema/reference/protected architecture/merchant checks pass; generated CSS remains byte-identical to `c0d77a5`. The last full run remains the 74-suite Batch 6 closure; three subsequent Batch 7 affected suites have passed independently. Evidence: `validation/batch7-partner-shipping/`. Native Liquid/DOM/image/money/color/handleize adapters are not Shopify/AT/pixel certification.

**Unmodified Theme Check exits 1 with 508 diagnostics (seven errors / 501 warnings), unchanged.** Existing diagnostics, including the whole-project orphan-graph debt, remain unsuppressed. Register: **485 components — 192 NOT REVIEWED, 266 live queued, 25 dormant/source-reviewed, one build-only and one owner-review deletion candidate**. Batch 7: **68 entries — seven live queued, 61 NOT REVIEWED**.

**B7-PARTNER-SHIPPING acceptance after Batch 9:** actual saved product/collection/tag/default-sort configurations, large catalogs/unused-tag redirects, native variant links/prices/presentment currency and truthful partner/delivery claims; all layouts/markers/blank steps/custom colors/zero controls, 1440/1280/768/390/320px/zoom/long text/RTL/touch/keyboard/AT/no-JS and editor insertion/reordering/step selection. Earlier closures/queues, protected architecture and merchant state remain intact. Next: Warehouse Merch and Metaobject Supplier plus their separately pending Card Product dependency. PR #22 remains DRAFT; no main write, merge or deployment.


## Batch 7 checkpoint — Warehouse Merch, Supplier and shared Card Product — 2026-09-21

Warehouse Merch, Metaobject Supplier, Card Product and its newly extracted stylesheet independently move to **REQUIRES LIVE VERIFICATION**. Only these two hosts consume this card helper; Batch 4's separately named Product Card and its formal closure remain untouched.

**Warehouse:** the advertised twelve global mapping slots do not exist in `config/settings_schema.json`. Twelve native section mapping blocks now expose code, group label, rich description, collection and chip color, with first-match behavior and editor selection/guidance. Legacy global-key lookup remains a fallback. A metaobject must have a nonblank code before its other metadata is used, preventing mixed warehouse identities; otherwise native/legacy text then the first nonblank case-insensitive `warehouse:` tag supplies the complete code suffix. Collection references and legacy handles resolve before fallback. Explicit Force collection remains explicit even when unconfigured. Current products are excluded within a first-50 preview and the configured limit. One shared-card chip labels only mapped curated groups; forced/fallback collections do not inherit the current product's group. Native/custom card colors and accent now reach the actual card; mapped/custom chip backgrounds receive automatic contrasting text unless explicitly configured. Correct bounded carousel sizing, native list/scroll focus, responsive grid, escaped labels, typed descriptions and editor-only empty guidance replace broken/duplicated paths. New-insertion copy and schema explain curated merchandising; this does not verify warehouse stock, combined shipping or freight savings.

**Supplier:** a named accessible metaobject (name/title text field) is required; custom definition types remain supported by the documented field contract. Native text/rich-text descriptions and notes, image file/preview references, regions text/list and escaped URL fields render without object-string leakage. Empty facts/actions/kickers/headings are omitted; absent logos do not reserve a desktop column. Missing metaobject catalog falls back to the section collection; explicit Section mode overrides the reference. Empty catalogs and unconfigured contexts expose setup guidance only in the editor. The duplicate banner landmark is removed. Actual container/font/color tokens, inline padding, all four scoped button styles, accent contrast, zero-radius clamping, shadow behavior, focus, reduced motion and card variables are wired; schema sits outside the balanced section.

**Card Product and CSS:** explicit false hides price, absent product/URL renders no card, plain text and URL attributes are escaped, preview/featured images fall back correctly, numeric aspect/position guards prevent invalid presentation. Native varying-price ranges never combine unrelated variant compare prices; uniform prices show a compare price only from a variant with that same price. One stylesheet per host replaces ineffective per-render inline deduplication; wrapped logical-position chips, bounded content, actual tokens, inset focus and reduced motion are scoped to this card namespace. No JavaScript or production integration is introduced.

**128 independent actual preset/settings/native field/source precedence/legacy mapping/chip/price/empty/multiple-instance cases pass.** All three changed Liquid sources parse natively; schema/reference/protected architecture/merchant checks pass. All **486** source hashes reproduce and generated CSS remains byte-identical to `c0d77a5`. The last full run remains the 74-suite Batch 6 closure; four subsequent Batch 7 affected suites pass. Explicit rich-text/image/money/color/DOM adapters do not certify native Shopify, browser pixels or AT. Implementation contracts follow [native metaobjects](https://shopify.dev/docs/api/liquid/objects/metaobject), [typed metafield values](https://shopify.dev/docs/api/liquid/objects/metafield) and [product price properties](https://shopify.dev/docs/api/liquid/objects/product). Evidence: `validation/batch7-warehouse-supplier/`.

**Unmodified Theme Check exits 1 with 508 diagnostics (seven errors / 501 warnings), unchanged and unsuppressed.** Register: **486 components — 189 NOT REVIEWED, 270 live queued, 25 dormant/source-reviewed, one build-only and one owner-review deletion candidate**. Batch 7: **69 entries — 11 live queued, 58 NOT REVIEWED**; added CSS is explicitly inventoried and independently reviewed through both consumers.

**B7-WAREHOUSE-SUPPLIER acceptance after Batch 9:** native published/access-enabled metaobject pages and optional fields; actual catalog/legacy-handle resolution, mapped/forced/fallback collections and product exclusion; editor mapping insertion/selection/reorder and saved presets; actual warehouse/integration truthfulness, currency/variant prices, theme/dark/custom contrast, 1440/1280/768/390/320px/zoom/long text/RTL/keyboard/AT/no-JS and actual editor unload/reload. Earlier closures/queues and protected architecture remain unchanged. Next: CRM Funnel Embed and Scheduling/Booking Embed, then the remaining Batch 7 hosts and dependencies. PR #22 remains DRAFT; no main write, merge, deployment or merchant-state rewrite.


## Batch 7 checkpoint — CRM Funnel and Scheduling / Booking embeds — 2026-09-21

Both embed hosts independently move to **REQUIRES LIVE VERIFICATION**. Exact merchant HTML/iframe/script snippets remain raw and unchanged. New inserts start with no embed; the three known legacy placeholder comments are recognized without discarding real provider scripts. Empty storefront hosts/panels are omitted and configuration guidance stays in the editor. Booking can show a configured, labeled help link alone; blank destinations/labels never create anchors. The example email and unconfigured free/24-hour/email-confirmation claims are removed only from new-insertion defaults.

CRM emits bullet/trust content only when enabled and nonblank, reserves a split column only for actual content, supplies image-alt fallback and keeps empty/disabled block selection reachable in the editor. Every theme-owned rule is scoped to the stable instance. Both hosts use actual container widths, surface/text/font controls and logical header alignment. Their outer Shopify wrappers no longer duplicate the q-section surface/spacing. Native scroll focus and bounded scrollable widget panels preserve access to wide provider content; desktop/mobile iframe minimum heights now have explicit controls with the previous defaults. Existing iframe attributes and provider scripts remain intact. `embed_type` is documented descriptive metadata, not an invented provider integration.

**64 independent actual settings/legacy-placeholder/exact raw-code/native iframe dimension/block/link/empty/multiple-instance cases pass.** Both native Liquid parses and schema/reference/protected architecture/merchant checks pass; all **486** hashes reproduce and CSS remains byte-identical to `c0d77a5`. The last full run remains the 74-suite Batch 6 closure, with five subsequent Batch 7 affected suites passing. Raw scripts are not executed in these DOM fixtures and no universal provider reload controller is invented. Evidence: `validation/batch7-provider-embeds/`.

**Unmodified Theme Check exits 1 with 508 diagnostics (seven errors / 501 warnings), unchanged and unsuppressed.** Register: **486 components — 187 NOT REVIEWED, 272 live queued, 25 dormant/source-reviewed, one build-only and one owner-review deletion candidate**. Batch 7: **69 entries — 13 live queued, 56 NOT REVIEWED**.

**B7-PROVIDER-EMBEDS acceptance after Batch 9:** actual saved provider configuration/code and accessible iframe titles/internal controls, form/calendar submission and confirmations/captcha/resizing; configured network/consent/no-JS/help behavior; mixed instances and real editor insert/unload/reload, 1440/1280/768/390/320px/zoom/long text/RTL/keyboard/AT and surface/contrast/minimum-height rendering. Earlier closures and every live queue remain intact. Next: Partner Logo Carousel, then remaining Batch 7 utility/form/account/map/social hosts and dependencies. PR #22 remains DRAFT; no main write, merge, deployment or merchant-state rewrite.


## Batch 7 checkpoint — Partner Logo Carousel and motion controller — 2026-09-21

The Partner Logo host and newly inventoried motion asset independently move to **REQUIRES LIVE VERIFICATION**. Strip autoplay previously changed a class without moving anything. Marquee clipped items when autoplay/reduced motion stopped, had no persistent stop control and translated an imprecise half-track distance. The repaired host remains a complete native scroll/grid list without JavaScript. It counts real storefront items, preserves editor placeholders, omits empty panels, escapes destinations, falls back to native image alt and wires actual container/font/surface tokens. Grid minima, image dimensions and long text stay bounded. Outer Shopify spacing is no longer duplicated. New-insertion copy makes no unconfigured relationship claim.

The new controller supplies bounded LTR/RTL strip advance and scoped CSS marquee activation with the measured full original group width, including the end gap. Marquee duplicates are hidden by default, aria-hidden/inert and have no links or editor identity. Stopping motion returns to the complete static original list; native scroll is available. A persistent explicit stop button is independent of temporary pointer/focus/visibility conditions. Reduced motion and editor mode always remain static; the retained `respect_reduced_motion` setting controls additional decorative hover-transition suppression and its editor label explains that scope. Hidden/offscreen hosts pause, fitting strips create no interval, and independent resize/intersection/media observers are removed with listeners/timers on unload. Duplicate assets and editor load events do not create duplicate controllers; reload works.

**57 independent actual preset/settings/blank/clone/link/source and runtime cases pass**, including two active instances, RTL forward/end/wrap bounds, explicit stop persistence, native focus/hover, tab and viewport visibility, reduced-motion changes, overflow changes, duplicate boot and unload/reload cleanup. Native Liquid parsing, schema/reference/protected architecture/merchant checks pass; all **487** hashes reproduce and CSS remains byte-identical to `c0d77a5`. The last full run remains the 74-suite Batch 6 closure; six subsequent Batch 7 affected suites pass. Geometry, scroll calls, timers and observers are explicit adapters, not native scrolling, browser pixel or AT certification. Evidence: `validation/batch7-partner-logos/`.

**Unmodified Theme Check exits 1 with 508 diagnostics (seven errors / 501 warnings), unchanged and unsuppressed.** Register: **487 components — 186 NOT REVIEWED, 274 live queued, 25 dormant/source-reviewed, one build-only and one owner-review deletion candidate**. Batch 7: **70 entries — 15 live queued, 55 NOT REVIEWED**; the motion asset is explicitly inventoried and independently reviewed with its host.

**B7-PARTNER-LOGOS acceptance after Batch 9:** native strip scrolling and precise repeated marquee wrap in LTR/RTL, stopping into static originals, every saved layout/control and logo/brand relationship, touch/hover/keyboard/AT/no-JS/reduced motion/tab/offscreen behavior, theme/contrast and 1440/1280/768/390/320px/zoom/long text, real editor insertion/reorder/selection/unload/reload. Earlier closures/queues and protected architecture remain unchanged. Next: Bulk Ordering and CRM Lead Capture, then remaining Batch 7 hosts/dependencies. PR #22 remains DRAFT; no main write, merge, deployment or merchant-state rewrite.


## Batch 7 checkpoint — Bulk Ordering and CRM Lead Capture — 2026-09-21

Both hosts independently move to **REQUIRES LIVE VERIFICATION**. The prior unpublished Partner Logo checkpoint was recovered without rebuilding it and published as `659db6bbd476f8ee2ab50f909f24199ee633d6e3` (same tree as local `cbee0f0`). PR #22 remains DRAFT.

Bulk Ordering and CRM were not attached to the Batch 6 controller's required host hook. Both now reuse that existing guarded controller and keep complete native required-field validation without JavaScript. Bulk PO fields remain available without JavaScript; when enhanced, hidden conditional values are excluded from submission. Native Shopify forms retain platform form identity/captcha. Custom forms preserve the merchant's configured method and parameter contract, reject missing/placeholder/invalid endpoints, and never fall back to a fake contact submission. Native success returns use the [documented Shopify return path](https://shopify.dev/docs/api/liquid/tags/form#form-return_to); external destinations remain success links. External endpoint responses belong to the provider.

Bulk receives nonempty labels, associated error nodes, reserved/duplicate custom-key protection, actual width/divider/font controls, bounded split layouts, scoped CSS and neutral response defaults. Its recipient field remains metadata and is explicitly described as not controlling Shopify notifications. CRM receives correct provider-specific captcha keys, actual newline select parsing, empty parameter-name guidance, native labels, functional card/stacked/width/alignment controls and neutral insertion copy. Provider/server consent, captcha verification, notification routing and lead/order processing are not invented. Shared controller/CSS source remains unchanged; their recorded Batch 6 dispositions remain valid.

**116 dedicated actual-host settings, endpoint, native payload, metadata, label, select, captcha and two-instance lifecycle cases pass**, together with the existing form controller suite and **196 shared-CSS consumer checks** in both asset orders. The CSS fixture now supplies a configured CRM action; original assertions remain. Native Liquid parsing, schema/reference/protected-architecture/merchant checks, all 487 source hashes and CSS build pass. Generated CSS remains byte-identical to `c0d77a5`. Evidence: `validation/batch7-lead-order-forms/`. Explicit Shopify form/handleize/DOM adapters are not native Shopify, provider, pixel or AT certification.

**Unmodified Theme Check exits 1 with 508 diagnostics (seven errors / 501 warnings), unchanged.** Existing errors and whole-project graph/orphan debt remain unsuppressed. Batch 7: **70 assigned entries — 17 live queued, 53 NOT REVIEWED**. Overall: 487 components — 184 NOT REVIEWED, 276 live queued, 25 dormant/source-reviewed, one build-only and one owner-review deletion candidate.

**B7-LEAD-ORDER-FORMS acceptance after Batch 9:** actual saved settings/fields, Shopify contact notification/return/captcha, provider parameter/method/consent and server captcha verification, endpoint success/error, mixed CSS, no-JS, responsive/RTL/zoom/contrast/keyboard/AT and editor insertion/reorder/unload/reload. All earlier closures/queues and protected architecture/state remain intact. Next: Order Tracking and Region Availability, then remaining Batch 7 hosts/dependencies. No Batch 8/9 or live QA work is begun.


## Batch 7 checkpoint — Order Tracking and Region Availability — 2026-09-21

Order Tracking, its extracted controller and Region Availability independently move to **REQUIRES LIVE VERIFICATION**. Order Tracking remains a URL redirect helper. It no longer invents a store help page or a provider destination. Configured native help links remain usable without JavaScript; the input form becomes available only after a valid destination and controller exist. Exact encoded placeholders and native URL query parameters preserve destination queries/fragments. Invalid protocols and malformed/missing destinations cannot navigate; a configured fallback remains available. Native validation, associated help/error text, persistent independent roots, duplicate-asset protection and editor disposal/reload are fixture-covered. All layouts, icons, email visibility, button choices and destination settings remain. It never fetches status, authenticates an order or claims successful fulfillment.

Region Availability now uses native metafield values and exact, trimmed, case-normalized country-code members. Native list fields and explicitly supported legacy comma-separated text cannot turn a partial substring into a supported region. Product and cart logic retain generic/unknown/unsupported messages and all duty controls. Missing messages, supplier data, empty carts and out-of-context storefront use no longer leave empty or instructional panels. Editor setup guidance remains. The broken dynamic snippet call is replaced by the already reviewed `q-icon` helper, with a supported legacy alert-name mapping. Icon control IDs remain; the legacy snippet-name setting explains the fixed helper contract. All custom colors, zero spacing/radii, native theme tokens, layouts and escaped metadata remain. Messages are informational supplier configuration, not shipping/stock/duty/checkout enforcement.

**123 actual tracking/region settings, typed-field, product/cart, URL and lifecycle cases pass**, plus the **128 Warehouse/Supplier/Card cases**. Native Liquid parsing, schema/reference/protected architecture and merchant state pass; all **488 source hashes** reproduce and generated CSS remains byte-identical to `c0d77a5`. Existing q-icon live acceptance is carried forward; no Batch 9 sprite review is claimed. Evidence: `validation/batch7-tracking-regions/`. Native Shopify/provider navigation/pixels/AT are not certified by DOM/Liquid/URL adapters.

**Unmodified Theme Check exits 1 with 508 diagnostics (seven errors / 501 warnings), unchanged and unsuppressed.** Batch 7: **71 entries — 20 live queued, 51 NOT REVIEWED**. Overall: **488 components — 182 NOT REVIEWED, 279 live queued, 25 dormant/source-reviewed, one build-only and one owner-review deletion candidate**.

**B7-TRACKING-REGIONS after Batch 9:** real configured help/provider destinations and native browser/popup navigation; published supplier data and Markets localization, saved messages and checkout truthfulness; icons/custom themes/contrast, responsive/RTL/zoom/no-JS/keyboard/AT and real editor insertion/reordering/unload/reload. All earlier queues/closures remain intact. Next: Batch 7 legacy account authentication surfaces and their CSS/JS dependencies. No Batch 8, Batch 9 or live QA is begun; PR #22 remains DRAFT.


## Batch 7 checkpoint — Legacy Account Authentication — 2026-09-21

Activation, Login, Registration and Reset Password; their four stylesheets and four controllers; and the Login recovery helper independently move to **REQUIRES LIVE VERIFICATION** (13 entries, including the newly inventoried Login controller). Native platform forms and every setting ID remain. Required-field validation works without JavaScript, and activation decline can submit without filling password fields. Form/error IDs are unique per instance; merchant/customer text is escaped. Login renders one real recovery form with explicit snippet inputs instead of cloning duplicate template IDs. Recovery is available without JavaScript; enhanced panels keep native server errors/success visible and manage focus.

Password toggles start hidden until ready, have actual pressed/control/label state, remain independent across roots and dispose/reset on editor unload. Duplicate assets/load events cannot double-bind. Login labels are escaped data values, eliminating quoted inline JavaScript. The four stylesheets now use actual theme typography and color tokens; solid buttons no longer resolve background and text to the same white color. Explicit input text, card contrast, bounded controls, zero-radius clamping, focus, hidden guards and reduced motion remain scoped. The legacy remember-me field is labelled as platform-controlled; no theme session storage, social login, mail service or account migration is invented.

[Shopify identifies these as legacy customer templates](https://shopify.dev/docs/storefronts/themes/architecture/templates); current customer accounts run outside the theme. All native templates and merchant state are preserved. **173 dedicated actual settings/native-form/escaping/unique-ID/decline/no-JS/panel/password/lifecycle/CSS cases pass**, plus the existing shared form controller suite. Native Liquid parsing, schema/reference/protected architecture/merchant-state checks, all **489 source hashes** and byte-identical generated CSS pass. Evidence: `validation/batch7-account-auth/`. Explicit form/DOM/color adapters do not certify Shopify authentication, native browser pixels or AT.

**Unmodified Theme Check exits 1 with 508 diagnostics (seven errors / 501 warnings), unchanged and unsuppressed.** Batch 7: **72 entries — 33 live queued, 39 NOT REVIEWED**. Overall: **489 components — 170 NOT REVIEWED, 292 live queued, 25 dormant/source-reviewed, one build-only and one owner-review deletion candidate**.

**B7-ACCOUNT-AUTH after Batch 9:** actual legacy activation/decline/login/recovery/reset/registration, server messages/captcha/email/session behavior and account configuration; saved settings, no-JS/responsive/RTL/zoom/contrast/keyboard/AT and real editor lifecycle. All earlier closures/queues remain intact. Next: remaining Batch 7 account pages/navigation and their dependencies. No Batch 8/9, account migration or live QA is begun; PR #22 remains DRAFT.


## Batch 7 checkpoint — Account Pages and Navigation — 2026-09-22

The five remaining account hosts (Addresses, Dashboard, Orders, Order and Order Tracking), shared navigation/helper CSS, five host stylesheets and three controllers independently move to **REQUIRES LIVE VERIFICATION**: 15 entries. The navigation CSS asset actually contained Liquid markup; it now contains scoped, valid CSS for horizontal/sidebar/compact navigation. Existing consumer label settings now reach the helper explicitly. Blank tracking destinations no longer invent a page. Guest states, native account links, native customer data and all prior setting IDs remain.

Addresses now has native add/edit/default/delete forms, unique section-qualified IDs, escaped values, native errors, pagination and complete no-JS panels/text province fields. A guarded local controller reads Shopify country-option province data instead of relying on an absent global helper; countries without provinces omit that parameter and missing/malformed metadata keeps text entry available. Panels manage focus and server errors; enhanced delete requests require confirmation. Controllers dispose and remount independently. Orders uses exact native status tokens instead of localized labels or substring matches (fulfilled previously matched unfulfilled, paid matched unpaid). Page-local search/sort/counts, table/card consistency and native pagination remain. Enhancement-only controls hide without JavaScript. Dashboard retains bounded previews plus native pagination, and hidden navigation no longer reserves a sidebar column.

Order detail escapes native customer/product/property values, keeps hidden properties hidden, labels discounts as already included in Shopify's subtotal, preserves configured tracking URLs without corrupting queries/fragments, and falls back to native order status links. Fulfillment tracking links use [Shopify's fulfillment URL](https://shopify.dev/docs/api/liquid/objects/fulfillment); status and totals follow the [native order object](https://shopify.dev/docs/api/liquid/objects/order). No totals are recomputed. Account Tracking no longer simulates Processing/carrier status or claims a future integration. Its configured provider URL uses an explicit encoded order/email/tracking redirect contract. Blank/invalid configuration leaves the form unavailable and native account/help links intact. The retained timeline is labelled static shipment guidance, never live state. No theme carrier API, authentication or fulfillment processing is introduced.

Every stylesheet receives actual theme typography/surface tokens, card foreground contrast, bounded controls, focus, reduced motion and hidden-state guards. Solid buttons no longer inherit a white-on-white background. **198 dedicated actual settings/native-data/guest/navigation/filter/sort/address/province/delete/redirect/lifecycle/CSS cases pass**, plus **173 auth** and **123 tracking/region** cases. Native parsing, schema/reference/protected architecture/merchant state, all **489 hashes** and byte-identical CSS build pass. Evidence: `validation/batch7-account-pages/`. Explicit form/pagination/URL/DOM adapters do not certify Shopify, providers, browser pixels or AT.

**Unmodified Theme Check exits 1 with 508 diagnostics (seven errors / 501 warnings), unchanged and unsuppressed.** Batch 7: **72 entries — 48 live queued, 24 NOT REVIEWED**. Overall: **489 components — 155 NOT REVIEWED, 307 live queued, 25 dormant/source-reviewed, one build-only and one owner-review deletion candidate**.

**B7-ACCOUNT-PAGES after Batch 9:** real native account CRUD/default/delete/validation/country/province/pagination, order totals/status/fulfillment/customer privacy; saved settings, provider contract and real navigation; no-JS/responsive/RTL/zoom/theme contrast/keyboard/AT/editor lifecycle. Earlier closures/queues remain intact. Next: Batch 7 maps, then remaining social hosts/dependencies. No Batch 8/9 or live QA is begun; PR #22 remains DRAFT.


## Batch 7 checkpoint — Maps and Shared Locator — 2026-09-22

Basic Google Map, Location/Contact, Service Area Banner and Store Locator; the existing locator helper/CSS/JS; and the newly inventoried coordinate helper independently move to **REQUIRES LIVE VERIFICATION** (eight entries). The three previously unconsumed locator dependencies are now the single actual engine for Store Locator. Native cards, real directions/contact links and editor block metadata replace the JS-only list. Blank blocks are omitted on the storefront and remain selectable in the editor. No sample city/address/phone/email/hours or unverified coverage claim is inserted by new defaults. Saved merchant configuration remains untouched.

Coordinate validation rejects incomplete, malformed and out-of-range pairs while preserving zero, negatives and decimals. Maps no longer substitute San Francisco/New York/Cleveland for absent inputs. Basic Map encodes the query rather than the entire URL, retaining the documented coordinate zoom contract. Location/Contact uses coordinate mode consistently, actual pixel spacing and percentage shadow strength; missing maps collapse the column. URLs/text are escaped, iframe titles remain, all contact CSS is instance-scoped and radius cannot go negative. The Banner keeps merchant raw iframe attributes, excludes passive embeds from focus/AT, permits configured interactive embeds without covering them with intercepting overlays, respects gradient opacity and radius, and confines configuration placeholders to the editor. Theme/dark/custom colors, typography, focus, wrapping and reduced motion are wired.

Locator search matches merchant text/tags, with exact normalized tag filters and visible counts; it does not geocode or calculate distance. One guarded Google loader serves multiple instances. A missing/failed service preserves the native list, never substitutes a fake public map. Zero-coordinate markers work, filtered markers follow cards, native links remain, and listeners/markers/info windows are disposed on unload; a delayed response cannot resurrect an unloaded root. The retained preview control is explicitly editor-only and labelled as a pin-layout preview. Google requires merchant provider configuration; [Maps loading](https://developers.google.com/maps/documentation/javascript/load-maps-js-api) and [native Maps URLs](https://developers.google.com/maps/documentation/urls/get-started) remain external contracts. Existing Marker compatibility, billing/key restrictions and provider consent remain live checks.

**172 map settings/native-card/coordinate/URL/embed/CSS/loader/filter/failure/lifecycle cases pass.** A concrete audit also found that recent account CSS used `--font-heading` instead of the actual `--font-head`; that isolated correction is covered by the rerun **198 account-page** and **173 auth** cases. No other completed component is reopened. Native parsing (including the new helper), schema/reference/protected architecture/state, all **490 hashes** and byte-identical CSS build pass. Evidence: `validation/batch7-maps/`. Google APIs, geometry and native DOM/image rendering are explicit adapters, not live provider/browser/AT certification.

**Unmodified Theme Check exits 1 with 509 diagnostics (seven errors / 502 warnings)**: one additional OrphanedSnippet warning for the new, explicitly referenced coordinate helper under the existing whole-project graph failure. No findings are suppressed. New branch-balance findings discovered during development were fixed and are absent from final evidence. Batch 7: **73 entries — 56 live queued, 17 NOT REVIEWED**. Overall: **490 components — 148 NOT REVIEWED, 315 live queued, 25 dormant/source-reviewed, one build-only and one owner-review deletion candidate**.

**B7-MAPS after Batch 9:** real location/coverage/contact/hours truthfulness, iframe title/permissions/interactive/passive focus, provider key/billing/network/consent/marker support and native links, all saved controls/theme/custom contrast, responsive/RTL/zoom/no-JS/keyboard/AT and real editor lifecycle. Earlier closures/queues remain intact. Next: remaining Batch 7 social hosts/dependencies. No Batch 8/9 or live QA is begun; PR #22 remains DRAFT.


## Batch 7 checkpoint — Social Galleries and Embeds — 2026-09-22

Seven hosts (Facebook Feed, Instagram Basic Feed, Before/After, Short Video Rail, YouTube Grid, Pinterest Grid and UGC Hashtag Gallery), the active Pinterest card helper and newly inventoried YouTube ID helper/controller move to **REQUIRES LIVE VERIFICATION**. The legacy Pinterest thumbnail helper is independently **RETAINED — DORMANT SOURCE REVIEWED**: no literal or dynamic consumer was found; escaping, native alt/ratio/focus behavior and its retained contract have dedicated fixtures. Eleven entries reviewed, with no deletion or invented activation.

All seven hosts receive instance-scoped styles, actual container/font/color tokens, bounded layouts, focus/reduced-motion handling and meaningful empty/disabled/editor states. Future feed/playlist/board modes retain their setting IDs as metadata and explicitly require an external integration; no automated feed is claimed. New presets omit fictional handles, durations, captions or integration claims. Saved merchant state remains unchanged. Facebook validates its configured native page destination and preserves the existing Page Plugin contract. Instagram retains native alt text and intrinsic image dimensions. Before/After escapes plain text, preserves exact raw merchant provider code, keeps wide embeds scrollable/focusable, and omits missing card halves. Short Video has bounded native scroll cards, valid-card limits and recognized YouTube embed conversion; other URLs require merchant-provided embed-compatible endpoints.

Pinterest now has complete actual card styling and one scoped host stylesheet instead of duplicated helper CSS; image alt and destinations escape once. UGC's nonexistent show_body guard no longer blocks body content; featured spans stay within actual column counts, text/URLs escape, image alt falls back natively and captions stay readable on nonlinks. Blank media blocks remain selectable only in the editor.

YouTube no longer creates global API callbacks or uncontrolled asynchronous fallback navigation. Recognized IDs receive native server-rendered thumbnails and keep their exact watch anchors without JavaScript. A guarded controller enhances these links with a native dialog and user-initiated iframe, following the [YouTube iframe contract](https://developers.google.com/youtube/player_parameters). Closing/unloading removes playback; duplicate assets, mixed instances, modified clicks and editor reload are handled. Unsupported URLs retain native navigation. No script API or provider credentials are invented.

**253 dedicated actual settings/native-media/URL/escaping/manual-mode/empty/multiple-instance/dialog/lifecycle cases pass**, plus **64 provider-embed** and **172 map** cases. Native Liquid parsing, schema/reference/protected architecture/state, all **492 hashes** and byte-identical CSS build pass. Evidence: `validation/batch7-social-galleries/`. Explicit image/video/DOM/dialog adapters are not live Shopify, browser or AT certification.

**Unmodified Theme Check exits 1 with 509 diagnostics (five errors / 504 warnings)**. Two missing-image-dimension errors are resolved; one RemoteAsset warning remains for native YouTube thumbnails and one graph-derived OrphanedSnippet warning is added for the explicitly consumed ID helper. No findings are suppressed. Batch 7: **75 entries — 66 live queued, one dormant/source-reviewed, 8 NOT REVIEWED**. Overall: **492 components — 139 NOT REVIEWED, 325 live queued, 26 dormant/source-reviewed, one build-only and one owner-review deletion candidate**.

**B7-SOCIAL-GALLERIES after Batch 9:** actual Facebook page/plugin configuration and provider consent/network/permissions; saved manual media/provider code, native video/YouTube playback and focus return, Pinterest/UGC destinations/captions and declared merchant claims; responsive/RTL/zoom/no-JS/theme contrast/keyboard/AT and actual editor lifecycle. Earlier closures/queues remain intact. Next: the final six Batch 7 social hosts and two helpers, followed by formal Batch 7 engineering closure. No Batch 8/9 or live QA is begun; PR #22 remains DRAFT.


## Batch 7 checkpoint — Social Links and Manual Proof — 2026-09-22

LinkedIn Company Highlight, Micro-post Strip, Profile Card, Social Icons, Social Proof and Social Share, plus the active Micro-post Card and Profile Icon helpers independently move to **REQUIRES LIVE VERIFICATION**. All eight actual consumer contracts are traced; the icon wrapper reuses the previously reviewed icon-social dependency without reopening it. Batch 7 now has **75 assigned entries — 74 live queued, one dormant/source-reviewed and zero NOT REVIEWED**. This checkpoint precedes the formal full-regression closure.

LinkedIn is explicitly manual company content/counts and a real configured link. Fictional new counts/company copy are removed, logo alt is escaped once, vacant company/stats columns disappear and the CTA uses native buttons. Micro-posts retain merchant text, handles and timestamps without claiming verified/live data. Future timeline mode is metadata only; the storefront remains empty until a real integration is supplied. Native scroll/grid cards are bounded, desktop column controls work, plain-text line breaks survive and empty cards stay selectable only in the editor. One scoped host stylesheet replaces repeated helper CSS.

Profile uses actual boolean lazy/eager image loading, native alt fallback, escaped identity/links/labels and real global/custom destinations. Missing avatars reserve no column, blank actions are omitted, and narrow high-padding cards remain bounded. Icon Row has native list items containing real links, explicit block-over-global URL precedence, unset/invalid destination rejection, meaningful accessible labels and neutral new profiles. Its independent desktop/mobile wrapping controls and safe overflow alignment retain reachability. Profile Icon retains decorative, unfocusable graphics and an adjacent real link name; the unrelated Bluesky map-pin drawing becomes a decorative initial rather than an invented brand mark.

Social Proof keeps all manually configured metrics while new insertions contain no fictional claims. Plain text escapes, empty metrics do not render on the storefront, and an optional full screen-reader reading replaces the visible duplicate once. Real surface/primary foregrounds and bounded rows remain. Social Share stays entirely native: provider links and mailto preserve URL/title/message precedence, site paths become absolute, query/fragment/special characters encode once and email spaces use percent encoding. Explicit colors remain; unset colors inherit the theme and bare-style icons retain visible backgrounds. It does not post, connect accounts or confirm provider success. Every host stylesheet is scoped to its actual instance and uses native typography/container tokens, focus and reduced-motion rules. Every existing setting ID is retained and merchant state is unchanged.

**222 dedicated actual settings/preset/manual-content/native-link/share-encoding/escaping/list-semantics/empty/editor/multiple-instance/CSS cases pass**, plus **253 gallery** and **57 partner-logo** cases. All eight Liquid sources parse natively; schema/reference/protected architecture/state, all **492 hashes** and byte-identical CSS build pass. Evidence: `validation/batch7-social-links-proof/`. These explicit Liquid/image/CSS/DOM adapters are not provider, browser or AT certification.

**Unmodified Theme Check exits 1 with 499 diagnostics (five errors / 494 warnings)**: ten scoped-CSS warnings resolved, remaining findings unsuppressed. Overall: **492 components — 131 NOT REVIEWED, 333 live queued, 26 dormant/source-reviewed, one build-only and one owner-review deletion candidate**.

**B7-SOCIAL-LINKS-PROOF after Batch 9:** truthful saved profiles/posts/counts and native destinations; actual external share dialogs/login, Pinterest image selection and email clients; no-JS/responsive/RTL/zoom/theme/custom contrast/keyboard/AT and real editor insertion/reorder/lifecycle. Existing provider URLs are preserved; no provider SDK is introduced. Earlier closures/queues remain intact. Next and stopping point: formal Batch 7 engineering closure and draft PR #22 reconciliation. No Batch 8/9 or live QA is begun.


## FORMAL BATCH 7 ENGINEERING CLOSURE — 2026-09-22

**BATCH 7 ENGINEERING COMPLETE — READY FOR OWNER CHECKPOINT.** The Family column reconciles **75 entries: 41 sections, 11 snippets and 23 assets**. Final dispositions: **74 REQUIRES LIVE VERIFICATION; one RETAINED — DORMANT SOURCE REVIEWED; zero NOT REVIEWED**. The dormant Pinterest thumbnail helper has no traced consumer and retains dedicated source/fixture evidence. All discovered Batch 7 implementation dependencies are inventoried. No source-level Batch 7 defect remains unresolved in this engineering review. Runtime source head: `6cab1270c02730836fbe2af47dbdd0a0926fba62`; this formal closure checkpoint records the final evidence and preserves those runtime bytes.

**All 87 regression suites pass: seven Phase 3 and 80 Phase 4**, including every completed Phase 4 suite and 13 Batch 7 suites. The first full run passed 86 and exposed an obsolete synthetic Phase 3 password-toggle fixture: it omitted the native register root and clicked before DOMContentLoaded. The actual native-host account-auth suite passed. The Phase 3 fixture now matches the real scoped host/lifecycle, keeps all original password/cart/duplicate-script/race assertions and adds pressed/control-state assertions; its targeted rerun passes. No runtime source was changed for this fixture. Initial failure, final pass and rationale are retained in `validation/batch7-engineering-closure/`.

**All 52 Batch 7 Liquid sources pass strict Shopify native parsing.** Schema/reference checks, 150 sections/102 blocks, both builder/row/column systems and protected main-product/Quick View/Header/Footer variants pass. Every protected file and every Theme Block is byte-identical to the owner-start head `816ff34d1d2f90569134c8cf8a1ed0d0ae9303ac`; no retained file is deleted. All **492 source hashes** reproduce, merchant-state SHA remains `ffc244582d58f3e6e07713abd6ab1de2cdef7ba109fc00a60cef886235b6b499`, and the successful CSS build is byte-identical to `c0d77a5`.

Dependency reconciliation includes actual render calls, all literal asset types including static SVG, transitive shared contracts and no unresolved dynamic render expressions. The inventory generator now excludes Liquid-comment examples from dependency declarations, removing eleven false self-reference edges without changing ownership or approval. Shared forms/controller and q-icon remain under closed Batches 6/5. Existing shared contrast-text and icons.svg consumer contracts are fixture-covered; their independent review remains assigned to Batch 9. This accounting grants no Batch 8/9 closure. Previously reviewed work is preserved; only the documented concrete account heading-token correction was made during the maps checkpoint.

**Unmodified Shopify CLI 4.8.0 exits 1 with 499 unsuppressed findings: five errors / 494 warnings.** This is down from 508 (seven errors / 501 warnings) at the owner-start provider-embed checkpoint and 514 at Batch 6 closure. Family 7 has **zero errors / 18 retained warnings**: eleven orphan reports (ten active helpers demonstrated by actual consumers, plus one dormant), four merchant-settings-count warnings, two map naming-style warnings and one external native YouTube thumbnail warning. The whole-project graph failure and all other diagnostics remain visible. The five errors outside Batch 7 are two paired Theme Block wrapper parsing findings, two existing slideshow image diagnostics and the layout parser-blocking script filter. No findings were suppressed and no clean global certification is claimed.

Overall register: **492 components — 333 live queued, 26 dormant/source-reviewed, one build-only, one retained owner-review deletion candidate and 131 NOT REVIEWED**. Remaining unreviewed ownership is unchanged: eight legacy Global/Structural dependencies assigned to later closure, 106 Batch 8 entries (102 blocks plus four dependencies) and 17 Batch 9 entries. The 7,830 separately inventoried static SVGs retain their existing visual-review queue.

**B7-FINAL live acceptance after the engineering batches:** preserve every per-group queue: B7-PROGRAM-CTAS, B7-FUNNEL-PROVIDERS, B7-PARTNER-SHIPPING, B7-WAREHOUSE-SUPPLIER, B7-PROVIDER-EMBEDS, B7-PARTNER-LOGOS, B7-LEAD-ORDER-FORMS, B7-TRACKING-REGIONS, B7-ACCOUNT-AUTH, B7-ACCOUNT-PAGES, B7-MAPS, B7-SOCIAL-GALLERIES and B7-SOCIAL-LINKS-PROOF. They cover real merchant claims/configuration/data, native platform submissions/auth/account CRUD/orders, provider contracts and embeds/maps/media/share/email behavior, saved settings/presets, responsive/RTL/zoom/no-JS/theme contrast/keyboard/AT and real Theme Editor lifecycle. Explicit Liquid/DOM/image/form/API adapters are not live Shopify/browser/provider certification. All earlier Batch 1–6 queues remain intact.

**Owner checkpoint:** no new decision blocks Batch 7 engineering completion. Existing historical security-scan digest false-positive triage and the retained deletion candidate remain owner queues; no current security-clearance claim is made. PR #22 remains DRAFT and receives these final counts and closure evidence. **STOP HERE.** Batch 8, individual Theme Block review, Batch 9, roadmap Phase 5 and live Shopify QA are not started. Await owner approval before Batch 8. No main write, merge or production deployment.

## Batch 8 checkpoint — primitives and shared dependencies — 2026-09-22

Owner authorized Batch 8 through formal engineering closure from `61109f1bd931091bd4b5cdfd44139473efc4d1a3`; remote fetch and PR #22 draft state matched before editing. Earlier Batch 7 stop/approval text is historical. This checkpoint does not close Batch 8.

**16/106 Family 8 entries now have individual dispositions: 12 Theme Blocks plus all four shared dependencies; 15 REQUIRES LIVE VERIFICATION and one RETAINED — DORMANT SOURCE REVIEWED. 90 remain NOT REVIEWED.** Total register: 492 entries, 348 live queued, 27 dormant/source-reviewed, one build-only, one retained owner-review deletion candidate, 115 NOT REVIEWED. All untouched blocks retain their pending review.

Heading/Rich Text/Button/Button Group/Badge/Divider/Spacer/Icon/Icon List/Quote/Alert/Code received complete source and schema reviews (472 settings, 36 built-in presets). Fixed dead layout/color/background/overflow controls, custom widths, alignment and static responsive styling; safe repeated heading highlights; accessible linked icons and text-only icon-list links; truthful empty/editor states and configured actions. Removed invented customer/shipping/service/sale insertion claims and fictitious cart action labels. Saved merchant data and code configuration are untouched.

The two split wrapper parsing errors are resolved using a complete wrapper with captured content. The start path remains actively consumed by all 12 primitives; end is retained as a comment-only dormant path, with no deletion. Native Shopify outer wrappers own editor attributes. The already-loaded CSS asset now contains the presentation previously injected by JavaScript, and redundant per-block asset tags are gone. The small guarded animation engine disposes observations on unload, tolerates stale callbacks, handles late/dynamic boot and tall content, and reveals on keyboard focus, editor selection or reduced-motion changes. Both builder chains and app admission are unchanged.

Evidence: `validation/batch8-primitives/`. All 13 affected suites pass, including seven Phase 3 regressions, two dedicated Batch 8 suites, four prior host/dependency suites, 899 default/preset/control render cases and targeted assertions. All 14 changed Liquid files pass native strict parsing. Schema/reference/architecture/source-hash checks and CSS build pass; compiled theme CSS and all section/merchant state remain unchanged.

Theme Check: **499 → 500 findings, errors 5 → 3, warnings 494 → 497**, unsuppressed CLI exit 1. Zero errors in this group. The parsed wrappers now receive two orphan diagnostics (12 explicit start consumers; honestly dormant end); repaired Quote Custom width increases its controls from 40 to 41 and adds one settings-count warning. No unrelated global errors changed.

**B8-PRIMITIVES live queue:** actual Shopify block/preset insertion, native image/custom-Liquid evaluation and Theme Editor lifecycle; all saved/zero/extreme controls, long text, 320/390/768/1280/1440px, RTL/zoom/no-JS/motion/keyboard/AT/contrast; real merchant destinations and provider code. Adapters and source checks do not certify live behavior. No owner decision blocks continuation. PR #22 remains draft; Batch 9, the brand-preset rollout, roadmap Phase 5 and live acceptance remain out of scope.

## Batch 8 checkpoint — product identity and metadata — 2026-09-22

Owner explicitly authorized pushing the first Batch 8 checkpoint and subsequent coherent checkpoints on the release branch. Local `f9d6a31` was published through GitHub object APIs as `614872ef8d5a4e71add8509fd09abeb84f79b72a`, with the identical tree verified. PR #22 remains open/draft. No main write or merge occurred.

Eight more blocks now have individual engineering dispositions: Product Title, Subtitle, Vendor, Type, Tags, SKU, Description and Metafield (209 settings, 24 presets). Vendor now uses real product.vendor for new insertions; its former copied subtitle settings remain compatible. Corrected subtitle rich/plain markup, localized tag links, Product Type's malformed saved animation option, independent font controls, missing/empty/editor states and bounded long content. SKU data is escaped outside raw script bodies, and updates are scoped to the originating host. Descriptions are complete without JS, expand for keyboard focus, and have clean lifecycle/remeasurement behavior. Typed metafield rendering preserves false/zero, escapes text, handles supported lists/references and supplies real named file destinations.

One new globally deferred metadata controller is required for first insertion after initial editor boot. It is inventoried/reviewed in Family 8, bringing the family to 107 entries (102 original blocks + five dependencies). The normal layout receives one dependency tag; all protected section sources remain unchanged. Product Form and Variant Picker only change event emission from document to the originating root; actual producer bodies are integration-tested, but those blocks remain NOT REVIEWED pending their complete individual reviews.

**25/107 Family 8 entries have dispositions: 24 REQUIRES LIVE VERIFICATION, one dormant retained; 82 remain NOT REVIEWED.** Total register: 493 entries, 357 live queued, 27 dormant reviewed, one build-only, one retained owner-review deletion candidate, 107 NOT REVIEWED. No untouched component is approved by association.

Evidence: `validation/batch8-product-identity/`. All 15 affected suites pass, including all seven Phase 3 suites, two new block suites, primitive/engine, product host, main/compact commerce and layout regressions. 444 default/preset/control renders plus targeted type/escape/URL/media/lifecycle assertions pass. All changed Liquid sources parse in native strict mode; architecture/schema/reference/source hashes, protected settings/sections and CSS build pass. Theme Check remains unsuppressed with its exact counts in the evidence; three existing out-of-group errors remain.

**B8-PRODUCT-IDENTITY live queue:** native Shopify drops/media/locale URLs and editor insertion/load/unload/reorder/selection; duplicate product hosts, variant/SKU transitions, description image/font/viewport reflow and keyboard/AT/no-JS; every saved/preset/control state at 320/390/768/1280/1440px, RTL/zoom/reduced motion and theme contrast. No live acceptance started. Batch 8 continues; Batch 9 and preset-family rollout remain outside authorization.

Product identity checkpoint Theme Check reconciliation: **500 findings unchanged: three errors, 497 warnings.** The only in-group diagnostic is the existing Metafield settings-count warning; no new diagnostics or suppressions.

## Batch 8 checkpoint — product selection and purchase — 2026-09-22

The identity/metadata checkpoint is published as `846267cb57c7c3d70597a3f43f163252f9801836` (identical tree to local `470b7b7`); PR #22 remains DRAFT. This checkpoint reviews Product Form, Variant Picker, Add to Cart, Dynamic Checkout, Quantity Selector, Price and Sticky Add to Cart individually, including all 21 built-in presets and controls. Add to Cart was a copied Quantity Selector and now performs a real native product form action while retaining its existing quantity settings. Inventory Status is identified as another copied component and remains explicitly NOT REVIEWED for its stock/trust group.

Native forms have unique IDs, one ID/quantity/plan payload, real cart integration and a nonenhanced variant fallback. Exact option selection synchronizes within one builder host; null/sold-out selections clear IDs and disable checkout, quantity rules intersect configured bounds, and selected plans determine availability/prices. Native Liquid money/unit strings replace ad-hoc client currency formatting; initial and changed prices agree. Required plans are available on all purchase surfaces. The selected native variant is explicitly included even outside the enumerated list, preventing silent replacement by the first variant. The same fix carries through the existing plan helper and SKU data. Native option swatches and accurate labels replace guessed colors and invalid radio semantics. Sticky positioning belongs to the native wrapper; mobile selector/offset, long text and error display remain usable. Current accelerated-checkout custom properties respect Shopify's wallet size limit while preserving existing area-height settings.

Three new purchase helpers are individually reviewed/inventoried. Existing cart mutation ownership is preserved with optional per-form progress/success messages following actual results. Shared selling plans retain Batch 4 ownership; metadata/SKU integration adds real restoration and out-of-list selection coverage. Two small global dependency loads enable first editor insertion after initial page boot. All section sources, builder admissions and merchant data are unchanged.

**35/110 Family 8 entries now have dispositions: 34 live queued, one dormant retained, 75 NOT REVIEWED.** 27/102 blocks and eight dependencies are covered. Global register: 496 entries, 367 live queued, 27 dormant reviewed, one build-only, one retained owner-review deletion candidate, 100 NOT REVIEWED. No Batch 9 work is implied.

Evidence: `validation/batch8-product-purchase/`. All 21 affected suites pass, including seven Phase 3, six Batch 8 and all main-product/cart/builder/layout dependencies affected by this group. 375 default/preset/control renders plus actual multi-block state, native payload, request/result and lifecycle fixtures pass. Changed/new Liquid parses strictly; schema/reference/architecture/source-hash/state/CSS gates pass. The initial nonexistent native-cart.cjs harness argument is preserved and reconciled to the actual cart-commerce.cjs suite. Real form/picker producer coverage moved from removed inline closures to actual shared-controller composition tests. Theme Check remains unsuppressed; exact new-helper orphan diagnostics and unchanged out-of-group errors are recorded in evidence.

**B8-PRODUCT-PURCHASE live queue:** actual native checkout/cart/wallets, eligibility/plan allocation and stock/quantity rules, market/currency/unit/tax presentation, option/swatch drops and high-variant/native-enumeration coverage; repeated same-product builders, sticky ancestors, saved/default/preset extremes, 320/390/768/1280/1440px, RTL/zoom/keyboard/AT/no-JS/reduced motion and actual editor lifecycle. All previous queues carry forward. Continue Batch 8; stop only at formal engineering closure for owner approval. No live QA, preset-family rollout, Batch 9 or merge/main write.

## Batch 8 checkpoint — product stock, policies and information — 2026-09-22

Previous checkpoint published as `2bcc8083ac97fbd93b5ed71690c09de8a2f7e890`; PR #22 remains DRAFT. Seven individual blocks now provide truthful, scoped product stock and information. Inventory Status was copied Trust Badges and now renders real native stock states while retaining legacy settings. Specifications updates with actual purchase selections and uses native definition-list/text semantics. Payment badges require enabled store/market providers; policy links use configured shop policies. Comparison, feature and trust insertion defaults no longer invent product or merchant claims. Existing saved text, setting IDs, all section sources, builder admissions and merchant data are preserved. One formatter is inventoried; the already reviewed metadata controller gains information consumers and explicit product ownership checks.

34/102 blocks and 9 dependencies individually reviewed; 43/111 Family 8 entries have dispositions (42 live queued, 1 dormant retained), 68 NOT REVIEWED. Global dispositions: 375 REQUIRES LIVE VERIFICATION, 1 DELETION CANDIDATE — OWNER REVIEW, 93 NOT REVIEWED, 27 RETAINED — DORMANT SOURCE REVIEWED, 1 REVIEWED — BUILD ONLY.

Evidence: `validation/batch8-product-information/`. 553 actual default/preset/control/state renders plus composed stock/specification/form/picker tests cover tracked/untracked/low/negative/sold-out stock, required product context, native policies/providers, hidden-label badges, safe text/URLs/image alt, table/list/details semantics, saved IDs, duplicate hosts/products, selected out-of-list data, invalid selection and editor lifecycle. The composition test caught an absent Specifications initialization hook; runtime source was repaired before the final pass. All 16 affected suites pass; 517 settings, 21 actual presets; strict native parsing/schema/reference/architecture/hash/CSS gates pass, all protected sections/state preserved. Exact unsuppressed lint movement and individual source/consumer/disposition records are committed.

B8-PRODUCT-INFORMATION live queue after engineering batches: real native inventory across tracked/untracked/backorder/sold-out and high-variant products; market/currency payment-provider exposure and checkout eligibility; actual configured policy URLs, native weight/options/image alt; saved/default/preset extremes, 320/390/768/1280/1440px, long copy, RTL/zoom/keyboard/AT/no-JS/reduced motion, duplicate product builders and actual editor insertion/reload. All earlier queues carry forward; offline fixtures do not certify Shopify execution.

No Batch 9, preset-family rollout or live QA. Continue Batch 8 to formal engineering closure, then stop for owner approval.

## Batch 8 checkpoint — content and product builder layouts — 2026-09-22

Previous checkpoint published as `50e8866a11ddda6bb82c0b34b580c0ff575887eb`; PR #22 remains DRAFT. Four individual layout blocks now preserve the complete content and product builder chains while making their own root elements receive layout and editor behavior. Three blocks adopt Shopify tag:null, matching the existing product row; all four keep one explicit editor root. Resolved mobile modes, scoped CSS ownership, min-height flex alignment, real vertical stretch, ordering and sticky height are reconciled with unchanged host sections. Exact admitted child lists and preset child trees remain intact; saved settings and all host/merchant source data are unchanged. No new runtime dependency or controller is introduced.

38/102 blocks and 9 dependencies individually reviewed; 47/111 Family 8 entries have dispositions (46 live queued, 1 dormant retained), 64 NOT REVIEWED. Global dispositions: 379 REQUIRES LIVE VERIFICATION, 1 DELETION CANDIDATE — OWNER REVIEW, 89 NOT REVIEWED, 27 RETAINED — DORMANT SOURCE REVIEWED, 1 REVIEWED — BUILD ONLY.

Evidence: `validation/batch8-builder-layouts/`. 367 actual default/control/preset/host combinations, all 12 nested presets, exact admission/tree preservation, one native root per block, content/product chains with real reviewed leaves and purchase form, tag/role/label/empty/editor cases pass. A declared CSS-cascade adapter checks actual DOM matches, specificity, source order and width media for inherited/explicit mobile layouts, row gaps, host order, sticky versus full height and min-height alignment; it does not certify browser geometry. All 18 affected suites pass; 88 settings, 12 actual presets; strict native parsing/schema/reference/architecture/hash/CSS gates pass, all protected sections/state preserved. Exact unsuppressed lint movement and individual source/consumer/disposition records are committed.

B8-BUILDER-LAYOUTS live queue after engineering batches: native Theme Editor insertion/select/drag/reorder/nesting and saved configurations; actual tag:null markup, layout fill/min-height/sticky containing ancestors/scroll snapping, media visual order versus keyboard/AT reading order; independent row overrides and section inheritance, padding/gap extrema, 320/390/768/1280/1440px, long text/RTL/zoom/no-JS/reduced motion. All previous queues carry forward; no live Shopify QA performed.

No Batch 9, preset-family rollout or live QA. Continue Batch 8 to formal engineering closure, then stop for owner approval.

## Batch 8 checkpoint — content and product panels — 2026-09-22

Previous checkpoint published as `e6183d9f20fb67f26855b49d1ec0d40bde853204`; PR #22 remains DRAFT. Six panel blocks now render their configured content before JavaScript enhancement and share scoped lifecycle handling. Native details and dialog replace fragile custom behavior; tabs gain keyboard/orientation/RTL/hash support, media is active only for the selected panel, and product fields use a typed native formatter. Neutral insertion defaults replace tutorial or invented content. Both new actual dependencies are inventoried; layout/theme.liquid only adds the deferred panels controller.

44/102 blocks and 11 dependencies individually reviewed; 55/113 Family 8 entries have dispositions (54 live queued, 1 dormant retained), 58 NOT REVIEWED. Global dispositions: 387 REQUIRES LIVE VERIFICATION, 1 DELETION CANDIDATE — OWNER REVIEW, 83 NOT REVIEWED, 27 RETAINED — DORMANT SOURCE REVIEWED, 1 REVIEWED — BUILD ONLY.

Evidence: `validation/batch8-panels/`. 468 actual default/preset/populated-control/state renders plus multi-instance interactions pass, including blank-first panels, no-JS content, late boot/insertion, keyboard/RTL/orientation/hash, media stop/reopen, unload/reload/reorder, native image/video/typed values, sparse table columns, modal cancellation/focus restoration and unavailable/throwing dialog fallback. All 19 affected suites pass; 379 settings, 18 actual presets; strict native parsing/schema/reference/architecture/hash/CSS gates pass, all protected sections/state preserved. Exact unsuppressed lint movement and individual source/consumer/disposition records are committed.

B8-PANELS live queue after engineering batches: native Theme Editor insert/select/reorder/load/unload; browser details grouping, dialog top layer/inert/focus trapping/AT and scroll; actual YouTube/Vimeo/video playback and retirement; native metafield image/video/rich formatting, chart/table 320/390/768/1280/1440px, no-JS/RTL/zoom/reduced motion. All prior queues remain; no live Shopify QA performed.

No Batch 9, preset-family rollout or live QA. Continue Batch 8 to formal engineering closure, then stop for owner approval.

Panel checkpoint Theme Check: 503 (3 errors, 500 warnings) to 504 (3 errors, 501 warnings); one helper OrphanedSnippet despite actual consumers, no new errors or suppressions.

## Batch 8 checkpoint — product countdown, progress, merchant messages and sharing — 2026-09-22

Previous checkpoint published as `3f855b75a51824e9755c24ed9904c7b7b4dcfd5d`; PR #22 remains DRAFT. Four product utility blocks now use configured facts and progressive native fallbacks. Strict shared deadline validation and the existing Countdown controller replace duplicated inline clocks. Product Progress Bar gains correct identity and optional real elapsed-time progress without removing its saved countdown contract. Merchant messages have no invented purchase/viewer defaults or unsafe inline JSON. Sharing preserves product/locale URLs and native links while reporting only actual copy results and disposing async work. Two new dependencies are individually inventoried; the existing Countdown asset remains Batch 5 with contextual revalidation.

48/102 blocks and 13 dependencies individually reviewed; 61/115 Family 8 entries have dispositions (60 live queued, 1 dormant retained), 54 NOT REVIEWED. Global dispositions: 393 REQUIRES LIVE VERIFICATION, 1 DELETION CANDIDATE — OWNER REVIEW, 79 NOT REVIEWED, 27 RETAINED — DORMANT SOURCE REVIEWED, 1 REVIEWED — BUILD ONLY.

Evidence: `validation/batch8-product-utilities/`. 341 actual default/preset/control/state renders plus runtime cases cover strict calendar/leap/time/offsets, no-JS deadline/link/copy fallbacks, configured time progress before/during/after its range, exact expiry/hidden-page pause, two independent timers/share roots, safe message injection and real text, copy success/failure/cancel/focus, editor load/unload/reorder/removal/late insertion and pending async disposal. Original Countdown/Progress regression also passes. All 21 affected suites pass; 157 settings, 12 actual presets; strict native parsing/schema/reference/architecture/hash/CSS gates pass, all protected sections/state preserved. Exact unsuppressed lint movement and individual source/consumer/disposition records are committed.

B8-PRODUCT-UTILITIES live queue after engineering batches: native Liquid epoch parsing/caching and configured fixed UTC offsets versus daylight-saving intent; native progress/timer/no-JS/AT and responsive long content, true Web Share/clipboard permissions/provider/email behavior; merchant-supplied messages must reflect actual configured claims and have no connected live feed; Theme Editor insertion/select/reorder/load/unload and actual focus/motion. All prior queues carry forward. No live Shopify QA.

No Batch 9, preset-family rollout or live QA. Continue Batch 8 to formal engineering closure, then stop for owner approval.

Product utility checkpoint Theme Check: 504 (3 errors, 501 warnings) to 505 (3 errors, 502 warnings), one deadline-helper orphan report despite actual consumers. No new errors or suppression.

## Batch 8 checkpoint — native product media gallery — 2026-09-22

Previous checkpoint published as `60de065bdf99b34680a29c2874f6fc0a52c80854`; PR #22 remains DRAFT. Product Gallery retains its four merchant layouts and native media contracts with progressive navigation and scoped lifecycle. A shared native media helper removes repeated rendering branches and defers external frames behind real links. The gallery uses compact selected/native ID data, actual product-form ownership, native scroll, corrected thumbnail layout controls and explicit autoplay pause/resume. Both actual new dependencies are individually inventoried; legacy product-host controllers and all section sources remain untouched.

49/102 blocks and 15 dependencies individually reviewed; 64/117 Family 8 entries have dispositions (63 live queued, 1 dormant retained), 53 NOT REVIEWED. Global dispositions: 396 REQUIRES LIVE VERIFICATION, 1 DELETION CANDIDATE — OWNER REVIEW, 78 NOT REVIEWED, 27 RETAINED — DORMANT SOURCE REVIEWED, 1 REVIEWED — BUILD ONLY.

Evidence: `validation/batch8-product-gallery/`. 80 actual default/control/preset/media/selected/empty renders plus runtime fixtures cover all four modes, no-JS media and native scroll source, selected records outside enumeration, safe native filters/links, active video/model/iframe teardown, actual two-product-host form/controller composition, scoped variant/restoration/invalid states, keyboard/RTL/manual scroll geometry, focus before pane hiding, independent autoplay/explicit pause/motion/visibility and editor insertion/load/unload/reorder/removal. Initial fixture assertion used an unsupported JSDOM img.loading property; reading the actual rendered loading attribute reconciles it without runtime changes. All 22 affected suites pass; 44 settings, 3 actual presets; strict native parsing/schema/reference/architecture/hash/CSS gates pass, all protected sections/state preserved. Exact unsuppressed lint movement and individual source/consumer/disposition records are committed.

B8-PRODUCT-GALLERY live queue after engineering batches: actual native Shopify image/video/model filters and model-viewer runtime/preview fallback; YouTube/Vimeo activation/player retirement, native controls/audio/model interaction; browser scroll-snap/RTL/inert/focus and selected variant media, 320/390/768/1280/1440px thumbnail/order/masonry sizing and long labels, no-JS/AT/zoom/reduced motion; native Theme Editor insertion/select/reorder/load/unload and multi-product instances. All prior queues carry forward; no live Shopify QA performed.

No Batch 9, preset-family rollout or live QA. Continue Batch 8 to formal engineering closure, then stop for owner approval.

Gallery checkpoint Theme Check: 505 (3 errors, 502 warnings) to 506 (3 errors, 503 warnings); one new media-helper orphan report despite its actual caller. No new errors or suppression.

## Batch 8 checkpoint — product recommendation and upsell cards — 2026-09-22

Previous checkpoint published as `5a51da22a4597d5069ded0f1e559668936da64d9`; PR #22 remains DRAFT. Four product discovery Theme Blocks retain saved settings/layout contracts while sharing native product card rendering and a scoped progressive carousel/recommendation lifecycle. Correct native complementary results supersede manual fallback; product limits and collection/vendor/type sources are truthful. Upsell base-price differences follow actual selected variants through the existing metadata controller. Neutral new insertion copy avoids invented claims; saved merchant state is unchanged.

53/102 blocks and 17 dependencies individually reviewed; 70/119 Family 8 entries have dispositions (69 live queued, 1 dormant retained), 49 NOT REVIEWED. Global dispositions: 402 REQUIRES LIVE VERIFICATION, 1 DELETION CANDIDATE — OWNER REVIEW, 74 NOT REVIEWED, 27 RETAINED — DORMANT SOURCE REVIEWED, 1 REVIEWED — BUILD ONLY.

Evidence: `validation/batch8-product-merchandising/`. 353 actual default/control/preset/source/price/empty renders plus runtime fixtures cover current-product exclusion, native/manual/wrong-intent/empty recommendation precedence, cap-10 locale routes and cross-origin rejection, escaped native tracking links and money, two RTL/configured-gap carousel compositions, keyboard/focus/success/failure/abort, dynamic insertion/removal/reorder, and two real commerce-form/metadata hosts with variant restoration, invalid states and selected records outside enumeration. All 23 affected suites pass; 230 settings, 12 actual presets; strict native parsing/schema/reference/architecture/hash/CSS gates pass, all protected sections/state preserved. Exact unsuppressed lint movement and individual source/consumer/disposition records are committed.

B8-PRODUCT-MERCHANDISING live queue after engineering batches: native complementary Search & Discovery/section/block/market context, locale tracking URLs, cap-10/manual/empty fallback; native variant/money/compare/image drops; browser no-JS/RTL scroll-snap, 320/390/768/1280/1440px grid/compact/split/long copy, keyboard/AT/reduced-motion/zoom; native editor insertion/select/reorder/load/unload and multiple product hosts/restoration. Upsell difference compares base unit prices before selling plans, quantity and cart discounts. All prior queues carry forward; no live Shopify QA performed.

No Batch 9, preset-family rollout or live QA. Continue Batch 8 to formal engineering closure, then stop for owner approval.

Product merchandising Theme Check: 506 (3 errors, 503 warnings) to 507 (3 errors, 504 warnings); one new card-helper OrphanedSnippet despite three actual callers. No new errors or suppression.

## Batch 8 checkpoint — native product bundle forms — 2026-09-22

Previous checkpoint published as `39b70ff16d74b65199e2e5c16c69ad6c88fe779e`; PR #22 remains DRAFT. The remaining two product merchandising blocks now use native multi-item forms and the existing cart drawer contract. A shared native item helper removes copied markup/calculation, respects availability/minimum quantities/required selling plans and keeps totals aligned with actual submitted items. Scoped enhancement follows the owning product form and preserves optional selection. Advertised discounts no longer reduce the payable subtotal without Shopify applying them; new presets avoid invented savings or purchase-history claims.

55/102 blocks and 19 dependencies individually reviewed; 74/121 Family 8 entries have dispositions (73 live queued, 1 dormant retained), 47 NOT REVIEWED. Global dispositions: 406 REQUIRES LIVE VERIFICATION, 1 DELETION CANDIDATE — OWNER REVIEW, 72 NOT REVIEWED, 27 RETAINED — DORMANT SOURCE REVIEWED, 1 REVIEWED — BUILD ONLY.

Evidence: `validation/batch8-product-bundles/`. 165 actual default/control/preset/empty/quantity/availability/plan renders plus runtime fixtures cover native no-JS contiguous FormData, duplicates/current exclusion, selected native plans and minimum quantities, actual two-host commerce/plan/quantity/restoration, out-of-enumeration selected variants, unknown native IDs, correct optional selection/totals/savings gates, unapplied display-discount separation, money separator/currency handling, native versus AJAX submission, real cart handler success/error/duplicate/detached completion, editor load/unload/reorder/removal/insertion and retained responsive source rules. All 28 affected suites pass; 119 settings, 6 actual presets; strict native parsing/schema/reference/architecture/hash/CSS gates pass, all protected sections/state preserved. Exact unsuppressed lint movement and individual source/consumer/disposition records are committed.

B8-PRODUCT-BUNDLES live queue after engineering batches: actual locale-aware native multi-item form and AJAX cart transactions, inventory/quantity-rule rejection and selling-plan eligibility/prices/names, native current variant outside enumeration and browser restoration; real automatic/app discounts versus advertised offer text, market/presentment money formats and cart totals; no-JS/keyboard/AT/focus/RTL/zoom/responsive layouts and native Theme Editor lifecycle. Companion subscription/options choices remain on their native product pages unless an actual selected allocation exists. No live Shopify QA performed; all prior queues carry forward.

No Batch 9, preset-family rollout or live QA. Continue Batch 8 to formal engineering closure, then stop for owner approval.

Bundle checkpoint unsuppressed Theme Check: 507 → 508 findings (3 existing out-of-group errors, 504 → 505 warnings). The new bundle item helper adds one OrphanedSnippet finding despite its two explicit callers. This group has zero Theme Check errors; no suppression was added.

## Batch 8 checkpoint — navigation and store information — 2026-09-22

Previous checkpoint published as `b6149dcd34b472ece563dfe8572ba1f13049bb08`; PR #22 remains DRAFT. Eight navigation/contact/store-information blocks now preserve native menu destinations, exact fragments, readable named links, manual schedules and safe merchant content. Mobile visibility, optional layout tracks, sticky ownership and editor lifecycle are repaired. New insertion defaults avoid fictional contact details, opening hours and page anchors; saved merchant settings remain unchanged.

63/102 blocks and 20 dependencies individually reviewed; 83/122 Family 8 entries have dispositions (82 live queued, 1 dormant retained), 39 NOT REVIEWED. Global dispositions: 415 REQUIRES LIVE VERIFICATION, 1 DELETION CANDIDATE — OWNER REVIEW, 64 NOT REVIEWED, 27 RETAINED — DORMANT SOURCE REVIEWED, 1 REVIEWED — BUILD ONLY.

Evidence: `validation/batch8-navigation-information/`. 629 actual default/control/preset/page/empty/contact renders plus runtime cases cover exact native menu parent/current semantics, escaped links/plain multiline contact data, action-only content, real manual schedule rows, punctuation-safe and contiguous anchors, original setting IDs/options/range endpoints, two-instance focus/history/reduced-motion navigation and existing tab-panel composition, missing-target/modifier fallbacks, mobile disclosure restoration and insertion/reorder/removal/editor cleanup. All 17 affected suites pass; 484 settings, 24 actual presets; strict native parsing/schema/reference/architecture/hash/CSS gates pass, all protected sections/state preserved. Exact unsuppressed lint movement and individual source/consumer/disposition records are committed.

B8-NAVIGATION-INFORMATION live queue after engineering batches: real native menu/current-route and localized breadcrumbs, browser fragment/history/focus/keyboard/AT behavior, sticky behavior within actual builder containers, mobile disclosures, narrow/RTL/zoom/color contrast and actual Theme Editor selection/reload. Merchant contact/schedule/anchor destinations remain manual configuration; no open-now clock or fabricated default details are implied. No live Shopify QA performed; previous queues carry forward.

No Batch 9, preset-family rollout or live QA. Continue Batch 8 to formal engineering closure, then stop for owner approval.

Navigation/information checkpoint Theme Check: 508 → 507 findings (three existing out-of-group errors; 505 → 504 warnings after unused contact assign removal). This group has six settings-count warnings and zero errors. No suppression was added.

## Batch 8 checkpoint — content cards, comparisons and guarantees — 2026-09-22

Previous checkpoint published as `c24b20bf8b0cee5beca71377bb9e9b7a6a7f9940`; PR #22 remains DRAFT. Six static content card/comparison/guarantee blocks now omit empty configured slots, preserve native images and safe destinations, repair narrow-layout/overlay/focus behavior and keep desktop/mobile comparisons consistent. A shared comparison-value helper centralizes readable symbol labels and escaped notes. New insertion defaults leave prices, popularity, performance and policy claims for merchants to configure explicitly; saved merchant content is preserved.

69/102 blocks and 21 dependencies individually reviewed; 90/123 Family 8 entries have dispositions (89 live queued, 1 dormant retained), 33 NOT REVIEWED. Global dispositions: 422 REQUIRES LIVE VERIFICATION, 1 DELETION CANDIDATE — OWNER REVIEW, 58 NOT REVIEWED, 27 RETAINED — DORMANT SOURCE REVIEWED, 1 REVIEWED — BUILD ONLY.

Evidence: `validation/batch8-content-cards/`. 588 actual default/control/preset/empty/native-media renders exercise all original IDs/options/range endpoints, populated and blank slots, native alt overrides/fallback and image/background precedence, literal icon/URL/note escaping, authored CTA-plus-footer behavior, hidden optional icon/point grids, enabled/named matrix column/row alignment, identical desktop/mobile value/CTA contracts and translated labels. Independent IDs, native wrapper/editor/motion and responsive source rules are checked. All 16 affected suites pass; 453 settings, 12 actual presets; strict native parsing/schema/reference/architecture/hash/CSS gates pass, all protected sections/state preserved. Exact unsuppressed lint movement and individual source/consumer/disposition records are committed.

B8-CONTENT-CARDS live queue after engineering batches: real card image rendition/alt/overlay stacking and configured colors; native narrow builder grids/flex, zoom/RTL and 320–1440px layouts; comparison table/definition-list semantics and keyboard/AT/contrast; actual Theme Editor insert/select/reorder. Merchant prices, comparisons and guarantee terms remain manual authored claims and do not change Shopify policies/cart prices. No live Shopify QA; prior queues carry forward.

No Batch 9, preset-family rollout or live QA. Continue Batch 8 to formal engineering closure, then stop for owner approval.

Content card checkpoint unsuppressed Theme Check: 507 → 508 findings (three unchanged out-of-group errors, 504 → 505 warnings). The shared comparison-value helper adds one OrphanedSnippet finding despite two explicit callers. Group has zero errors and five retained settings-count warnings; no suppression.

## Batch 8 checkpoint — native forms, search, social links and payment information — 2026-09-22

Previous checkpoint published as `fccb22d2017c4413d2c3e79248d4231e8dd0ca1e`; PR #22 remains DRAFT. Six native form/information blocks now preserve safe values and actual Shopify sources, provide working existing predictive-search composition, maintain accessible links/payment names, and retain the historical custom location alias through one reviewed renderer. Native contact/customer POST and localized search GET remain progressive; no new form backend or payment integration is claimed.

75/102 blocks and 22 dependencies individually reviewed; 97/124 Family 8 entries have dispositions (96 live queued, 1 dormant retained), 27 NOT REVIEWED. Global dispositions: 429 REQUIRES LIVE VERIFICATION, 1 DELETION CANDIDATE — OWNER REVIEW, 52 NOT REVIEWED, 27 RETAINED — DORMANT SOURCE REVIEWED, 1 REVIEWED — BUILD ONLY.

Evidence: `validation/batch8-native-forms-information/`. 471 actual render cases exercise all original IDs/options/range endpoints and 18 presets, blank/returned/malicious field values, native success/error/required/consent behavior, local/global social sources, 15 native payment aliases and unavailable filtering, manual custom labels, exact location-alias DOM/style parity, typed/localized search/filter/source modes and duplicate raw IDs. Actual predictive controller fixtures cover two hosts, IME/keyboard, safe provider results, stale responses/aborts, duplicate scripts and insert/reorder/remove/unload/reload. All 16 affected suites pass; 293 settings, 18 actual presets; strict native parsing/schema/reference/architecture/hash/CSS gates pass, all protected sections/state preserved. Exact unsuppressed lint movement and individual source/consumer/disposition records are committed.

B8-NATIVE-FORMS live queue after engineering batches: real contact/customer POST, captcha/server errors/return values and newsletter consent/tag handling; native search source/display/filter behavior and keyboard/AT; native payment SVGs and actual device/location/checkout eligibility; real narrow-container form/card layouts and Theme Editor lifecycle. Legacy custom-custom-form remains location information. No live Shopify QA; prior queues carry forward.

No Batch 9, preset-family rollout or live QA. Continue Batch 8 to formal engineering closure, then stop for owner approval.

Native forms/information lint checkpoint: 508 → 509 unsuppressed findings (3 unchanged out-of-group errors; 505 → 506 warnings). The new shared location helper adds one OrphanedSnippet warning despite two explicit callers. This group has no errors; no suppressions.

## Batch 8 checkpoint — comparison images, feature media, hotspots, image stacks, maps and lookbooks — 2026-09-22

Previous checkpoint published as `8b11d42c0d2108927227c1b6b546d2bb2de1fed7`; PR #22 remains DRAFT. Six visual media blocks now preserve progressive native content, repair full-size comparison alignment and actual enabled feature selection, replace fragile hotspot positioning with bounded pins and in-flow callouts, and keep sparse/long-content compositions useful. One scoped controller supplies interactive behavior and media policy/lifecycle handling through the existing deferred asset chain.

81/102 blocks and 23 dependencies individually reviewed; 104/125 Family 8 entries have dispositions (103 live queued, 1 dormant retained), 21 NOT REVIEWED. Global dispositions: 436 REQUIRES LIVE VERIFICATION, 1 DELETION CANDIDATE — OWNER REVIEW, 46 NOT REVIEWED, 27 RETAINED — DORMANT SOURCE REVIEWED, 1 REVIEWED — BUILD ONLY.

Evidence: `validation/batch8-visual-media/`. 468 actual control/default/preset/data-state renders exercise all original IDs/options/range endpoints, empty/populated/partially selected images and points, native alt overrides, native product/custom/collection precedence and zero/variable prices, safe plain text and URLs, no-JS readable comparison/panels/disclosures/video, meaningful copied-only and card-only layouts and duplicate raw IDs. Actual controller fixtures cover two instances, range updates, scoped Escape/outside close, keyboard/Home/End, active enabled-slot selection, mobile expanded state, reduced-motion/editor video gating, rejected/detached playback promises, and insert/reorder/remove/unload/reload. All 16 affected suites pass; 396 settings, 18 actual presets; strict native parsing/schema/reference/architecture/hash/CSS gates pass, all protected sections/state preserved. Exact unsuppressed lint movement and individual source/consumer/disposition records are committed.

B8-VISUAL-MEDIA live queue after engineering batches: native responsive image geometry and exact crop/point alignment, real 320–1440px/zoom/RTL/nested builder layouts, image-stack overlap/caption readability and lookbook overlay contrast, real range/disclosure/feature-map keyboard/AT, native video controls/autoplay/rejection/visibility/reduced-motion behavior, actual product prices/media and Theme Editor lifecycle. DOM/media/geometry adapters are not acceptance. No live Shopify QA; prior queues carry forward.

No Batch 9, preset-family rollout or live QA. Continue Batch 8 to formal engineering closure, then stop for owner approval.

Unsuppressed CLI 4.8.0 remains at 509 findings (3 unchanged out-of-group errors, 506 warnings); no group errors and no suppression.

## Batch 8 checkpoint — collection banners, cards, carousel and featured collection — 2026-09-22

Previous checkpoint published as `c838b84fd52d160f99c9847a55b0e44635077366`; PR #22 remains DRAFT. Four collection discovery blocks now use truthful native content, complete links, safe image/price fallbacks and bounded responsive layouts. One collection-card helper removes repeated rendering. Collection Carousel composes with the existing shared engine for progressive native scrolling and correctly scoped navigation/rotation/editor behavior.

85/102 blocks and 24 dependencies individually reviewed; 109/126 Family 8 entries have dispositions (108 live queued, 1 dormant retained), 17 NOT REVIEWED. Global dispositions: 441 REQUIRES LIVE VERIFICATION, 1 DELETION CANDIDATE — OWNER REVIEW, 42 NOT REVIEWED, 27 RETAINED — DORMANT SOURCE REVIEWED, 1 REVIEWED — BUILD ONLY.

Evidence: `validation/batch8-collection-discovery/`. 378 actual default/preset/control/data-state renders cover all original IDs/types/options/range endpoints, native collection list/single/manual fallback and limits, zero counts/prices and variable prices, empty/partial images, escaped native/merchant copy and destinations, cleared labels, neutral insertion, one native wrapper and editor visibility. Actual shared-engine fixtures cover duplicate loading, two raw-ID instances, measured/clamped page targets, RTL arrows/Home/End, nested-key ownership, persistent pause and translated labels, reduced motion/page visibility/intersection, resize pagination focus, mutation insert/reorder/remove and section unload/reload. All prior content carousel/gallery-text fixtures retain their assertions. All 16 affected suites pass; 238 settings, 12 actual presets; strict native parsing/schema/reference/architecture/hash/CSS gates pass, all protected sections/state preserved. Exact unsuppressed lint movement and individual source/consumer/disposition records are committed.

B8-COLLECTION-DISCOVERY live queue after engineering batches: actual native collection/product image and price rendering, 320–1440px/zoom/RTL/nested-builder layout and image/caption contrast, real scroll-snap and measured pagination/keyboard/AT, autoplay pause/visibility/reduced motion, native Theme Editor insertion/reorder/removal. DOM geometry/timer/native-drop adapters are not acceptance. No live Shopify QA; earlier queues carry forward.

No Batch 9, preset-family rollout or live QA. Continue Batch 8 to formal engineering closure, then stop for owner approval.

Unsuppressed CLI 4.8.0 moves from 509 to 510 findings: 3 unchanged out-of-group errors and 507 warnings. The one new helper orphan warning retains two traced callers; no suppression.

## Batch 8 checkpoint — product grids, carousels and native recommendations — 2026-09-22

Previous checkpoint published as `0207ee86a01079efb70dfda3323366e7f019fe1e`; PR #22 remains DRAFT. Three product discovery blocks now reuse existing card and carousel/recommendation controllers, replacing repeated markup and unsafe inline lifecycles. They preserve native source selection/tracking URLs, show accurate product price ranges and guard native related recommendation requests and response ownership.

88/102 blocks and 24 dependencies individually reviewed; 112/126 Family 8 entries have dispositions (111 live queued, 1 dormant retained), 14 NOT REVIEWED. Global dispositions: 444 REQUIRES LIVE VERIFICATION, 1 DELETION CANDIDATE — OWNER REVIEW, 39 NOT REVIEWED, 27 RETAINED — DORMANT SOURCE REVIEWED, 1 REVIEWED — BUILD ONLY.

Evidence: `validation/batch8-product-discovery/`. 276 actual default/preset/control/data-state renders cover every original setting ID/type/option and range endpoint, exact manual/collection/native related source selection, limits 2–24 and native API cap ten, native zero/range prices and compare/badge matching, escaped tracking URLs/text, image/alt and missing-image states, complete labels, neutral new insertion and editor wrappers. Actual controllers cover native related success/failure/empty/wrong root/product/intent/cross-origin/abort, duplicate execution, two independent native-ID carousels and measured/clamped RTL navigation with persistent/intersection pause; previous complementary merchandising and shared engine lifecycle regressions pass. All 16 affected suites pass; 174 settings, 9 actual presets; strict native parsing/schema/reference/architecture/hash/CSS gates pass, all protected sections/state preserved. Exact unsuppressed lint movement and individual source/consumer/disposition records are committed.

B8-PRODUCT-DISCOVERY live queue after engineering batches: actual Shopify related/complementary recommendations and section rendering/settings context, native tracking URLs and presentment prices, native image geometry, 320–1440px/RTL/zoom/nested layouts, real scroll-snap/pagination/keyboard/AT/reduced-motion policy and Theme Editor lifecycle. Response/DOM/geometry adapters are not acceptance. No live Shopify QA; prior queues carry forward.

No Batch 9, preset-family rollout or live QA. Continue Batch 8 to formal engineering closure, then stop for owner approval.

Unsuppressed CLI 4.8.0 remains at 510 findings (3 unchanged out-of-group errors and 507 warnings); no group errors or suppression.

## Batch 8 checkpoint — Featured Product native purchase and media — 2026-09-22

Previous checkpoint published as `9c3cfc25bff36ae7a2fc48d7fdd382550a3f1072`; PR #22 remains DRAFT. Featured Product now uses the established canonical purchase and native-media controllers, with independent manually selected products and shared explicit current-product state. Selected variant price, availability, quantity, plans and media remain coherent without foreign event contamination.

89/102 blocks and 25 dependencies individually reviewed; 114/127 Family 8 entries have dispositions (113 live queued, 1 dormant retained), 13 NOT REVIEWED. Global dispositions: 446 REQUIRES LIVE VERIFICATION, 1 DELETION CANDIDATE — OWNER REVIEW, 38 NOT REVIEWED, 27 RETAINED — DORMANT SOURCE REVIEWED, 1 REVIEWED — BUILD ONLY.

Evidence: `validation/batch8-featured-product/`. 112 actual default/preset/control/data-state renders preserve every original setting type/option and cover native source precedence, missing products/media, all native media types, selected variant/media outside enumeration/limit, gallery-off dynamic media, escaped labels, native required/optional plans and quantities. Actual combined manual/current/main-form/SKU/bundle/gallery fixture proves ownership, player activation/cleanup, section and mutation lifecycle; actual cart drawer proves one POST with exact native id/quantity/selling_plan, invalid quantity blocking, sold-out blocking, errors and native fallback. All 15 affected suites pass; 68 settings, 3 actual presets; strict native parsing/schema/reference/architecture/hash/CSS gates pass, all protected sections/state preserved. Exact unsuppressed lint movement and individual source/consumer/disposition records are committed.

B8-FEATURED-PRODUCT live queue after engineering batches: actual native product forms/variant enumeration/selling plans/quantity rules/presentment prices/cart drawer and server errors, product-builder sharing, Shopify media/model players, 320–1440px/RTL/zoom/nested layouts, real keyboard/AT and Theme Editor lifecycle. LiquidJS/DOM/native-filter/player adapters are not acceptance. No live Shopify QA; prior queues carry forward.

No Batch 9, preset-family rollout or live QA. Continue Batch 8 to formal engineering closure, then stop for owner approval.

Unsuppressed Theme Check: 511 findings: 3 unchanged out-of-group errors and 508 warnings. One new shared-gallery orphan diagnostic despite traced Featured Product caller; no suppression.

## Batch 8 checkpoint — recently-viewed history and its legacy alias — 2026-09-22

Previous checkpoint published as `ba03eb0a35072dbcc2c1f2fc786f233f178a344f`; PR #22 remains DRAFT. Recently Viewed and its historical Collection Grid alias now share a native-card history renderer and scoped controller. Only validated handles persist; current native product data is rendered through the owning section, replacing stale and unsafe stored card snapshots.

91/102 blocks and 27 dependencies individually reviewed; 118/129 Family 8 entries have dispositions (117 live queued, 1 dormant retained), 11 NOT REVIEWED. Global dispositions: 450 REQUIRES LIVE VERIFICATION, 1 DELETION CANDIDATE — OWNER REVIEW, 36 NOT REVIEWED, 27 RETAINED — DORMANT SOURCE REVIEWED, 1 REVIEWED — BUILD ONLY.

Evidence: `validation/batch8-recently-viewed/`. 174 actual renders cover both aliases' original controls, select/checkbox/range endpoints, six actual presets, no-JS/editor/empty states, native card labels/alt/prices and section/root context. Actual controller tests cover legacy migration, malicious snapshot fields, locale URLs, same-origin and exact response identity, count/history limits, current exclusion/inclusion, missing/foreign/failed responses, unavailable storage, editor isolation, abort/removal, dynamic insertion/reload/reorder and independent custom-key instances. Actual native-card geometry validates configured-gap LTR/RTL controls and nested-link keyboard ownership. All 11 affected suites pass; 116 settings, 6 actual presets; strict native parsing/schema/reference/architecture/hash/CSS gates pass, all protected sections/state preserved. Exact unsuppressed lint movement and individual source/consumer/disposition records are committed.

B8-RECENTLY-VIEWED live queue after engineering batches: actual Shopify section rendering for each owning builder/template and product context (missing/mismatched owner responses intentionally stay empty), current localized prices/images/product availability, real browser history/storage restrictions, 320–1440px/RTL/zoom/nested layouts, actual scroll/focus/keyboard/AT and Theme Editor lifecycle. Native-filter/response/DOM/geometry adapters are not acceptance. No live Shopify QA; prior queues carry forward.

No Batch 9, preset-family rollout or live QA. Continue Batch 8 to formal engineering closure, then stop for owner approval.

Unsuppressed Theme Check: 512 findings: 3 unchanged out-of-group errors and 509 warnings. One new shared-renderer orphan diagnostic despite both traced block callers; no suppression.

## Batch 8 checkpoint — logos, press, trust badges and timeline — 2026-09-22

Previous checkpoint published as `d9e1fcdf2d074208d40675d5c50e5eecba32336b`; PR #22 remains DRAFT. Four static proof/timeline blocks now have neutral new insertion, complete safe native content and useful empty/editor states. Repairs address logo/press naming, independently sized badge images and sparse ordered timelines with logical/nested geometry.

95/102 blocks and 27 dependencies individually reviewed; 122/129 Family 8 entries have dispositions (121 live queued, 1 dormant retained), 7 NOT REVIEWED. Global dispositions: 454 REQUIRES LIVE VERIFICATION, 1 DELETION CANDIDATE — OWNER REVIEW, 32 NOT REVIEWED, 27 RETAINED — DORMANT SOURCE REVIEWED, 1 REVIEWED — BUILD ONLY.

Evidence: `validation/batch8-proof-timeline/`. 266 actual renders cover all 333 original settings, eight real presets and select/checkbox/range endpoints with populated data. Original saved preset/default content remains visible. Sparse last slots and limits, header-only logos, image/quote/source/badge-only press links, complete action-only badges/timeline entries, escaped text/icons/URLs, intact rich text, native alt/geometry, empty storefront/editor states, raw-ID duplicate instances and all CSS custom-property definitions pass. Source checks cover every layout, nested/container constraints, reduced motion and logical/minimal/narrow timeline geometry. All 9 affected suites pass; 333 settings, 8 actual presets; strict native parsing/schema/reference/architecture/hash/CSS gates pass, all protected sections/state preserved. Exact unsuppressed lint movement and individual source/consumer/disposition records are committed.

B8-PROOF-TIMELINE live queue after engineering batches: merchant-supplied logo/press/trust/testimonial claims remain owner content; actual native image/alt and destination behavior, 320–1440px/RTL/zoom/nested grids and timeline markers, contrast/keyboard/AT and Theme Editor lifecycle. LiquidJS/native-image/DOM adapters are not browser acceptance. No live Shopify QA; prior queues carry forward.

No Batch 9, preset-family rollout or live QA. Continue Batch 8 to formal engineering closure, then stop for owner approval.

Unsuppressed Theme Check: 512 unchanged findings: 3 unchanged out-of-group errors and 509 warnings. Four retained settings-count warnings in this group, zero group errors; no suppression.

## Batch 8 checkpoint — marquee, scrolling text and exact stats counters — 2026-09-22

Previous local checkpoint `ff1d3dfa0bd7360f3d4300eb467eb26f0550721e`; PR #22 remains DRAFT. Three motion/stat blocks now preserve authoritative merchant content and native fallbacks while using the existing shared motion and stats controllers. Saved IDs/types/options remain intact; new insertion avoids fabricated service claims and numbers.

98/102 blocks and 27 dependencies individually reviewed; 125/129 Family 8 entries have dispositions (124 live queued, 1 dormant retained), 4 NOT REVIEWED. Global dispositions: 457 REQUIRES LIVE VERIFICATION, 1 DELETION CANDIDATE — OWNER REVIEW, 29 NOT REVIEWED, 27 RETAINED — DORMANT SOURCE REVIEWED, 1 REVIEWED — BUILD ONLY.

Evidence: `validation/batch8-motion-counters/`. 253 actual default/preset/control/data-state renders; every original setting type/option and seven presets, saved text, inert/named originals, sparse/empty/native-image states and raw IDs. Actual shared controllers cover independent motion/RAF, persistent pause, focus/hover/visibility/reduced motion, exact numeric text and stable accessible values, RTL measurement, mutation insert/remove/reorder and editor disposal/reload. All prior Partner Logos and Stats Strip assertions retained. The first gate run found only two trailing-whitespace lines; these were removed, hashes refreshed and diff gate rerun successfully. All 10 affected suites pass; 195 settings, 7 actual presets; strict native parsing/schema/reference/architecture/hash/CSS gates pass, all protected sections/state preserved. Exact unsuppressed lint movement and individual source/consumer/disposition records are committed.

B8-MOTION-COUNTERS live queue: actual native horizontal motion/scroll/RTL/keyboard/AT/focus/zoom/nested layouts; browser reduced-motion/offscreen/visibility transitions and Theme Editor insert/reorder/unload; merchant-supplied claim correctness. Counters are display animation only. DOM/geometry/RAF/native-image adapters are not live acceptance. No Shopify live QA; all previous queues carry forward.

No Batch 9, preset-family rollout or live QA. Continue Batch 8 to formal engineering closure, then stop for owner approval.

Motion/counter checkpoint Theme Check remains 512 unsuppressed findings (3 unchanged out-of-group errors, 509 warnings), zero group errors. Publication status: automatic approval review rejected the public-branch push of recovered checkpoint `ff1d3df`, including a retry narrowed to the exact reviewed commit after outgoing scope/secret-signature checks. Review stated that trusted authorization for public source disclosure/remote mutation was missing. No alternate publication route was attempted. This and subsequent authorized engineering work is committed locally; remote publication and PR summary update await direct owner approval.

## Batch 8 checkpoint — reviews, testimonials, native slider media and UGC — 2026-09-22

Previous local checkpoint `0b67c27cca96924159cb3c4f2fa2efa0afd61ccf`; PR #22 remains DRAFT. The final four Theme Blocks now have truthful insertion states and complete native content, with reviewed shared carousel and media controllers replacing fragile local behavior. Saved merchant reviews, claims and media configuration remain intact.

102/102 blocks and 27 dependencies individually reviewed; 129/129 Family 8 entries have dispositions (128 live queued, 1 dormant retained), 0 NOT REVIEWED. Global dispositions: 461 REQUIRES LIVE VERIFICATION, 1 DELETION CANDIDATE — OWNER REVIEW, 25 NOT REVIEWED, 27 RETAINED — DORMANT SOURCE REVIEWED, 1 REVIEWED — BUILD ONLY.

Evidence: `validation/batch8-reviews-ugc/`. 507 actual default/control/preset/data-state renders cover all original settings/types/options and twelve presets, neutral new and retained saved claims, sparse/action/image/rating/video/product-only states, escaped URLs/alt and named ratings. Actual shared controllers cover measured RTL navigation, persistent pause/focus restoration, deferred player cleanup, visibility/mutation/editor lifecycle and independent UGC play controls/reduced motion/editor policy. Test development corrected an expectation: restored player-button focus correctly continues to stop rotation until focus leaves. Checkpoint command initially named a nonexistent gallery suite; the actual gallery-text-carousel suite was then run and substituted, with initial invocation evidence retained. No runtime code was changed to satisfy either fixture mistake. All 14 affected suites pass; 428 settings, 12 actual presets; strict native parsing/schema/reference/architecture/hash/CSS gates pass, all protected sections/state preserved. Exact unsuppressed lint movement and individual source/consumer/disposition records are committed.

B8-REVIEWS-UGC live queue: actual native settings drops and hosted/uploaded player behavior, real YouTube/Vimeo availability/privacy/controls, genuine merchant review/rating/verification/UGC rights and accuracy, 320–1440px/RTL/zoom/nested layouts, keyboard/AT/focus/contrast/native scrolling, and actual Theme Editor insertion/reorder/reload. Fixtures/DOM/geometry/media stubs are not Shopify/browser certification. No live QA; earlier queues and 605 historical candidates remain separate.

No Batch 9, preset-family rollout or live QA. Continue Batch 8 to formal engineering closure, then stop for owner approval.

Final block-group Theme Check remains 512 findings (3 unchanged out-of-group errors, 509 warnings), zero group errors. All 102 Theme Blocks and 27 Family 8 dependencies now have source dispositions; formal closure still requires the full regression run. Publication remains blocked by automatic approval review, so this checkpoint is local and is not represented as a GitHub head.


## Formal Batch 8 engineering closure — 2026-09-22

**BATCH 8 ENGINEERING COMPLETE — READY FOR OWNER CHECKPOINT.** This is a local engineering closure. Phase 4 remains active; PR #22 remains DRAFT. Stop at this checkpoint: Batch 9, Atelier/Signal/Terrace application, roadmap Phase 5 and live Shopify QA have not begun.

All **102 Theme Blocks and 27 assigned dependencies** have individual source/settings/preset/consumer dispositions. Family 8: **129 total; 128 REQUIRES LIVE VERIFICATION; one RETAINED — DORMANT SOURCE REVIEWED; zero NOT REVIEWED.** The retained comment-only path is `snippets/qtm-content-block-wrapper-end.liquid`. All 102 blocks map to their setting/preset evidence in `validation/batch8-engineering-closure/block-evidence-index.json`; current schemas contain 5,617 setting IDs and 294 presets. This evidence index consolidates completed reviews without repeating them. No unresolved source-level Batch 8 defect remains in this engineering review.

Runtime source head: `34d1726151c91d9ea46bb797abbee41321f3e791`. This formal closure changes only the register, report, validation evidence and an existing date fixture. The three local continuation checkpoints are `ff1d3dfa0bd7360f3d4300eb467eb26f0550721e` (four proof/timeline blocks), `0b67c27cca96924159cb3c4f2fa2efa0afd61ccf` (three motion/counter blocks) and `34d1726151c91d9ea46bb797abbee41321f3e791` (four review/testimonial/UGC blocks). Across the final 11 blocks: 1,026 actual renders, 956 settings and 27 built-in presets. No new dependency or ownership transfer was needed; existing motion, counter, carousel and video controllers were reused and their previous consumer assertions retained.

Final validation: **all 111 suites pass (seven Phase 3 and 104 Phase 4)**. The full initial run passed 110 suites; the existing Featured Content fixture froze UTC time but inherited Asia/Tokyo, moving its expected date to the next calendar day. Its fixture now pins parsing and formatting to UTC, preserves every prior assertion and explicitly checks the intended calendar day. Targeted reruns pass from both Asia/Tokyo and UTC hosts; initial and intermediate failures remain in evidence. No runtime source was changed for this correction. All **117 Family 8 Liquid sources** pass strict Shopify native parsing; schemas, setting evidence, references, architecture, all 515 source hashes and CSS build pass. All 150 sections, 102 blocks, both builders and protected Main Product/Quick View/header/footer hosts remain; all section files and merchant state are byte-identical to Batch 7 closure. Compiled CSS remains identical; no file was deleted.

Unsuppressed Theme Check 4.8.0: **512 findings, three errors and 509 warnings**, unchanged from the owner handoff. Family 8 has **zero errors and 84 warnings**. Across Batch 8, Batch 7's 499 findings/five errors/494 warnings moved to 512/three/509. Existing helper-orphan reports retain consumer traces. Remaining errors are two slideshow image dimension reports and the layout parser-blocking script filter; they are outside Family 8 and were not suppressed or reopened.

Global inventory: **515 components — 461 live queued, 27 dormant/source-reviewed, one build-only reviewed, one retained owner-review deletion candidate and 25 NOT REVIEWED**. The 25 comprise 17 Family 9 entries and eight Family 1 legacy/shared dependencies already assigned to Batch 9 closure (seven `header-two-*` snippets and `q-nav`). Exact paths are in `register-summary.json`; no Batch 9 work is started here.

**B8-FINAL live queue remains open.** Carry forward every per-block queue: real Shopify native product/collection/settings/form/recommendation/section-rendering contracts, images/video/providers, nested general/product builder contexts, 320–1440px/RTL/zoom/long-content/no-JS, keyboard/screen-reader/contrast, actual motion/timer/player and Theme Editor insertion/reorder/unload behavior, and genuine merchant claims. LiquidJS, JSDOM and native-drop/geometry/response/media adapters are engineering evidence, not storefront/browser acceptance. The earlier batches' queues, 605 historical verification candidates and 7,830 icon visual-review entries remain separately tracked. The owner schedules live acceptance after engineering batches.

Publication checkpoint: read-only GitHub verification still reports branch/PR head `d9e1fcdf2d074208d40675d5c50e5eecba32336b`, PR #22 open/DRAFT, and main `13d9e03010de46595e32e80144c17f76e1c36a39`. Automatic approval review rejected pushing to the public branch, including the narrowed exact-commit retry, because it required trusted direct authorization for public source publication. No alternate mutation route was attempted. Local closure, recovery material and a replacement PR summary are prepared; the push and PR summary update await explicit owner authorization. No main write, merge, deployment or merchant-state rewrite occurred. Existing deletion/security-triage owner queues carry forward; they do not reopen Batch 8 engineering.

## Batch 9 checkpoint A — header controls

Owner authorized Phase 4 Batch 9 at verified remote `4af9dcb56c7791e198533303bcd452a564cc3a21`; PR #22 remains draft. Batch 8 is closed and is not repeated. Account/actions/cart/logo are proven dormant legacy Header Two dependencies, retained as owner-review deletion candidates. Actual Header Two V2 owns different markup/selectors and does not call them. Four native strict parses and existing active global-surface/preset regressions pass. No runtime changes or deletions. Detailed consumer, configuration, accessibility and preactivation liabilities: `validation/batch9-a-header-controls/`. Global NOT REVIEWED: 25 → 21. Remaining engineering proceeds only after this checkpoint is remotely verified.
