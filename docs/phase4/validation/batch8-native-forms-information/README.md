# Batch 8 checkpoint — native forms, search, social links and payment information

75/102 blocks and 22 dependencies individually reviewed; 97/124 Family 8 entries have dispositions (96 live queued, 1 dormant retained), 27 NOT REVIEWED.

Six native form/information blocks now preserve safe values and actual Shopify sources, provide working existing predictive-search composition, maintain accessible links/payment names, and retain the historical custom location alias through one reviewed renderer. Native contact/customer POST and localized search GET remain progressive; no new form backend or payment integration is claimed.

Individual findings:

- `blocks/content-contact-form.liquid`: Individually reviewed all 49 settings and three presets. Retains native contact POST without AJAX interception, required name/email/message, optional required phone/subject and explicit unchecked consent. Unique native form ID prevents duplicate default form targets; returned form/customer values are escaped and real native default_errors are visible at the matching email description target. Cleared labels, submit/success/error/consent copy have meaningful fallbacks. Intro-free layouts use the full track; zero-minimum grids and container queries support narrow nested columns. Native wrapper ownership, independent tablet visibility, editor visibility/motion and bounded padding preserved. New defaults remove unsupported privacy/follow-up promises; saved values remain intact.
- `blocks/content-newsletter.liquid`: Individually reviewed all 38 settings and three presets. Keeps native customer newsletter POST, supplied tags, hidden marketing flag or explicit required unchecked consent. Unique native form ID, escaped returned email before customer fallback, visible native errors and meaningful blank-label/feedback fallbacks. Container-aware email/button and card layouts avoid overflow and missing-intro gaps. Editor/native wrapper/device/motion behavior repaired. New presets no longer imply an automatic discount, VIP enrollment benefit or email delivery integration; configured customer tags remain merchant data.
- `blocks/content-search-form.liquid`: Individually reviewed all 54 settings and three presets. Replaces disconnected predictive hooks and nonexistent global flag with the existing predictive controller, unique real listbox/status targets and complete global source/display/style contracts. Block result type intersects global sources; native localized GET, provider groups and view-all match; all full sources off prevents an accidental unfiltered query, with a collection-only progressive mode when permitted. Native unavailable/prefix controls propagate to full-page links, while only the supported availability parameter reaches the predictive API. Safe names/queries/URLs, complete explicit suggested terms and blank-list guards retained. Narrow container/grid/editor/motion/wrapper behavior fixed; new suggested searches are merchant configured.
- `blocks/content-social-links.liquid`: Individually reviewed all 43 settings and three presets. Each of seven real platform URL overrides falls back to its actual global setting; visibility toggles still gate the result. Captures actual links once and hides an empty storefront group with setup guidance in editor. Every destination is escaped; custom and navigation labels have accessible fallbacks, native new-tab noopener behavior and keyboard focus retained. Long labels and nested layouts wrap, independent device visibility/editor motion/native wrapper ownership repaired. No profiles or community claims are invented.
- `blocks/content-payment-icons.liquid`: Individually reviewed all 51 settings and three presets. Native shop.enabled_payment_types is the source of truth; all 13 original toggles filter corresponding native types and known native aliases. Native payment_type_svg_tag replaces fake text brand marks, with persistent readable labels whether visible or visually hidden. Two explicit merchant custom-method slots remain manual, escaped and labeled, without being treated as native availability. Empty groups hide on storefront and guide editor; bounded auto-fit/icon tracks and wrapped custom labels support narrow builders. Existing icon text/accent controls operate on custom marks. New copy avoids unsupported availability/trust claims; customer/device/country eligibility remains native checkout/live validation.
- `blocks/custom-custom-form.liquid`: Individually reviewed all 58 settings and three presets. Baseline bytes prove this file was an exact copy of content-location-info, including its schema; it has never supplied a form transport. Preserves all saved IDs, types, options and its location role, while clarifying the editor name as Legacy location info. Delegates to the already hardened location renderer with all actual image/alt, escaped contact/address, valid action, empty/editor/device/motion and narrow-layout behavior. New insertion defaults mirror the neutral reviewed location presets; no custom form transport is invented.
- `snippets/qtm-block-location-info.liquid`: Individually reviewed both location-block and legacy-alias callers. Extracts the complete already hardened location markup and scoped styles without behavioral changes; all 58 setting consumers, real image fallback, safe contact links/text, meaningful actions, independent device/editor/reduced-motion rules and native default wrapper ownership are shared. Actual caller DOM/style equivalence is checked.

471 actual render cases exercise all original IDs/options/range endpoints and 18 presets, blank/returned/malicious field values, native success/error/required/consent behavior, local/global social sources, 15 native payment aliases and unavailable filtering, manual custom labels, exact location-alias DOM/style parity, typed/localized search/filter/source modes and duplicate raw IDs. Actual predictive controller fixtures cover two hosts, IME/keyboard, safe provider results, stale responses/aborts, duplicate scripts and insert/reorder/remove/unload/reload. All 16 affected suites pass. This group covers 293 settings and 18 actual presets. Strict parsing, schema/reference/architecture/source hashes and CSS build pass. All 150 sections, 102 blocks and settings_data are preserved; compiled CSS matches Batch 7. Exact unsuppressed Theme Check movement is in themecheck-comparison.json.

B8-NATIVE-FORMS live queue after engineering batches: real contact/customer POST, captcha/server errors/return values and newsletter consent/tag handling; native search source/display/filter behavior and keyboard/AT; native payment SVGs and actual device/location/checkout eligibility; real narrow-container form/card layouts and Theme Editor lifecycle. Legacy custom-custom-form remains location information. No live Shopify QA; prior queues carry forward.

Native contracts checked against Shopify [form tag](https://shopify.dev/docs/api/liquid/tags/form), [Predictive Search API](https://shopify.dev/docs/api/ajax/reference/predictive-search) and [payment_type_svg_tag](https://shopify.dev/docs/api/liquid/filters/payment_type_svg_tag). The predictive API does not expose a prefix parameter; the setting remains a full-results control.

PR #22 stays DRAFT. No merge/main write, Batch 9, Atelier/Signal/Terrace rollout or live Shopify QA. Continue only Batch 8 to formal engineering closure.

Native payment SVG IDs, aria-labelledby, gradient URLs and internal href references are namespaced by raw block ID plus payment type. Documentation-faithful title/gradient/reference fixtures verify repeated payment surfaces retain unique IDs and connected native references.
