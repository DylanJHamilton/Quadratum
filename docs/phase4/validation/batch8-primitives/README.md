# Batch 8 checkpoint — primitive blocks and shared dependencies

Phase 4 remains in progress; PR #22 stays DRAFT. This is not Batch 8 closure.

12 individually reviewed Theme Blocks: Heading, Rich Text, Button, Button Group, Badge, Divider, Spacer, Icon, Icon List, Quote, Alert and Code. Their full schemas total 472 settings; all 36 built-in presets and schema defaults are inspected and rendered. Four shared dependencies are independently reconciled. **15 entries require live verification; one retained comment-only legacy wrapper-end is dormant/source-reviewed. Family 8 has 90 entries still NOT REVIEWED.**

`settings-and-presets.json` preserves each control/default/option, direct/shared/indexed source consumer, preset settings and per-block findings. The register gives individual dispositions. The complete wrapper replaces invalid split opening/closing fragments; 12 block consumers pass captured content to the retained start path. No files/features were deleted. Custom width had no actual control in any primitive; the missing companion range is now provided. Native Theme Block outer wrappers retain their editor attributes; no duplicate inner metadata. Layout already loads the stylesheet and engine, so per-block script tags and JS-injected presentation are removed. Shared styles cover these 12 consumers only; no other block is approved by association.

13 affected suites pass: seven Phase 3, two new Batch 8 suites and four existing Phase 4 host/dependency suites. 899 default/preset/option/range/checkbox render cases plus targeted text/link/media/empty-state/CSS/lifecycle assertions pass. All 14 changed Liquid files pass Shopify native strict parsing. Register/hash/schema/reference/architecture checks, CSS build and protected merchant/section state pass. `assets/theme.css` remains identical to the Batch 7 baseline. No protected host source changed.

Theme Check 4.8.0 remains unsuppressed and exits 1: **499 → 500 total, 5 → 3 errors, 494 → 497 warnings**. The two wrapper parsing defects are fixed. The newly parsed helpers now receive two orphan warnings: start has 12 explicit consumers (retained graph diagnostic), end is honestly dormant. The repaired Quote Custom width control increases its count to 41 and adds a settings-count warning. Existing three non-Batch-8 errors remain untouched. There are zero errors in this checkpoint group.

B8-PRIMITIVES live queue: actual block/preset insertion and native wrapper/editor lifecycle; responsive geometry and saved controls at 320/390/768/1280/1440px, long text, RTL/zoom/no-JS/contrast/keyboard/AT/reduced motion. Real image/custom-Liquid evaluation, merchant URLs and provider code remain live responsibilities. Static/DOM/media/native-wrapper/observer adapters are not Shopify or browser certification. Native `liquid` input remains the single platform evaluator; Code mode now honestly supplies guidance/embed sizing, without simulated execution modes. No provider is faked.

Shopify references checked: [Theme Block schema and native wrapper](https://shopify.dev/docs/storefronts/themes/architecture/blocks/theme-blocks/schema#tag), [input settings](https://shopify.dev/docs/storefronts/themes/architecture/settings/input-settings#liquid). No Atelier/Signal/Terrace rollout, Batch 9, roadmap Phase 5 or live acceptance started.
