{% if product != blank %}
  <div data-wishlist-title="{{ product.title | escape }}">
    {% render 'product-card', product: product, enable_quick_add: false, enable_quick_view: false %}
  </div>
{% endif %}
{% schema %}
{ "name": "Wishlist card renderer", "settings": [] }
{% endschema %}
