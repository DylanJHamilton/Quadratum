# Batch 8 checkpoint — comparison images, feature media, hotspots, image stacks, maps and lookbooks

81/102 blocks and 23 dependencies individually reviewed; 104/125 Family 8 entries have dispositions (103 live queued, 1 dormant retained), 21 NOT REVIEWED.

Six visual media blocks now preserve progressive native content, repair full-size comparison alignment and actual enabled feature selection, replace fragile hotspot positioning with bounded pins and in-flow callouts, and keep sparse/long-content compositions useful. One scoped controller supplies interactive behavior and media policy/lifecycle handling through the existing deferred asset chain.

Individual findings:

- `blocks/content-before-after.liquid`: Individually reviewed all 33 original settings and three presets. Comparison enhancement now clips a full-size after image instead of resizing it into a shrinking crop, preserving alignment with the before image. Native range and decorative handle are hidden until the shared controller mounts; no-JS shows both full images and associated labels. Single native images survive without storefront setup placeholders, image alt falls back to native metadata, and labels/links are safely handled. Actual complete CTA/copy/media guards remove empty grid columns; partial images take the full comparison width. Unknown motion cannot leave opacity zero; independent device/editor ownership and bounded nested sizing preserved.
- `blocks/content-feature-showcase.liquid`: Individually reviewed all 78 original settings and three presets. Only enabled populated features render; absent icons or media/copy no longer reserve empty columns, and cleared new feature claims remain editor setup data. Native image/video/external-link fallback is preserved. Native videos start paused with controls available without JavaScript; the scoped controller honors requested controls/autoplay while supplying a usable play/pause action, respecting reduced motion/editor/viewport/page visibility and handling rejected or detached playback. No external embed transport is invented. Progressive reveal has visible base styling, safe links and focus; sticky/stacked content yields in narrow builder containers.
- `blocks/content-image-hotspots.liquid`: Individually reviewed all 89 original settings plus one translated price-range prefix and three presets. Native image/details fallback retained; enabled points require actual content/destination, numbered labels and URLs are escaped, and explicit custom destination then product then collection precedence is preserved. Product preview remains the actual native product; missing preview images collapse the grid and variable prices are labeled From. Container-relative pin coordinates stay tied to the image while callouts flow below it, eliminating transformed fixed-position tooltips and edge overflow; without container units native disclosures remain usable. Pulse is bounded to two cycles with editor/reduced-motion bypass. Shared controller limits close/Escape/focus/listeners to actual owners.
- `blocks/content-image-stack.liquid`: Individually reviewed all 47 original settings and three presets. Only enabled native images or explicit captions render; fake storefront placeholder imagery is removed. Sparse selections use in-flow cards instead of reserving missing absolute slots; absent intro/media uses a single column, and narrow builder containers provide a readable stacked composition. Actual card-background control now colors media, and tablet visual height propagates to dependent image minimum heights. Native alt/responsive image candidates, safe captions/CTA, shadow/color controls and saved four-image overlap layout preserved. New defaults omit invented material/quality claims; reduced-motion disables hover zoom.
- `blocks/content-interactive-feature-map.liquid`: Individually reviewed all 85 original settings and three presets. Both pin and panel loops use identical enabled/populated rules; every actual panel is readable and dead pin buttons hidden without JavaScript. Enhancement selects the first actual enabled pin, so disabled slot one cannot hide all content. Keyboard arrows/Home/End respect RTL; mobile/narrow views expose all panels with truthful expanded state, and focused content is protected during a collapse. Native image and authored panel-only states stay useful; coordinates are bounded away from clipped edges, long labels escaped, zero-minimum tracks and independent device/editor rules repaired. Pulses finish after two cycles and scoped resize/observer state is disposed on removal.
- `blocks/content-lookbook.liquid`: Individually reviewed all 61 original settings plus explicit caption color and translated price-range prefix controls, and three presets. Enabled items render only actual media/copy/actions or an allowed native product card; product-only content does not reserve a fake hero image, and disabling product cards removes otherwise empty slots. Native product URLs/titles/images/prices and explicit caption links remain distinct and safely rendered; variable native prices are labeled From and missing card imagery collapses its track. Image/overlay/caption share an in-flow grid cell, so long captions grow the media area instead of being clipped. Sparse grids avoid missing named areas and nested/mobile minimum heights actually win. All saved layout choices and merchant content preserved; new editorial claims removed.
- `assets/qtm-block-visual-media.js`: Individually reviewed all four actual consumers and lifecycle paths. One deferred singleton registry enhances native comparison ranges, native hotspot disclosures, feature-map controls and native video; removed/reloaded roots lose handlers/observers/state and added/reordered roots initialize once. Range state is isolated and restored; hotspot Escape/outside close restores focus only for its actual disclosure. Feature maps pair real IDs/panels and use resize observation plus keyboard handling; no-JS state is restored on disposal. Video autoplay is attempted once when eligible/visible, native and custom manual playback remain available, policy/page/removal pauses media, and stale/rejected play promises do not revive disposed media. No commerce mutation or external media provider added.

468 actual control/default/preset/data-state renders exercise all original IDs/options/range endpoints, empty/populated/partially selected images and points, native alt overrides, native product/custom/collection precedence and zero/variable prices, safe plain text and URLs, no-JS readable comparison/panels/disclosures/video, meaningful copied-only and card-only layouts and duplicate raw IDs. Actual controller fixtures cover two instances, range updates, scoped Escape/outside close, keyboard/Home/End, active enabled-slot selection, mobile expanded state, reduced-motion/editor video gating, rejected/detached playback promises, and insert/reorder/remove/unload/reload. All 16 affected suites pass. This group covers 396 settings and 18 actual presets. Strict parsing, schema/reference/architecture/source hashes and CSS build pass. All 150 sections, 102 blocks and settings_data are preserved; compiled CSS matches Batch 7. Exact unsuppressed Theme Check movement is in themecheck-comparison.json.

B8-VISUAL-MEDIA live queue after engineering batches: native responsive image geometry and exact crop/point alignment, real 320–1440px/zoom/RTL/nested builder layouts, image-stack overlap/caption readability and lookbook overlay contrast, real range/disclosure/feature-map keyboard/AT, native video controls/autoplay/rejection/visibility/reduced-motion behavior, actual product prices/media and Theme Editor lifecycle. DOM/media/geometry adapters are not acceptance. No live Shopify QA; prior queues carry forward.



PR #22 stays DRAFT. No merge/main write, Batch 9, Atelier/Signal/Terrace rollout or live Shopify QA. Continue only Batch 8 to formal engineering closure.
