# Batch 8 checkpoint — recently-viewed history and its legacy alias

91/102 blocks and 27 dependencies individually reviewed; 118/129 Family 8 entries have dispositions (117 live queued, 1 dormant retained), 11 NOT REVIEWED.

Recently Viewed and its historical Collection Grid alias now share a native-card history renderer and scoped controller. Only validated handles persist; current native product data is rendered through the owning section, replacing stale and unsafe stored card snapshots.

Individual findings:

- `blocks/content-recently-viewed-products.liquid`: Individually reviewed all 57 original controls and three presets; added one translated price-range prefix. Reuses a traced shared renderer and native product-card helper. Legacy product snapshots migrate to validated native handles; stale stored prices, URLs, images and titles are never rendered. Current native section cards retain presentment money/ranges, images/alt and truthful discounts; native responses must match owner root, section, handle and product. Preserve configured key/count/current exclusion/grid/cards/carousel and empty-state controls, including no-JS behavior. Header visibility no longer removes carousel controls; shared engine supplies actual-spacing RTL scroll. Removed setup copy in new insertions; native wrapper, safe labels, bounded nested layouts, editor guidance and no-JS state repaired.
- `blocks/content-collection-grid.liquid`: Individually compared and reviewed every original setting/default/preset: this historical file is byte-identical to Recently Viewed in the protected baseline and actually implements recently-viewed history. Preserved that behavior and all 57 original IDs/types/options rather than inventing collection-grid semantics. Shared renderer/controller applies the same native-history, price, scope, accessibility, responsive and lifecycle repairs; added the translated price prefix. Editor name/preset labels now identify the legacy alias. Both files have separate complete control/default/preset fixture and consumer records.
- `snippets/qtm-block-recently-viewed.liquid`: Actual two alias callers and all 58 settings traced, including nested native card helper consumers. Produces one raw-ID owner with locale root/section/native product context, inert server-native card template, guarded native header/empty/editor content and progressively enhanced scroll controls. Reuses existing native card styling/control contracts; no duplicate snapshot renderer or product-price formatter. Bounds widths/padding/nested columns, preserves native alt and meaningful links, bypasses editor hiding/entry motion. No storefront sample product/recommendation or stale-price fallback.
- `assets/qtm-block-recently-viewed.js`: Singleton actual Theme Block lifecycle controller. Validated Unicode native handles only; migrates legacy object snapshots, deduplicates/bounds history to 24 and fetches up to configured 2–12 cards with concurrency three. Real product-page context records once coherently across instances; editor reads/writes/fetches no shopper history. Local/session/encoded legacy-cookie/page-memory fallbacks avoid storage exceptions and preserve custom keys. Locale/same-origin section requests accept only exact owner root/section/handle/product templates; failures stay empty. Aborts on unload/removal, ignores detached responses, defers replacement while old cards hold focus and updates existing carousel controls after insertion. Actual alias, multi-key, limit, response rejection, RTL geometry and mutation/reorder/load/unload tests pass.

174 actual renders cover both aliases' original controls, select/checkbox/range endpoints, six actual presets, no-JS/editor/empty states, native card labels/alt/prices and section/root context. Actual controller tests cover legacy migration, malicious snapshot fields, locale URLs, same-origin and exact response identity, count/history limits, current exclusion/inclusion, missing/foreign/failed responses, unavailable storage, editor isolation, abort/removal, dynamic insertion/reload/reorder and independent custom-key instances. Actual native-card geometry validates configured-gap LTR/RTL controls and nested-link keyboard ownership. All 11 affected suites pass. This group covers 116 settings and 6 actual presets. Strict parsing, schema/reference/architecture/source hashes and CSS build pass. All 150 sections, 102 blocks and settings_data are preserved; compiled CSS matches Batch 7. Exact unsuppressed Theme Check movement is in themecheck-comparison.json.

B8-RECENTLY-VIEWED live queue after engineering batches: actual Shopify section rendering for each owning builder/template and product context (missing/mismatched owner responses intentionally stay empty), current localized prices/images/product availability, real browser history/storage restrictions, 320–1440px/RTL/zoom/nested layouts, actual scroll/focus/keyboard/AT and Theme Editor lifecycle. Native-filter/response/DOM/geometry adapters are not acceptance. No live Shopify QA; prior queues carry forward.

Primary contract: [Shopify Section Rendering API](https://shopify.dev/docs/api/ajax/section-rendering), checked 2026-09-22: locale-aware section_id HTML in requested product context, existing section settings, and missing-section errors. Only exact owning block/section responses are accepted.

PR #22 stays DRAFT. No merge/main write, Batch 9, Atelier/Signal/Terrace rollout or live Shopify QA. Continue only Batch 8 to formal engineering closure.
