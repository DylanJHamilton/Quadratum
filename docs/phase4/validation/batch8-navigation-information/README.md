# Batch 8 checkpoint — navigation and store information

63/102 blocks and 20 dependencies individually reviewed; 83/122 Family 8 entries have dispositions (82 live queued, 1 dormant retained), 39 NOT REVIEWED.

Eight navigation/contact/store-information blocks now preserve native menu destinations, exact fragments, readable named links, manual schedules and safe merchant content. Mobile visibility, optional layout tracks, sticky ownership and editor lifecycle are repaired. New insertion defaults avoid fictional contact details, opening hours and page anchors; saved merchant settings remain unchanged.

Individual findings:

- `blocks/content-anchor-navigation.liquid`: Individually reviewed all 79 settings and three presets. Native anchor IDs retain internal punctuation and are encoded once, numbering/first-active follow actual rendered links, scoped deferred enhancement replaces inline listeners. Smooth navigation respects modifier clicks, missing targets, reduced motion, focus cleanup and fragment history/other panel consumers. tag:null places sticky behavior and editor attributes on the actual one root; desktop/tablet/mobile visibility and configured offsets are independent. New inserts require real target IDs rather than fictional anchors; existing settings preserved.
- `blocks/content-table-of-contents.liquid`: Individually reviewed all 77 settings and three presets. Native encoded links retain exact target IDs, sequential visible numbering and named navigation. Conditional number/arrow tracks avoid orphan columns, long content shrinks/wraps, native single-root ownership makes desktop sticky relative to its real builder container. New insertion target IDs are empty with editor-only setup guidance; saved IDs/layouts retained. No JS dependency added.
- `blocks/content-breadcrumbs.liquid`: Individually reviewed all 33 settings and three presets. Derives the current title from the actual native page type with an optional native collection/blog parent; separators only separate real entries, no empty labels or fabricated parents. Home is a named current page on index and native locale-aware link elsewhere. Cleared navigation/home/cart/search/account wording has meaningful fallback. Escaped native URLs and long labels remain readable; previously unconsumed heading-color setting now explicitly controls focus outline with clarified label.
- `blocks/content-link-list.liquid`: Individually reviewed all 94 settings and three presets. One captured copy serves linked and deliberately unlinked manual items instead of duplicate markup; native URL escaping/new-tab rel retained. Conditional icon/arrow grid tracks, pills/buttons wrapping, bounded widths and configured one-column tablet behavior repaired. New enabled links/claims are unset and neutral rather than fake best-seller destinations; empty editor guidance and store visibility repaired. All eight item slots and prior IDs preserved.
- `blocks/content-menu-list.liquid`: Individually reviewed all 34 settings and three presets. Restores the native parent destination inside each nested disclosure, preserves all three native menu levels and active styling, uses exact native link.current for aria-current rather than ancestor URL matching. Native details work without JS; disabling mobile accordion now expands the links on mobile and restores the previous desktop disclosure state with focus/listener cleanup. Blank menu guidance is editor-only, and merchant controls for defaults/grandchildren/columns remain operative. Preserves the actual native data-sharing opt-out icon when Shopify supplies it.
- `blocks/content-contact-info.liquid`: Individually reviewed all 42 settings and three presets. Escapes phone/mail/native URLs and plain multiline addresses/hours, preserves telephone plus prefix and line breaks. Optional icons no longer leave misplaced grid columns. New presets omit fictional phone/email/address/hours/response-time promises. Empty editor guidance and authored support/CTA-only fallback remain meaningful; explicit text, links and existing saved merchant data are preserved. Removed unused phone-clean assign.
- `blocks/content-location-info.liquid`: Individually reviewed all 58 settings and three presets. Escapes native contact/directions/action URLs and plain multiline address/pickup text; image alt falls back to actual native image alt. Independent button guards fix right-to-left Liquid boolean evaluation hiding a primary-only action. Split layout handles missing image without empty column; nested tracks shrink and detail columns retain a configured one-column choice on tablet. New inserts omit fictional contact/pickup/showroom facts; manual authored note/action-only content remains available.
- `blocks/content-store-hours.liquid`: Individually reviewed all 67 settings and three presets. Renders only named days with explicit hours or a selected closed state; retains manual highlights and truthful closed labels, native table caption/row headings or list semantics. Status badge is explicitly manual rather than a clock calculation. New presets no longer assert unconfigured hours/open/support status. Bounded fixed table/wrapping and configured tablet columns preserve narrow builder behavior; badge color now also supplies configurable button text color.
- `assets/qtm-block-navigation.js`: Individually reviewed two fixed block callers and one deferred layout inclusion. Singleton Map/AbortController initialization handles insertion/reorder/removal/editor load/unload. Native links/details remain the fallback; anchor enhancement preserves modified clicks and missing targets, native hash/history and other fragment consumers, reduced-motion scroll, target keyboard focus and temporary tabindex cleanup. Current-location state is scoped per root. Mobile disclosure enhancement remembers/restores native open state and removes listeners on disposal; no timer/network or cart behavior.

629 actual default/control/preset/page/empty/contact renders plus runtime cases cover exact native menu parent/current semantics, escaped links/plain multiline contact data, action-only content, real manual schedule rows, punctuation-safe and contiguous anchors, original setting IDs/options/range endpoints, two-instance focus/history/reduced-motion navigation and existing tab-panel composition, missing-target/modifier fallbacks, mobile disclosure restoration and insertion/reorder/removal/editor cleanup. All 17 affected suites pass. This group covers 484 settings and 24 actual presets. Strict parsing, schema/reference/architecture/source hashes and CSS build pass. All 150 sections, 102 blocks and settings_data are preserved; compiled CSS matches Batch 7. Exact unsuppressed Theme Check movement is in themecheck-comparison.json.

B8-NAVIGATION-INFORMATION live queue after engineering batches: real native menu/current-route and localized breadcrumbs, browser fragment/history/focus/keyboard/AT behavior, sticky behavior within actual builder containers, mobile disclosures, narrow/RTL/zoom/color contrast and actual Theme Editor selection/reload. Merchant contact/schedule/anchor destinations remain manual configuration; no open-now clock or fabricated default details are implied. No live Shopify QA performed; previous queues carry forward.

References: native [Shopify link object](https://shopify.dev/docs/api/liquid/objects/link) and [Theme Block wrapper schema](https://shopify.dev/docs/storefronts/themes/architecture/blocks/theme-blocks/schema#tag).

PR #22 stays DRAFT. No merge/main write, Batch 9, Atelier/Signal/Terrace rollout or live Shopify QA. Continue only Batch 8 to formal engineering closure.
