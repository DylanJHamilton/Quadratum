PASS canonical/legacy/local CAPTCHA precedence across nine hosts; unsafe social links and endpoint credentials rejected; CSS delimiter containment; all three trusted-code gates and sentinel values. Provider services and consent remain external contracts.
