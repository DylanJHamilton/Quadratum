# Batch 8 checkpoint — product selection and purchase

Seven individually reviewed blocks: Product Form, Variant Picker, Add to Cart, Dynamic Checkout, Quantity Selector, Price and Sticky Add to Cart. All 21 actual presets and every control/default/option have traced consumers and render/state evidence. Three new helpers are inventoried in Family 8. **35/110 family entries now have dispositions: 34 live queued, one dormant retained; 75 remain NOT REVIEWED.** All 102 blocks remain; 27 have individual dispositions. Eight family dependencies now cover the four original paths, metadata lifecycle and three purchase helpers.

`settings-and-presets.json` preserves individual source/control/preset findings. `theme-block-commerce-render.cjs` performs 375 default/preset/select/checkbox/range renders plus no-product/native-form/label/ID/plan/rule/price/JSON cases. `theme-block-commerce.cjs` uses actual eight-block compositions in two same-product hosts and the existing cart controller. It covers valid/null/sold-out variants, every form payload, native/option/button selectors, allocations/unit prices, quantity min/max/step/default/conflicts, real cart success/error/duplicate guard/native fallback, independent messages, URLs/back, dynamic boot and editor unload/reload/reorder. Explicit selected variants outside the enumerated list remain selected in forms, prices, plans and SKU data instead of switching to the first record.

Add to Cart was another copied Quantity Selector; it now performs a real native form action while preserving all previous quantity setting IDs and styling. Inventory Status is separately identified as copied Trust Badges and remains NOT REVIEWED until its stock/trust group. No untouched block is approved by association. Variant Picker's fieldset/toggle semantics, native swatches, actual selected/status labels and combination navigation are reconciled; true native links/forms remain before enhancement. All purchase roots share scoped selection, quantity and plan state, and invalid selection clears IDs and disables payment instead of retaining a stale purchase. Presentment-money and unit strings are generated by Liquid; no guessed currency formatter is used. Saved merchant tax text remains, but unsubstantiated Including Tax insertion copy is removed. The sticky preset now says Add to cart for its actual action, and its native wrapper owns sticky positioning.

Native Shopify forms and payment_button remain intact. Current accelerated checkout custom properties consume wallet size/radius; documented 55px wallet maximum is respected while larger existing area settings add spacing. Actual closed-shadow wallet behavior remains a live gate. Required/optional plans reuse the reviewed Batch 4 snippet/controller. Their selected-variant data now includes an explicitly selected variant outside the enumerated list. The reviewed cart controller gains only optional per-form progress/success labels, tied to actual results; no second mutation controller or optimistic global success event was introduced. Native fallback and existing errors remain. SKU/metadata integration fixes the actual restoration event and preserves the selected out-of-list SKU.

All **21 affected suites pass**: all seven Phase 3 suites, six Batch 8 suites, all five main-product/compact host commerce coverage, cart commerce, product builder host and layout coverage. Native strict parsing covers all changed/new Liquid files, including both data helpers and the shared plan snippet. Schema/reference/architecture/source hashes and CSS build pass. All 150 sections and settings_data remain identical to the Batch 7 baseline; compiled theme.css remains identical. An initial harness invocation named nonexistent native-cart.cjs; the actual cart-commerce.cjs suite was then run. That invocation and reconciliation are retained, with no runtime change made to satisfy a missing test path. Earlier metadata tests now test the real restoration event; real form/picker integration moved from extracted inline functions into actual shared-controller compositions.

Unsuppressed Theme Check and exact movement are in `themecheck-comparison.json`. The new serializer paths receive orphan warnings despite seven direct block consumers and the transitive record-helper consumer; these are retained with dependency evidence. No new group errors or scanner suppression.

B8-PRODUCT-PURCHASE live queue: real native form/cart/accelerated checkout and eligible selling plans; market/currency/unit/tax data, true option/swatch drops, high-variant catalogs/native enumeration, selected out-of-list products; duplicate builder hosts and selected variants, quantities/stock/plans; sticky containing ancestors; all defaults/presets/control states at 320/390/768/1280/1440px, long labels/RTL/zoom/keyboard/AT/no-JS/motion and editor lifecycle. Offline adapters do not certify actual purchases or Shopify execution.

References: [variant contracts](https://shopify.dev/docs/api/liquid/objects/variant), [native option values](https://shopify.dev/docs/api/liquid/objects/product_option_value), [JSON filter](https://shopify.dev/docs/api/liquid/filters/json), [accelerated checkout and supported CSS properties](https://shopify.dev/docs/storefronts/themes/pricing-payments/accelerated-checkout). PR #22 stays DRAFT. No merge/main write, Batch 9, preset-family rollout or live QA.
