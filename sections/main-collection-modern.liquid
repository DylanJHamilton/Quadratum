{%- comment -%}
  Quadratum — Collection Modern (Max Capability)
  File: sections/collection-modern.liquid
{%- endcomment -%}

{{ 'section-collection-simple.css' | asset_url | stylesheet_tag }}
{{ 'section-collection-modern.css' | asset_url | stylesheet_tag }}

{%- liquid
  assign s = section.settings

  assign is_search_context = false
  if request.page_type == 'search' and search != blank
    assign is_search_context = true
  endif

  assign use_alt = false
  if s.use_alt_collection and s.alt_collection != blank
    assign use_alt = true
  endif

  assign results_title = ''
  assign results_url = ''
  assign results_description = ''

  assign products = nil
  assign filters = nil
  assign sort_options = nil
  assign sort_by = ''
  assign default_sort_by = ''

  assign terms_q = ''
  if is_search_context
    assign terms_q = search.terms | default: ''
  endif

  if use_alt
    assign c = s.alt_collection
    assign products = c.products
    assign filters = c.filters
    assign sort_options = c.sort_options
    assign sort_by = c.sort_by
    assign default_sort_by = c.default_sort_by
    assign results_title = c.title
    assign results_url = c.url
    assign results_description = c.description
  elsif is_search_context
    assign products = search.results
    assign filters = search.filters
    assign sort_options = search.sort_options
    assign sort_by = search.sort_by
    assign default_sort_by = search.default_sort_by
    assign results_title = 'Search results'
    assign results_url = routes.search_url
    assign results_description = ''
  else
    assign c = collection
    assign products = c.products
    assign filters = c.filters
    assign sort_options = c.sort_options
    assign sort_by = c.sort_by
    assign default_sort_by = c.default_sort_by
    assign results_title = c.title
    assign results_url = c.url
    assign results_description = c.description
  endif

  assign layout = s.layout_preset | default: 'modern_grid'
  assign filter_mode = s.filter_presentation | default: 'sidebar'
  assign pagination_style = s.pagination_style | default: 'numbered'
  assign per_page = s.products_per_page | default: 24

  assign scope_id = section.id
  assign root_id = 'QtmCollectionModern-' | append: scope_id

  assign grid_cols_desktop = s.columns_desktop | default: 4
  assign grid_cols_tablet  = s.columns_tablet  | default: 3
  assign grid_cols_mobile  = s.columns_mobile  | default: 2

  assign has_filters = false
  if s.show_filters and filters != blank and filters.size > 0
    assign has_filters = true
  endif

  assign sticky_toolbar = false
  if s.sticky_toolbar
    assign sticky_toolbar = true
  endif

  assign enable_drawer = false
  if s.show_filters and filter_mode == 'drawer'
    assign enable_drawer = true
  endif

  assign enable_drawer_str = 'false'
  if enable_drawer
    assign enable_drawer_str = 'true'
  endif

  assign effective_mode = filter_mode
  if has_filters == false
    assign effective_mode = 'none'
  endif

  assign card_engine = s.card_engine | default: 'default_theme_card'
-%}

<section
  id="{{ root_id }}"
  class="q-collection-modern q-collection-simple qcm layout-{{ layout }} cardstyle-{{ s.card_style }} density-{{ s.card_density }} hover-{{ s.hover_effects }}"
  data-section-id="{{ scope_id }}"
  data-layout="{{ layout }}"
  data-pagination-style="{{ pagination_style }}"
  data-filter-mode="{{ filter_mode }}"
  data-enable-drawer="{{ enable_drawer_str }}"
  data-sticky-toolbar="{{ sticky_toolbar }}"
  data-sticky-offset="{{ s.sticky_toolbar_offset | default: 0 }}"
>
  <style>
    #{{ root_id }}{
      --qcm-maxw: {{ s.max_width }};
      --qcm-gap: {{ s.gap }}px;
      --qcm-radius: {{ s.border_radius }}px;

      --qcm-cols-d: {{ grid_cols_desktop }};
      --qcm-cols-t: {{ grid_cols_tablet }};
      --qcm-cols-m: {{ grid_cols_mobile }};

      --qcm-truncate-lines: {{ s.truncated_lines }};

      --qcm-sticky-offset: {{ s.sticky_toolbar_offset | default: 0 }}px;

      /* Modern card typography + color controls */
      --qcm-vendor-color: {{ s.vendor_color }};
      --qcm-title-color: {{ s.title_color }};
      --qcm-price-color: {{ s.price_color }};

      --qcm-vendor-size-set: {{ s.vendor_font_size }}px;
      --qcm-title-size-set: {{ s.title_font_size }}px;
      --qcm-price-size-set: {{ s.price_font_size }}px;

      /* Card styling controls */
      --qcm-card-bg: {{ s.card_bg }};
      --qcm-card-border: {{ s.card_border }};
      --qcm-card-border-style: {{ s.card_border_style }};

      /* Button styling controls */
      --qcm-btn-bg: {{ s.button_bg }};
      --qcm-btn-text: {{ s.button_text }};
      --qcm-btn-bg-hover: {{ s.button_bg_hover }};
      --qcm-btn-text-hover: {{ s.button_text_hover }};
      --qcm-btn-border: {{ s.button_border }};

      /* Ratings colors */
      --qcm-rating-star-color: {{ s.rating_star_color }};
      --qcm-rating-text-color: {{ s.rating_text_color }};
    }
  </style>

  <div class="qcm__container {% if s.max_width_toggle == 'contained' %}qcm__container--contained{% endif %}">
    {% render 'collection-modern-toolbar',
      section: section,
      s: s,
      is_search_context: is_search_context,
      results_title: results_title,
      results_url: results_url,
      results_description: results_description,
      terms_q: terms_q,
      has_filters: has_filters,
      filters: filters,
      sort_options: sort_options,
      sort_by: sort_by,
      default_sort_by: default_sort_by,
      sticky_toolbar: sticky_toolbar,
      filter_mode: filter_mode
    %}

    <div class="qcm__body qcm__body--mode-{{ effective_mode }}">
      {%- if has_filters and filter_mode == 'sidebar' -%}
        <aside class="qcm__sidebar" aria-label="Filters">
          {% render 'collection-modern-facets',
            section: section,
            s: s,
            is_search_context: is_search_context,
            results_url: results_url,
            terms_q: terms_q,
            filters: filters,
            sort_options: sort_options,
            sort_by: sort_by,
            default_sort_by: default_sort_by,
            placement: 'sidebar'
          %}
        </aside>
      {%- endif -%}

      <div class="qcm__main">
        {%- if has_filters and filter_mode == 'drawer' -%}
          {% render 'collection-modern-facets',
            section: section,
            s: s,
            is_search_context: is_search_context,
            results_url: results_url,
            terms_q: terms_q,
            filters: filters,
            sort_options: sort_options,
            sort_by: sort_by,
            default_sort_by: default_sort_by,
            placement: 'drawer'
          %}

          <noscript>
            <details class="qcm__drawer-noscript">
              <summary class="qcm__btn">Filters</summary>
              {% render 'collection-modern-facets',
                section: section,
                s: s,
                is_search_context: is_search_context,
                results_url: results_url,
                terms_q: terms_q,
                filters: filters,
                sort_options: sort_options,
                sort_by: sort_by,
                default_sort_by: default_sort_by,
                placement: 'drawer_noscript'
              %}
            </details>
          </noscript>
        {%- endif -%}

        {%- paginate products by per_page -%}
          {%- assign page_has_products = false -%}
          {%- if products != blank and products.size > 0 -%}
            {%- assign page_has_products = true -%}
          {%- endif -%}

          {%- if page_has_products -%}
            <div class="qcm__gridWrap">
              <div
                class="qcm__grid"
                data-qcm-grid
                aria-live="{% if pagination_style == 'infinite_scroll' %}polite{% else %}off{% endif %}"
              >
                {%- for item in products -%}
                  {%- liquid
                    assign p = item
                    if is_search_context
                      if item.object_type != 'product'
                        continue
                      endif
                      assign p = item
                    endif
                  -%}

                  <div class="qcm__item" data-qcm-item>
                    {%- if card_engine == 'modern_card' -%}
                      {% render 'collection-modern-product-card', product: p, s: s, section: section %}
                    {%- else -%}
                      {% render 'product-card', product: p %}
                    {%- endif -%}
                  </div>
                {%- endfor -%}
              </div>
            </div>

            {%- if s.show_pagination -%}
              {% render 'collection-modern-pagination',
                section: section,
                s: s,
                paginate: paginate,
                pagination_style: pagination_style
              %}
            {%- endif -%}
          {%- else -%}
            {% render 'collection-modern-empty-state',
              section: section,
              s: s,
              results_url: results_url,
              is_search_context: is_search_context,
              terms_q: terms_q
            %}
          {%- endif -%}
        {%- endpaginate -%}
      </div>
    </div>
  </div>

  <script src="{{ 'section-collection-modern.js' | asset_url }}" defer></script>
</section>

{% schema %}
{
  "name": "Main Collection Modern",
  "tag": "section",
  "class": "section",
  "settings": [
    { "type": "header", "content": "Content" },
    { "type": "checkbox", "id": "show_title", "label": "Show title", "default": true },
    { "type": "checkbox", "id": "show_description", "label": "Show description", "default": false },
    {
      "type": "select",
      "id": "description_style",
      "label": "Description style",
      "default": "truncated",
      "options": [
        { "value": "full", "label": "Full" },
        { "value": "truncated", "label": "Truncated" }
      ]
    },
    { "type": "range", "id": "truncated_lines", "label": "Truncated lines", "min": 2, "max": 8, "step": 1, "default": 4 },

    { "type": "header", "content": "Data" },
    { "type": "checkbox", "id": "use_alt_collection", "label": "Use alternate collection", "default": false },
    { "type": "collection", "id": "alt_collection", "label": "Alternate collection" },

    { "type": "header", "content": "Toolbar" },
    { "type": "checkbox", "id": "sticky_toolbar", "label": "Sticky toolbar", "default": false },
    { "type": "range", "id": "sticky_toolbar_offset", "label": "Sticky toolbar offset (px)", "min": 0, "max": 160, "step": 2, "default": 0 },

    { "type": "header", "content": "Layout" },
    {
      "type": "select",
      "id": "layout_preset",
      "label": "Layout preset",
      "default": "modern_grid",
      "options": [
        { "value": "modern_grid", "label": "Modern grid" },
        { "value": "editorial_grid", "label": "Editorial grid" },
        { "value": "split_feature", "label": "Split feature" },
        { "value": "minimal_grid", "label": "Minimal grid" },
        { "value": "cardless_list", "label": "Cardless list" },
        { "value": "masonry_lite", "label": "Masonry lite (CSS columns)" }
      ]
    },
    { "type": "range", "id": "columns_desktop", "label": "Columns (desktop)", "min": 2, "max": 6, "step": 1, "default": 4 },
    { "type": "range", "id": "columns_tablet", "label": "Columns (tablet)", "min": 1, "max": 4, "step": 1, "default": 3 },
    { "type": "range", "id": "columns_mobile", "label": "Columns (mobile)", "min": 1, "max": 3, "step": 1, "default": 2 },

    {
      "type": "select",
      "id": "image_ratio",
      "label": "Image ratio (modern card)",
      "default": "adapt",
      "options": [
        { "value": "adapt", "label": "Adapt" },
        { "value": "square", "label": "Square" },
        { "value": "portrait", "label": "Portrait" },
        { "value": "landscape", "label": "Landscape" }
      ]
    },
    {
      "type": "select",
      "id": "card_engine",
      "label": "Card engine",
      "default": "default_theme_card",
      "options": [
        { "value": "default_theme_card", "label": "Default theme card" },
        { "value": "modern_card", "label": "Modern (collection-modern-product-card)" }
      ]
    },

    {
      "type": "select",
      "id": "card_density",
      "label": "Card density",
      "default": "normal",
      "options": [
        { "value": "roomy", "label": "Roomy" },
        { "value": "normal", "label": "Normal" },
        { "value": "compact", "label": "Compact" }
      ]
    },
    {
      "type": "select",
      "id": "card_style",
      "label": "Card style",
      "default": "glass",
      "options": [
        { "value": "glass", "label": "Glass" },
        { "value": "solid", "label": "Solid" },
        { "value": "outline", "label": "Outline" },
        { "value": "none", "label": "None" }
      ]
    },
    {
      "type": "select",
      "id": "hover_effects",
      "label": "Hover effects",
      "default": "lift",
      "options": [
        { "value": "none", "label": "None" },
        { "value": "lift", "label": "Lift" },
        { "value": "glow", "label": "Glow" },
        { "value": "tilt_lite", "label": "Tilt-lite" }
      ]
    },

    { "type": "header", "content": "Discovery" },
    { "type": "checkbox", "id": "show_filters", "label": "Show filters", "default": true },
    {
      "type": "select",
      "id": "filter_presentation",
      "label": "Filter presentation",
      "default": "sidebar",
      "options": [
        { "value": "sidebar", "label": "Sidebar" },
        { "value": "drawer", "label": "Drawer" },
        { "value": "bar", "label": "Bar (toolbar)" }
      ]
    },
    { "type": "checkbox", "id": "sticky_filter_bar", "label": "Sticky filter bar (bar only)", "default": false },

    { "type": "checkbox", "id": "show_sort", "label": "Show sort", "default": true },
    {
      "type": "select",
      "id": "sort_style",
      "label": "Sort style",
      "default": "dropdown",
      "options": [
        { "value": "dropdown", "label": "Dropdown" },
        { "value": "pills", "label": "Pills" }
      ]
    },
    { "type": "checkbox", "id": "show_result_count", "label": "Show result count", "default": true },
    { "type": "checkbox", "id": "show_active_filters", "label": "Show active filters chips", "default": true },
    { "type": "checkbox", "id": "show_clear_all", "label": "Show clear all", "default": true },

    { "type": "header", "content": "Product card toggles (modern card)" },
    { "type": "checkbox", "id": "show_vendor", "label": "Show vendor", "default": false },
    { "type": "checkbox", "id": "show_price", "label": "Show price", "default": true },
    { "type": "checkbox", "id": "show_badges", "label": "Show badges (sale/sold out)", "default": true },
    { "type": "checkbox", "id": "show_rating", "label": "Show rating", "default": false },
    { "type": "checkbox", "id": "show_quick_add", "label": "Show quick add", "default": false },
    { "type": "checkbox", "id": "show_second_image", "label": "Second image on hover", "default": true },

    { "type": "header", "content": "Ratings" },
    {
      "type": "select",
      "id": "rating_source",
      "label": "Rating source",
      "default": "auto",
      "options": [
        { "value": "auto", "label": "Auto (Shopify native → metafields)" },
        { "value": "shopify", "label": "Shopify native" },
        { "value": "metafields", "label": "Metafields" }
      ]
    },
    { "type": "text", "id": "rating_metafield", "label": "Rating metafield (namespace.key)", "default": "reviews.rating" },
    { "type": "text", "id": "rating_count_metafield", "label": "Rating count metafield (namespace.key)", "default": "reviews.rating_count" },
    { "type": "color", "id": "rating_star_color", "label": "Star color", "default": "#111111" },
    { "type": "color", "id": "rating_text_color", "label": "Review count color", "default": "#111111" },

    { "type": "header", "content": "Modern card styles" },
    { "type": "color", "id": "vendor_color", "label": "Vendor color", "default": "#000000" },
    { "type": "color", "id": "title_color", "label": "Title color", "default": "#000000" },
    { "type": "color", "id": "price_color", "label": "Price color", "default": "#000000" },
    { "type": "range", "id": "vendor_font_size", "label": "Vendor font size (px)", "min": 10, "max": 18, "step": 1, "default": 12 },
    { "type": "range", "id": "title_font_size", "label": "Title font size (px)", "min": 12, "max": 26, "step": 1, "default": 16 },
    { "type": "range", "id": "price_font_size", "label": "Price font size (px)", "min": 12, "max": 24, "step": 1, "default": 16 },

    { "type": "color", "id": "card_bg", "label": "Card background", "default": "#FFFFFF" },
    { "type": "color", "id": "card_border", "label": "Card border color", "default": "#00000026" },
    {
      "type": "select",
      "id": "card_border_style",
      "label": "Card border style",
      "default": "solid",
      "options": [
        { "value": "none", "label": "None" },
        { "value": "solid", "label": "Solid" },
        { "value": "dashed", "label": "Dashed" },
        { "value": "dotted", "label": "Dotted" }
      ]
    },

    { "type": "color", "id": "button_bg", "label": "Button background", "default": "#10B981" },
    { "type": "color", "id": "button_text", "label": "Button text", "default": "#FFFFFF" },
    { "type": "color", "id": "button_bg_hover", "label": "Button background (hover)", "default": "#000000" },
    { "type": "color", "id": "button_text_hover", "label": "Button text (hover)", "default": "#FFFFFF" },
    { "type": "color", "id": "button_border", "label": "Button border", "default": "#00000026" },

    {
      "type": "select",
      "id": "button_size",
      "label": "Button size (modern card)",
      "default": "md",
      "options": [
        { "value": "sm", "label": "Small" },
        { "value": "md", "label": "Medium" },
        { "value": "lg", "label": "Large" }
      ]
    },
    { "type": "text", "id": "add_to_cart_label", "label": "Add to cart label", "default": "Add to cart" },
    { "type": "text", "id": "choose_options_label", "label": "Choose options label", "default": "Choose options" },
    { "type": "checkbox", "id": "show_buy_now", "label": "Show Buy now (single-variant only)", "default": false },
    { "type": "text", "id": "buy_now_label", "label": "Buy now label", "default": "Buy now" },

    { "type": "header", "content": "Pagination" },
    { "type": "range", "id": "products_per_page", "label": "Products per page", "min": 8, "max": 48, "step": 1, "default": 24 },
    { "type": "checkbox", "id": "show_pagination", "label": "Show pagination", "default": true },
    {
      "type": "select",
      "id": "pagination_style",
      "label": "Pagination style",
      "default": "numbered",
      "options": [
        { "value": "numbered", "label": "Numbered" },
        { "value": "load_more", "label": "Load more (JS)" },
        { "value": "infinite_scroll", "label": "Infinite scroll (JS)" }
      ]
    },

    { "type": "header", "content": "Design tokens + spacing" },
    {
      "type": "select",
      "id": "max_width_toggle",
      "label": "Max width",
      "default": "contained",
      "options": [
        { "value": "contained", "label": "Contained" },
        { "value": "full", "label": "Full width" }
      ]
    },
    {
      "type": "select",
      "id": "max_width",
      "label": "Contained max width token",
      "default": "var(--qtm-page-maxw, 1200px)",
      "options": [
        { "value": "var(--qtm-page-maxw, 1200px)", "label": "Page max" },
        { "value": "var(--qtm-page-maxw-wide, 1440px)", "label": "Wide" },
        { "value": "var(--qtm-page-maxw-narrow, 960px)", "label": "Narrow" }
      ]
    },
    { "type": "range", "id": "gap", "label": "Grid gap (px)", "min": 0, "max": 40, "step": 1, "default": 18 },
    { "type": "range", "id": "border_radius", "label": "Border radius (px)", "min": 0, "max": 32, "step": 1, "default": 16 }
  ],
  "presets": [
    { "name": "Main Collection Modern" }
  ]
}
{% endschema %}