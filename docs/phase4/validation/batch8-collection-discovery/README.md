# Batch 8 checkpoint — collection banners, cards, carousel and featured collection

85/102 blocks and 24 dependencies individually reviewed; 109/126 Family 8 entries have dispositions (108 live queued, 1 dormant retained), 17 NOT REVIEWED.

Four collection discovery blocks now use truthful native content, complete links, safe image/price fallbacks and bounded responsive layouts. One collection-card helper removes repeated rendering. Collection Carousel composes with the existing shared engine for progressive native scrolling and correctly scoped navigation/rotation/editor behavior.

Individual findings:

- `blocks/content-collection-banner.liquid`: Individually reviewed all 56 original controls and three presets. Native selected collection remains the primary title/image/link source with configured custom and fallback sources preserved. Complete label/destination guards remove empty anchors and empty action rows; meaningful media/copy/count state replaces unconditional custom-banner shells. Missing images no longer reserve a split column, and image-only banners omit empty copy. Descriptions, labels and URLs are safely rendered, including native zero product counts. New preset insertion omits setup copy, invented limited-time/seasonal claims and unconfigured secondary-only banners; saved merchant content remains untouched. Native wrapper ownership, editor visibility, bounded padding/widths, independent device ranges and narrow split layout repaired.
- `blocks/content-collection-cards.liquid`: Individually reviewed all 54 original controls plus one overlay text-color control and three presets. Shared native collection renderer replaces duplicated single/manual card paths while preserving featured-selection precedence, its existing manual-list fallback and configured limits. Only actual collection links render; absent images and empty descriptions/buttons no longer produce fake media or empty content. Compact tracks and images shrink inside nested builders; image-free overlay cards use normal readable text colors. Native titles/descriptions/URLs/counts and alt fallback are safely rendered, all image candidates load lazily, and overlay color is configurable. New insertion is neutral/editor-guided; all prior IDs/options and saved values retained.
- `blocks/content-collection-carousel.liquid`: Individually reviewed all 63 original controls plus overlay text color and three translated rotation labels, and three presets. Actual list/single/manual fallback uses the same native card renderer as Collection Cards. Inline listeners/timers and offsetLeft navigation are replaced with the reviewed shared carousel engine, measured clamped pages and RTL-aware native scrolling; arrows work independently of header visibility. Native links and viewport remain usable without JavaScript, while dead controls stay hidden until enhancement. In-flow image/caption cells grow with long text instead of clipping; missing image color, bounded peek overflow, mobile columns, touch-size pagination, keyboard/focus and narrow sizing repaired. Real pause/resume, hover/focus/page/intersection/reduced-motion/design-mode policy and insert/reorder/remove teardown are exercised. Popular/high-traffic example claims removed from new insertion; saved content and all original choices retained.
- `blocks/content-featured-collection.liquid`: Individually reviewed all 59 original controls plus a translated native price-range prefix and three presets. Native collection title, description, media, URL, product count and actual preview list remain authoritative. Missing hero/preview images collapse their tracks, badges and actions require real labels, and blank descriptions/actions omit empty wrappers. Product previews show the native product minimum price with an explicit range prefix when price varies, including zero price; links/labels/descriptions are escaped and native alt is retained. New insertion omits campaign claims and setup marketing copy. Native one-wrapper editor ownership, visible progressive motion, bounded padding/nested grids and independent device rules repaired.
- `snippets/qtm-block-collection-card.liquid`: Individually reviewed both actual callers and all card inputs/settings. Native collection source supplies title, URL, image/featured-image fallback, count and stripped description; configured fallback copy is used only when the caller exposes it. One complete link contains plain-text spans without nested actions, lazy responsive native images with alt fallback, guarded configured button text and explicit image-free class. Fixed caller prefix scopes styles; helper owns no editor wrapper or global state. Both collection list and featured-single consumers pass actual render fixtures.

378 actual default/preset/control/data-state renders cover all original IDs/types/options/range endpoints, native collection list/single/manual fallback and limits, zero counts/prices and variable prices, empty/partial images, escaped native/merchant copy and destinations, cleared labels, neutral insertion, one native wrapper and editor visibility. Actual shared-engine fixtures cover duplicate loading, two raw-ID instances, measured/clamped page targets, RTL arrows/Home/End, nested-key ownership, persistent pause and translated labels, reduced motion/page visibility/intersection, resize pagination focus, mutation insert/reorder/remove and section unload/reload. All prior content carousel/gallery-text fixtures retain their assertions. All 16 affected suites pass. This group covers 238 settings and 12 actual presets. Strict parsing, schema/reference/architecture/source hashes and CSS build pass. All 150 sections, 102 blocks and settings_data are preserved; compiled CSS matches Batch 7. Exact unsuppressed Theme Check movement is in themecheck-comparison.json.

B8-COLLECTION-DISCOVERY live queue after engineering batches: actual native collection/product image and price rendering, 320–1440px/zoom/RTL/nested-builder layout and image/caption contrast, real scroll-snap and measured pagination/keyboard/AT, autoplay pause/visibility/reduced motion, native Theme Editor insertion/reorder/removal. DOM geometry/timer/native-drop adapters are not acceptance. No live Shopify QA; earlier queues carry forward.

Reuses the existing native collection Liquid contracts and the reviewed shared carousel engine; no external provider or commerce mutation.

PR #22 stays DRAFT. No merge/main write, Batch 9, Atelier/Signal/Terrace rollout or live Shopify QA. Continue only Batch 8 to formal engineering closure.
