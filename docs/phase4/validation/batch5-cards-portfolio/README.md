# Batch 5 — Cards and Portfolio Grid

Independent source/settings/preset dispositions for both hosts and the new scoped flip controller: REQUIRES LIVE VERIFICATION.

- Actual Liquid settings/select/range/checkbox matrix and insertion/empty/unknown-block states pass.
- All grid/row/timeline/card variants and link modes preserve separate rich-text, primary and back destinations; no nested interactive markup.
- Native image/alt adapters, zero controls, custom colors, duplicate identities, scoped bounds and selected mobile columns pass.
- Flip DOM fixtures cover no-JS content, keyboard/Escape/focus/ARIA/inert state, hover ownership, mobile/reduced-motion/editor fallback, multiple instances, duplicate boot and unload/reload.
- Affected regression suite passes. Earlier complete 61-suite run is preserved under batch5-people-timeline; no fresh all-suite certification is claimed at this checkpoint.
- Schema/reference/protected-state checks pass: 150 sections, 102 Theme Blocks, no deletions, unchanged merchant settings. CSS build passes and generated CSS is byte-identical.
- Shopify CLI 4.8.0 exits 1 with unchanged 632 diagnostics (16 errors / 616 warnings); no suppression. Historical scanner findings remain unresolved.
- B5-CARDS-PORTFOLIO live acceptance is queued after Batch 9; fixtures do not certify browser or Shopify behavior.
