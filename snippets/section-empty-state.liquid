{% if request.design_mode %}
  <div role="status" style="padding: 1.5rem; border: 1px dashed currentColor;">{{ message | default: 'Add content blocks to display this section.' | escape }}</div>
{% else %}
  <style>#shopify-section-{{ section.id }} { display: none; }</style>
{% endif %}
