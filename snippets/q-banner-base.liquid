{% if section.id != blank %}
<style>
  #shopify-section-{{ section.id }}{
    --q-foreground:#111; --q-card:#fff; --q-border:#e5e7eb;
    --q-primary:#111; --q-primary-foreground:#fff;
  }
  #shopify-section-{{ section.id }} .q-wrap{max-width:1280px;margin:0 auto;padding:0 16px;}
  @media(min-width:768px){#shopify-section-{{ section.id }} .q-wrap{padding:0 24px}}
  #shopify-section-{{ section.id }} .q-panel{background:var(--q-card);border:1px solid var(--q-border);border-radius:16px;padding:32px}
  @media(min-width:768px){#shopify-section-{{ section.id }} .q-panel{padding:40px}}
  #shopify-section-{{ section.id }} .q-text{color:var(--q-foreground)}
  #shopify-section-{{ section.id }} .q-btn{display:inline-flex;align-items:center;justify-content:center;border-radius:10px;padding:10px 20px;font-size:14px;font-weight:500;text-decoration:none}
  #shopify-section-{{ section.id }} .q-btn--primary{background:var(--q-primary);color:var(--q-primary-foreground)}
  #shopify-section-{{ section.id }} .q-btn--secondary{background:#fff;border:1px solid var(--q-border);color:var(--q-foreground)}
  /* Give CTAs a base height and vertical centering */
  #shopify-section-{{ section.id }} .q-panel--center{min-height:240px;display:flex;flex-direction:column;justify-content:center}
</style>

{% endif %}
