# Batch 8 checkpoint — content cards, comparisons and guarantees

69/102 blocks and 21 dependencies individually reviewed; 90/123 Family 8 entries have dispositions (89 live queued, 1 dormant retained), 33 NOT REVIEWED.

Six static content card/comparison/guarantee blocks now omit empty configured slots, preserve native images and safe destinations, repair narrow-layout/overlay/focus behavior and keep desktop/mobile comparisons consistent. A shared comparison-value helper centralizes readable symbol labels and escaped notes. New insertion defaults leave prices, popularity, performance and policy claims for merchants to configure explicitly; saved merchant content is preserved.

Individual findings:

- `blocks/content-feature-cards.liquid`: Individually reviewed all 84 original settings and the one actual preset. Captures actual populated slots once; configured empty cards are omitted, with editor-only guidance for an empty group. Preserves all eight slots/counts and desktop/mobile column controls. Escapes text icons and native destination attributes; native image alt falls back from optional override to image.alt with lazy responsive widths. Repairs border-box sizing, bounded nested padding, keyboard focus and editor visibility/motion; unknown motion values cannot leave the root opacity zero. New insertion copy omits invented product/performance claims.
- `blocks/content-content-cards.liquid`: Individually reviewed all 95 original settings, one needed overlay-color control and the one actual preset. Populated-slot capture preserves all eight item fields/layouts; blank slots omitted without discarding image/badge/eyebrow-only manual content. Native alt/URL handling repaired, responsive image candidates retained, horizontal flex body shrinks to remaining media space. Image-radius control works across layouts; overlay text/link color is explicitly configurable with original white default. Native wrapper/editor/motion/focus/nested padding fixed, and new Popular/conversion claims removed without rewriting merchant state.
- `blocks/content-promo-card.liquid`: Individually reviewed all 43 original settings, one overlay text-color control and the one actual preset. One native chosen image preserves primary-image-over-background precedence and actual image alt fallback. Real complete action guards omit empty action containers; escaped links and visible keyboard focus make the previously unused link color operative. Horizontal content flexes within remaining media space; explicit nonnegative media/overlay/content stacking preserves the image above card background, and image-radius control is no longer overridden in every mode. New defaults omit invented urgency and theme-development copy; empty editor guidance/native wrapper visibility/motion repaired.
- `blocks/content-benefits-vs-drawbacks.liquid`: Individually reviewed all 50 settings and three presets. Populated advantages/tradeoffs panels are counted independently, and the active column count avoids an empty half-width panel. Real authored explanation/CTA/footer survives; footer notes no longer disappear when a CTA is configured. Icons now use the explicit panel accent background instead of resolving both foreground/background to white; redundant first-letter overrides removed. Safe links, long labels, bounded layout/device/editor behavior preserved. New presets use neutral comparison headings and leave unverified advantage/competitor claims empty.
- `blocks/content-comparison-matrix.liquid`: Individually reviewed all 138 original settings and three needed translated value labels plus all three presets. Every table/card surface uses the same enabled-and-named column/row guards; empty headers cannot leave mismatched cells. Native table caption/scopes and keyboard-scroll region retained, mobile definition lists remain equivalent, and show_cta_row now also governs mobile links. One shared value renderer supplies escaped literal values/notes or decorative symbols plus actual readable labels. Narrow table width follows actual columns and mobile notes can shrink/wrap. New presets require explicit options/rows and omit invented prices/popularity/feature notes; manual price text is clearly distinguished from live product/cart data.
- `blocks/content-guarantee-box.liquid`: Individually reviewed all 38 original settings and three presets. Explicit merchant terms/policy links and all support-point IDs retained. Hidden/blank icons no longer leave an empty grid column, including mobile banner mode; blank point lists are omitted, oversized icon width is bounded, and native wrapper/visibility/reduced-motion/focus rules remain usable in nested builders. New insertion presets leave return/payment/satisfaction promises unset and clarify that authored text does not create a store policy.
- `snippets/qtm-block-comparison-value.liquid`: Individually reviewed both desktop/mobile matrix callers and all label consumers. Replaces duplicated case/value/note markup with one native-safe path. Yes/no/partial symbols are decorative, paired with real visually hidden configurable text; generic spans are no longer incorrectly aria-labeled. Literal selected values and merchant notes are escaped; safe class escaping, fallback labels and native no-JS behavior retained.

588 actual default/control/preset/empty/native-media renders exercise all original IDs/options/range endpoints, populated and blank slots, native alt overrides/fallback and image/background precedence, literal icon/URL/note escaping, authored CTA-plus-footer behavior, hidden optional icon/point grids, enabled/named matrix column/row alignment, identical desktop/mobile value/CTA contracts and translated labels. Independent IDs, native wrapper/editor/motion and responsive source rules are checked. All 16 affected suites pass. This group covers 453 settings and 12 actual presets. Strict parsing, schema/reference/architecture/source hashes and CSS build pass. All 150 sections, 102 blocks and settings_data are preserved; compiled CSS matches Batch 7. Exact unsuppressed Theme Check movement is in themecheck-comparison.json.

B8-CONTENT-CARDS live queue after engineering batches: real card image rendition/alt/overlay stacking and configured colors; native narrow builder grids/flex, zoom/RTL and 320–1440px layouts; comparison table/definition-list semantics and keyboard/AT/contrast; actual Theme Editor insert/select/reorder. Merchant prices, comparisons and guarantee terms remain manual authored claims and do not change Shopify policies/cart prices. No live Shopify QA; prior queues carry forward.



PR #22 stays DRAFT. No merge/main write, Batch 9, Atelier/Signal/Terrace rollout or live Shopify QA. Continue only Batch 8 to formal engineering closure.
