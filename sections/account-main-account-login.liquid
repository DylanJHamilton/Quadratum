{% assign account_form_id = 'Account-login-' | append: section.id %}
{% capture account_card_text %}{% render 'contrast-text', background: section.settings.card_background, preferred: settings.text_base %}{% endcapture %}
{% comment %}
  Quadratum Account System
  File 1 of 5 — Account Entry Only

  Handles:
  - Login
  - Recover Password
  - Create Account CTA

  Does NOT handle:
  - Dashboard
  - Orders
  - Order Details
  - Order Tracking
  - Addresses
{% endcomment %}

{{ 'account-main-account.css' | asset_url | stylesheet_tag }}

{%- liquid
  assign create_account_url = section.settings.create_account_url
  if create_account_url == blank
    assign create_account_url = routes.account_register_url
  endif

  assign recover_display = section.settings.recover_display
  assign layout_style = section.settings.layout_style
-%}

<section
  id="QtmAccountMainLogin-{{ section.id }}"
  class="
    account-main-account-login
    account-main-account-login--{{ layout_style }}
    account-main-account-login--recover-{{ recover_display }}
  "
  style="
    --account-main-account-login-card-foreground: {{ account_card_text | strip }};
    --account-main-account-login-heading-size: {{ section.settings.heading_font_size }}px;
    --account-main-account-login-subheading-size: {{ section.settings.subheading_font_size }}px;
    --account-main-account-login-body-size: {{ section.settings.body_font_size }}px;
    --account-main-account-login-field-radius: {{ section.settings.field_radius }}px;
    --account-main-account-login-field-padding: {{ section.settings.field_padding }}px;
    --account-main-account-login-field-spacing: {{ section.settings.field_spacing }}px;
    --account-main-account-login-button-radius: {{ section.settings.button_radius }}px;
    --account-main-account-login-button-padding: {{ section.settings.button_padding }}px;
    --account-main-account-login-card-background: {{ section.settings.card_background }};
    --account-main-account-login-card-border: {{ section.settings.card_border }};
    --account-main-account-login-card-radius: {{ section.settings.card_radius }}px;
    --account-main-account-login-card-padding: {{ section.settings.card_padding }}px;
  "
  data-account-main-account-login
  data-recover-display="{{ recover_display }}"
>
  <div class="account-main-account-login__shell">
    <div class="account-main-account-login__content">
      {% if layout_style == 'centered' and section.settings.logo != blank %}
        <div class="account-main-account-login__logo">
          {{ section.settings.logo | image_url: width: 220 | image_tag: loading: 'lazy', alt: shop.name }}
        </div>
      {% endif %}

      {% if section.settings.heading != blank %}
        <h1 class="account-main-account-login__heading">
          {{ section.settings.heading | escape }}
        </h1>
      {% endif %}

      {% if section.settings.subheading != blank %}
        <p class="account-main-account-login__subheading">
          {{ section.settings.subheading | escape }}
        </p>
      {% endif %}

      {% if section.settings.content != blank %}
        <div class="account-main-account-login__richtext rte">
          {{ section.settings.content }}
        </div>
      {% endif %}

      {% if section.settings.show_social_placeholder %}
        <div class="account-main-account-login__social-placeholder" role="note">
          {{ section.settings.social_placeholder_text | escape }}
        </div>
      {% endif %}
    </div>

    <div class="account-main-account-login__auth">
      <div class="account-main-account-login__card">
        <div
          class="account-main-account-login__panel account-main-account-login__panel--login is-active"
          data-account-main-account-login-panel="login"
        >
          {% form 'customer_login', id: account_form_id, class: 'account-main-account-login__form' %}
            <div class="account-main-account-login__form-header">
              <h2 class="account-main-account-login__form-title">
                {{ section.settings.login_heading | escape }}
              </h2>
            </div>

            {% if form.errors %}
              <div
                id="AccountErrors-login-{{ section.id }}" class="account-main-account-login__errors"
                role="alert"
                aria-live="assertive"
                tabindex="-1"
              >
                {{ form.errors | default_errors }}
              </div>
            {% endif %}

            <div class="account-main-account-login__field">
              <label for="AccountMainLoginEmail-{{ section.id }}">
                {{ section.settings.email_label | default: 'Email' | escape }}
              </label>
              <input
                id="AccountMainLoginEmail-{{ section.id }}"
                  {% if form.errors %}aria-describedby="AccountErrors-login-{{ section.id }}"{% endif %}
                type="email"
                name="customer[email]"
                autocomplete="email"
                autocorrect="off"
                autocapitalize="off"
                {% if form.errors contains 'form' %}
                  aria-invalid="true"
                {% endif %}
                required
              >
            </div>

            <div class="account-main-account-login__field">
              <label for="AccountMainLoginPassword-{{ section.id }}">
                {{ section.settings.password_label | default: 'Password' | escape }}
              </label>

              <div class="account-main-account-login__password-wrap">
                <input
                  id="AccountMainLoginPassword-{{ section.id }}"
                  {% if form.errors %}aria-describedby="AccountErrors-login-{{ section.id }}"{% endif %}
                  type="password"
                  name="customer[password]"
                  autocomplete="current-password"
                  {% if form.errors contains 'form' %}
                    aria-invalid="true"
                  {% endif %}
                  required
                >

                {% if section.settings.show_password_toggle %}
                  <button
                    type="button"
                    class="account-main-account-login__password-toggle"
                    aria-label="{{ section.settings.show_password_label | escape }}"
                    data-account-main-account-login-password-toggle hidden aria-pressed="false"
                    data-show-label="{{ section.settings.show_password_label | escape }}"
                    data-hide-label="{{ section.settings.hide_password_label | escape }}"
                    data-show-text="{{ section.settings.show_password_text | escape }}" data-hide-text="{{ section.settings.hide_password_text | escape }}"
                  >
                    <span data-account-main-account-login-password-toggle-text>
                      {{ section.settings.show_password_text | escape }}
                    </span>
                  </button>
                {% endif %}
              </div>
            </div>

            {% if section.settings.show_remember_me %}
              <div class="account-main-account-login__checkbox">
                <input
                  id="AccountMainLoginRemember-{{ section.id }}"
                  {% if form.errors %}aria-describedby="AccountErrors-login-{{ section.id }}"{% endif %}
                  type="checkbox"
                  name="remember_me"
                  value="1"
                >
                <label for="AccountMainLoginRemember-{{ section.id }}">
                  {{ section.settings.remember_me_label | escape }}
                </label>
              </div>
            {% endif %}

            <div class="account-main-account-login__actions">
              <button
                type="submit"
                class="account-main-account-login__button account-main-account-login__button--{{ section.settings.button_style }}"
              >
                {{ section.settings.login_button_label | default: 'Sign in' | escape }}
              </button>
            </div>

            {% if recover_display == 'toggle' or recover_display == 'panel' %}
              <button
                type="button"
                class="account-main-account-login__text-button"
                data-account-main-account-login-recover-trigger hidden aria-expanded="false" aria-controls="AccountRecovery-{{ section.id }}"
              >
                {{ section.settings.forgot_password_label | escape }}
              </button>
            {% endif %}
          {% endform %}

          {% if recover_display == 'inline' or recover_display == 'toggle' %}
            <div
              class="
                account-main-account-login__recover-inline

              "
              id="AccountRecovery-{{ section.id }}" data-account-main-account-login-recover-inline
            >
              {% render 'account-main-account-login-recover-form-placeholder', section: section %}
            </div>
          {% endif %}
        </div>

        {% if recover_display == 'panel' %}
          <div
            class="account-main-account-login__panel account-main-account-login__panel--recover"
            id="AccountRecovery-{{ section.id }}" data-account-main-account-login-panel="recover"
          >
            {% render 'account-main-account-login-recover-form-placeholder', section: section %}
          </div>
        {% endif %}

        {% if section.settings.show_create_account %}
          <div class="account-main-account-login__create">
            {% if section.settings.create_account_text != blank %}
              <p>{{ section.settings.create_account_text | escape }}</p>
            {% endif %}

            <a
              href="{{ create_account_url | escape }}"
              class="account-main-account-login__create-link"
            >
              {{ section.settings.create_account_label | default: 'Create account' | escape }}
            </a>
          </div>
        {% endif %}
      </div>
    </div>
  </div>


</section>

<script src="{{ 'account-main-account-login.js' | asset_url }}" defer></script>

{% schema %}
{
  "name": "Account Main Login",
  "tag": "section",
  "class": "qtm-account-main-login",
  "settings": [
    {"type":"paragraph","content":"Legacy customer account template. Shopify owns account authentication, email delivery and session rules. Current customer accounts operate outside the theme; this section does not migrate accounts or add social login."},
    {
      "type": "select",
      "id": "layout_style",
      "label": "Layout style",
      "default": "split",
      "options": [
        {
          "value": "split",
          "label": "Split Layout"
        },
        {
          "value": "centered",
          "label": "Centered Layout"
        },
        {
          "value": "card",
          "label": "Card Layout"
        }
      ]
    },
    {
      "type": "image_picker",
      "id": "logo",
      "label": "Logo",
      "info": "Used in centered layout."
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Welcome Back"
    },
    {
      "type": "text",
      "id": "subheading",
      "label": "Subheading",
      "default": "Sign in to access your account."
    },
    {
      "type": "richtext",
      "id": "content",
      "label": "Optional content",
      "default": "<p>View your account details, access customer support, and manage your shopping experience.</p>"
    },
    {
      "type": "text",
      "id": "login_heading",
      "label": "Login form heading",
      "default": "Sign in"
    },
    {
      "type": "text",
      "id": "email_label",
      "label": "Email label",
      "default": "Email"
    },
    {
      "type": "text",
      "id": "password_label",
      "label": "Password label",
      "default": "Password"
    },
    {
      "type": "text",
      "id": "login_button_label",
      "label": "Login button label",
      "default": "Sign In"
    },
    {
      "type": "text",
      "id": "forgot_password_label",
      "label": "Forgot password label",
      "default": "Forgot your password?"
    },
    {
      "type": "select",
      "id": "recover_display",
      "label": "Recover password display",
      "default": "toggle",
      "options": [
        {
          "value": "inline",
          "label": "Inline"
        },
        {
          "value": "toggle",
          "label": "Toggle"
        },
        {
          "value": "panel",
          "label": "Separate Panel"
        }
      ]
    },
    {
      "type": "text",
      "id": "recover_heading",
      "label": "Recover heading",
      "default": "Reset your password"
    },
    {
      "type": "text",
      "id": "recover_subheading",
      "label": "Recover subheading",
      "default": "Enter your email and we’ll send you a password reset link."
    },
    {
      "type": "text",
      "id": "recover_button_label",
      "label": "Recover button label",
      "default": "Reset Password"
    },
    {
      "type": "text",
      "id": "recover_success_message",
      "label": "Recover success message",
      "default": "We’ve sent you an email with a link to update your password."
    },
    {
      "type": "text",
      "id": "back_to_login_label",
      "label": "Back to login label",
      "default": "Back to sign in"
    },
    {
      "type": "checkbox",
      "id": "show_create_account",
      "label": "Show create account prompt",
      "default": true
    },
    {
      "type": "text",
      "id": "create_account_text",
      "label": "Create account text",
      "default": "New here?"
    },
    {
      "type": "text",
      "id": "create_account_label",
      "label": "Create account label",
      "default": "Create Account"
    },
    {
      "type": "url",
      "id": "create_account_url",
      "label": "Create account URL"
    },
    {
      "type": "checkbox",
      "id": "show_remember_me",
      "label": "Show legacy remember-me field",
      "info": "Shopify controls sign-in sessions. This native field does not save passwords or add a theme-managed session service.",
      "default": false
    },
    {
      "type": "text",
      "id": "remember_me_label",
      "label": "Remember me label",
      "default": "Remember me"
    },
    {
      "type": "checkbox",
      "id": "show_password_toggle",
      "label": "Show password toggle",
      "default": true
    },
    {
      "type": "text",
      "id": "show_password_text",
      "label": "Show password visible text",
      "default": "Show"
    },
    {
      "type": "text",
      "id": "hide_password_text",
      "label": "Hide password visible text",
      "default": "Hide"
    },
    {
      "type": "text",
      "id": "show_password_label",
      "label": "Show password accessible label",
      "default": "Show password"
    },
    {
      "type": "text",
      "id": "hide_password_label",
      "label": "Hide password accessible label",
      "default": "Hide password"
    },
    {
      "type": "checkbox",
      "id": "show_social_placeholder",
      "label": "Show social-login information (no integration)",
      "default": false
    },
    {
      "type": "text",
      "id": "social_placeholder_text",
      "label": "Social placeholder text",
      "default": "Additional sign-in options require a compatible app."
    },
    {
      "type": "header",
      "content": "Typography"
    },
    {
      "type": "range",
      "id": "heading_font_size",
      "label": "Heading font size",
      "min": 24,
      "max": 72,
      "step": 1,
      "unit": "px",
      "default": 44
    },
    {
      "type": "range",
      "id": "subheading_font_size",
      "label": "Subheading font size",
      "min": 14,
      "max": 28,
      "step": 1,
      "unit": "px",
      "default": 18
    },
    {
      "type": "range",
      "id": "body_font_size",
      "label": "Body font size",
      "min": 13,
      "max": 22,
      "step": 1,
      "unit": "px",
      "default": 16
    },
    {
      "type": "header",
      "content": "Form fields"
    },
    {
      "type": "range",
      "id": "field_radius",
      "label": "Field radius",
      "min": 0,
      "max": 32,
      "step": 1,
      "unit": "px",
      "default": 12
    },
    {
      "type": "range",
      "id": "field_padding",
      "label": "Field padding",
      "min": 10,
      "max": 24,
      "step": 1,
      "unit": "px",
      "default": 14
    },
    {
      "type": "range",
      "id": "field_spacing",
      "label": "Field spacing",
      "min": 8,
      "max": 28,
      "step": 1,
      "unit": "px",
      "default": 16
    },
    {
      "type": "header",
      "content": "Buttons"
    },
    {
      "type": "select",
      "id": "button_style",
      "label": "Button style",
      "default": "solid",
      "options": [
        {
          "value": "solid",
          "label": "Solid"
        },
        {
          "value": "outline",
          "label": "Outline"
        },
        {
          "value": "ghost",
          "label": "Ghost"
        }
      ]
    },
    {
      "type": "range",
      "id": "button_radius",
      "label": "Button radius",
      "min": 0,
      "max": 32,
      "step": 1,
      "unit": "px",
      "default": 32
    },
    {
      "type": "range",
      "id": "button_padding",
      "label": "Button padding",
      "min": 10,
      "max": 24,
      "step": 1,
      "unit": "px",
      "default": 15
    },
    {
      "type": "header",
      "content": "Card"
    },
    {
      "type": "color",
      "id": "card_background",
      "label": "Card background",
      "default": "#ffffff"
    },
    {
      "type": "color",
      "id": "card_border",
      "label": "Card border",
      "default": "#e5e7eb"
    },
    {
      "type": "range",
      "id": "card_radius",
      "label": "Card radius",
      "min": 0,
      "max": 40,
      "step": 1,
      "unit": "px",
      "default": 24
    },
    {
      "type": "range",
      "id": "card_padding",
      "label": "Card padding",
      "min": 20,
      "max": 64,
      "step": 2,
      "unit": "px",
      "default": 36
    }
  ],
  "presets": [
    {
      "name": "Account Main Login"
    }
  ]
}
{% endschema %}