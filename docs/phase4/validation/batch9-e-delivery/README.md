# Batch 9 checkpoint E — delivery and build closure

D is remotely verified at `46e40f93fce2ada7a59fd717ba56b4cbb3774596`. The final five Family 9 entries are dispositioned: **two active/live queued (one generated), one build-only reviewed, two dormant retained owner-review deletion candidates**. The engineering register now has zero NOT REVIEWED entries; formal closure follows full regression validation and remote publication of this runtime checkpoint.

| Asset | Actual ownership and consumers | Source/runtime disposition |
|---|---|---|
| `section-slideshow.css` | No runtime import; broad old q-hero/q-slide/q-btn selectors for the obsolete transform carousel | DORMANT retained deletion candidate. Duplicated rules and unscoped generic selectors could collide if activated. Actual Slideshow Banner owns inline qsl styles and `slideshow-banner.js`; similar carousel child attributes elsewhere do not load this asset. No activation/rewrite. |
| `section-slideshow.js` | No asset consumer or exact `[data-q-carousel]` root; Tailwind scans its JS as a build input | DORMANT retained deletion candidate. Undefined `slideVideos` would throw on activation; it also lacks unload disposal/duplicate global guards, persistent manual pause, reduced-motion video policy and inactive-slide keyboard isolation. None executes in the current theme. Retain pending owner decision, with repair/re-review required before any reuse. |
| `styles.css` | ACTIVE normal and password layout foundation, loaded after the native token wrapper | Restored heading/button uppercase controls using their existing tokens, removed generic `a:hover` color inheritance that overrode component button/link colors, and made mobile heading minimums respect configured sizes below the old fixed floor. C's aliases now supply its existing qtm contracts. Global hidden/focus/choice-control/motion rules, scoped popup selectors and delivery order retained. |
| `tailwind.css` | BUILD-ONLY source: `npm run build:css` / `watch:css`; no runtime stylesheet link | SOURCE REVIEWED, unchanged. Tailwind 3.4.17 directives plus existing HSL light/OS-dark token utilities; native c-/qtm- tokens remain a distinct owner. Content paths include retained sections, snippets, blocks, layouts/templates and asset JS, with vendor model-viewer excluded. Existing safelist preserved. No architecture or preset rollout. |
| `theme.css` | GENERATED ACTIVE output from tailwind.css; normal/password layouts own one link each | SOURCE/BUILD REVIEWED, unchanged generated bytes. `npm run build:css` reproduces SHA-256 `e95c623df26ff2eff84bf749f025593fc667e4b71419c898b5e0987c5a0c8f16`. Removed the duplicate theme.css link from the normal-layout playground template, preventing a second late preflight/token copy. Minification retained; no hand edit. |

## Adjacent delivery and media errors

The three pre-existing Theme Check errors were traced during this delivery review. Quick View's global script now uses `defer` after its existing host; its initialization depends on that DOM host and event-driven purchase hooks, not an immediate inline call. Existing Quick View and layout regressions pass. Both actual Slideshow Banner image branches now receive the selected native image's intrinsic dimensions, using the mobile image when no desktop image is selected. No fabricated dimensions or lint suppression. Six actual source renders cover split/overlay with desktop+mobile, mobile-only and absent images. No slideshow setting, preset or controller changed.

The legacy slideshow assets remain separate; these three small host corrections do not connect or resurrect them. Existing Family 2/4 ownership and live queues are retained.

## Evidence and boundaries

`delivery-before.txt` reproduces the generic-hover contract failure. `batch9-delivery.cjs` passes source/DOM checks for global ownership, stylesheet syntax, one compiled-CSS owner, deferred Quick View and selected-image dimensions. Eight relevant suites pass; native strict parsing covers modified layout/template/slideshow sources. Build versions and exact before/after hashes are in `build-integrity.json`; full unsuppressed Theme Check output/comparison is saved here. The final all-suite run belongs to formal closure.

Source fixes can change visible configured typography/colors and link hover appearance; no merchant values were rewritten. Actual cascade, browser fetching, font metrics, responsive geometry, native images, Quick View timing/checkout and Theme Editor interaction remain live acceptance. CSS parsing, generated-hash equality and fixtures do not replace it.

No files deleted. The five Main Product hosts, Quick View, Headers/Footers 1–5, both builders, 150 sections, 102 blocks and protected settings remain intact. No live Shopify QA or later roadmap work began.

Fresh Theme Check 4.8.0: **509 findings, zero errors, 509 warnings; exit 0**. Baseline was 512 findings / three errors / 509 warnings. Exact unsuppressed movement is in `themecheck-comparison.json`.
