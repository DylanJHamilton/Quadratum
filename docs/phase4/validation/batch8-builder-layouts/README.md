# Batch 8 checkpoint — content and product builder layouts

38/102 blocks and 9 dependencies individually reviewed; 47/111 Family 8 entries have dispositions (46 live queued, 1 dormant retained), 64 NOT REVIEWED.

Four individual layout blocks now preserve the complete content and product builder chains while making their own root elements receive layout and editor behavior. Three blocks adopt Shopify tag:null, matching the existing product row; all four keep one explicit editor root. Resolved mobile modes, scoped CSS ownership, min-height flex alignment, real vertical stretch, ordering and sticky height are reconciled with unchanged host sections. Exact admitted child lists and preset child trees remain intact; saved settings and all host/merchant source data are unchanged. No new runtime dependency or controller is introduced.

Individual findings:

- `blocks/layout-row.liquid`: Individually reviewed all row controls and three complete column presets. Native tag:null plus one explicit root editor attribute makes the actual row a direct builder child. Saved mobile classes remain while a resolved mobile attribute handles inherited versus explicit stack/scroll/grid; scoped selector specificity and resets preserve row ownership against host styles. Scrollable rows are named keyboard regions; tag allowlist, box sizing and long-content bounds added. All three insertion presets explicitly use their configured spacing; merchant inheritance control remains functional. Child admission and all preset child trees preserved exactly.
- `blocks/layout-column.liquid`: Individually reviewed all 20 controls and three presets, retaining all 62 child admissions. Native tag:null makes columns actual row grid items, fixes hidden-column gaps and duplicate metadata. Flex root/inner repairs min-height vertical alignment; actual child flex growth replaces invalid justify-content:stretch, and native leaf wrappers receive shrinking/stretch constraints. Tag/role allowlists and named region/group semantics, editor visibility and actual captured child-projection empty guidance repaired. Saved settings and child content preserved.
- `blocks/product-layout-row.liquid`: Retained existing native tag:null and single root metadata, all controls and three actual nested presets. Added named keyboard access for explicit/inherited scrolling, safe HTML tags, resolved mobile mode and scoped CSS that overrides broad product-host mobile rules including resetting inherited grid-auto-flow/overflow. Row gaps now win against host defaults; primary-row isolation, columns and vertical alignment retain consumers. One admitted Product Column chain and all child preset trees unchanged.
- `blocks/product-layout-column.liquid`: Individually reviewed 22 controls/three presets and all 99 content/product child admissions. Native tag:null restores host media/content ordering and sticky positioning to the actual grid item. Sticky enabled columns use natural height and start alignment so full-height defaults cannot defeat stickiness; mobile remains static per host contract. Flex min-height/vertical stretch/native leaf sizing, safe tags/roles/named regions, design visibility and captured projection empty guidance repaired. All saved controls and admissions retained.

367 actual default/control/preset/host combinations, all 12 nested presets, exact admission/tree preservation, one native root per block, content/product chains with real reviewed leaves and purchase form, tag/role/label/empty/editor cases pass. A declared CSS-cascade adapter checks actual DOM matches, specificity, source order and width media for inherited/explicit mobile layouts, row gaps, host order, sticky versus full height and min-height alignment; it does not certify browser geometry. All 18 affected suites pass. This group covers 88 settings and 12 actual presets. Strict parsing, schema/reference/architecture/source hashes and CSS build pass. All 150 sections, 102 blocks and settings_data are preserved; compiled CSS matches Batch 7. Exact unsuppressed Theme Check movement is in themecheck-comparison.json.

B8-BUILDER-LAYOUTS live queue after engineering batches: native Theme Editor insertion/select/drag/reorder/nesting and saved configurations; actual tag:null markup, layout fill/min-height/sticky containing ancestors/scroll snapping, media visual order versus keyboard/AT reading order; independent row overrides and section inheritance, padding/gap extrema, 320/390/768/1280/1440px, long text/RTL/zoom/no-JS/reduced motion. All previous queues carry forward; no live Shopify QA performed.

Reference: [Shopify Theme Block schema, nested content and tag:null contract](https://shopify.dev/docs/storefronts/themes/architecture/blocks/theme-blocks/schema#tag), [native block object](https://shopify.dev/docs/api/liquid/objects/block).

PR #22 stays DRAFT. No merge/main write, Batch 9, Atelier/Signal/Terrace rollout or live Shopify QA. Continue only Batch 8 to formal engineering closure.
