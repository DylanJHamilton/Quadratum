<script type="application/json" data-product-purchase-data>
[
{% for variant in product.variants %}
  {"id":{{ variant.id | json }},"available":{{ variant.available | json }},"price":{{ variant.price | money | json }},"compare":{{ variant.compare_at_price | money | json }},"onSale":{% if variant.compare_at_price > variant.price %}true{% else %}false{% endif %},"sku":{{ variant.sku | json }},"allocations":{{ variant.selling_plan_allocations | json }}}{% unless forloop.last %},{% endunless %}
{% endfor %}
]
</script>
