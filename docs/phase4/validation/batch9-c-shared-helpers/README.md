# Batch 9 checkpoint C — shared Liquid helpers

Checkpoint B is remotely verified at `9d828a933d540925411326f22f248a28d6195f0b`. Seven Family 9 helpers reviewed: **three ACTIVE / live queued, one retained DORMANT source-reviewed, three retained deletion candidates requiring owner review**. No file was deleted or activated.

| Component | Real ownership/consumers | Review and disposition |
|---|---|---|
| `boxed-content-media` | No runtime consumer; original boxed-content-banner now owns its media inline | Retained deletion candidate. Image guard, responsive source sizes and `image_tag` exist. Legacy URL video autoplays without the active banner's pause/reduced-motion/editor policy and unescaped URL input assumes a trusted caller. Do not reactivate this duplicate. |
| `contrast-text` | 27 retained sections; 41 actual calls across forms, accounts, maps, commerce, hero and third-party surfaces | ACTIVE; fixed backwards-compatible `bg` alias while `background` retains precedence. Consumers using `bg` previously computed contrast against the fallback dark color, potentially returning white on a white surface. Preferred color remains when >=4.5; otherwise the higher-contrast black/white candidate wins. No listener/ID/editor ownership; each render is isolated. Native Shopify color objects/transparency and actual final composed backgrounds remain live checks. |
| `icon-sprite` | No runtime consumer; not the deployed `assets/icons.svg` sprite | Retained deletion candidate. Placeholder `d="..."` paths, non-Liquid placeholder comment and document-global symbol IDs make it unsuitable for activation. Actual icons use the external deployed sprite and are reviewed in D. |
| `q-disclosure` | No runtime consumer or `data-q-disclosure` JS enhancer | Retained deletion candidate. Native details mode is a plausible primitive; button mode stays hidden without a controller and repeats its fallback ID if called without identity. Raw label/class/content contracts and caller-owned ID uniqueness must be established before reuse. No new controller is invented. |
| `q-spacing` | No runtime consumer; trusted numeric inline-attribute primitive scanned only by Tailwind | RETAINED — DORMANT SOURCE REVIEWED. Four supplied numeric values map to px with zero preserved; no listener/ID/state. Does not sanitize arbitrary strings; only trusted numeric callers would be valid. No evidence justifies deletion or new wiring. |
| `theme-tokens` | `global-theme-vars` in normal and password layout heads, after compiled CSS | ACTIVE; canonical schema-backed colors/fonts/type/layout/button/badge variables retained. Added aliases consumed by existing `styles.css`, whose `--qtm-*` variables previously had no definitions and silently used defaults. Uses the same canonical values and schema units, including rem ×100 and zero. No settings/schema/delivery rewrite. Root dark override remains conditional on existing `enable_dark_mode` and matching theme attribute; no theme switcher added. Existing legacy `custom_css` compatibility output is retained although no current schema control defines it. |
| `utilities` | Same head wrapper immediately after theme-tokens | ACTIVE, unchanged. Container breakpoint uses current `container_xl`; spacing/headings read canonical tokens. Later global/section rules deliberately participate in the cascade; duplicate heading declarations are identical and are not a reason for a broad rewrite. Width/gutter cascade remains a live geometry check. |

The alias bridge restores existing controls for global body/headings, common containers, opt-in global buttons/section spacing and badges. This can visibly change stores that had customized settings previously masked by fallback values; merchant state is unchanged. Inspect actual colors, typography, spacing, password layout, long content and contrast during the scheduled live pass.

`batch9-shared-helpers.cjs` preserves the before failure and passes both contrast parameter contracts, precedence/blank/default handling, all **41 actual consumer call expressions**, token graphs at schema default/min/max, zero values and explicit dark=false. Its opaque-color adapter and CSS parser do not certify native Shopify color drops or browser cascade. Seven native strict Liquid parses, affected existing fixtures, source hashes and reference/schema checks are recorded alongside consumer evidence. All 27 contrast consumers are enumerated in `consumers.json`.

Global NOT REVIEWED after C: **10**, all remaining asset entries in Family 9. Batch 1 deferred count remains zero. Active queues, 605 historical candidates and 7,830 icon visual entries remain separate.
