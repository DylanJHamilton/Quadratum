# Batch 8 checkpoint — product identity and metadata

Eight individually reviewed Theme Blocks: Title, Subtitle, Vendor, Type, Tags, SKU, Description, Metafield. 209 settings and all 24 built-in presets reviewed with actual source consumers. One newly required controller is inventoried under Family 8. **25/107 Family 8 entries now have dispositions: 24 live queued, one dormant retained; 82 remain NOT REVIEWED.** Original 102 blocks remain intact; 20 have individual dispositions. The dependency count grew from four to five because the SKU/description lifecycle defect required an explicit shared controller.

`settings-and-presets.json` records every setting/default/option/consumer, all presets and individual repairs. `theme-block-product-identity.cjs` performs 444 default/preset/control renders plus targeted missing/typed/escaped metadata and media checks. `theme-block-product-metadata.cjs` checks singleton/late/dynamic boot, two instances of the same product, native and scoped variant events, actual form/picker producer bodies, progressive description controls/short-long/resize/image load/focus/selection, unload/reload/reorder/removal and stale callbacks. All 15 affected suites pass, including seven Phase 3 suites and existing host/purchase/layout/primitive regressions. Native strict parsing covers all ten changed block sources plus the one-line layout dependency load. All source hashes, schema/reference/architecture/merchant state and CSS build pass. All 150 sections and settings_data remain identical to Batch 7; compiled theme.css is unchanged.

The vendor file was a copied subtitle implementation; actual product.vendor is now the insertion default while every legacy setting/source remains available. Subtitle custom rich text retains valid nested markup; plain metadata is escaped. Tag slugs are applied only to the tag, preserving locale/catalog paths. Product Type accepts the previously saved malformed Fade right option and now offers the corrected value. Explicit mobile sizes and long-content containment no longer depend on a clamp with incompatible saved bounds.

SKU JSON lives in an escaped HTML data attribute and contains only IDs/SKUs. Variant events are accepted only from the same host, an explicit matching section ID, or an unambiguous single-product scope for legacy document events. The form and picker receive only the two-line producer-target repair needed for this contract; their full component reviews remain NOT REVIEWED. The new global deferred asset is needed to initialize the first metadata block inserted after editor boot; no host structure or app admission changed.

Descriptions remain complete without JavaScript or in the editor. Only initialized, overflowing content collapses; short content has no toggle. Focus expands clipped content, image/font/size changes are remeasured, labels and aria-controls remain valid, and unloaded instances release listeners/observers. Metafield scalar false/zero, lists, native rich text, references, images, videos and generic files receive type-aware handling. Native filters are not called for unsupported list types. All native metafield/drop/media behavior is still a Shopify live gate.

Unsuppressed Theme Check output and exact check-count movement are in `themecheck-comparison.json`; the existing three errors are outside this group. No diagnostics are suppressed.

B8-PRODUCT-IDENTITY live queue: actual Shopify preset/saved-setting insertion and editor lifecycle; true vendor/metafield drop values and localized tag/type destinations; real file/image/video URLs; duplicate same-product hosts and variant/SKU transitions; description image/font reflow, keyboard/AT/no-JS, 320/390/768/1280/1440px, RTL/zoom/motion/theme contrast. Offline adapters are not native/browser certification.

References: [metafield object/list/value contracts](https://shopify.dev/docs/api/liquid/objects/metafield), [metafield text](https://shopify.dev/docs/api/liquid/filters/metafield_text), [metafield tags](https://shopify.dev/docs/api/liquid/filters/metafield_tag), [generic files](https://shopify.dev/docs/api/liquid/objects/generic_file). PR #22 stays DRAFT. No Batch 9, preset-family rollout or live Shopify QA began.

Product identity checkpoint Theme Check reconciliation: **500 findings unchanged: three errors, 497 warnings.** The only in-group diagnostic is the existing Metafield settings-count warning; no new diagnostics or suppressions.
