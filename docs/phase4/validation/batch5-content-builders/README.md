# Batch 5 — Column Builder, Grid Builder, Split Content

Three individually reviewed hosts are live queued; the existing icon helper is exercised in its third consumer. The separate Theme Block builders/chains are retained, with child reviews pending.

- All seven Phase 3 + 52 Phase 4 suites pass (59 total).
- Actual host/block presets, every select/range/checkbox endpoint, native color/image adapters, scoped CSS, tokens, empty/editor states, actions and independent instances are covered. Pixel layout, contrast, native SVG/media and Shopify editor behavior remain live acceptance.
- Schema/reference/protected-state checks and CSS build pass. 150 sections and 102 Theme Blocks remain; protected merchant state unchanged.
- Shopify CLI 4.8.0 exits 1: 632 diagnostics (16 errors / 616 warnings), one image-dimension error and three unused-assignment warnings removed, no added signatures or suppression.
- 32 provisional family assignments are reconciled by actual owners; their dispositions are unchanged. Batch 5 now has 43 components: 26 live queued, one dormant retained, 16 NOT REVIEWED. This does not approve pending forms/social/Theme Block/global dependencies.
- B5-BUILDERS live queue remains after Batch 9. Historical GitGuardian checksum false-positive incident 37480944 remains for owner triage; it is not reported as a passed check.
