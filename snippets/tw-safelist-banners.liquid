{%- comment -%}
Tailwind JIT safelist for classes assembled via settings/logic.
This file only needs to exist in the repo so Tailwind’s content scan sees it.
{%- endcomment -%}
<div class="hidden">
  <!-- text alignment -->
  <div class="text-left text-center text-right"></div>

  <!-- layout variants -->
  <div class="max-w-2xl max-w-3xl max-w-4xl"></div>
  <div class="px-4 md:px-6 p-6 md:p-8 md:p-10 p-8 md:p-12"></div>
  <div class="py-8 md:py-12 py-12 md:py-16 py-16 md:py-24"></div>

  <!-- grid spans -->
  <div class="lg:col-span-4 lg:col-span-5 lg:col-span-6 lg:col-span-7"></div>

  <!-- placement (used by slideshow) -->
  <div class="items-start items-center items-end justify-start justify-center justify-end"></div>
  <div class="md:items-start md:items-center md:items-end md:justify-start md:justify-center md:justify-end"></div>

  <!-- token-based colors -->
  <div class="bg-card text-foreground ring-border border-input bg-background bg-primary text-primary-foreground hover:bg-accent hover:text-accent-foreground"></div>

  <!-- buttons/rings -->
  <div class="focus-visible:ring-2 focus-visible:ring-offset-2"></div>

  <!-- direction-aware (if you enable rtl plugin later) -->
  <div class="rtl:text-right rtl:flex-row-reverse"></div>
</div>
