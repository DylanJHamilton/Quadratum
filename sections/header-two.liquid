{%- comment -%}
  Section: Header Two — Mega Menu Builder
  Quadratum Theme

  This version does NOT use Shopify Navigation as the primary menu source.
  Header nav items and mega-menu content are built from section blocks.

  Global contracts preserved:
    data-search-popup-open
    data-cart-drawer-open
    data-cart-count
{%- endcomment -%}

{{ 'header-two.css' | asset_url | stylesheet_tag }}
<script src="{{ 'header-two.js' | asset_url }}" defer></script>

{%- liquid
  assign header_placement = section.settings.header_placement | default: 'two_rows'
  assign header_width = section.settings.header_width | default: 'full_width'
  assign sticky_mode = section.settings.sticky_mode | default: 'sticky_shadow'
  assign search_display = section.settings.search_display | default: 'inline'
  assign search_placeholder = section.settings.search_placeholder | default: 'Search products, collections, and pages'
  assign mobile_drawer_style = section.settings.mobile_drawer_style | default: 'left'

  assign sticky_positioned = false
  if sticky_mode == 'sticky' or sticky_mode == 'sticky_shadow' or sticky_mode == 'hide_on_scroll'
    assign sticky_positioned = true
  endif
-%}

<style>
  #shopify-section-{{ section.id }}{
    margin: 0 !important;
    padding: 0 !important;
    width: 100% !important;
    max-width: none !important;
  }

  {%- if sticky_positioned -%}
    #shopify-section-{{ section.id }}{
      position: sticky;
      top: 0;
      z-index: {{ section.settings.sticky_z_index }};
      transition: transform .28s ease;
    }

    #shopify-section-{{ section.id }}.qh2b-section-hidden{
      transform: translateY(-100%);
    }
  {%- endif -%}

  #HeaderTwoAnnounce-{{ section.id }}{
    --qh2b-announce-bg: {{ section.settings.announcement_bg }};
    --qh2b-announce-text: {{ section.settings.announcement_text_color }};
    --qh2b-announce-font-size: {{ section.settings.announcement_font_size }}px;
  }

  #HeaderTwo-{{ section.id }}{
    --qh2b-bg: {{ section.settings.header_bg }};
    --qh2b-text: {{ section.settings.header_text }};
    --qh2b-muted: {{ section.settings.muted_text }};
    --qh2b-link: {{ section.settings.header_link }};
    --qh2b-link-hover: {{ section.settings.header_link_hover }};
    --qh2b-border: {{ section.settings.header_border }};
    --qh2b-dropdown-bg: {{ section.settings.dropdown_bg }};
    --qh2b-dropdown-text: {{ section.settings.dropdown_text }};
    --qh2b-dropdown-muted: {{ section.settings.dropdown_muted }};
    --qh2b-dropdown-border: {{ section.settings.dropdown_border }};
    --qh2b-badge-bg: {{ section.settings.badge_bg }};
    --qh2b-badge-text: {{ section.settings.badge_text }};
    --qh2b-button-bg: {{ section.settings.button_bg }};
    --qh2b-button-text: {{ section.settings.button_text }};
    --qh2b-button-hover-bg: {{ section.settings.button_hover_bg }};
    --qh2b-button-hover-text: {{ section.settings.button_hover_text }};
    --qh2b-menu-font-size: {{ section.settings.menu_font_size }}px;
    --qh2b-menu-font-weight: {{ section.settings.menu_font_weight }};
    --qh2b-dropdown-font-size: {{ section.settings.dropdown_font_size }}px;
    --qh2b-logo-desktop: {{ section.settings.logo_height_desktop }}px;
    --qh2b-logo-mobile: {{ section.settings.logo_height_mobile }}px;
    --qh2b-pad-y: {{ section.settings.header_padding_y }}px;
    --qh2b-pad-x: {{ section.settings.header_padding_x }}px;
    --qh2b-menu-gap: {{ section.settings.menu_gap }}px;
    --qh2b-action-gap: {{ section.settings.action_gap }}px;
    --qh2b-search-width: {{ section.settings.search_max_width }}px;
    --qh2b-dropdown-pad: {{ section.settings.dropdown_padding }}px;
    --qh2b-button-radius: {{ section.settings.button_radius }}px;
    --qh2b-dropdown-radius: {{ section.settings.dropdown_radius }}px;
    --qh2b-card-radius: {{ section.settings.card_radius }}px;
    --qh2b-contained-width: 1180px;
    --qh2b-wide-width: 1440px;
    --qh2b-drawer-width: {{ section.settings.mobile_drawer_width }}px;
    --qh2b-overlay-z: {{ section.settings.mobile_overlay_z_index }};
  }
</style>

{%- if section.settings.announcement_enable and section.settings.announcement_text != blank -%}
  <div
    id="HeaderTwoAnnounce-{{ section.id }}"
    class="qh2bAnnounce qh2bAnnounce--align-{{ section.settings.announcement_alignment }}"
    data-qh2b-announce
  >
    <div class="qh2bAnnounce__inner">
      <p class="qh2bAnnounce__text">
        <span>{{ section.settings.announcement_text }}</span>
        {%- if section.settings.announcement_link_label != blank and section.settings.announcement_link_url != blank -%}
          <a class="qh2bAnnounce__link" href="{{ section.settings.announcement_link_url }}">
            {{ section.settings.announcement_link_label | escape }}
          </a>
        {%- endif -%}
      </p>

      {%- if section.settings.announcement_show_close -%}
        <button class="qh2bAnnounce__close" type="button" aria-label="Dismiss announcement" data-qh2b-announce-close>
          <svg viewBox="0 0 24 24" fill="none" aria-hidden="true">
            <path d="M6 6L18 18M18 6L6 18" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"/>
          </svg>
        </button>
      {%- endif -%}
    </div>
  </div>
{%- endif -%}

<header
  id="HeaderTwo-{{ section.id }}"
  class="qh2b qh2b--placement-{{ header_placement }} qh2b--width-{{ header_width }} qh2b--sticky-{{ sticky_mode }} qh2b--drawer-{{ mobile_drawer_style }} qh2b--nav-align-{{ section.settings.nav_alignment }}"
  data-qh2b-header
  data-qh2b-sticky-mode="{{ sticky_mode }}"
  aria-label="Site header"
>
  <div class="qh2b__shell">
    <div class="qh2b__top-row">
      <button
        class="qh2b__mobile-toggle qh2b__icon-button"
        type="button"
        aria-label="Open menu"
        aria-expanded="false"
        aria-controls="HeaderTwoMobileDrawer-{{ section.id }}"
        data-qh2b-mobile-open
      >
        <svg class="qh2b__icon" viewBox="0 0 24 24" fill="none" aria-hidden="true">
          <path d="M4 7H20M4 12H20M4 17H20" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"/>
        </svg>
      </button>

      <a class="qh2b__brand" href="{{ routes.root_url }}" aria-label="{{ shop.name | escape }}">
        {%- if settings.logo_primary != blank -%}
          <img class="qh2b__logo qh2b__logo--desktop" src="{{ settings.logo_primary | image_url: width: 700 }}" alt="{{ shop.name | escape }}" loading="eager">
        {%- endif -%}

        {%- if settings.logo_mobile != blank -%}
          <img class="qh2b__logo qh2b__logo--mobile" src="{{ settings.logo_mobile | image_url: width: 500 }}" alt="{{ shop.name | escape }}" loading="eager">
        {%- endif -%}

        {%- if settings.logo_primary == blank and settings.logo_mobile == blank -%}
          <span class="qh2b__brand-name">{{ shop.name }}</span>
        {%- endif -%}
      </a>

      {%- if header_placement == 'one_row' -%}
        <nav class="qh2b__nav qh2b__nav--inline" aria-label="Primary navigation">
          <ul class="qh2b__nav-list">
            {%- for block in section.blocks -%}
              {%- case block.type -%}
                {%- when 'nav_link' -%}
                  {%- unless block.settings.hide_desktop -%}
                    <li class="qh2b__nav-item" {{ block.shopify_attributes }}>
                      <a class="qh2b__nav-link{% if block.settings.url == request.path %} qh2b__nav-link--active{% endif %}" href="{{ block.settings.url | default: '#' }}" {% if block.settings.open_new_tab %}target="_blank" rel="noopener"{% endif %}>
                        <span>{{ block.settings.label | default: 'Menu item' }}</span>
                        {%- if block.settings.badge != blank -%}<span class="qh2b__badge">{{ block.settings.badge }}</span>{%- endif -%}
                      </a>
                    </li>
                  {%- endunless -%}

                {%- when 'mega_menu' -%}
                  {%- unless block.settings.hide_desktop -%}
                    {%- liquid
                      assign mega_key = block.settings.mega_key | default: block.settings.label | handleize
                      assign panel_id = 'HeaderTwoMega-' | append: section.id | append: '-' | append: block.id
                    -%}
                    <li class="qh2b__nav-item qh2b__nav-item--mega" data-qh2b-mega-item {{ block.shopify_attributes }}>
                      <div class="qh2b__nav-pair">
                        <a class="qh2b__nav-link" href="{{ block.settings.url | default: '#' }}">
                          <span>{{ block.settings.label | default: 'Mega menu' }}</span>
                          {%- if block.settings.badge != blank -%}<span class="qh2b__badge">{{ block.settings.badge }}</span>{%- endif -%}
                        </a>
                        <button class="qh2b__nav-caret" type="button" aria-label="Open {{ block.settings.label | escape }} menu" aria-expanded="false" aria-controls="{{ panel_id }}" data-qh2b-mega-trigger>
                          <svg class="qh2b__caret-icon" viewBox="0 0 12 8" fill="none" aria-hidden="true"><path d="M1 1.25L6 6.25L11 1.25" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
                        </button>
                      </div>

                      <div id="{{ panel_id }}" class="qh2b__mega" hidden data-qh2b-mega-panel>
                        <div class="qh2b__mega-inner qh2b__mega-inner--{{ block.settings.panel_layout }}" style="--qh2b-mega-cols: {{ block.settings.columns }};">
                          <div class="qh2b__mega-columns">
                            {%- for column_block in section.blocks -%}
                              {%- if column_block.type == 'mega_column' -%}
                                {%- assign column_parent = column_block.settings.belongs_to | handleize -%}
                                {%- if column_parent == mega_key -%}
                                  {%- assign column_key = column_block.settings.column_key | default: column_block.settings.heading | handleize -%}
                                  <div class="qh2b__mega-column" {{ column_block.shopify_attributes }}>
                                    {%- if column_block.settings.heading != blank -%}
                                      {%- if column_block.settings.heading_url != blank -%}
                                        <a class="qh2b__mega-heading" href="{{ column_block.settings.heading_url }}">{{ column_block.settings.heading }}</a>
                                      {%- else -%}
                                        <p class="qh2b__mega-heading">{{ column_block.settings.heading }}</p>
                                      {%- endif -%}
                                    {%- endif -%}

                                    <ul class="qh2b__mega-links">
                                      {%- for link_block in section.blocks -%}
                                        {%- if link_block.type == 'mega_link' -%}
                                          {%- assign link_parent = link_block.settings.belongs_to | handleize -%}
                                          {%- assign link_column = link_block.settings.column_key | handleize -%}
                                          {%- if link_parent == mega_key and link_column == column_key -%}
                                            <li {{ link_block.shopify_attributes }}>
                                              <a class="qh2b__mega-link" href="{{ link_block.settings.url | default: '#' }}" {% if link_block.settings.open_new_tab %}target="_blank" rel="noopener"{% endif %}>
                                                <span>{{ link_block.settings.label | default: 'Mega link' }}</span>
                                                {%- if link_block.settings.badge != blank -%}<span class="qh2b__mini-badge">{{ link_block.settings.badge }}</span>{%- endif -%}
                                              </a>
                                            </li>
                                          {%- endif -%}
                                        {%- endif -%}
                                      {%- endfor -%}
                                    </ul>
                                  </div>
                                {%- endif -%}
                              {%- endif -%}
                            {%- endfor -%}
                          </div>

                          <div class="qh2b__mega-aside">
                            {%- for promo_block in section.blocks -%}
                              {%- if promo_block.type == 'promo_item' -%}
                                {%- assign promo_parent = promo_block.settings.belongs_to | handleize -%}
                                {%- if promo_parent == mega_key -%}
                                  <a class="qh2b__promo-card" href="{{ promo_block.settings.url | default: '#' }}" {{ promo_block.shopify_attributes }}>
                                    {%- if promo_block.settings.image != blank -%}
                                      <span class="qh2b__promo-media">
                                        <img class="qh2b__promo-img" src="{{ promo_block.settings.image | image_url: width: 520 }}" alt="{{ promo_block.settings.heading | escape }}" loading="lazy">
                                      </span>
                                    {%- endif -%}
                                    <span class="qh2b__promo-body">
                                      {%- if promo_block.settings.eyebrow != blank -%}<span class="qh2b__promo-eyebrow">{{ promo_block.settings.eyebrow }}</span>{%- endif -%}
                                      {%- if promo_block.settings.heading != blank -%}<span class="qh2b__promo-heading">{{ promo_block.settings.heading }}</span>{%- endif -%}
                                      {%- if promo_block.settings.text != blank -%}<span class="qh2b__promo-text">{{ promo_block.settings.text }}</span>{%- endif -%}
                                      {%- if promo_block.settings.link_label != blank -%}<span class="qh2b__promo-link-label">{{ promo_block.settings.link_label }}</span>{%- endif -%}
                                    </span>
                                  </a>
                                {%- endif -%}
                              {%- elsif promo_block.type == 'featured_collection' -%}
                                {%- assign collection_parent = promo_block.settings.belongs_to | handleize -%}
                                {%- if collection_parent == mega_key and promo_block.settings.collection != blank -%}
                                  {%- liquid
                                    assign featured_collection = promo_block.settings.collection
                                    assign collection_title = promo_block.settings.custom_heading
                                    if collection_title == blank
                                      assign collection_title = featured_collection.title
                                    endif
                                  -%}
                                  <a class="qh2b__featured" href="{{ featured_collection.url }}" {{ promo_block.shopify_attributes }}>
                                    {%- if promo_block.settings.show_image -%}
                                      <span class="qh2b__featured-media">
                                        {%- if featured_collection.image != blank -%}
                                          <img class="qh2b__featured-img" src="{{ featured_collection.image | image_url: width: 220, height: 220, crop: 'center' }}" alt="{{ collection_title | escape }}" loading="lazy">
                                        {%- elsif featured_collection.featured_image != blank -%}
                                          <img class="qh2b__featured-img" src="{{ featured_collection.featured_image | image_url: width: 220, height: 220, crop: 'center' }}" alt="{{ collection_title | escape }}" loading="lazy">
                                        {%- endif -%}
                                      </span>
                                    {%- endif -%}
                                    <span class="qh2b__featured-body">
                                      <span class="qh2b__featured-eyebrow">Featured collection</span>
                                      <span class="qh2b__featured-title">{{ collection_title }}</span>
                                    </span>
                                  </a>
                                {%- endif -%}
                              {%- endif -%}
                            {%- endfor -%}
                          </div>
                        </div>
                      </div>
                    </li>
                  {%- endunless -%}
              {%- endcase -%}
            {%- endfor -%}
          </ul>
        </nav>
      {%- endif -%}

      {%- if search_display == 'inline' -%}
        <div class="qh2b__search">
          <form class="qh2b__search-form" action="{{ routes.search_url }}" method="get" role="search">
            <label class="qh2b__visually-hidden" for="HeaderTwoSearch-{{ section.id }}">Search</label>
            <input id="HeaderTwoSearch-{{ section.id }}" class="qh2b__search-input" type="search" name="q" value="{{ search.terms | escape }}" placeholder="{{ search_placeholder | escape }}" autocomplete="off">
            {%- if section.settings.show_inline_search_button -%}
              <button class="qh2b__search-submit" type="submit">Search</button>
            {%- endif -%}
          </form>
        </div>
      {%- endif -%}

      <div class="qh2b__actions">
        {%- if search_display == 'popup' -%}
          <button class="qh2b__icon-button" type="button" data-search-popup-open aria-label="Open search" aria-haspopup="dialog">
            <svg class="qh2b__icon" viewBox="0 0 24 24" fill="none" aria-hidden="true"><circle cx="11" cy="11" r="6.5" stroke="currentColor" stroke-width="1.8"/><path d="M16 16L21 21" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"/></svg>
            <span class="qh2b__action-label">Search</span>
          </button>
        {%- elsif search_display == 'page' -%}
          <a class="qh2b__icon-button" href="{{ routes.search_url }}" aria-label="Search">
            <svg class="qh2b__icon" viewBox="0 0 24 24" fill="none" aria-hidden="true"><circle cx="11" cy="11" r="6.5" stroke="currentColor" stroke-width="1.8"/><path d="M16 16L21 21" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"/></svg>
            <span class="qh2b__action-label">Search</span>
          </a>
        {%- endif -%}

        {%- if section.settings.show_account -%}
          <details class="qh2b__account" data-qh2b-account>
            <summary class="qh2b__icon-button qh2b__account-summary" aria-label="Account">
              <svg class="qh2b__icon" viewBox="0 0 24 24" fill="none" aria-hidden="true"><circle cx="12" cy="8" r="3.4" stroke="currentColor" stroke-width="1.8"/><path d="M5 20C5.75 16.5 8.25 14.55 12 14.55C15.75 14.55 18.25 16.5 19 20" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"/></svg>
              {%- if section.settings.account_display != 'icon' -%}<span class="qh2b__action-label">{% if customer %}Account{% else %}Sign in{% endif %}</span>{%- endif -%}
            </summary>
            <div class="qh2b__account-menu">
              {%- if customer -%}
                <a class="qh2b__account-link" href="{{ routes.account_url }}">Account</a>
                <a class="qh2b__account-link" href="{{ routes.account_url }}">Orders</a>
                <a class="qh2b__account-link" href="{{ routes.account_addresses_url }}">Addresses</a>
                {%- if section.settings.tracking_label != blank and section.settings.tracking_url != blank -%}<a class="qh2b__account-link" href="{{ section.settings.tracking_url }}">{{ section.settings.tracking_label }}</a>{%- endif -%}
                <a class="qh2b__account-link qh2b__account-link--muted" href="{{ routes.account_logout_url }}">Log out</a>
              {%- else -%}
                <a class="qh2b__account-link" href="{{ routes.account_login_url }}">Sign in</a>
                <a class="qh2b__account-link" href="{{ routes.account_register_url }}">Create account</a>
                {%- if section.settings.tracking_label != blank and section.settings.tracking_url != blank -%}<a class="qh2b__account-link" href="{{ section.settings.tracking_url }}">{{ section.settings.tracking_label }}</a>{%- endif -%}
              {%- endif -%}
            </div>
          </details>
        {%- endif -%}

        <button class="qh2b__icon-button qh2b__cart" type="button" data-cart-drawer-open aria-label="Open cart">
          <span class="qh2b__cart-icon-wrap">
            <svg class="qh2b__icon" viewBox="0 0 24 24" fill="none" aria-hidden="true"><path d="M6.5 7.5H19.5L18.25 17H7.75L6.5 7.5Z" stroke="currentColor" stroke-width="1.8" stroke-linejoin="round"/><path d="M9 7.5C9 4.95 10.15 3.5 12 3.5C13.85 3.5 15 4.95 15 7.5" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"/></svg>
            <span class="qh2b__cart-count" data-cart-count>{{ cart.item_count }}</span>
          </span>
          {%- if section.settings.cart_display == 'icon_text' -%}<span class="qh2b__action-label">Cart</span>{%- endif -%}
        </button>
      </div>
    </div>

    {%- if header_placement == 'two_rows' -%}
      <div class="qh2b__bottom-row">
        <nav class="qh2b__nav qh2b__nav--below" aria-label="Primary navigation">
          <ul class="qh2b__nav-list">
            {%- for block in section.blocks -%}
              {%- case block.type -%}
                {%- when 'nav_link' -%}
                  {%- unless block.settings.hide_desktop -%}
                    <li class="qh2b__nav-item" {{ block.shopify_attributes }}>
                      <a class="qh2b__nav-link" href="{{ block.settings.url | default: '#' }}" {% if block.settings.open_new_tab %}target="_blank" rel="noopener"{% endif %}>
                        <span>{{ block.settings.label | default: 'Menu item' }}</span>
                        {%- if block.settings.badge != blank -%}<span class="qh2b__badge">{{ block.settings.badge }}</span>{%- endif -%}
                      </a>
                    </li>
                  {%- endunless -%}

                {%- when 'mega_menu' -%}
                  {%- unless block.settings.hide_desktop -%}
                    {%- liquid
                      assign mega_key = block.settings.mega_key | default: block.settings.label | handleize
                      assign panel_id = 'HeaderTwoMega-' | append: section.id | append: '-' | append: block.id
                    -%}
                    <li class="qh2b__nav-item qh2b__nav-item--mega" data-qh2b-mega-item {{ block.shopify_attributes }}>
                      <div class="qh2b__nav-pair">
                        <a class="qh2b__nav-link" href="{{ block.settings.url | default: '#' }}">
                          <span>{{ block.settings.label | default: 'Mega menu' }}</span>
                          {%- if block.settings.badge != blank -%}<span class="qh2b__badge">{{ block.settings.badge }}</span>{%- endif -%}
                        </a>
                        <button class="qh2b__nav-caret" type="button" aria-label="Open {{ block.settings.label | escape }} menu" aria-expanded="false" aria-controls="{{ panel_id }}" data-qh2b-mega-trigger>
                          <svg class="qh2b__caret-icon" viewBox="0 0 12 8" fill="none" aria-hidden="true"><path d="M1 1.25L6 6.25L11 1.25" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
                        </button>
                      </div>

                      <div id="{{ panel_id }}" class="qh2b__mega" hidden data-qh2b-mega-panel>
                        <div class="qh2b__mega-inner qh2b__mega-inner--{{ block.settings.panel_layout }}" style="--qh2b-mega-cols: {{ block.settings.columns }};">
                          <div class="qh2b__mega-columns">
                            {%- for column_block in section.blocks -%}
                              {%- if column_block.type == 'mega_column' -%}
                                {%- assign column_parent = column_block.settings.belongs_to | handleize -%}
                                {%- if column_parent == mega_key -%}
                                  {%- assign column_key = column_block.settings.column_key | default: column_block.settings.heading | handleize -%}
                                  <div class="qh2b__mega-column" {{ column_block.shopify_attributes }}>
                                    {%- if column_block.settings.heading != blank -%}
                                      {%- if column_block.settings.heading_url != blank -%}
                                        <a class="qh2b__mega-heading" href="{{ column_block.settings.heading_url }}">{{ column_block.settings.heading }}</a>
                                      {%- else -%}
                                        <p class="qh2b__mega-heading">{{ column_block.settings.heading }}</p>
                                      {%- endif -%}
                                    {%- endif -%}

                                    <ul class="qh2b__mega-links">
                                      {%- for link_block in section.blocks -%}
                                        {%- if link_block.type == 'mega_link' -%}
                                          {%- assign link_parent = link_block.settings.belongs_to | handleize -%}
                                          {%- assign link_column = link_block.settings.column_key | handleize -%}
                                          {%- if link_parent == mega_key and link_column == column_key -%}
                                            <li {{ link_block.shopify_attributes }}>
                                              <a class="qh2b__mega-link" href="{{ link_block.settings.url | default: '#' }}" {% if link_block.settings.open_new_tab %}target="_blank" rel="noopener"{% endif %}>
                                                <span>{{ link_block.settings.label | default: 'Mega link' }}</span>
                                                {%- if link_block.settings.badge != blank -%}<span class="qh2b__mini-badge">{{ link_block.settings.badge }}</span>{%- endif -%}
                                              </a>
                                            </li>
                                          {%- endif -%}
                                        {%- endif -%}
                                      {%- endfor -%}
                                    </ul>
                                  </div>
                                {%- endif -%}
                              {%- endif -%}
                            {%- endfor -%}
                          </div>

                          <div class="qh2b__mega-aside">
                            {%- for promo_block in section.blocks -%}
                              {%- if promo_block.type == 'promo_item' -%}
                                {%- assign promo_parent = promo_block.settings.belongs_to | handleize -%}
                                {%- if promo_parent == mega_key -%}
                                  <a class="qh2b__promo-card" href="{{ promo_block.settings.url | default: '#' }}" {{ promo_block.shopify_attributes }}>
                                    {%- if promo_block.settings.image != blank -%}
                                      <span class="qh2b__promo-media">
                                        <img class="qh2b__promo-img" src="{{ promo_block.settings.image | image_url: width: 520 }}" alt="{{ promo_block.settings.heading | escape }}" loading="lazy">
                                      </span>
                                    {%- endif -%}
                                    <span class="qh2b__promo-body">
                                      {%- if promo_block.settings.eyebrow != blank -%}<span class="qh2b__promo-eyebrow">{{ promo_block.settings.eyebrow }}</span>{%- endif -%}
                                      {%- if promo_block.settings.heading != blank -%}<span class="qh2b__promo-heading">{{ promo_block.settings.heading }}</span>{%- endif -%}
                                      {%- if promo_block.settings.text != blank -%}<span class="qh2b__promo-text">{{ promo_block.settings.text }}</span>{%- endif -%}
                                      {%- if promo_block.settings.link_label != blank -%}<span class="qh2b__promo-link-label">{{ promo_block.settings.link_label }}</span>{%- endif -%}
                                    </span>
                                  </a>
                                {%- endif -%}
                              {%- elsif promo_block.type == 'featured_collection' -%}
                                {%- assign collection_parent = promo_block.settings.belongs_to | handleize -%}
                                {%- if collection_parent == mega_key and promo_block.settings.collection != blank -%}
                                  {%- liquid
                                    assign featured_collection = promo_block.settings.collection
                                    assign collection_title = promo_block.settings.custom_heading
                                    if collection_title == blank
                                      assign collection_title = featured_collection.title
                                    endif
                                  -%}
                                  <a class="qh2b__featured" href="{{ featured_collection.url }}" {{ promo_block.shopify_attributes }}>
                                    {%- if promo_block.settings.show_image -%}
                                      <span class="qh2b__featured-media">
                                        {%- if featured_collection.image != blank -%}
                                          <img class="qh2b__featured-img" src="{{ featured_collection.image | image_url: width: 220, height: 220, crop: 'center' }}" alt="{{ collection_title | escape }}" loading="lazy">
                                        {%- elsif featured_collection.featured_image != blank -%}
                                          <img class="qh2b__featured-img" src="{{ featured_collection.featured_image | image_url: width: 220, height: 220, crop: 'center' }}" alt="{{ collection_title | escape }}" loading="lazy">
                                        {%- endif -%}
                                      </span>
                                    {%- endif -%}
                                    <span class="qh2b__featured-body">
                                      <span class="qh2b__featured-eyebrow">Featured collection</span>
                                      <span class="qh2b__featured-title">{{ collection_title }}</span>
                                    </span>
                                  </a>
                                {%- endif -%}
                              {%- endif -%}
                            {%- endfor -%}
                          </div>
                        </div>
                      </div>
                    </li>
                  {%- endunless -%}
              {%- endcase -%}
            {%- endfor -%}
          </ul>
        </nav>
      </div>
    {%- endif -%}
  </div>

  <div id="HeaderTwoMobileOverlay-{{ section.id }}" class="qh2b__mobile-overlay" hidden data-qh2b-mobile-close></div>

  <aside
    id="HeaderTwoMobileDrawer-{{ section.id }}"
    class="qh2b__mobile-drawer"
    hidden
    aria-hidden="true"
    aria-label="Mobile navigation"
    data-qh2b-mobile-drawer
  >
    <div class="qh2b__mobile-head">
      <a class="qh2b__mobile-brand" href="{{ routes.root_url }}" aria-label="{{ shop.name | escape }}">
        {%- if settings.logo_mobile != blank -%}
          <img class="qh2b__mobile-logo" src="{{ settings.logo_mobile | image_url: width: 420 }}" alt="{{ shop.name | escape }}" loading="eager">
        {%- elsif settings.logo_primary != blank -%}
          <img class="qh2b__mobile-logo" src="{{ settings.logo_primary | image_url: width: 420 }}" alt="{{ shop.name | escape }}" loading="eager">
        {%- else -%}
          <span class="qh2b__mobile-brand-name">{{ shop.name }}</span>
        {%- endif -%}
      </a>

      <button class="qh2b__mobile-close" type="button" aria-label="Close menu" data-qh2b-mobile-close>
        <span aria-hidden="true"></span>
        <span aria-hidden="true"></span>
      </button>
    </div>

    {%- if search_display != 'none' -%}
      <div class="qh2b__mobile-search">
        {%- if search_display == 'popup' -%}
          <button class="qh2b__mobile-search-button" type="button" data-search-popup-open data-qh2b-mobile-system-action>
            Search
          </button>
        {%- else -%}
          <form class="qh2b__search-form" action="{{ routes.search_url }}" method="get" role="search">
            <label class="qh2b__visually-hidden" for="HeaderTwoMobileSearch-{{ section.id }}">Search</label>
            <input id="HeaderTwoMobileSearch-{{ section.id }}" class="qh2b__search-input" type="search" name="q" placeholder="{{ search_placeholder | escape }}" autocomplete="off">
            <button class="qh2b__search-submit" type="submit">Search</button>
          </form>
        {%- endif -%}
      </div>
    {%- endif -%}

    <nav class="qh2b__mobile-nav" aria-label="Mobile navigation">
      <ul class="qh2b__mobile-list">
        {%- for block in section.blocks -%}
          {%- case block.type -%}
            {%- when 'nav_link' -%}
              {%- unless block.settings.hide_mobile -%}
                <li class="qh2b__mobile-item" {{ block.shopify_attributes }}>
                  <a class="qh2b__mobile-link" href="{{ block.settings.url | default: '#' }}" {% if block.settings.open_new_tab %}target="_blank" rel="noopener"{% endif %}>
                    <span>{{ block.settings.label | default: 'Menu item' }}</span>
                    {%- if block.settings.badge != blank -%}<span class="qh2b__badge">{{ block.settings.badge }}</span>{%- endif -%}
                  </a>
                </li>
              {%- endunless -%}

            {%- when 'mega_menu' -%}
              {%- unless block.settings.hide_mobile -%}
                {%- liquid
                  assign mega_key = block.settings.mega_key | default: block.settings.label | handleize
                  assign mobile_panel_id = 'HeaderTwoMobileMega-' | append: section.id | append: '-' | append: block.id
                -%}
                <li class="qh2b__mobile-item qh2b__mobile-item--mega" {{ block.shopify_attributes }}>
                  <div class="qh2b__mobile-row">
                    <a class="qh2b__mobile-link" href="{{ block.settings.url | default: '#' }}">
                      <span>{{ block.settings.label | default: 'Mega menu' }}</span>
                      {%- if block.settings.badge != blank -%}<span class="qh2b__badge">{{ block.settings.badge }}</span>{%- endif -%}
                    </a>
                    <button class="qh2b__mobile-subtoggle" type="button" aria-expanded="false" aria-controls="{{ mobile_panel_id }}" data-qh2b-mobile-subtoggle>
                      <svg class="qh2b__caret-icon" viewBox="0 0 12 8" fill="none" aria-hidden="true"><path d="M1 1.25L6 6.25L11 1.25" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
                    </button>
                  </div>

                  <div id="{{ mobile_panel_id }}" class="qh2b__mobile-submenu" hidden>
                    {%- for column_block in section.blocks -%}
                      {%- if column_block.type == 'mega_column' -%}
                        {%- assign column_parent = column_block.settings.belongs_to | handleize -%}
                        {%- if column_parent == mega_key -%}
                          {%- assign column_key = column_block.settings.column_key | default: column_block.settings.heading | handleize -%}
                          <div class="qh2b__mobile-column">
                            {%- if column_block.settings.heading != blank -%}<p class="qh2b__mobile-column-heading">{{ column_block.settings.heading }}</p>{%- endif -%}
                            {%- for link_block in section.blocks -%}
                              {%- if link_block.type == 'mega_link' -%}
                                {%- assign link_parent = link_block.settings.belongs_to | handleize -%}
                                {%- assign link_column = link_block.settings.column_key | handleize -%}
                                {%- if link_parent == mega_key and link_column == column_key -%}
                                  <a class="qh2b__mobile-sublink" href="{{ link_block.settings.url | default: '#' }}">{{ link_block.settings.label | default: 'Mega link' }}</a>
                                {%- endif -%}
                              {%- endif -%}
                            {%- endfor -%}
                          </div>
                        {%- endif -%}
                      {%- endif -%}
                    {%- endfor -%}
                  </div>
                </li>
              {%- endunless -%}
          {%- endcase -%}
        {%- endfor -%}
      </ul>
    </nav>

    <div class="qh2b__mobile-utilities">
      {%- if section.settings.show_account -%}
        {%- if customer -%}
          <a class="qh2b__mobile-utility" href="{{ routes.account_url }}">Account</a>
          <a class="qh2b__mobile-utility" href="{{ routes.account_addresses_url }}">Addresses</a>
        {%- else -%}
          <a class="qh2b__mobile-utility" href="{{ routes.account_login_url }}">Sign in</a>
          {%- if section.settings.mobile_show_register -%}<a class="qh2b__mobile-utility" href="{{ routes.account_register_url }}">Create account</a>{%- endif -%}
        {%- endif -%}
      {%- endif -%}

      {%- if section.settings.tracking_label != blank and section.settings.tracking_url != blank -%}
        <a class="qh2b__mobile-utility" href="{{ section.settings.tracking_url }}">{{ section.settings.tracking_label }}</a>
      {%- endif -%}

      <button class="qh2b__mobile-utility" type="button" data-cart-drawer-open data-qh2b-mobile-system-action>
        Cart <span data-cart-count>{{ cart.item_count }}</span>
      </button>
    </div>
  </aside>
</header>

{% schema %}
{
  "name": "Header Two Builder",
  "class": "qtm-header-two-section",
  "settings": [
    { "type": "header", "content": "Layout" },
    {
      "type": "select",
      "id": "header_placement",
      "label": "Header placement",
      "default": "two_rows",
      "options": [
        { "value": "one_row", "label": "One row" },
        { "value": "two_rows", "label": "Two rows" }
      ]
    },
    {
      "type": "select",
      "id": "header_width",
      "label": "Header width",
      "default": "full_width",
      "options": [
        { "value": "full_width", "label": "Full width" },
        { "value": "wide", "label": "Wide" },
        { "value": "contained", "label": "Contained" }
      ]
    },
    {
      "type": "select",
      "id": "sticky_mode",
      "label": "Sticky mode",
      "default": "sticky_shadow",
      "options": [
        { "value": "none", "label": "None" },
        { "value": "sticky", "label": "Sticky" },
        { "value": "sticky_shadow", "label": "Sticky with shadow" },
        { "value": "hide_on_scroll", "label": "Hide on scroll down" }
      ]
    },
    { "type": "range", "id": "sticky_z_index", "label": "Sticky z-index", "min": 20, "max": 500, "step": 10, "default": 90 },
    {
      "type": "select",
      "id": "nav_alignment",
      "label": "Navigation alignment",
      "default": "center",
      "options": [
        { "value": "left", "label": "Left" },
        { "value": "center", "label": "Center" },
        { "value": "right", "label": "Right" }
      ]
    },
    { "type": "header", "content": "Announcement bar" },
    { "type": "checkbox", "id": "announcement_enable", "label": "Enable announcement bar", "default": false },
    { "type": "text", "id": "announcement_text", "label": "Announcement text", "default": "Free shipping on select orders" },
    { "type": "text", "id": "announcement_link_label", "label": "Announcement link label" },
    { "type": "url", "id": "announcement_link_url", "label": "Announcement link URL" },
    {
      "type": "select",
      "id": "announcement_alignment",
      "label": "Alignment",
      "default": "center",
      "options": [
        { "value": "left", "label": "Left" },
        { "value": "center", "label": "Center" },
        { "value": "right", "label": "Right" }
      ]
    },
    { "type": "color", "id": "announcement_bg", "label": "Background", "default": "#111111" },
    { "type": "color", "id": "announcement_text_color", "label": "Text color", "default": "#ffffff" },
    { "type": "range", "id": "announcement_font_size", "label": "Font size", "min": 11, "max": 18, "step": 1, "default": 13, "unit": "px" },
    { "type": "checkbox", "id": "announcement_show_close", "label": "Show close button", "default": false },
    { "type": "header", "content": "Search" },
    {
      "type": "select",
      "id": "search_display",
      "label": "Search display",
      "default": "inline",
      "options": [
        { "value": "none", "label": "No search" },
        { "value": "inline", "label": "Inline search form" },
        { "value": "popup", "label": "Search button opens popup" },
        { "value": "page", "label": "Search button goes to search page" }
      ]
    },
    { "type": "text", "id": "search_placeholder", "label": "Search placeholder", "default": "Search products, collections, and pages" },
    { "type": "checkbox", "id": "show_inline_search_button", "label": "Show inline search button", "default": true },
    { "type": "range", "id": "search_max_width", "label": "Inline search max width", "min": 260, "max": 760, "step": 20, "default": 520, "unit": "px" },
    { "type": "header", "content": "Account and cart" },
    { "type": "checkbox", "id": "show_account", "label": "Show account", "default": true },
    {
      "type": "select",
      "id": "account_display",
      "label": "Account display",
      "default": "icon",
      "options": [
        { "value": "icon", "label": "Icon" },
        { "value": "icon_text", "label": "Icon and text" }
      ]
    },
    {
      "type": "select",
      "id": "cart_display",
      "label": "Cart display",
      "default": "icon",
      "options": [
        { "value": "icon", "label": "Icon" },
        { "value": "icon_text", "label": "Icon and text" }
      ]
    },
    { "type": "text", "id": "tracking_label", "label": "Tracking label", "default": "Track order" },
    { "type": "url", "id": "tracking_url", "label": "Tracking URL" },
    { "type": "header", "content": "Mobile drawer" },
    {
      "type": "select",
      "id": "mobile_drawer_style",
      "label": "Mobile drawer style",
      "default": "left",
      "options": [
        { "value": "left", "label": "Left drawer" },
        { "value": "right", "label": "Right drawer" },
        { "value": "fullscreen", "label": "Fullscreen" }
      ]
    },
    { "type": "range", "id": "mobile_drawer_width", "label": "Drawer width", "min": 300, "max": 520, "step": 10, "default": 420, "unit": "px" },
    { "type": "range", "id": "mobile_overlay_z_index", "label": "Mobile overlay z-index", "min": 500, "max": 9500, "step": 100, "default": 4000 },
    { "type": "checkbox", "id": "mobile_show_register", "label": "Show register link in mobile drawer", "default": true },
    { "type": "header", "content": "Colors" },
    { "type": "color", "id": "header_bg", "label": "Header background", "default": "#ffffff" },
    { "type": "color", "id": "header_text", "label": "Header text", "default": "#111111" },
    { "type": "color", "id": "muted_text", "label": "Muted text", "default": "#6b7280" },
    { "type": "color", "id": "header_link", "label": "Link color", "default": "#111111" },
    { "type": "color", "id": "header_link_hover", "label": "Link hover", "default": "#2563eb" },
    { "type": "color", "id": "header_border", "label": "Border", "default": "#e5e7eb" },
    { "type": "color", "id": "dropdown_bg", "label": "Mega menu background", "default": "#ffffff" },
    { "type": "color", "id": "dropdown_text", "label": "Mega menu text", "default": "#111111" },
    { "type": "color", "id": "dropdown_muted", "label": "Mega menu muted text", "default": "#6b7280" },
    { "type": "color", "id": "dropdown_border", "label": "Mega menu border", "default": "#e5e7eb" },
    { "type": "color", "id": "badge_bg", "label": "Badge background", "default": "#111111" },
    { "type": "color", "id": "badge_text", "label": "Badge text", "default": "#ffffff" },
    { "type": "color", "id": "button_bg", "label": "Button background", "default": "#111111" },
    { "type": "color", "id": "button_text", "label": "Button text", "default": "#ffffff" },
    { "type": "color", "id": "button_hover_bg", "label": "Button hover background", "default": "#2563eb" },
    { "type": "color", "id": "button_hover_text", "label": "Button hover text", "default": "#ffffff" },
    { "type": "header", "content": "Typography" },
    { "type": "range", "id": "menu_font_size", "label": "Menu font size", "min": 12, "max": 20, "step": 1, "default": 15, "unit": "px" },
    {
      "type": "select",
      "id": "menu_font_weight",
      "label": "Menu font weight",
      "default": "600",
      "options": [
        { "value": "400", "label": "Regular" },
        { "value": "500", "label": "Medium" },
        { "value": "600", "label": "Semibold" },
        { "value": "700", "label": "Bold" }
      ]
    },
    { "type": "range", "id": "dropdown_font_size", "label": "Mega menu font size", "min": 12, "max": 18, "step": 1, "default": 14, "unit": "px" },
    { "type": "header", "content": "Spacing" },
    { "type": "range", "id": "header_padding_y", "label": "Header vertical padding", "min": 0, "max": 32, "step": 2, "default": 12, "unit": "px" },
    { "type": "range", "id": "header_padding_x", "label": "Header horizontal padding", "min": 0, "max": 48, "step": 2, "default": 20, "unit": "px" },
    { "type": "range", "id": "menu_gap", "label": "Menu gap", "min": 0, "max": 40, "step": 2, "default": 8, "unit": "px" },
    { "type": "range", "id": "action_gap", "label": "Action gap", "min": 4, "max": 24, "step": 2, "default": 10, "unit": "px" },
    { "type": "range", "id": "dropdown_padding", "label": "Mega menu padding", "min": 12, "max": 48, "step": 2, "default": 24, "unit": "px" },
    { "type": "header", "content": "Radius" },
    { "type": "range", "id": "button_radius", "label": "Button radius", "min": 0, "max": 32, "step": 1, "default": 14, "unit": "px" },
    { "type": "range", "id": "dropdown_radius", "label": "Mega menu radius", "min": 0, "max": 32, "step": 1, "default": 18, "unit": "px" },
    { "type": "range", "id": "card_radius", "label": "Card radius", "min": 0, "max": 32, "step": 1, "default": 16, "unit": "px" },
    { "type": "header", "content": "Logo" },
    { "type": "range", "id": "logo_height_desktop", "label": "Logo height desktop", "min": 20, "max": 120, "step": 2, "default": 40, "unit": "px" },
    { "type": "range", "id": "logo_height_mobile", "label": "Logo height mobile", "min": 18, "max": 80, "step": 2, "default": 30, "unit": "px" }
  ],
  "blocks": [
    {
      "type": "nav_link",
      "name": "Menu item",
      "settings": [
        { "type": "text", "id": "label", "label": "Label", "default": "Menu item" },
        { "type": "url", "id": "url", "label": "URL" },
        { "type": "text", "id": "badge", "label": "Badge text" },
        { "type": "checkbox", "id": "open_new_tab", "label": "Open in new tab", "default": false },
        { "type": "checkbox", "id": "hide_desktop", "label": "Hide on desktop", "default": false },
        { "type": "checkbox", "id": "hide_mobile", "label": "Hide on mobile", "default": false }
      ]
    },
    {
      "type": "mega_menu",
      "name": "Mega menu item",
      "settings": [
        { "type": "text", "id": "label", "label": "Top-level label", "default": "Collections" },
        { "type": "text", "id": "mega_key", "label": "Mega key", "default": "collections", "info": "Use the same key on Mega column, Mega link, Promo item, and Featured collection blocks that belong inside this dropdown." },
        { "type": "url", "id": "url", "label": "Top-level URL" },
        { "type": "text", "id": "badge", "label": "Badge text" },
        {
          "type": "select",
          "id": "panel_layout",
          "label": "Panel layout",
          "default": "columns_promos",
          "options": [
            { "value": "columns_promos", "label": "Columns with promo rail" },
            { "value": "columns_only", "label": "Columns only" },
            { "value": "promos_right", "label": "Promos emphasized right" }
          ]
        },
        {
          "type": "select",
          "id": "columns",
          "label": "Column count",
          "default": "4",
          "options": [
            { "value": "2", "label": "2" },
            { "value": "3", "label": "3" },
            { "value": "4", "label": "4" },
            { "value": "5", "label": "5" },
            { "value": "6", "label": "6" }
          ]
        },
        { "type": "checkbox", "id": "hide_desktop", "label": "Hide on desktop", "default": false },
        { "type": "checkbox", "id": "hide_mobile", "label": "Hide on mobile", "default": false }
      ]
    },
    {
      "type": "mega_column",
      "name": "Mega column",
      "settings": [
        { "type": "text", "id": "belongs_to", "label": "Belongs to mega key", "default": "collections" },
        { "type": "text", "id": "column_key", "label": "Column key", "default": "shop-by-category", "info": "Mega Link blocks use this same column key to appear in this column." },
        { "type": "text", "id": "heading", "label": "Heading", "default": "Shop by category" },
        { "type": "url", "id": "heading_url", "label": "Heading URL" }
      ]
    },
    {
      "type": "mega_link",
      "name": "Mega link",
      "settings": [
        { "type": "text", "id": "belongs_to", "label": "Belongs to mega key", "default": "collections" },
        { "type": "text", "id": "column_key", "label": "Column key", "default": "shop-by-category" },
        { "type": "text", "id": "label", "label": "Label", "default": "Mega link" },
        { "type": "url", "id": "url", "label": "URL" },
        { "type": "text", "id": "badge", "label": "Badge text" },
        { "type": "checkbox", "id": "open_new_tab", "label": "Open in new tab", "default": false }
      ]
    },
    {
      "type": "promo_item",
      "name": "Promo item",
      "settings": [
        { "type": "text", "id": "belongs_to", "label": "Belongs to mega key", "default": "collections" },
        { "type": "image_picker", "id": "image", "label": "Image" },
        { "type": "text", "id": "eyebrow", "label": "Eyebrow", "default": "Featured" },
        { "type": "text", "id": "heading", "label": "Heading", "default": "New arrivals" },
        { "type": "textarea", "id": "text", "label": "Text", "default": "Highlight a collection, sale, or product launch." },
        { "type": "text", "id": "link_label", "label": "Link label", "default": "Shop now" },
        { "type": "url", "id": "url", "label": "URL" }
      ]
    },
    {
      "type": "featured_collection",
      "name": "Featured collection",
      "settings": [
        { "type": "text", "id": "belongs_to", "label": "Belongs to mega key", "default": "collections" },
        { "type": "collection", "id": "collection", "label": "Collection" },
        { "type": "text", "id": "custom_heading", "label": "Custom heading" },
        { "type": "checkbox", "id": "show_image", "label": "Show image", "default": true }
      ]
    }
  ],
  "presets": [
    {
      "name": "Header Two Builder",
      "blocks": [
        { "type": "nav_link", "settings": { "label": "Home" } },
        { "type": "mega_menu", "settings": { "label": "Collections", "mega_key": "collections" } },
        { "type": "mega_column", "settings": { "belongs_to": "collections", "column_key": "shop-by-category", "heading": "Shop by category" } },
        { "type": "mega_link", "settings": { "belongs_to": "collections", "column_key": "shop-by-category", "label": "Protein" } },
        { "type": "mega_link", "settings": { "belongs_to": "collections", "column_key": "shop-by-category", "label": "Pre-workout" } },
        { "type": "mega_link", "settings": { "belongs_to": "collections", "column_key": "shop-by-category", "label": "Vitamins" } },
        { "type": "mega_column", "settings": { "belongs_to": "collections", "column_key": "popular", "heading": "Popular" } },
        { "type": "mega_link", "settings": { "belongs_to": "collections", "column_key": "popular", "label": "Best sellers" } },
        { "type": "mega_link", "settings": { "belongs_to": "collections", "column_key": "popular", "label": "New arrivals" } },
        { "type": "promo_item", "settings": { "belongs_to": "collections", "eyebrow": "Featured", "heading": "Build your stack", "text": "Use this promo item for a collection, sale, or product launch.", "link_label": "Shop now" } },
        { "type": "nav_link", "settings": { "label": "Blog" } },
        { "type": "nav_link", "settings": { "label": "Learn More" } }
      ]
    }
  ]
}
{% endschema %}
