# Claude: targeted Phase 3 closure regression

Use the unpublished theme tracking `release/v1-phase-3-systemic-repair`; record its synced commit from `closure-commit-map.json` / PR #21. Do not merge, publish, reset settings or overwrite saved templates. Record PASS/FAIL with product/section configuration and reproduction for failures.

1. **Routes:** normal product URL and page URL without suffix, `/collections`; applicable legacy customer login/register/account/order/addresses/activation/reset; storefront password; issued gift-card balance/code route. Verify themed output and HTTP success.
2. **Classic variant pipeline:** Gift Card $10 → $25 → $50 → $100; compare visible price, selected state, `?variant`, submitted/cart variant and charged line price. Test browser Back/Forward, equal-price variants, sold-out and compare-at product. Repeat price/submission check on Master. Check no-JS native fallback. No imagery claim without variant-specific images.
3. **Blocking product preset insertion:** use real Theme Editor insertion for Classic, Simple, Compact, Modern Variant and Master. Confirm title, price, variants, quantity as intended, ATC, sold-out and correct submitted variant. **Compact:** specifically change denomination and compare submitted hidden ID; source has no evident option-to-ID resolver. Distinguish preset insertion from zero-block Section Rendering API defaults. Report Compact/Modern failures before further layout repair.
4. **Subscriptions:** on `selling-plans-ski-wax`, select purchase option and variant; confirm intended `selling_plan` in request and cart allocation. Test optional one-time and required-plan behavior, including variant availability. Cover main sections and Product Form block.
5. **Locale:** search generated DOM/accessibility labels for `Translation missing:`; expected zero on tested routes.
6. **Contrast:** new/default Hero CTAs, Video Hero kicker/subheading/primary button, Grid hero/tile without media, Footer Two body (spot-check other footer body overrides), H5 visible search and populated cart badge, cross-sell Add to Bag, Subscribe and Verified Purchase badge. Measure computed foreground/background and visual result with media/no media, overlay on/off, light/dark backgrounds. Explicit saved overrides are retained.
7. **Social URLs:** populate Facebook, Instagram, Pinterest, TikTok, X, YouTube and LinkedIn; verify Footer Two, Footer Three, Content Social Links. Blank network absent; block-specific URLs retain precedence.
8. **Fields and variants:** consent checkbox/radio must avoid full-width text geometry; text fields remain normal. Enhanced picker alone is visually presented after JS initialization; native select still submits. Verify real radios, actual option name, keyboard selection and focus.
9. **H5 predictive search:** real typing and suggestions, no results, ArrowUp/Down, Enter, Escape, outside dismissal; clear query during pending response and ensure it stays closed. Verify narrow-screen placement and locale search destination.
10. **H5 mobile drawer:** focus enters close button, Tab stays in dialog, Escape/overlay/close restores opener, scroll unlocks; repeat with reduced motion and rapid open/close.
11. **Headings:** inspect new insertions on representative homepage/product/collection/playground. Reusable sections default H2; explicit H1 choice works. Existing saved H1 values intentionally remain unchanged.
12. **Wishlist/recommendations:** saved item fetch targets `product-wishlist-card-renderer` on product URL and returns 200 + actual card; remove link works. Confirm product recommendations load from the configured section endpoint.
13. **Empty sections:** Stats with zero blocks and configured-empty values, Slideshow, Web3 slideshow and representative Cards/Media Gallery/Timeline helper. Storefront collapses; editor provides prompt; populated content renders. Web3 single-hero mode must remain visible from settings.
14. **Icon/default and motion:** CTA Icon List default and saved QA configuration load shipped worm icon; optional HTTPS host works. Existing icon duration/easing/lift remains unchanged. CTA Web3 effects respect reduced motion and do not throw a root-reference error.
15. **Blog categories:** removed Show count control is absent; existing category/tag navigation remains correct.

Stop after targeted regression. P0-3 remains open until real preset insertion/purchase results resolve it. Previously pending editor-only scenarios outside this changed-behavior queue remain on the historical QA register; this handoff does not certify them.
