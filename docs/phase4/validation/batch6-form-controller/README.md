Batch 6 scoped controller checkpoint. Both legacy JS entry points are source-reviewed/live queued; all host/snippet/style reviews remain independently pending. Native and controller regressions use explicit platform/DOM/provider adapters and do not submit forms. Structural/reference/merchant checks and CSS build pass. Native Theme Check debt is unsuppressed; one new orphan warning inherits the existing whole-project graph failure. The focused native graph proves the new helper's direct render relationship and is not a substitute for the unmodified native checker. See engineering report for exact counts, remaining work and live queue after Batch 9.
