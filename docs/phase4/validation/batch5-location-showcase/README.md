# Batch 5 — Location Panels and 3D Showcase

Individual source/settings/preset dispositions: both hosts, scoped controller and owned model runtime are REQUIRES LIVE VERIFICATION.

Affected actual-source fixture passes; schema/reference/protected state and CSS build pass. 150 sections and 102 Theme Blocks remain, merchant state unchanged. Vendor code is excluded from Tailwind utility extraction; generated CSS remains byte-identical. Shopify CLI 4.8.0 exits 1: 631 diagnostics (16 errors / 615 warnings), one obsolete variable-name warning removed and no added signatures. Existing debt remains unsuppressed.

Model runtime: pinned upstream 4.3.1 source digest verified, notices/license/provenance retained in docs/vendor/model-viewer. Only its local qtm-model-viewer registration is adapted, avoiding replacement of Shopify's native model-viewer. Loading/progress/error/timeout/visibility/motion/editor behavior is tested with explicit adapters. Actual WebGL, AR, model formats, compressed-model decoder requests/CSP, and coexistence with native product media require live acceptance after Batch 9. No actual 3D rendering is claimed by these fixtures.
