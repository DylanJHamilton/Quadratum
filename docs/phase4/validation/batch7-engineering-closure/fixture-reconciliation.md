The initial 87-suite run passed 86 suites. shared-ui-tests used a synthetic password field outside the native account register root and clicked before DOMContentLoaded. Batch 7 intentionally scopes/disposes controls at that existing root; the actual native-host account-auth suite passed. The Phase 3 fixture now includes the production root, native input ID, hidden/pressed initial state and deferred DOM-ready event, then retains every original password/cart/duplicate-load/race assertion and adds control/pressed assertions. The corrected shared-ui suite passes. No runtime source was altered to satisfy this fixture. Initial output and failure are retained separately.
